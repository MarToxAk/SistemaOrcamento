---
phase: 25-upload-anexos-smb
plan: 25-02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - apps/backend/src/modules/integrations/athos/athos.controller.test.ts
  - apps/backend/src/modules/integrations/athos/athos.service.test.ts
  - apps/backend/src/modules/integrations/athos/athos-anexo.util.test.ts
autonomous: true
requirements: [ANEX-01, ANEX-02, ANEX-03]

must_haves:
  truths:
    - "Suite Jest cobre auth fail-closed no endpoint POST /athos/contas-pagar/:id/anexo"
    - "Suite Jest cobre saneamento do nome e composicao do path UNC"
    - "Suite Jest cobre sucesso da persistencia, falha de SMB e cleanup quando INSERT em anexo falha"
    - "Testes do modulo Athos passam com o endpoint de anexo incluido"
  artifacts:
    - path: "apps/backend/src/modules/integrations/athos/athos.controller.test.ts"
      provides: "Cenarios de auth e delegacao do endpoint de upload"
      contains: "contas-pagar/:id/anexo"
    - path: "apps/backend/src/modules/integrations/athos/athos.service.test.ts"
      provides: "Mocks de pg/fs cobrindo anexarContaPagar"
      contains: "anexarContaPagar"
    - path: "apps/backend/src/modules/integrations/athos/athos-anexo.util.test.ts"
      provides: "Cobertura dos helpers puros de sanitizacao/path"
      contains: "sanitizeAthosAttachmentName"
  key_links:
    - from: "athos.service.test.ts"
      to: "athos.service.ts anexarContaPagar"
      via: "mock de pg.Pool + fs/promises"
      pattern: "anexarContaPagar"
    - from: "athos-anexo.util.test.ts"
      to: "athos-anexo.util.ts"
      via: "assert de basename/ext/path UNC"
      pattern: "buildContaPagarAnexoPaths"
---

<objective>
Adicionar cobertura de testes unitarios para o upload de anexos da conta a pagar, cobrindo auth, validacao de arquivo, path sanitization e consistencia entre share SMB e tabela `anexo`.

Purpose: Detectar regressao cedo no fluxo mais sensivel da fase 25, especialmente traversal, falha parcial e protecao fail-closed.

Output:
- expansao de `athos.controller.test.ts`
- expansao de `athos.service.test.ts`
- novo `athos-anexo.util.test.ts`
</objective>

<execution_context>
@D:\Projetos\cloudeproject\SistemaOrcamento\.planning\phases\25-upload-anexos-smb\25-RESEARCH.md
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\Projetos\cloudeproject\SistemaOrcamento\.planning\PROJECT.md
@D:\Projetos\cloudeproject\SistemaOrcamento\.planning\phases\25-upload-anexos-smb\25-01-PLAN.md

<interfaces>
Expected new signatures from Plan 25-01:
```typescript
export class UploadContaPagarAnexoDto {
  idfuncionario?: number;
  // plus idclientehistorico only if option-a was chosen in the checkpoint
}

export function sanitizeAthosAttachmentName(originalName: string): string;
export function buildContaPagarAnexoPaths(idcontapagar: number, fileName: string): {
  directoryPath: string;
  fullPath: string;
  fileName: string;
};

async anexarContaPagar(input: {
  idcontapagar: number;
  file: Express.Multer.File;
  idfuncionario?: number;
  // plus idclientehistorico according to checkpoint result
}): Promise<{ idanexo: number; idcontapagar: number; arquivo: string; caminhoanexo: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cobrir controller e helpers de upload</name>
  <files>apps/backend/src/modules/integrations/athos/athos.controller.test.ts, apps/backend/src/modules/integrations/athos/athos-anexo.util.test.ts</files>
  <behavior>
    - Controller segue fail-closed: sem `ATHOS_API_TOKEN` -> 500; token invalido -> 401.
    - Helper rejeita nomes com traversal, separadores e extensoes nao permitidas.
    - Helper constroi path UNC dentro de `\\192.168.3.203\html\Anexo\contapagar\{id}` com basename sanitizado.
  </behavior>
  <action>
    Expandir `athos.controller.test.ts` com cenarios do endpoint de upload reutilizando o padrao atual de validacao do metodo privado `validateAthosToken()`, e criar `athos-anexo.util.test.ts` cobrindo os helpers puros.

    Casos obrigatorios:
    - token ausente/env ausente -> fail-closed
    - token valido -> controller delega ao service com `idcontapagar` numerico
    - `sanitizeAthosAttachmentName()` remove/recusa `../`, barras, `:` e extensoes fora da whitelist
    - `buildContaPagarAnexoPaths()` retorna diretoria e caminho final coerentes com UNC Windows
  </action>
  <verify>
    <automated>npm --workspace @bomcusto/backend run test -- athos --no-coverage</automated>
  </verify>
  <done>Controller tests e util tests cobrem auth e saneamento/path sem depender de share real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cobrir AthosService para sucesso, falha de SMB e cleanup compensatorio</name>
  <files>apps/backend/src/modules/integrations/athos/athos.service.test.ts</files>
  <behavior>
    - Upload bem-sucedido grava arquivo, executa INSERT e retorna metadados do anexo.
    - Falha na escrita SMB impede INSERT no banco.
    - Falha no INSERT apos gravacao dispara remocao best-effort do arquivo escrito.
    - `client.release()` sempre e chamado.
  </behavior>
  <action>
    Expandir `athos.service.test.ts` com mocks de `fs/promises` e `pg` seguindo o padrao ja usado no arquivo.

    Casos obrigatorios:
    - sucesso completo com `rows: [{ idanexo: 77, ... }]`
    - erro de `writeFile` / `mkdir` -> excecao controlada e zero INSERT
    - erro no `INSERT INTO anexo` -> `unlink` best-effort chamado para cleanup
    - `idfuncionario` default 1 quando ausente
    - Se a decisao do checkpoint tiver tornado `idclientehistorico` obrigatorio, cobrir tambem o contrato escolhido
  </action>
  <verify>
    <automated>npm --workspace @bomcusto/backend run test -- athos --no-coverage</automated>
  </verify>
  <done>Service tests passam cobrindo fluxo feliz e falhas parciais sem banco/share reais.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> controller/service | testes precisam provar que inputs maliciosos sao barrados sem infraestrutura real |
| service mocks -> fs/pg | simulacoes devem refletir escrita em share e falha parcial de persistencia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06 | T | helper de nome/path | mitigate | Testar traversal, caracteres reservados e extensoes invalidadas em unidade pura |
| T-25-07 | D | fluxo multipart em memoria | mitigate | Testar limites e falha antecipada via contrato esperado do endpoint e validators |
| T-25-08 | R | consistencia SMB/banco | mitigate | Testar cleanup compensatorio apos falha de INSERT para evitar residuos sem rastreio |
</threat_model>

<verification>
- `npm --workspace @bomcusto/backend run test -- athos --no-coverage`
- `npm --workspace @bomcusto/backend run build`
</verification>

<success_criteria>
- Todos os testes Athos passam com o novo fluxo de anexo.
- O comportamento de auth, sanitizacao e cleanup compensatorio fica coberto por teste automatizado.
- Nenhum teste depende de share SMB real ou banco Athos real.
</success_criteria>

<output>
After completion, create `.planning/phases/25-upload-anexos-smb/25-02-SUMMARY.md`
</output>