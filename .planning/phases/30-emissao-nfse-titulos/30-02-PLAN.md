---
phase: 30-emissao-nfse-titulos
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - apps/backend/src/modules/cobranca/cobranca.service.ts
  - apps/backend/src/modules/cobranca/cobranca.controller.ts
  - apps/backend/src/modules/cobranca/cobranca.module.ts
  - apps/backend/src/modules/cobranca/dto/emitir-nfse-cobranca.dto.ts
autonomous: true
requirements:
  - NFR-01
  - NFR-02
  - NFR-03
  - NFR-04

must_haves:
  truths:
    - "POST /cobranca/nfse requer x-internal-api-key (InternalAuthGuard global — sem @Public())"
    - "EmitirNfseCobrancaDto valida: idclienteAthos (Int, @IsPositive), idcontasReceber (array @ArrayMinSize(1), @IsInt @IsPositive each), valor (@IsNumber @Min(0.01)), descricaoServico/servicoCodigo opcionais"
    - "CobrancaService.emitirNfse() verifica duplicidade por idvenda (D-08): se NfseEmitida.idvenda existe retorna 400 com mensagem 'NFS-e já emitida para esta venda (Nº {existente.numeroNfse})'"
    - "CobrancaService.emitirNfse() pula verificação de duplicidade quando idvenda for null (D-10)"
    - "Após emissão bem-sucedida, salva NfseEmitida com idvenda, numeroNfse, numeroRps, idclienteAthos, valorServico, e NfseEmitidaTitulo[] com createMany"
    - "CobrancaModule importa NfseModule (sem forwardRef) — NfseService injetado no CobrancaService"
    - "Resposta do endpoint inclui: nfseEmitidaId, numeroNfse, numeroRps, valor"
  artifacts:
    - path: "apps/backend/src/modules/cobranca/dto/emitir-nfse-cobranca.dto.ts"
      provides: "EmitirNfseCobrancaDto com class-validator decorators"
      exports: ["EmitirNfseCobrancaDto"]
    - path: "apps/backend/src/modules/cobranca/cobranca.service.ts"
      provides: "CobrancaService.emitirNfse(dto) com duplicidade, delegação e persistência"
      exports: ["emitirNfse"]
    - path: "apps/backend/src/modules/cobranca/cobranca.controller.ts"
      provides: "POST /cobranca/nfse — @Post('nfse') com @Body() EmitirNfseCobrancaDto"
    - path: "apps/backend/src/modules/cobranca/cobranca.module.ts"
      provides: "CobrancaModule com imports [EfiModule, AthosModule, NfseModule]"
  key_links:
    - from: "cobranca.controller.ts POST /cobranca/nfse"
      to: "cobrancaService.emitirNfse(dto)"
      via: "@Body() EmitirNfseCobrancaDto (class-validator)"
      pattern: "emitirNfse"
    - from: "cobranca.service.ts emitirNfse()"
      to: "prisma.nfseEmitida.findFirst({ where: { idvenda } })"
      via: "verificação de duplicidade D-08"
      pattern: "nfseEmitida\\.findFirst"
    - from: "cobranca.service.ts emitirNfse()"
      to: "nfseService.emitirParaContaReceber()"
      via: "delegação sem quoteId"
      pattern: "emitirParaContaReceber"
    - from: "cobranca.service.ts emitirNfse()"
      to: "prisma.nfseEmitida.create() com titulos.createMany"
      via: "nested write Prisma após emissão bem-sucedida"
      pattern: "nfseEmitida\\.create"
    - from: "cobranca.module.ts"
      to: "NfseModule"
      via: "imports: [EfiModule, AthosModule, NfseModule]"
      pattern: "NfseModule"
---

<objective>
Implementar a camada de aplicação backend para emissão de NFS-e via contas a receber:
DTO com validação, CobrancaService.emitirNfse() com verificação de duplicidade e persistência,
CobrancaController com o endpoint POST /cobranca/nfse, e CobrancaModule com NfseModule importado.

Purpose: Wave 2 — depende do Plano 01 (NfseService.emitirParaContaReceber disponível).
Este plano entrega o endpoint que o frontend do Plano 03 vai chamar.

Output:
- apps/backend/src/modules/cobranca/dto/emitir-nfse-cobranca.dto.ts (NOVO)
- apps/backend/src/modules/cobranca/cobranca.service.ts (adicionar emitirNfse)
- apps/backend/src/modules/cobranca/cobranca.controller.ts (adicionar POST nfse)
- apps/backend/src/modules/cobranca/cobranca.module.ts (adicionar NfseModule)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-emissao-nfse-titulos/30-CONTEXT.md
@.planning/phases/30-emissao-nfse-titulos/30-RESEARCH.md
@.planning/phases/30-emissao-nfse-titulos/30-PATTERNS.md
@.planning/phases/30-emissao-nfse-titulos/30-01-SUMMARY.md

<interfaces>
<!-- EmitirNfseCobrancaDto — campos obrigatórios e opcionais (30-PATTERNS.md linhas 230-266) -->
<!-- idclienteAthos: number (@IsInt @IsPositive) -->
<!-- idcontasReceber: number[] (@IsArray @ArrayMinSize(1) @IsInt({each:true}) @IsPositive({each:true})) -->
<!-- valor: number (@IsNumber @Min(0.01)) — NFR-02: valor editado pelo operador -->
<!-- descricaoServico?: string (@IsOptional @IsString) -->
<!-- servicoCodigo?: string (@IsOptional @IsString) — ex: "24.01", "13.05", "14.08" -->

<!-- CobrancaService — estado atual (30-PATTERNS.md linhas 103-197) -->
<!-- imports presentes: BadRequestException, Injectable, InternalServerErrorException, Logger, ConfigService, AthosService, PrismaService, CriarBoletoDto -->
<!-- constructor: (AthosService, PrismaService, ConfigService) — ADICIONAR NfseService -->
<!-- criarBoleto() padrão: buscarTitulosClienteContasReceber() + filter + loop validação + create Prisma -->

<!-- Padrão buscar + filtrar títulos (linhas 137-148 de PATTERNS.md) -->
<!-- const todosTitulos = await this.athosService.buscarTitulosClienteContasReceber(dto.idclienteAthos) -->
<!-- const titulosFiltrados = todosTitulos.filter((t) => dto.idcontasReceber.includes(t.idcontareceber)) -->
<!-- for (const id of dto.idcontasReceber) { if (!titulosFiltrados.find(t => t.idcontareceber === id)) throw 400 } -->

<!-- Padrão duplicidade idvenda (linhas 150-162 de PATTERNS.md) -->
<!-- const idvenda = titulosFiltrados[0]?.idvenda ?? null; -->
<!-- if (idvenda !== null) { const existente = await this.prisma.nfseEmitida.findFirst({ where: { idvenda } }) -->
<!--   if (existente) throw new BadRequestException(`NFS-e já emitida para esta venda (Nº ${existente.numeroNfse})`) } -->

<!-- Padrão nested write NfseEmitida (linhas 164-184 de PATTERNS.md) -->
<!-- prisma.nfseEmitida.create({ data: { numeroNfse, numeroRps, idclienteAthos, valorServico: dto.valor, -->
<!--   idvenda, titulos: { createMany: { data: titulosFiltrados.map(t => ({ idcontareceber: t.idcontareceber, valor: t.valor })) } } } }) -->

<!-- Retorno do endpoint: { nfseEmitidaId: nfseEmitida.id, numeroNfse: resultado.numero, numeroRps: resultado.numeroRps, valor: dto.valor } -->

<!-- NfseModule já exporta NfseService — apenas adicionar NfseModule nos imports de CobrancaModule (linha 649 de PATTERNS.md) -->
<!-- CobrancaModule estado atual: imports: [EfiModule, AthosModule] → ALTERAR para: imports: [EfiModule, AthosModule, NfseModule] -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EmitirNfseCobrancaDto + CobrancaModule com NfseModule</name>
  <files>
    apps/backend/src/modules/cobranca/dto/emitir-nfse-cobranca.dto.ts,
    apps/backend/src/modules/cobranca/cobranca.module.ts
  </files>
  <read_first>
    - apps/backend/src/modules/cobranca/dto/criar-boleto.dto.ts — ler COMPLETO para replicar exatamente os imports de class-validator e a estrutura dos decorators
    - apps/backend/src/modules/cobranca/cobranca.module.ts — ler COMPLETO para ver imports atuais ([EfiModule, AthosModule]) e adicionar NfseModule sem remover os existentes
    - apps/backend/src/modules/integrations/nfse/nfse.module.ts — confirmar que exports: [NfseService] está presente (necessário para injeção)
  </read_first>
  <behavior>
    - EmitirNfseCobrancaDto com valor=0 falha na validação (MinValue 0.01)
    - EmitirNfseCobrancaDto com idcontasReceber=[] falha (ArrayMinSize(1))
    - EmitirNfseCobrancaDto com idclienteAthos=-1 falha (@IsPositive)
    - EmitirNfseCobrancaDto com todos campos válidos passa validação
    - CobrancaModule lista NfseModule em imports após alteração
  </behavior>
  <action>
    CRIAR apps/backend/src/modules/cobranca/dto/emitir-nfse-cobranca.dto.ts:
    - Imports: `ArrayMinSize, IsArray, IsInt, IsNumber, IsOptional, IsPositive, IsString, Min` de `class-validator`
    - Classe exportada: `EmitirNfseCobrancaDto`
    - Campo `idclienteAthos`: decorators `@IsInt()` e `@IsPositive()`
    - Campo `idcontasReceber`: decorators `@IsArray()`, `@ArrayMinSize(1)`, `@IsInt({ each: true })`, `@IsPositive({ each: true })`
    - Campo `valor`: decorators `@IsNumber()`, `@Min(0.01)` — NFR-02: valor editado pelo operador, não calculado
    - Campo opcional `descricaoServico?: string`: decorators `@IsOptional()`, `@IsString()`
    - Campo opcional `servicoCodigo?: string`: decorators `@IsOptional()`, `@IsString()` — ex: "24.01", "13.05", "14.08" para código de serviço ISS

    EDITAR apps/backend/src/modules/cobranca/cobranca.module.ts:
    - Adicionar `NfseModule` nos imports: `imports: [EfiModule, AthosModule, NfseModule]`
    - Adicionar import no topo: `import { NfseModule } from '../integrations/nfse/nfse.module';`
    - NÃO usar forwardRef — não há ciclo circular (per RESEARCH.md Pitfall 2)
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc --noEmit 2>&1 | grep -E "(emitir-nfse|cobranca\.module)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo emitir-nfse-cobranca.dto.ts criado com os 5 campos (3 obrigatórios + 2 opcionais)
    - `grep -n "Min(0.01)" apps/backend/src/modules/cobranca/dto/emitir-nfse-cobranca.dto.ts` retorna linha com @Min(0.01) em campo valor
    - `grep -n "ArrayMinSize(1)" apps/backend/src/modules/cobranca/dto/emitir-nfse-cobranca.dto.ts` retorna linha com @ArrayMinSize(1) em campo idcontasReceber
    - `grep -n "NfseModule" apps/backend/src/modules/cobranca/cobranca.module.ts` retorna pelo menos 2 linhas (import e no array imports[])
    - `npx tsc --noEmit` sem erros nos arquivos modificados
  </acceptance_criteria>
  <done>DTO criado com validações corretas, CobrancaModule importa NfseModule, compilação limpa</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CobrancaService.emitirNfse() + CobrancaController POST /cobranca/nfse</name>
  <files>
    apps/backend/src/modules/cobranca/cobranca.service.ts,
    apps/backend/src/modules/cobranca/cobranca.controller.ts
  </files>
  <read_first>
    - apps/backend/src/modules/cobranca/cobranca.service.ts — ler COMPLETO: imports (linhas 1-12), constructor (linhas 24-32), criarBoleto() COMPLETO incluindo buscar + filtrar títulos, tratamento de erro InternalServerErrorException (linhas ~199-207) e padrão de Prisma create com nested write
    - apps/backend/src/modules/cobranca/cobranca.controller.ts — ler COMPLETO: imports presentes, @Post("boleto") para replicar estrutura do novo @Post("nfse")
    - apps/backend/src/modules/integrations/athos/athos.service.ts — confirmar assinatura de buscarTitulosClienteContasReceber() para saber o tipo de retorno (campo idvenda disponível nos títulos)
  </read_first>
  <behavior>
    - emitirNfse() com idvenda existente em NfseEmitida: lança BadRequestException 400 "NFS-e já emitida para esta venda (Nº {numero})"
    - emitirNfse() com idvenda null: pula verificação de duplicidade e prossegue normalmente
    - emitirNfse() com título não encontrado para o cliente: lança BadRequestException "Título {id} não encontrado para este cliente."
    - emitirNfse() bem-sucedido: retorna { nfseEmitidaId, numeroNfse, numeroRps, valor }
    - emitirNfse() com erro SOAP: lança InternalServerErrorException "Não foi possível emitir a NFS-e."
  </behavior>
  <action>
    Em cobranca.service.ts — adicionar:
    1. Import: `import { NfseService } from '../integrations/nfse/nfse.service';`
    2. Import do DTO: `import { EmitirNfseCobrancaDto } from './dto/emitir-nfse-cobranca.dto';`
    3. No constructor, adicionar: `private readonly nfseService: NfseService`
    4. Novo método `async emitirNfse(dto: EmitirNfseCobrancaDto)`:

    Fluxo do método:
    a. Buscar e filtrar títulos: `buscarTitulosClienteContasReceber(dto.idclienteAthos)` + filter por idcontasReceber + loop validando que cada id existe (padrão idêntico a criarBoleto() — ver PATTERNS.md linhas 137-148)
    b. Extrair idvenda: `const idvenda = titulosFiltrados[0]?.idvenda ?? null` — todos os títulos de uma mesma venda têm o mesmo idvenda
    c. Verificação duplicidade (D-08, D-09, D-10): se `idvenda !== null`, executar `this.prisma.nfseEmitida.findFirst({ where: { idvenda } })` — se existente, throw `BadRequestException(\`NFS-e já emitida para esta venda (Nº ${existente.numeroNfse})\`)`
    d. Emitir via NfseService: dentro de try/catch, chamar `await this.nfseService.emitirParaContaReceber({ clienteAthosId: dto.idclienteAthos, valor: dto.valor, servicoCodigo: dto.servicoCodigo, discriminacao: dto.descricaoServico })`
    e. Persistir em NfseEmitida com nested write (padrão PATTERNS.md linhas 164-184): `this.prisma.nfseEmitida.create({ data: { numeroNfse: resultado.numero, numeroRps: resultado.numeroRps, idclienteAthos: dto.idclienteAthos, valorServico: dto.valor, idvenda, titulos: { createMany: { data: titulosFiltrados.map(t => ({ idcontareceber: t.idcontareceber, valor: t.valor })) } } } })`
    f. Catch: log com `this.logger.error(...)` e throw `InternalServerErrorException("Não foi possível emitir a NFS-e.")` — padrão idêntico ao criarBoleto() (PATTERNS.md linhas 186-196)
    g. Retornar: `{ nfseEmitidaId: nfseEmitida.id, numeroNfse: resultado.numero, numeroRps: resultado.numeroRps, valor: dto.valor }`

    Em cobranca.controller.ts — adicionar:
    1. Import do DTO: `import { EmitirNfseCobrancaDto } from './dto/emitir-nfse-cobranca.dto';`
    2. Novo endpoint seguindo o padrão de @Post("boleto"):
       ```
       @Post("nfse")
       async emitirNfse(@Body() dto: EmitirNfseCobrancaDto) {
         return this.cobrancaService.emitirNfse(dto);
       }
       ```
    3. NÃO adicionar @Public() — InternalAuthGuard é global e protege automaticamente (T-30-05 mitigado)
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc --noEmit 2>&1 | grep -E "(cobranca\.service|cobranca\.controller)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "emitirNfse" apps/backend/src/modules/cobranca/cobranca.service.ts` retorna pelo menos 2 linhas (método + referência)
    - `grep -n "emitirNfse" apps/backend/src/modules/cobranca/cobranca.controller.ts` retorna pelo menos 1 linha (@Post endpoint)
    - `grep -n "nfseEmitida.findFirst" apps/backend/src/modules/cobranca/cobranca.service.ts` retorna linha da verificação de duplicidade
    - `grep -n "emitirParaContaReceber" apps/backend/src/modules/cobranca/cobranca.service.ts` retorna linha de delegação ao NfseService
    - `grep -n "nfseEmitida.create" apps/backend/src/modules/cobranca/cobranca.service.ts` retorna linha do nested write
    - `grep -n "@Post" apps/backend/src/modules/cobranca/cobranca.controller.ts | grep nfse` retorna a declaração @Post("nfse")
    - `grep -n "@Public" apps/backend/src/modules/cobranca/cobranca.controller.ts | grep nfse` retorna 0 linhas — endpoint NÃO é público
    - `npx tsc --noEmit` retorna 0 erros nos arquivos modificados
    - `cd apps/backend && npx jest --testPathPattern "cobranca" --passWithNoTests` — testes existentes passam (sem regressão)
  </acceptance_criteria>
  <done>CobrancaService.emitirNfse() com duplicidade/delegação/persistência, endpoint POST /cobranca/nfse protegido por InternalAuthGuard, compilação limpa</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend Route Handler → POST /cobranca/nfse | Body JSON controlado pelo cliente — validado pelo DTO com class-validator |
| CobrancaService → NfseService.emitirParaContaReceber() | Chamada interna NestJS — sem boundary de rede |
| CobrancaService → Prisma nfse_emitida | Escrita no banco próprio — sem dados externos injetados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Elevation of Privilege | POST /cobranca/nfse | mitigate | InternalAuthGuard global rejeita requests sem x-internal-api-key válido — sem @Public() no endpoint |
| T-30-06 | Tampering | EmitirNfseCobrancaDto.valor | mitigate | @Min(0.01) no DTO impede valor zero ou negativo; class-validator rejeita com 400 antes de chegar ao serviço |
| T-30-07 | Tampering | EmitirNfseCobrancaDto.idcontasReceber | mitigate | @IsInt({each:true}) @IsPositive({each:true}) impede injeção de valores negativos ou não-inteiros no array |
| T-30-08 | Tampering | Duplicidade NFS-e por idvenda | mitigate | prisma.nfseEmitida.findFirst({ where: { idvenda } }) antes de chamar SOAP — BadRequestException 400 se encontrado |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
Verificação geral do Plano 02:

1. `cd apps/backend && npx tsc --noEmit` — 0 erros TypeScript
2. `grep -n "NfseModule" apps/backend/src/modules/cobranca/cobranca.module.ts` — NfseModule presente no array imports
3. `grep -n "emitirNfse" apps/backend/src/modules/cobranca/cobranca.controller.ts` — endpoint @Post("nfse") existe
4. `grep -n "nfseEmitida.findFirst" apps/backend/src/modules/cobranca/cobranca.service.ts` — verificação de duplicidade presente
5. `grep -n "emitirParaContaReceber" apps/backend/src/modules/cobranca/cobranca.service.ts` — delegação ao NfseService
6. `grep -n "nfseEmitida.create" apps/backend/src/modules/cobranca/cobranca.service.ts` — persistência presente
7. `cd apps/backend && npx jest --passWithNoTests` — suite completa sem regressão
</verification>

<success_criteria>
- POST /cobranca/nfse protegido, aceita EmitirNfseCobrancaDto com validação completa
- CobrancaService.emitirNfse() verifica duplicidade por idvenda, delega para NfseService, persiste NfseEmitida com títulos vinculados
- CobrancaModule importa NfseModule sem forwardRef
- Compilação TypeScript limpa
- Testes existentes sem regressão
</success_criteria>

<output>
Criar `.planning/phases/30-emissao-nfse-titulos/30-02-SUMMARY.md` ao concluir
</output>
