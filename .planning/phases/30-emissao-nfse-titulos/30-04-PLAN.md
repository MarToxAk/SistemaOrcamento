---
phase: 30-emissao-nfse-titulos
plan: 04
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - apps/backend/src/modules/integrations/nfse/nfse.service.ts
  - apps/frontend/src/app/orcamento/[id]/page.tsx
autonomous: true
requirements:
  - NFR-03
  - NFR-04

must_haves:
  truths:
    - "NfseService.emitir() salva idvenda em NfseEmitida após emissão bem-sucedida (D-17, D-24)"
    - "NfseService.emitir() obtém idvenda via quote.saleExternalId (BigInt -> Number) ao salvar NfseEmitida"
    - "Quando idvenda já tem NfseEmitida registrada, o modal de NFS-e na página /orcamento/[id] exibe aviso antes de emitir (D-18)"
    - "Fluxo de emissão em /orcamento/[id] NÃO é duplicado — apenas verificação de duplicidade adicionada (D-19)"
  artifacts:
    - path: "apps/backend/src/modules/integrations/nfse/nfse.service.ts"
      provides: "NfseService.emitir() salva idvenda no NfseEmitida após emissão"
    - path: "apps/frontend/src/app/orcamento/[id]/page.tsx"
      provides: "Verificação de NFS-e duplicada antes de emitir via /orcamento/[id]"
  key_links:
    - from: "nfse.service.ts emitir() — linha após parseLinkNfse"
      to: "prisma.nfseEmitida.create() com idvenda: Number(quote.saleExternalId)"
      via: "quote.saleExternalId ?? null; se null → salvar idvenda: null"
      pattern: "saleExternalId"
    - from: "orcamento/[id]/page.tsx botão emitir NFS-e"
      to: "verificação de NfseEmitida duplicada via API"
      via: "aviso antes de emitir quando já existe NFS-e para a venda"
      pattern: "nfse.*duplicad\|já emitida"
---

<objective>
Vincular o fluxo de emissão de NFS-e existente em /orcamento/[id] à tabela NfseEmitida
para completar a prevenção de duplicidade cross-flow (D-17, D-18, D-19, D-24).

Wave 2 — roda em paralelo com o Plano 02. Depende apenas do Plano 01 (campo idvenda
em NfseEmitida disponível). NÃO duplica funcionalidade: apenas adiciona salvamento de
idvenda no fluxo existente + aviso no frontend de orçamento.

Purpose: Sem este plano, um operador poderia emitir NFS-e tanto pelo fluxo de
/orcamento/[id] quanto pelo novo fluxo de /contas-receber/[idcliente], gerando NFS-e
duplicadas para a mesma venda Athos.

Output:
- apps/backend/src/modules/integrations/nfse/nfse.service.ts (salvar idvenda em emitir())
- apps/frontend/src/app/orcamento/[id]/page.tsx (aviso de duplicidade — D-18)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-emissao-nfse-titulos/30-CONTEXT.md
@.planning/phases/30-emissao-nfse-titulos/30-RESEARCH.md
@.planning/phases/30-emissao-nfse-titulos/30-01-SUMMARY.md

<interfaces>
<!-- NfseService.emitir() — bloco de persistência atual (30-PATTERNS.md linhas 84-99) -->
<!-- Linhas 704-713: await (this.prisma as any).quote.update({ where: { id: quote.id }, -->
<!--   data: { nfseNumero, nfseCodigoVerificacao, nfseLink, nfseEmitidaEm } }) -->
<!-- ADICIONAR APÓS este bloco: prisma.nfseEmitida.create() com idvenda de quote.saleExternalId -->

<!-- quote.saleExternalId: BigInt | null (campo do modelo Quote no Prisma) -->
<!-- Conversão segura: const idvenda = quote?.saleExternalId ? Number(quote.saleExternalId) : null -->
<!-- Number(BigInt) é válido em TS quando BigInt cabe em Number (idvenda são inteiros do Athos) -->

<!-- NfseEmitida.create() para fluxo de orçamento: -->
<!-- { numeroNfse, numeroRps: Number(rpsNumero), idclienteAthos: (clienteAthosId da resolução de tomador), -->
<!--   valorServico: (valor enviado ao SOAP), idvenda, titulos: { create: [] } } -->
<!-- NfseEmitidaTitulo: sem idcontareceber neste fluxo — array vazio ou omitido -->

<!-- Page /orcamento/[id]: verificar como o modal de emissão atual chama a API -->
<!-- Identificar a função/handler que aciona a emissão para adicionar verificação antes -->
<!-- D-18: "antes de emitir em /orcamento/[id], verificar se NfseEmitida.idvenda já tem registro" -->
<!-- A verificação deve ser feita via GET /api/nfse/verificar-duplicidade?idvenda=X ou lendo NfseEmitida existente -->
<!-- Alternativa simples: ao abrir o modal de NFS-e, verificar via GET se há NfseEmitida com mesmo idvenda -->
<!-- Se sim: exibir aviso informativo "NFS-e já emitida para esta venda pelo módulo de Contas a Receber." -->
<!-- O aviso NÃO bloqueia a emissão — é informativo (alinhado com D-03 para o outro fluxo) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NfseService.emitir() — salvar NfseEmitida com idvenda após emissão</name>
  <files>apps/backend/src/modules/integrations/nfse/nfse.service.ts</files>
  <read_first>
    - apps/backend/src/modules/integrations/nfse/nfse.service.ts — ler o método emitir() COMPLETO, especialmente:
      1. Como quote é carregado (findFirst com saleExternalId)
      2. O bloco de persistência nas linhas 704-713 (quote.update com nfseNumero, nfseCodigoVerificacao, nfseLink, nfseEmitidaEm)
      3. Quais variáveis estão disponíveis após o SOAP: numeroNfse, rpsNumero, codigoVerificacao, linkNfse
      4. Como idclienteAthos é resolvido no Caminho A — identificar qual variável guarda o clienteAthosId resolvido
    - apps/backend/prisma/schema.prisma — confirmar os campos de NfseEmitida após a migration do Plano 01 (numberOfNfse, numeroRps, idclienteAthos, valorServico, idvenda) e campos de NfseEmitidaTitulo
  </read_first>
  <action>
    Após o bloco existente de `quote.update({ data: { nfseNumero, nfseCodigoVerificacao, nfseLink, nfseEmitidaEm } })` e ANTES do `return` final do método `emitir()`:

    Adicionar bloco de salvamento em NfseEmitida — somente se a emissão foi nova (`!jaEmitida`):
    1. Extrair idvenda de quote.saleExternalId: `const idvendaParam = quote?.saleExternalId ? Number(quote.saleExternalId) : null`
    2. Verificar que a NfseEmitida para este idvenda não existe ainda (caso já tenha sido criada por outro fluxo): `if (idvendaParam !== null) { const existente = await this.prisma.nfseEmitida.findFirst({ where: { idvenda: idvendaParam } }); if (existente) { this.logger.warn(\`NfseEmitida já existe para idvenda ${idvendaParam} (id=${existente.id})\`); } else { /* criar */ } }` — se idvenda for null, criar sem idvenda
    3. Salvar NfseEmitida (quando não existente):
       `await this.prisma.nfseEmitida.create({ data: { numeroNfse: numeroNfse, numeroRps: Number(rpsNumero), idclienteAthos: input?.clienteAthosId ?? 0, valorServico: valorServicos ?? 0, idvenda: idvendaParam } })`
       - `input?.clienteAthosId ?? 0`: usar o clienteAthosId se disponível no input; caso o fluxo seja manual (sem clienteAthosId), salvar 0 como placeholder — não bloquear o fluxo existente
       - `valorServicos`: variável que guarda o valor enviado ao SOAP (identificar o nome correto lendo o código)
       - `titulos: {}` omitido — não há idcontareceber no fluxo de orçamento

    Importante: envolver o bloco de criação em try/catch separado para que falha na criação de NfseEmitida NÃO cause falha na emissão da NFS-e — o SOAP já foi executado com sucesso. Logar o erro e continuar.
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc --noEmit 2>&1 | grep "nfse.service" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` retorna 0 erros em nfse.service.ts
    - `grep -n "nfseEmitida.create\|nfseEmitida\.create" apps/backend/src/modules/integrations/nfse/nfse.service.ts` retorna pelo menos 1 linha — presença do bloco de persistência
    - `grep -n "saleExternalId" apps/backend/src/modules/integrations/nfse/nfse.service.ts` retorna linha onde idvenda é extraído de saleExternalId
    - O bloco de criação de NfseEmitida está dentro de try/catch separado (grep: `try.*nfseEmitida\|nfseEmitida.*catch` — verificar pela proximidade no arquivo)
    - `cd apps/backend && npx jest --testPathPattern "nfse" --passWithNoTests` — testes existentes de nfse.service passam sem regressão
  </acceptance_criteria>
  <done>NfseService.emitir() salva NfseEmitida com idvenda após emissão bem-sucedida, falha no salvamento não quebra o fluxo, compilação limpa</done>
</task>

<task type="auto">
  <name>Task 2: /orcamento/[id] — aviso informativo de NFS-e duplicada (D-18)</name>
  <files>apps/frontend/src/app/orcamento/[id]/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/orcamento/[id]/page.tsx — ler COMPLETO ou ao menos:
      1. Como o modal de NFS-e atual é acionado (função/handler de emissão existente)
      2. Onde o campo saleExternalId ou idvenda do quote está disponível na página
      3. Como os estados do modal existente são estruturados (estado confirm/loading/success do modal NFS-e de orçamento)
      4. Qual endpoint de NFS-e o modal atual usa para verificar se já foi emitida (campo jaEmitida na resposta ou similar)
    - apps/backend/src/modules/integrations/nfse/nfse.service.ts — confirmar que emitir() retorna `{ jaEmitida: boolean, ... }` (linha 753 do PATTERNS.md) — se jaEmitida=true significa que já havia NFS-e para este quote
  </read_first>
  <action>
    D-19 estabelece: NÃO duplicar funcionalidade — apenas adicionar a verificação de duplicidade.

    Verificar como o modal de NFS-e em /orcamento/[id] detecta o estado já emitida:
    - Se a resposta do endpoint de emissão inclui `jaEmitida: true`, o modal já pode exibir um aviso
    - D-18 pede aviso ANTES de emitir, não depois

    Identificar no código da página /orcamento/[id]:
    a. Se `quote.saleExternalId` (ou campo equivalente) está disponível como variável no estado da página
    b. Se o modal NFS-e atual tem estado "confirm" onde um aviso pode ser inserido

    Adicionar no estado confirm do modal NFS-e de /orcamento/[id]:
    - Se `quote.saleExternalId` é não-null e há uma forma de verificar NfseEmitida.idvenda, adicionar chamada GET ao verificar
    - Alternativa simples (se não houver endpoint de verificação): ao abrir o modal de NFS-e, checar via GET /api/cobranca/nfse/verificar?idvenda=X (se endpoint existir) ou simplesmente exibir o aviso quando a resposta de emissão retornar erro 400 com mensagem de duplicidade
    - Adicionar ao estado "error" do modal (ou criar aviso no estado "confirm") com texto: "NFS-e já emitida para esta venda pelo módulo de Contas a Receber. Verifique o histórico antes de emitir novamente."
    - O aviso é exibido quando `nfseError.message` contém "NFS-e já emitida" (texto retornado pelo backend)
    - NÃO bloquear a emissão — aviso informativo, operador pode tentar novamente se necessário

    Se o estado de erro do modal NFS-e de orçamento já exibe `data.message` da API, a mensagem "NFS-e já emitida para esta venda (Nº X)" já aparecerá automaticamente no estado error, sem necessidade de nova lógica. Nesse caso:
    - Verificar se o estado error já exibe `data.message`
    - Se sim: esta task confirma que o comportamento está correto e documenta no SUMMARY
    - Se não: adicionar exibição de `data.message ?? data.error` no estado de erro do modal
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | grep "orcamento" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` retorna 0 erros em apps/frontend/src/app/orcamento/[id]/page.tsx
    - O modal NFS-e de /orcamento/[id] exibe a mensagem de erro retornada pela API quando o backend retorna 400 (verificar via grep de `data.message\|data.error` na renderização do estado error do modal)
    - A mensagem "NFS-e já emitida para esta venda" do backend será exibida ao operador quando houver duplicidade cross-flow (via estado error do modal existente)
    - Nenhuma funcionalidade de emissão SOAP foi duplicada nesta task (grep de "emitirNfse\|emitir.*nfse\|soap" no arquivo para confirmar que não foram adicionadas chamadas diretas ao SOAP)
    - `cd apps/frontend && npx next build 2>&1 | tail -5` — build concluído sem erros de compilação
  </acceptance_criteria>
  <done>Modal NFS-e de /orcamento/[id] exibe mensagem de duplicidade cross-flow via estado error, sem duplicação de funcionalidade SOAP, compilação limpa</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NfseService.emitir() → prisma.nfseEmitida.create() | Dados internos do sistema — sem entrada externa neste bloco |
| /orcamento/[id] → /api/nfse endpoint (existente) | Endpoint autenticado via x-internal-api-key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-13 | Tampering | NfseService.emitir() dupla criação NfseEmitida | mitigate | findFirst({ where: { idvenda } }) antes de create — log warn se existente, skip sem erro para não quebrar fluxo SOAP já executado |
| T-30-14 | Repudiation | NfseEmitida criada via fluxo de orçamento sem idvenda | accept | Títulos antigos sem saleExternalId salvam idvenda: null — auditável pela presença do campo null; não há proteção perfeita conforme D-10 |
| T-30-15 | Tampering | saleExternalId BigInt → Number overflow | accept | IDs Athos são inteiros menores que Number.MAX_SAFE_INTEGER (9 quadrilhões) — conversão Number(BigInt) segura para IDs de venda práticos |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
Verificação geral do Plano 04:

1. `cd apps/backend && npx tsc --noEmit` — 0 erros TypeScript
2. `grep -n "nfseEmitida.create" apps/backend/src/modules/integrations/nfse/nfse.service.ts` — bloco de persistência presente em emitir()
3. `grep -n "saleExternalId" apps/backend/src/modules/integrations/nfse/nfse.service.ts` — idvenda extraído de saleExternalId
4. `cd apps/frontend && npx tsc --noEmit` — 0 erros TypeScript
5. `cd apps/backend && npx jest --testPathPattern "nfse" --passWithNoTests` — testes de nfse.service sem regressão
6. `cd apps/frontend && npx next build 2>&1 | tail -5` — build frontend concluído
</verification>

<success_criteria>
- NfseService.emitir() cria NfseEmitida com idvenda (de saleExternalId) após emissão bem-sucedida
- Falha no salvamento de NfseEmitida não interrompe o retorno do endpoint (try/catch separado)
- Modal NFS-e de /orcamento/[id] exibe mensagem de duplicidade cross-flow via estado error
- Nenhuma funcionalidade SOAP duplicada no frontend de orçamento
- Compilação TypeScript e build Next.js limpos
</success_criteria>

<output>
Criar `.planning/phases/30-emissao-nfse-titulos/30-04-SUMMARY.md` ao concluir
</output>
