---
phase: "13"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/quotes/quotes.service.ts
autonomous: true
requirements:
  - PCHK-01
  - PCHK-02
  - OBSV-01

must_haves:
  truths:
    - "getById dispara checkPaymentStatus de forma fire-and-forget para status PENDENTE e ENVIADO"
    - "enviarParaCliente dispara checkPaymentStatus de forma fire-and-forget antes ou apos envio"
    - "checkPaymentStatus loga resultado estruturado apos conciliacao (paid, idVenda, valor, statusUpdated)"
    - "Falha no disparo nao propaga excecao nem bloqueia resposta ao caller"
  artifacts:
    - path: "apps/backend/src/modules/quotes/quotes.service.ts"
      provides: "Disparo fire-and-forget em getById"
      contains: "checkPaymentStatus"
    - path: "apps/backend/src/modules/quotes/quotes.service.ts"
      provides: "Log estruturado de resultado de conciliacao"
      contains: "conciliacao_athos"
  key_links:
    - from: "apps/backend/src/modules/quotes/quotes.service.ts"
      to: "apps/backend/src/modules/integrations/athos/athos.service.ts"
      via: "checkPaymentStatus chama verificarPagamentoPorOrcamento"
      pattern: "checkPaymentStatus"
---

<objective>
Adicionar gatilhos automáticos de checagem de pagamento ao fluxo de abertura de detalhe e envio de orçamento, e logar resultado estruturado de cada conciliação.

Purpose: Atender PCHK-01, PCHK-02, OBSV-01 — conectar conciliação Athos ao fluxo operacional normal.
Output: quotes.service.ts com disparos fire-and-forget e logs estruturados de conciliação.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-gatilhos-checagem-sincronizacao/13-CONTEXT.md
@apps/backend/src/modules/quotes/quotes.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar log estruturado de resultado em checkPaymentStatus</name>
  <read_first>apps/backend/src/modules/quotes/quotes.service.ts</read_first>
  <files>apps/backend/src/modules/quotes/quotes.service.ts</files>
  <action>
No método `checkPaymentStatus`, logo após o bloco que resolve `statusUpdated` e `currentStatus` (antes do `return`), adicionar um `this.logger.log(...)` com resultado estruturado da conciliação. Formato:

```typescript
this.logger.log(
  `conciliacao_athos quoteId=${quote.id} paid=${payment.paid} idVenda=${resolvedIdVenda ?? "n/a"} valor=${payment.valor ?? 0} statusUpdated=${statusUpdated} currentStatus=${currentStatus}`,
);
```

Este log deve aparecer sempre que `checkPaymentStatus` for chamado (seja pelo endpoint ou pelos gatilhos que serão adicionados na Task 2).
  </action>
  <acceptance_criteria>
    - `quotes.service.ts` contém `conciliacao_athos` no corpo de `checkPaymentStatus`
    - `quotes.service.ts` contém `paid=` e `statusUpdated=` no mesmo log
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Adicionar disparo fire-and-forget em getById e enviarParaCliente</name>
  <read_first>apps/backend/src/modules/quotes/quotes.service.ts</read_first>
  <files>apps/backend/src/modules/quotes/quotes.service.ts</files>
  <action>
**Em `getById`**: após a resolução de `resolvedQuote` (após o bloco que tenta preencher `saleExternalId`), e antes de chamar `this.mapQuoteBody`, adicionar disparo fire-and-forget de checagem de pagamento quando o status for elegível:

```typescript
if (["PENDENTE", "ENVIADO"].includes(resolvedQuote.status)) {
  void this.checkPaymentStatus(resolvedQuote.id).catch((err: unknown) => {
    this.logger.warn(
      `Disparo de checagem de pagamento falhou para orcamento ${resolvedQuote.id}: ${err instanceof Error ? err.message : String(err)}`,
    );
  });
}
```

**Em `enviarParaCliente`**: no início do método, após buscar o orçamento (`const quote = await this.findQuoteByIdentifier(quoteId)`), adicionar disparo fire-and-forget similar:

```typescript
if (["PENDENTE", "ENVIADO"].includes(quote.status)) {
  void this.checkPaymentStatus(quote.id).catch((err: unknown) => {
    this.logger.warn(
      `Disparo de checagem de pagamento falhou ao enviar orcamento ${quote.id}: ${err instanceof Error ? err.message : String(err)}`,
    );
  });
}
```

O disparo deve ser fire-and-forget (`void ... .catch(...)`) para não bloquear nem propagar erros.
  </action>
  <acceptance_criteria>
    - `quotes.service.ts` contém ao menos dois `void this.checkPaymentStatus(` (um em getById, outro em enviarParaCliente)
    - Cada disparo tem `.catch(` imediatamente para swallow de erros
    - Os disparos verificam `["PENDENTE", "ENVIADO"].includes(` antes de chamar
  </acceptance_criteria>
  <verify>
    <automated>
      Select-String -Path "apps\backend\src\modules\quotes\quotes.service.ts" -Pattern "void this.checkPaymentStatus|conciliacao_athos|PENDENTE.*ENVIADO"
    </automated>
  </verify>
</task>

</tasks>
