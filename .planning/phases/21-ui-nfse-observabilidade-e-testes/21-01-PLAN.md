---
phase: 21-ui-nfse-observabilidade-e-testes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/app/api/athos/clientes/route.ts
  - apps/frontend/src/app/orcamento/[id]/page.tsx
autonomous: true
requirements:
  - NFUI-01
  - NFUI-02
  - NFUI-03
  - QUAL-03

must_haves:
  truths:
    - "Usuário pode digitar nome ou documento no modal NFS-e e clicar em Buscar para ver lista de clientes Athos"
    - "Clicar em Selecionar preenche documento, nome e limpa campos de endereço com preview do que será usado"
    - "Ao emitir, o body enviado ao backend inclui clienteAthosId quando um cliente foi selecionado"
    - "Rota proxy /api/athos/clientes passa x-internal-api-key e x-api-token ao backend"
  artifacts:
    - path: "apps/frontend/src/app/api/athos/clientes/route.ts"
      provides: "Next.js Route Handler que proxia GET /athos/clientes do backend"
      exports: ["GET"]
    - path: "apps/frontend/src/app/orcamento/[id]/page.tsx"
      provides: "Modal NFS-e com busca e seleção de cliente Athos"
      contains: "nfseClienteAthosSelecionado"
  key_links:
    - from: "apps/frontend/src/app/orcamento/[id]/page.tsx"
      to: "/api/athos/clientes"
      via: "fetch na função searchAthosClientes"
      pattern: "api/athos/clientes"
    - from: "apps/frontend/src/app/api/athos/clientes/route.ts"
      to: "backendFetch /athos/clientes"
      via: "backendFetch com x-api-token"
      pattern: "backendFetch.*athos/clientes"
    - from: "apps/frontend/src/app/orcamento/[id]/page.tsx"
      to: "handleEmitirNfse body"
      via: "clienteAthosId = nfseClienteAthosSelecionado"
      pattern: "clienteAthosId"
---

<objective>
Adicionar busca e seleção de cliente Athos ao modal de emissão NFS-e no frontend.

Purpose: Permitir que o operador selecione o tomador diretamente pelo sistema Athos, eliminando preenchimento manual propenso a erro (NFUI-01, NFUI-02, NFUI-03). A rota proxy garante compatibilidade com o guard x-internal-api-key e repassa ATHOS_API_TOKEN (QUAL-03).

Output: 1 arquivo novo (rota proxy) + modal atualizado com busca e seleção de cliente.
</objective>

<execution_context>
@C:/Users/Use/.copilot/get-shit-done/workflows/execute-plan.md
@C:/Users/Use/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- backendFetch (apps/frontend/src/lib/backend-client.ts) -->
```typescript
// Adiciona x-internal-api-key automaticamente. Aceita headers extras via init.headers.
export async function backendFetch(path: string, init?: RequestInit): Promise<Response>;
```

<!-- GET /athos/clientes — resposta do backend (apps/backend/src/modules/integrations/athos/athos.service.ts) -->
```typescript
interface BuscarClientesResult {
  total: number;
  page: number;
  take: number;
  items: Array<{
    idcliente: number;
    tipoPessoa: "fisico" | "juridico";
    nome: string;
    documento: string | null;
    endereco: {
      logradouro: string;
      numero: string;
      bairro: string;
      cep: string;
      codigoMunicipio: string;
      uf: string;
    } | null;
  }>;
}
// Protegido por: global InternalAuthGuard (x-internal-api-key) + manual ATHOS_API_TOKEN via x-api-token (se configurado)
```

<!-- handleEmitirNfse (apps/frontend/src/app/orcamento/[id]/page.tsx) — trecho relevante atual -->
```typescript
async function handleEmitirNfse() {
  setNfseModal(false);
  setNfseState("emitindo");
  const body: Record<string, string | number | boolean> = { servicoCodigo: nfseServico };
  const docLimpo = nfseTomadorDoc.replace(/\D/g, "");
  if (docLimpo) {
    if (nfseTomadorTipo === "cnpj") body.tomadorCnpj = docLimpo;
    else body.tomadorCpf = docLimpo;
  }
  // ... demais campos de endereço ...
  const res = await fetch(`/api/quotes/${encodeURIComponent(quoteId)}/nfse`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  });
}
// ADICIONAR: if (nfseClienteAthosSelecionado) body.clienteAthosId = nfseClienteAthosSelecionado;
// antes do fetch de emissão.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar rota proxy /api/athos/clientes no frontend</name>
  <files>apps/frontend/src/app/api/athos/clientes/route.ts</files>
  <action>
Criar arquivo `apps/frontend/src/app/api/athos/clientes/route.ts` como Next.js Route Handler:

```typescript
import { backendFetch } from "@/lib/backend-client";

export async function GET(req: Request) {
  const { searchParams } = new URL(req.url);
  const qs = searchParams.toString();

  const athosToken = process.env.ATHOS_API_TOKEN ?? "";
  const extraHeaders: Record<string, string> = athosToken
    ? { "x-api-token": athosToken }
    : {};

  try {
    const res = await backendFetch(`/athos/clientes${qs ? `?${qs}` : ""}`, {
      method: "GET",
      headers: extraHeaders,
    });
    const data = await res.json().catch(() => ({ error: "Resposta inválida do backend." }));
    if (!res.ok) return Response.json(data, { status: res.status });
    return Response.json(data);
  } catch {
    return Response.json({ error: "Falha ao conectar no backend." }, { status: 500 });
  }
}
```

Notas:
- `backendFetch` já adiciona `x-internal-api-key` automaticamente (QUAL-03)
- `x-api-token` é passado somente se `ATHOS_API_TOKEN` estiver definido no servidor frontend
- O handler repassa os query params recebidos do browser diretamente ao backend
  </action>
  <verify>
    <automated>cd apps/frontend; npx tsc --noEmit 2>&1 | Select-String "athos/clientes" || echo "TS OK"</automated>
  </verify>
  <done>Arquivo criado, sem erros TypeScript, GET handler exportado.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar busca e seleção de cliente Athos ao modal NFS-e</name>
  <files>apps/frontend/src/app/orcamento/[id]/page.tsx</files>
  <action>
Em `apps/frontend/src/app/orcamento/[id]/page.tsx`, aplicar as seguintes mudanças:

**1. Adicionar 5 novos estados** após o bloco de states existentes do NFS-e (após `nfseValorTotal`):

```typescript
const [nfseAthosQuery, setNfseAthosQuery] = useState("");
const [nfseAthosResults, setNfseAthosResults] = useState<Array<{
  idcliente: number;
  tipoPessoa: "fisico" | "juridico";
  nome: string;
  documento: string | null;
  endereco: { logradouro: string; numero: string; bairro: string; cep: string; codigoMunicipio: string; uf: string } | null;
}>>([]);
const [nfseAthosSearching, setNfseAthosSearching] = useState(false);
const [nfseClienteAthosSelecionado, setNfseClienteAthosSelecionado] = useState<number | null>(null);
const [nfseAthosError, setNfseAthosError] = useState("");
```

**2. Adicionar função `searchAthosClientes`** após `handleAbrirModalNfse`:

```typescript
async function searchAthosClientes() {
  const q = nfseAthosQuery.trim();
  if (!q) return;
  setNfseAthosSearching(true);
  setNfseAthosError("");
  setNfseAthosResults([]);
  try {
    const isDoc = /^\d{3,}$/.test(q.replace(/\D/g, "")) && q.replace(/\D/g, "").length >= 8;
    const param = isDoc ? `documento=${encodeURIComponent(q.replace(/\D/g, ""))}` : `nome=${encodeURIComponent(q)}`;
    const res = await fetch(`/api/athos/clientes?${param}&take=10`);
    const data = await res.json().catch(() => ({ error: "Resposta inválida." })) as {
      items?: typeof nfseAthosResults;
      error?: string;
    };
    if (!res.ok || data.error) {
      setNfseAthosError(data.error ?? "Erro ao buscar clientes.");
    } else {
      setNfseAthosResults(data.items ?? []);
      if ((data.items ?? []).length === 0) setNfseAthosError("Nenhum cliente encontrado.");
    }
  } catch {
    setNfseAthosError("Falha ao conectar ao backend.");
  } finally {
    setNfseAthosSearching(false);
  }
}

function selecionarClienteAthos(item: typeof nfseAthosResults[0]) {
  setNfseClienteAthosSelecionado(item.idcliente);
  setNfseTomadorTipo(item.tipoPessoa === "juridico" ? "cnpj" : "cpf");
  setNfseTomadorDoc(item.documento ?? "");
  setNfseTomadorNome(item.nome);
  // Preencher endereço se disponível
  setNfseTomadorEnderecoLogradouro(item.endereco?.logradouro ?? "");
  setNfseTomadorEnderecoNumero(item.endereco?.numero ?? "");
  setNfseTomadorEnderecoBairro(item.endereco?.bairro ?? "");
  setNfseTomadorEnderecoCep(item.endereco?.cep ?? "");
  setNfseTomadorEnderecoCodigoMunicipio(item.endereco?.codigoMunicipio ?? "");
  setNfseTomadorEnderecoUf(item.endereco?.uf ?? "");
  setNfseAthosResults([]);
  setNfseAthosQuery("");
}
```

**3. Modificar `handleAbrirModalNfse`** — no `return` / reset ao fechar modal, limpar estados Athos. Na função existente adicionar antes do `setNfseModal(true)`:

```typescript
setNfseAthosQuery("");
setNfseAthosResults([]);
setNfseAthosSearching(false);
setNfseClienteAthosSelecionado(null);
setNfseAthosError("");
```

**4. Modificar `handleEmitirNfse`** — logo ANTES do `const res = await fetch(...)` de emissão, adicionar:

```typescript
if (nfseClienteAthosSelecionado != null) {
  body.clienteAthosId = nfseClienteAthosSelecionado;
}
```

**5. Adicionar seção de busca Athos no modal NFS-e** — inserir ANTES da `<div className="mb-3">` do "Documento do tomador" (primeira div do modal):

```tsx
{/* Busca cliente Athos */}
<div className="mb-3">
  <label className="form-label fw-semibold">Buscar cliente Athos</label>
  {nfseClienteAthosSelecionado ? (
    <div className="alert alert-success py-2 d-flex justify-content-between align-items-center">
      <span>
        <i className="bi bi-person-check me-2" />
        <strong>{nfseTomadorNome}</strong>
        {nfseTomadorDoc ? ` — ${nfseTomadorDoc}` : ""}
        <small className="text-muted ms-2">(id {nfseClienteAthosSelecionado})</small>
      </span>
      <button
        type="button"
        className="btn-close btn-sm"
        aria-label="Remover seleção"
        onClick={() => {
          setNfseClienteAthosSelecionado(null);
          setNfseTomadorDoc("");
          setNfseTomadorNome("");
          setNfseAthosResults([]);
          setNfseAthosQuery("");
          setNfseAthosError("");
        }}
      />
    </div>
  ) : (
    <>
      <div className="input-group mb-1">
        <input
          className="form-control"
          placeholder="Nome ou CPF/CNPJ"
          value={nfseAthosQuery}
          onChange={e => setNfseAthosQuery(e.target.value)}
          onKeyDown={e => { if (e.key === "Enter") { e.preventDefault(); void searchAthosClientes(); } }}
        />
        <button
          type="button"
          className="btn btn-outline-secondary"
          onClick={() => void searchAthosClientes()}
          disabled={nfseAthosSearching || !nfseAthosQuery.trim()}
        >
          {nfseAthosSearching
            ? <span className="spinner-border spinner-border-sm" role="status" />
            : <i className="bi bi-search" />}
        </button>
      </div>
      {nfseAthosError && <div className="text-danger small">{nfseAthosError}</div>}
      {nfseAthosResults.length > 0 && (
        <ul className="list-group list-group-flush border rounded" style={{ maxHeight: 180, overflowY: "auto" }}>
          {nfseAthosResults.map(item => (
            <li key={item.idcliente} className="list-group-item list-group-item-action py-2 px-3 d-flex justify-content-between align-items-center">
              <span>
                <span className={`badge me-2 ${item.tipoPessoa === "juridico" ? "bg-info text-dark" : "bg-secondary"}`}>
                  {item.tipoPessoa === "juridico" ? "PJ" : "PF"}
                </span>
                <strong>{item.nome}</strong>
                {item.documento ? <small className="text-muted ms-2">{item.documento}</small> : null}
              </span>
              <button
                type="button"
                className="btn btn-sm btn-outline-primary"
                onClick={() => selecionarClienteAthos(item)}
              >
                Selecionar
              </button>
            </li>
          ))}
        </ul>
      )}
    </>
  )}
</div>
```

**6. Atualizar o alerta informativo** no modal (o que diz "Se o tomador não estiver associado no Athos...") para refletir a nova opção:
Substituir:
`Se o tomador não estiver associado no Athos, preencha documento e endereço manualmente para emitir a NFS-e.`
Por:
`Use a busca acima para selecionar um cliente Athos. Se preferir, preencha documento e endereço manualmente.`
  </action>
  <verify>
    <automated>npm --workspace @bomcusto/frontend run build 2>&1 | Select-String "error TS|Error:|Failed" | Select-Object -First 10 || echo "build OK"</automated>
  </verify>
  <done>
- Modal NFS-e exibe campo de busca Athos com input + botão Buscar
- Ao selecionar cliente: doc/nome/tipo/endereço preenchidos, badge de confirmação exibido
- `handleEmitirNfse` inclui `clienteAthosId` no body quando cliente selecionado
- Build frontend sem erros TypeScript
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| browser → frontend Route Handler | Input de busca não sanitizado (nome/documento) |
| frontend Route Handler → backend NestJS | ATHOS_API_TOKEN em env server-side, não exposto ao browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | /api/athos/clientes route | mitigate | ATHOS_API_TOKEN lido server-side em `process.env`, nunca enviado ao browser |
| T-21-02 | Tampering | busca Athos — query params | accept | Backend já valida: nome ≥3 chars, documento numérico, take max 50; frontend só repassa params |
| T-21-03 | Elevation of Privilege | proxy route sem autenticação de usuário | accept | InternalAuthGuard protege o backend; rota proxy só acessível server-side via backendFetch |
</threat_model>

<verification>
1. `npm --workspace @bomcusto/frontend run build` — sem erros TypeScript
2. Abrir modal NFS-e → campo "Buscar cliente Athos" visível acima do campo de documento
3. Digitar nome e clicar buscar → lista de resultados exibida (ou erro "Nenhum cliente encontrado")
4. Clicar Selecionar → badge verde com nome/doc, campos de documento e endereço preenchidos
5. Clicar Emitir NFS-e → request POST /api/quotes/:id/nfse contém `clienteAthosId` no body
6. Clicar X no badge → seleção limpa, campos voltam ao estado manual
</verification>

<success_criteria>
- Arquivo `apps/frontend/src/app/api/athos/clientes/route.ts` criado e exportando `GET`
- Modal de emissão NFS-e inclui seção de busca Athos funcional (NFUI-01)
- Seleção de cliente preenche preview do tomador (NFUI-02)
- Body de emissão inclui `clienteAthosId` quando cliente selecionado (NFUI-03)
- Proxy passa `x-internal-api-key` (via backendFetch) e `x-api-token` (ATHOS_API_TOKEN) (QUAL-03)
- Build frontend sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/21-ui-nfse-observabilidade-e-testes/21-01-SUMMARY.md`
</output>
