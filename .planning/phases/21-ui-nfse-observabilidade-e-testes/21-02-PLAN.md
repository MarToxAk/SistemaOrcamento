---
phase: 21-ui-nfse-observabilidade-e-testes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/nfse/nfse.service.ts
  - apps/backend/src/modules/integrations/athos/athos.service.ts
  - apps/backend/src/modules/integrations/nfse/nfse.service.test.ts
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02

must_haves:
  truths:
    - "Logs estruturados registram qual caminho de resolução foi usado (A/B/C), com clienteAthosId, nome e fallback"
    - "Logs de buscarClientes registram os filtros aplicados e total de resultados"
    - "Teste unitário cobre cenário PF (CPF) além do PJ já existente"
  artifacts:
    - path: "apps/backend/src/modules/integrations/nfse/nfse.service.ts"
      provides: "Logs estruturados na função emitir() para caminhos A, B e C"
      contains: "Tomador-A"
    - path: "apps/backend/src/modules/integrations/athos/athos.service.ts"
      provides: "Log estruturado em buscarClientes com filtros e resultado"
      contains: "Athos-busca"
    - path: "apps/backend/src/modules/integrations/nfse/nfse.service.test.ts"
      provides: "Teste PF (CPF) adicionado cobrindo QUAL-02"
      contains: "clienteAthosId PF"
  key_links:
    - from: "nfse.service.ts emitir()"
      to: "this.logger.log"
      via: "log após decisão de caminho A/B/C"
      pattern: "Tomador-A|Tomador-B|Tomador-C"
    - from: "athos.service.ts buscarClientes()"
      to: "this.logger.log"
      via: "log após execução da query"
      pattern: "Athos-busca"
---

<objective>
Adicionar logs estruturados de observabilidade ao fluxo NFS-e e teste unitário para o cenário PF.

Purpose: QUAL-01 exige rastreabilidade operacional do caminho de resolução do tomador e critério de busca. QUAL-02 exige cobertura de testes para PF, PJ, sem endereço, sem documento e cliente não encontrado — o cenário PF ainda não tinha teste explícito após a fase 20.

Output: logs estruturados em nfse.service.ts e athos.service.ts + 1 novo teste PF em nfse.service.test.ts.
</objective>

<execution_context>
@C:/Users/Use/.copilot/get-shit-done/workflows/execute-plan.md
@C:/Users/Use/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/backend/src/modules/integrations/nfse/nfse.service.test.ts

<interfaces>
<!-- Estrutura do emitir() — caminhos existentes (nfse.service.ts linhas 538-600) -->
```typescript
// Caminho A — clienteAthosId explícito
if (input?.clienteAthosId != null && Number.isFinite(input.clienteAthosId) && input.clienteAthosId > 0) {
  const info = await this.athosService.buscarClientePorId(input.clienteAthosId);
  // ...preenche tomadorCnpj/tomadorCpf/tomadorNome/tomadorEndereco...
  // LOG JÁ EXISTE (linha 563): this.logger.log(`[Tomador] resolvido via clienteAthosId=...`)
  // → MANTER log existente; ADICIONAR prefixo "[Tomador-A]" e incluir nome
} else if (...condição caminho C) {
  // lookup completo via orçamento
  // → ADICIONAR: this.logger.log(`[Tomador-C] quoteId=${quoteId} lookup completo via orçamento`)
} else {
  // Caminho B — documento manual informado
  // → ADICIONAR: this.logger.log(`[Tomador-B] documento manual, lookup endereço via orçamento se ausente`)
}
```

<!-- buscarClientes retorno (athos.service.ts linha 706) -->
```typescript
// No final do try{} de buscarClientes, antes do return { total, page, take, items }:
// → ADICIONAR:
// this.logger.log(`[Athos-busca] nome="${nomeFilter??''}" doc="${documentoFilter??''}" idcliente=${idclienteFilter??''} → ${total} resultado(s) page=${page} take=${take}`);
```

<!-- Cenários cobertos em nfse.service.test.ts (fase 20) -->
// 1. clienteAthosId PJ válido → SOAP OK  ✓
// 2. clienteAthosId não encontrado → BadRequest  ✓
// 3. sem documento → BadRequest  ✓
// 4. sem endereço → BadRequest  ✓
// 5. sem clienteAthosId → legado, buscarClientePorId não chamado  ✓
// FALTANDO: 6. clienteAthosId PF → CPF no XML SOAP  (QUAL-02)

<!-- Mock pattern de buscarClientePorId (nfse.service.test.ts) -->
```typescript
jest.spyOn(service as any, "buscarClientePorId").mockResolvedValue({
  type: "juridico",
  documento: "12345678000195",
  nome: "Empresa Teste LTDA",
  endereco: { logradouro: "Rua X", numero: "1", bairro: "Centro", cep: "01001000", codigoMunicipio: "3550308", uf: "SP" },
});
// Para PF: type: "fisico", documento: "12345678901", nome: "João Silva"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar logs estruturados ao emitir() e buscarClientes()</name>
  <files>
    apps/backend/src/modules/integrations/nfse/nfse.service.ts,
    apps/backend/src/modules/integrations/athos/athos.service.ts
  </files>
  <behavior>
    - Log "[Tomador-A]" após resolução via clienteAthosId: inclui id, tipo, doc (mascarado) e nome
    - Log "[Tomador-B]" quando documento manual informado (caminho B)
    - Log "[Tomador-C]" quando nenhum clienteAthosId e nenhum documento manual
    - Log "[Athos-busca]" em buscarClientes() com nome/doc/idcliente usados e total de resultados
    - Nenhum log vaza conteúdo sensível completo (CPF/CNPJ mascarado nos logs do caminho A)
  </behavior>
  <action>
**Em nfse.service.ts:**

1. Localizar a linha que já tem `this.logger.log(`[Tomador] resolvido via clienteAthosId=...`)` (aprox. linha 563).
   Substituir por:
   ```typescript
   this.logger.log(
     `[Tomador-A] clienteAthosId=${input.clienteAthosId} tipo=${info.type} nome="${info.nome ?? "?"}" doc=${info.documento ? info.documento.slice(0, 4) + "****" : "null"}`,
   );
   ```

2. Após o bloco `else if` que é o Caminho C (lookup via orçamento), logo após o comentário `// Caminho C`, adicionar:
   ```typescript
   this.logger.log(`[Tomador-C] quoteId=${quoteId} — lookup completo de tomador via orçamento (sem clienteAthosId e sem documento manual)`);
   ```

3. Após o bloco `else` que é o Caminho B (documento manual), logo após o comentário `// Caminho B`, adicionar:
   ```typescript
   this.logger.log(`[Tomador-B] quoteId=${quoteId} — documento manual informado; buscando endereço via Athos/orçamento se ausente`);
   ```

**Em athos.service.ts (buscarClientes):**

4. Antes do `return { total, page, take, items }` no bloco `try{}`, adicionar:
   ```typescript
   this.logger.log(
     `[Athos-busca] nome="${nomeFilter ?? ""}" doc="${documentoFilter ?? ""}" idcliente=${idclienteFilter ?? ""} → ${total} resultado(s) page=${page} take=${take}`,
   );
   ```
   Verificar que `this.logger` já existe em AthosService (se não, adicionar `private readonly logger = new Logger(AthosService.name)` e importar `Logger` do `@nestjs/common`).
  </action>
  <verify>
    <automated>npm --workspace @bomcusto/backend run build 2>&1 | Select-String "error TS|Error:" | Select-Object -First 5 || echo "build OK"</automated>
  </verify>
  <done>
- nfse.service.ts contém strings "[Tomador-A]", "[Tomador-B]", "[Tomador-C]"
- athos.service.ts contém string "[Athos-busca]"
- Build sem erros TypeScript
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar teste unitário PF (CPF) em nfse.service.test.ts</name>
  <files>apps/backend/src/modules/integrations/nfse/nfse.service.test.ts</files>
  <behavior>
    - clienteAthosId aponta para cliente PF: buscarClientePorId retorna type="fisico", documento="12345678901"
    - handleEmitirNfse chama enviarSoap com XML contendo o CPF do tomador
    - Teste passa sem alterar os 5 cenários existentes
  </behavior>
  <action>
Abrir `apps/backend/src/modules/integrations/nfse/nfse.service.test.ts`.

No `describe` principal, adicionar APÓS o último `it(...)` existente (cenário 5), um novo caso:

```typescript
it("cenário 6: clienteAthosId PF — CPF do tomador presente no XML enviado ao SOAP", async () => {
  jest.spyOn(service as any, "buscarClientePorId").mockResolvedValueOnce({
    type: "fisico",
    documento: "12345678901",
    nome: "João Silva",
    endereco: {
      logradouro: "Rua das Flores",
      numero: "10",
      bairro: "Centro",
      cep: "01001000",
      codigoMunicipio: "3550308",
      uf: "SP",
    },
  });

  const soapSpy = jest
    .spyOn(service as any, "enviarSoap")
    .mockResolvedValueOnce(
      `<EnviarLoteRpsResponse><NumeroNfse>1002</NumeroNfse></EnviarLoteRpsResponse>`,
    );

  jest.spyOn(service as any, "getInfoNfse").mockResolvedValueOnce(null);

  await service.emitir("quote-pf-test", { clienteAthosId: 42, servicoCodigo: "24.01" });

  expect(soapSpy).toHaveBeenCalledTimes(1);
  const xmlSent: string = soapSpy.mock.calls[0][0] as string;
  // CPF deve estar presente no XML (sem pontuação)
  expect(xmlSent).toMatch(/12345678901/);
  // CNPJ não deve aparecer (é PF)
  expect(xmlSent).not.toMatch(/<CNPJ>12345678901<\/CNPJ>/);
});
```

Observações:
- O mock de `buscarClientePorId` usa `mockResolvedValueOnce` para não interferir com outros testes
- `getInfoNfse` mockado para evitar chamada HTTP real
- `soapSpy.mock.calls[0][0]` acessa o primeiro argumento da primeira chamada (o XML gerado)
  </action>
  <verify>
    <automated>npm --workspace @bomcusto/backend test -- --testPathPatterns="nfse.service.test" --no-coverage 2>&1 | Select-String "PASS|FAIL|Tests:|passed|failed" | Select-Object -First 5</automated>
  </verify>
  <done>
- 6 testes passando em nfse.service.test.ts (5 existentes + 1 novo PF)
- Cenário 6 verifica que CPF "12345678901" está no XML enviado ao SOAP
- QUAL-02 coberto: PF, PJ, sem endereço, sem documento, cliente não encontrado, sem clienteAthosId
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| logs NfseService → stdout/syslog | CPF/CNPJ parcialmente visível nos logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Information Disclosure | logs "[Tomador-A]" | mitigate | Mascarar doc: apenas primeiros 4 chars + "****" no log; nunca logar token completo |
| T-21-05 | Information Disclosure | logs "[Athos-busca]" | accept | Filtro de busca (nome/doc parcial) é dado de entrada do operador — baixo risco; não loga resultados completos |
</threat_model>

<verification>
1. `npm --workspace @bomcusto/backend run build` — sem erros TypeScript
2. `npm --workspace @bomcusto/backend test -- --testPathPatterns="nfse.service.test"` — 6/6 passando
3. Grep confirma strings de log: `grep -r "Tomador-A\|Tomador-B\|Tomador-C\|Athos-busca" apps/backend/src/`
</verification>

<success_criteria>
- nfse.service.ts contém logs "[Tomador-A]", "[Tomador-B]", "[Tomador-C]" (QUAL-01)
- athos.service.ts contém log "[Athos-busca]" com filtros e total (QUAL-01)
- nfse.service.test.ts tem 6 testes passando, incluindo cenário PF (QUAL-02)
- CPF mascarado no log de Caminho A (segurança)
- Build backend sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/21-ui-nfse-observabilidade-e-testes/21-02-SUMMARY.md`
</output>
