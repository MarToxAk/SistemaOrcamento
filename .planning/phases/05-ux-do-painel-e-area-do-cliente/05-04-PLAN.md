---
phase: 05-ux-do-painel-e-area-do-cliente
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/app/api/quotes/[id]/approve/route.ts
  - apps/frontend/src/app/orcamento/[id]/approve/page.tsx
  - apps/frontend/src/app/orcamento/[id]/status/page.tsx
autonomous: true
requirements:
  - FR-04.1
  - FR-04.2
  - FR-04.3

must_haves:
  truths:
    - "Cliente acessa /orcamento/:id/approve?token=TOKEN e vê resumo do orçamento"
    - "Botão 'Aprovar Orçamento' só dispara POST ao clicar — nunca auto-executa"
    - "Após aprovação, cliente vê mensagem de confirmação"
    - "Token inválido/expirado exibe mensagem específica"
    - "Orçamento já aprovado exibe mensagem específica"
    - "Cliente acessa /orcamento/:id/status e vê status atual sem autenticação"
    - "Ambas as páginas são mobile-friendly com layout de card centrado"
  artifacts:
    - path: "apps/frontend/src/app/api/quotes/[id]/approve/route.ts"
      provides: "Proxy POST /api/quotes/:id/approve → backend com x-internal-api-key"
      contains: "backendFetch"
    - path: "apps/frontend/src/app/orcamento/[id]/approve/page.tsx"
      provides: "Página pública de aprovação pelo cliente"
      contains: "Aprovar Orçamento"
    - path: "apps/frontend/src/app/orcamento/[id]/status/page.tsx"
      provides: "Página pública de status para o cliente"
      contains: "Status do Orçamento"
  key_links:
    - from: "apps/frontend/src/app/orcamento/[id]/approve/page.tsx"
      to: "/api/quotes/[id]/approve"
      via: "POST fetch no button click"
      pattern: "approve"
    - from: "apps/frontend/src/app/orcamento/[id]/status/page.tsx"
      to: "/api/quotes/[id]"
      via: "GET fetch no useEffect"
      pattern: "statusKey"
---

<objective>
Criar páginas públicas para o cliente: aprovação de orçamento via token e visualização de status.

Purpose: FR-04.1, FR-04.2, FR-04.3 — clientes precisam de uma UX adequada para aprovar e acompanhar orçamentos no mobile.
Output: Proxy route + 2 páginas Next.js novas com layout mobile-first Bootstrap 5, estados de loading/success/error.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-ux-do-painel-e-area-do-cliente/05-UI-SPEC.md
@.planning/phases/05-ux-do-painel-e-area-do-cliente/05-RESEARCH.md

<interfaces>
<!-- Backend endpoint existente (verificado) -->

POST /api/quotes/:id/approve?token=XXX
- Retorna 200: { approved: true, quoteId: string, status: string }
- Retorna 400: { message: "Token de aprovacao invalido" }
- Retorna 400: { message: "Token expirado" }
- Retorna 400: { message: "Token de aprovacao nao encontrado" }
- Retorna 400: { message: "Orçamento já aprovado anteriormente." } (ou similar)
- Retorna 404: NotFoundException

GET /api/quotes/:id
- Retorna: { id, statusKey, statusLabel, body: { cliente: { nome }, totais: { valor }, prazoEntrega, ... } }

<!-- Proxy pattern existente -->
// apps/frontend/src/app/api/quotes/[id]/route.ts
import { backendFetch } from "@/lib/backend-client";
export async function GET(_req: Request, { params }: { params: { id: string } }) {
  const res = await backendFetch(`/quotes/${encodeURIComponent(params.id)}`, { method: "GET" });
  const data = await res.json().catch(() => ({ error: "Resposta invalida do backend." }));
  if (!res.ok) return Response.json(data, { status: res.status });
  return Response.json(data, { status: 200 });
}

<!-- UI Design contract (UI-SPEC) -->
- Background body: #f9f7ed
- Header gradient: linear-gradient(135deg,#c5f2e8 0%,#cbe1f9 25%,#e7d8f9 50%,#f9e7f5 75%,#f0cacb 100%)
- Approve button: backgroundColor: "#7dc8aa", hover: "#6ab594"
- Card: className="card shadow-sm border-0 rounded-3"
- Container: className="container mt-4 mb-5"
- Col: className="col-12 col-sm-10 col-md-8 col-lg-6"
- Logo: /media/logo_new.svg, maxWidth:120px

<!-- Copywriting (da UI-SPEC) -->
Approval page:
- title: "Aprovar Orçamento — Bom Custo"
- heading: "Confirmar Aprovação do Orçamento"
- pre-approve instruction: "Revise os detalhes abaixo e clique em Aprovar para confirmar."
- CTA: "Aprovar Orçamento" (idle), "Aprovando..." (loading)
- success heading: "Orçamento Aprovado!"
- success body: "Recebemos sua aprovação. Em breve nossa equipe entrará em contato para confirmar os detalhes da produção."
- contact: "Dúvidas? Nos chame pelo WhatsApp: (12) 99648-4918"
- error generic: "Não foi possível processar sua aprovação. Por favor, tente novamente ou entre em contato: (12) 99648-4918"
- error token inválido: "Link inválido ou expirado. Por favor, solicite um novo link de aprovação pelo WhatsApp: (12) 99648-4918"
- error já aprovado: "Este orçamento já foi aprovado anteriormente. Obrigado!"
- no token: "Link inválido. Por favor, use o link recebido pelo WhatsApp ou entre em contato: (12) 99648-4918"

Status page:
- title: "Status do Orçamento — Bom Custo"
- heading: "Status do Orçamento"
- loading: "Carregando..."
- not found: "Não foi possível localizar este orçamento. Verifique o link ou entre em contato: (12) 99648-4918"
- contact footer: "Dúvidas? WhatsApp: (12) 99648-4918 — E-mail: orcamento@bomcustoilhabela.com.br"

Status pills (mesmos valores de status/page.tsx):
- aprovado: bg #e9f8ef, color #1f7a44
- em_producao: bg #eef4ff, color #2457a6
- pronto_para_entrega: bg #fff5e8, color #a65b12
- entregue: bg #ececec, color #444
- cancelado: bg #fdecec, color #b42318
- pendente: bg #fff8e1, color #f39c12
- enviado: bg #e8f4fd, color #1565c0
- recusado: bg #fdecec, color #b42318
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar proxy route + página de aprovação pelo cliente</name>
  <files>apps/frontend/src/app/api/quotes/[id]/approve/route.ts, apps/frontend/src/app/orcamento/[id]/approve/page.tsx</files>
  <action>
**Arquivo 1: `apps/frontend/src/app/api/quotes/[id]/approve/route.ts`**

Criar proxy POST que injeta a internal API key e encaminha o token ao backend:
```typescript
import { backendFetch } from "@/lib/backend-client";

export async function POST(
  req: Request,
  { params }: { params: { id: string } },
) {
  const id = params.id?.trim();
  if (!id) return Response.json({ error: "Id do orcamento nao informado." }, { status: 400 });

  const url = new URL(req.url);
  const token = url.searchParams.get("token") ?? "";

  try {
    const res = await backendFetch(
      `/quotes/${encodeURIComponent(id)}/approve?token=${encodeURIComponent(token)}`,
      { method: "POST" },
    );
    const data = await res.json().catch(() => ({ error: "Resposta invalida do backend." }));
    if (!res.ok) return Response.json(data, { status: res.status });
    return Response.json(data, { status: 200 });
  } catch {
    return Response.json({ error: "Falha ao conectar no backend." }, { status: 500 });
  }
}
```

---

**Arquivo 2: `apps/frontend/src/app/orcamento/[id]/approve/page.tsx`**

Criar página pública de aprovação. A estrutura completa:

```tsx
"use client";

import Script from "next/script";
import { useEffect, useState } from "react";
import { useParams, useSearchParams } from "next/navigation";

type ApproveState = "loading-quote" | "idle" | "submitting" | "success" | "error" | "no-token";

type QuoteInfo = {
  number?: number | string;
  clienteNome?: string;
  total?: number;
  prazoEntrega?: string;
  statusLabel?: string;
  statusKey?: string;
};

const STATUS_LABELS: Record<string, string> = {
  pendente: "Pendente",
  enviado: "Enviado",
  aprovado: "Aprovado",
  em_producao: "Em Produção",
  pronto_para_entrega: "Pronto para Entrega",
  entregue: "Entregue",
  cancelado: "Cancelado",
};

const STATUS_STYLES: Record<string, { backgroundColor: string; color: string }> = {
  aprovado: { backgroundColor: "#e9f8ef", color: "#1f7a44" },
  em_producao: { backgroundColor: "#eef4ff", color: "#2457a6" },
  pronto_para_entrega: { backgroundColor: "#fff5e8", color: "#a65b12" },
  entregue: { backgroundColor: "#ececec", color: "#444" },
  cancelado: { backgroundColor: "#fdecec", color: "#b42318" },
  pendente: { backgroundColor: "#fff8e1", color: "#f39c12" },
  enviado: { backgroundColor: "#e8f4fd", color: "#1565c0" },
};

export default function AprovacaoPage() {
  const params = useParams();
  const searchParams = useSearchParams();
  const id = String(params.id ?? "");
  const token = searchParams.get("token") ?? "";

  const [state, setState] = useState<ApproveState>("loading-quote");
  const [quoteInfo, setQuoteInfo] = useState<QuoteInfo>({});
  const [errorMessage, setErrorMessage] = useState("");

  useEffect(() => {
    if (!token) {
      setState("no-token");
      return;
    }

    const fetchQuote = async () => {
      try {
        const res = await fetch(`/api/quotes/${encodeURIComponent(id)}`, { cache: "no-store" });
        if (!res.ok) {
          setState("error");
          setErrorMessage("Não foi possível carregar os detalhes do orçamento.");
          return;
        }
        const data = (await res.json()) as any;
        setQuoteInfo({
          number: data.body?.idorcamento ?? data.body?.idorcamento_interno,
          clienteNome: data.body?.cliente?.nome,
          total: data.body?.totais?.valor,
          prazoEntrega: data.body?.prazoEntrega,
          statusLabel: data.statusLabel,
          statusKey: data.statusKey,
        });
        setState("idle");
      } catch {
        setState("error");
        setErrorMessage("Não foi possível carregar os detalhes do orçamento.");
      }
    };

    void fetchQuote();
  }, [id, token]);

  async function handleApprove() {
    setState("submitting");
    try {
      const res = await fetch(
        `/api/quotes/${encodeURIComponent(id)}/approve?token=${encodeURIComponent(token)}`,
        { method: "POST" },
      );
      const data = (await res.json().catch(() => ({}))) as { message?: string; error?: string };
      if (!res.ok) {
        const msg = data?.message ?? data?.error ?? "";
        if (msg.toLowerCase().includes("já aprovado") || msg.toLowerCase().includes("ja aprovado")) {
          setErrorMessage("Este orçamento já foi aprovado anteriormente. Obrigado!");
        } else if (
          msg.toLowerCase().includes("token") ||
          msg.toLowerCase().includes("invalido") ||
          msg.toLowerCase().includes("expirado")
        ) {
          setErrorMessage(
            "Link inválido ou expirado. Por favor, solicite um novo link de aprovação pelo WhatsApp: (12) 99648-4918",
          );
        } else {
          setErrorMessage(
            "Não foi possível processar sua aprovação. Por favor, tente novamente ou entre em contato: (12) 99648-4918",
          );
        }
        setState("error");
        return;
      }
      setState("success");
    } catch {
      setErrorMessage(
        "Não foi possível processar sua aprovação. Por favor, tente novamente ou entre em contato: (12) 99648-4918",
      );
      setState("error");
    }
  }

  const HEADER_GRADIENT =
    "linear-gradient(135deg,#c5f2e8 0%,#cbe1f9 25%,#e7d8f9 50%,#f9e7f5 75%,#f0cacb 100%)";

  return (
    <>
      <Script
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        strategy="beforeInteractive"
      />
      <link
        rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      />
      <link
        rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css"
      />
      <title>Aprovar Orçamento — Bom Custo</title>
      <div style={{ backgroundColor: "#f9f7ed", minHeight: "100vh" }}>
        <div className="container mt-4 mb-5">
          <div className="row justify-content-center">
            <div className="col-12 col-sm-10 col-md-8 col-lg-6">
              <div className="card shadow-sm border-0 rounded-3">
                {/* Header */}
                <div
                  className="card-header py-3 px-3 rounded-top"
                  style={{ background: HEADER_GRADIENT }}
                >
                  <div className="d-flex align-items-center gap-2">
                    <img
                      src="/media/logo_new.svg"
                      alt="Bom Custo"
                      style={{ maxWidth: 120, maxHeight: 60, background: "#fff", borderRadius: 6, padding: 4 }}
                    />
                    <div className="small text-dark">
                      <div className="fw-semibold">Bom Custo Papelaria & Gráfica Rápida</div>
                      <div>Ilhabela - SP</div>
                    </div>
                  </div>
                </div>

                {/* Body */}
                <div className="card-body p-4">
                  {state === "loading-quote" && (
                    <div className="text-center py-3">
                      <div className="spinner-border text-secondary" role="status">
                        <span className="visually-hidden">Carregando...</span>
                      </div>
                    </div>
                  )}

                  {state === "no-token" && (
                    <div className="alert alert-warning mb-0" role="alert">
                      Link inválido. Por favor, use o link recebido pelo WhatsApp ou entre em
                      contato: (12) 99648-4918
                    </div>
                  )}

                  {state === "idle" && (
                    <>
                      <h2 className="fw-semibold mb-2" style={{ fontSize: "1.1rem" }}>
                        Confirmar Aprovação do Orçamento
                      </h2>
                      <p className="text-muted small mb-3">
                        Revise os detalhes abaixo e clique em <strong>Aprovar</strong> para confirmar.
                      </p>

                      <ul className="list-group list-group-flush mb-3">
                        {quoteInfo.number && (
                          <li className="list-group-item px-0">
                            <span className="text-muted small">Orçamento:</span>{" "}
                            <strong>#{quoteInfo.number}</strong>
                          </li>
                        )}
                        {quoteInfo.clienteNome && (
                          <li className="list-group-item px-0">
                            <span className="text-muted small">Cliente:</span>{" "}
                            {quoteInfo.clienteNome}
                          </li>
                        )}
                        {quoteInfo.total != null && (
                          <li className="list-group-item px-0">
                            <span className="text-muted small">Total:</span>{" "}
                            <strong>
                              {quoteInfo.total.toLocaleString("pt-BR", {
                                style: "currency",
                                currency: "BRL",
                              })}
                            </strong>
                          </li>
                        )}
                        {quoteInfo.prazoEntrega && (
                          <li className="list-group-item px-0">
                            <span className="text-muted small">Prazo de Entrega:</span>{" "}
                            {quoteInfo.prazoEntrega}
                          </li>
                        )}
                        {quoteInfo.statusKey && (
                          <li className="list-group-item px-0">
                            <span className="text-muted small">Status atual:</span>{" "}
                            <span
                              className="badge"
                              style={{
                                ...STATUS_STYLES[(quoteInfo.statusKey ?? "").toLowerCase()],
                                padding: "0.3rem 0.75rem",
                                fontSize: "0.88rem",
                              }}
                            >
                              {quoteInfo.statusLabel ?? quoteInfo.statusKey}
                            </span>
                          </li>
                        )}
                      </ul>

                      <hr />

                      <button
                        type="button"
                        className="btn btn-lg w-100 py-3 fw-semibold"
                        style={{ backgroundColor: "#7dc8aa", color: "#fff", border: "none" }}
                        onMouseEnter={(e) => {
                          (e.currentTarget as HTMLButtonElement).style.backgroundColor = "#6ab594";
                        }}
                        onMouseLeave={(e) => {
                          (e.currentTarget as HTMLButtonElement).style.backgroundColor = "#7dc8aa";
                        }}
                        onClick={handleApprove}
                      >
                        Aprovar Orçamento
                      </button>
                    </>
                  )}

                  {state === "submitting" && (
                    <div className="text-center py-3">
                      <div className="spinner-border spinner-border-sm me-2" role="status">
                        <span className="visually-hidden">Aprovando...</span>
                      </div>
                      Aprovando...
                    </div>
                  )}

                  {state === "success" && (
                    <div className="alert alert-success mb-0" role="alert">
                      <h5 className="alert-heading">Orçamento Aprovado!</h5>
                      <p className="mb-2">
                        Recebemos sua aprovação. Em breve nossa equipe entrará em contato para
                        confirmar os detalhes da produção.
                      </p>
                      <hr />
                      <p className="mb-0 small">
                        Dúvidas? Nos chame pelo WhatsApp: (12) 99648-4918
                      </p>
                    </div>
                  )}

                  {state === "error" && (
                    <div className="alert alert-danger mb-0" role="alert">
                      {errorMessage}
                    </div>
                  )}
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </>
  );
}
```
  </action>
  <verify>npm run --workspace @bomcusto/frontend build 2>&1 | tail -5</verify>
  <done>Build passa. Proxy route criado em `api/quotes/[id]/approve/route.ts`. Página de aprovação criada com todos os estados (loading-quote, idle, submitting, success, error, no-token).</done>
</task>

<task type="auto">
  <name>Task 2: Criar página de status para o cliente</name>
  <files>apps/frontend/src/app/orcamento/[id]/status/page.tsx</files>
  <action>
Criar `apps/frontend/src/app/orcamento/[id]/status/page.tsx`:

```tsx
"use client";

import Script from "next/script";
import { useEffect, useState } from "react";
import { useParams } from "next/navigation";

type StatusInfo = {
  statusKey?: string;
  statusLabel?: string;
  number?: number | string;
  clienteNome?: string;
  total?: number;
  prazoEntrega?: string;
};

const STATUS_STYLES: Record<string, { backgroundColor: string; color: string }> = {
  aprovado: { backgroundColor: "#e9f8ef", color: "#1f7a44" },
  em_producao: { backgroundColor: "#eef4ff", color: "#2457a6" },
  pronto_para_entrega: { backgroundColor: "#fff5e8", color: "#a65b12" },
  entregue: { backgroundColor: "#ececec", color: "#444" },
  cancelado: { backgroundColor: "#fdecec", color: "#b42318" },
  pendente: { backgroundColor: "#fff8e1", color: "#f39c12" },
  enviado: { backgroundColor: "#e8f4fd", color: "#1565c0" },
  recusado: { backgroundColor: "#fdecec", color: "#b42318" },
};

export default function StatusClientePage() {
  const params = useParams();
  const id = String(params.id ?? "");

  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");
  const [info, setInfo] = useState<StatusInfo>({});

  useEffect(() => {
    const fetchStatus = async () => {
      try {
        const res = await fetch(`/api/quotes/${encodeURIComponent(id)}`, { cache: "no-store" });
        if (!res.ok) {
          setError(
            "Não foi possível localizar este orçamento. Verifique o link ou entre em contato: (12) 99648-4918",
          );
          return;
        }
        const data = (await res.json()) as any;
        setInfo({
          statusKey: data.statusKey,
          statusLabel: data.statusLabel,
          number: data.body?.idorcamento ?? data.body?.idorcamento_interno,
          clienteNome: data.body?.cliente?.nome,
          total: data.body?.totais?.valor,
          prazoEntrega: data.body?.prazoEntrega,
        });
      } catch {
        setError(
          "Não foi possível localizar este orçamento. Verifique o link ou entre em contato: (12) 99648-4918",
        );
      } finally {
        setLoading(false);
      }
    };

    if (id) void fetchStatus();
  }, [id]);

  const HEADER_GRADIENT =
    "linear-gradient(135deg,#c5f2e8 0%,#cbe1f9 25%,#e7d8f9 50%,#f9e7f5 75%,#f0cacb 100%)";
  const statusStyleKey = (info.statusKey ?? "").toLowerCase();

  return (
    <>
      <Script
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        strategy="beforeInteractive"
      />
      <link
        rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      />
      <title>Status do Orçamento — Bom Custo</title>
      <div style={{ backgroundColor: "#f9f7ed", minHeight: "100vh" }}>
        <div className="container mt-4 mb-5">
          <div className="row justify-content-center">
            <div className="col-12 col-sm-10 col-md-8 col-lg-6">
              <div className="card shadow-sm border-0 rounded-3">
                {/* Header */}
                <div
                  className="card-header py-3 px-3 rounded-top"
                  style={{ background: HEADER_GRADIENT }}
                >
                  <div className="d-flex align-items-center gap-2">
                    <img
                      src="/media/logo_new.svg"
                      alt="Bom Custo"
                      style={{ maxWidth: 120, maxHeight: 60, background: "#fff", borderRadius: 6, padding: 4 }}
                    />
                    <div className="small text-dark">
                      <div className="fw-semibold">Bom Custo Papelaria & Gráfica Rápida</div>
                      <div>Ilhabela - SP</div>
                    </div>
                  </div>
                </div>

                {/* Body */}
                <div className="card-body p-4">
                  {loading && (
                    <div className="text-center py-3">
                      <div className="spinner-border text-secondary" role="status">
                        <span className="visually-hidden">Carregando...</span>
                      </div>
                      <p className="text-muted mt-2 small">Carregando...</p>
                    </div>
                  )}

                  {!loading && error && (
                    <div className="alert alert-danger mb-0" role="alert">
                      {error}
                    </div>
                  )}

                  {!loading && !error && (
                    <>
                      <h5 className="mb-3">Status do Orçamento</h5>

                      {info.statusKey && (
                        <div className="text-center mt-2 mb-4">
                          <span
                            style={{
                              ...STATUS_STYLES[statusStyleKey],
                              padding: "0.5rem 1.25rem",
                              borderRadius: "0.4rem",
                              fontSize: "1.1rem",
                              fontWeight: 600,
                              display: "inline-block",
                            }}
                          >
                            {info.statusLabel ?? info.statusKey}
                          </span>
                        </div>
                      )}

                      <ul className="list-group list-group-flush mb-3">
                        {info.number && (
                          <li className="list-group-item px-0">
                            <span className="text-muted small">Orçamento:</span>{" "}
                            <strong>#{info.number}</strong>
                          </li>
                        )}
                        {info.clienteNome && (
                          <li className="list-group-item px-0">
                            <span className="text-muted small">Cliente:</span> {info.clienteNome}
                          </li>
                        )}
                        {info.total != null && (
                          <li className="list-group-item px-0">
                            <span className="text-muted small">Total:</span>{" "}
                            <strong>
                              {info.total.toLocaleString("pt-BR", {
                                style: "currency",
                                currency: "BRL",
                              })}
                            </strong>
                          </li>
                        )}
                        {info.prazoEntrega && (
                          <li className="list-group-item px-0">
                            <span className="text-muted small">Prazo de Entrega:</span>{" "}
                            {info.prazoEntrega}
                          </li>
                        )}
                      </ul>

                      <hr />
                      <p className="small text-muted text-center mb-0">
                        Dúvidas? WhatsApp: (12) 99648-4918 — E-mail: orcamento@bomcustoilhabela.com.br
                      </p>
                    </>
                  )}
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </>
  );
}
```
  </action>
  <verify>npm run --workspace @bomcusto/frontend build 2>&1 | tail -5</verify>
  <done>Build passa. Página `/orcamento/[id]/status` criada com estados loading, loaded e error. Status pill centralizado com tamanho 1.1rem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet→Frontend (páginas públicas) | Clientes externos acessam /orcamento/:id/approve e /orcamento/:id/status sem autenticação |
| Frontend→Next.js proxy→Backend | Proxy injeta x-internal-api-key; token de aprovação vai como query param |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Spoofing | Acesso à página de status sem auth | accept | IDs são UUIDs (128-bit entropy) — suficientemente opacos para status view não-sensível |
| T-05-04-02 | Spoofing | Token de aprovação via URL | accept | Token é validado no backend (hash, expiração, uso único); frontend não valida — é responsabilidade do backend |
| T-05-04-03 | Tampering | Auto-POST ao carregar a página | mitigate | `handleApprove` só é chamado no `onClick` do botão — nunca no `useEffect`. O `useEffect` faz apenas GET do orçamento. |
| T-05-04-04 | Injection | Token interpolado na URL do fetch | mitigate | Token é `encodeURIComponent`-encodado antes de ser usado na URL: `encodeURIComponent(token)` |
| T-05-04-05 | Information Disclosure | Dados do orçamento expostos publicamente via /orcamento/:id/status | accept | Aceito por design (FR-04.1); IDs são UUIDs; dados exibidos são nome do cliente e total — sem dados financeiros sensíveis. |
</threat_model>

<verification>
- `npm run --workspace @bomcusto/frontend build` passa sem erros
- `/orcamento/[id]/approve?token=XXX` carrega sem erros de compilação
- `/orcamento/[id]/status` carrega sem erros de compilação
- Proxy `api/quotes/[id]/approve/route.ts` criado e usa `backendFetch`
- Botão "Aprovar Orçamento" não dispara POST automaticamente
</verification>

<success_criteria>
- Build frontend sem erros TypeScript
- 3 novos arquivos criados: route.ts proxy, approve/page.tsx, status/page.tsx
- Página de aprovação tem 6 estados (loading-quote, idle, submitting, success, error, no-token)
- Página de status tem 3 estados (loading, loaded, error)
- Ambas as páginas são mobile-first com card centrado Bootstrap 5
- Token é encodado antes de usar em URL
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-ux-do-painel-e-area-do-cliente/05-04-SUMMARY.md`
</output>
