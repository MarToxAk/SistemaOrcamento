---
phase: 05-ux-do-painel-e-area-do-cliente
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/quotes/quotes.service.ts
  - apps/frontend/src/app/orcamento/[id]/page.tsx
autonomous: true
requirements:
  - FR-07.3

must_haves:
  truths:
    - "Badge 'PDF Gerado' aparece no detalhe quando quote tem PDF"
    - "Badge 'NFS-e #XXXX' aparece quando NFS-e foi emitida, com link para visualizar"
    - "Badge 'PIX Confirmado' aparece quando pagamento foi confirmado"
    - "Badge 'Aprovado pelo Cliente' aparece quando quote.approved === true"
    - "Campos nfse/payment/approval são retornados pela API GET /api/quotes/:id"
  artifacts:
    - path: "apps/backend/src/modules/quotes/quotes.service.ts"
      provides: "mapQuoteBody retorna nfseNumero, nfseLink, paymentConfirmedAt, approved"
      contains: "nfseNumero"
    - path: "apps/frontend/src/app/orcamento/[id]/page.tsx"
      provides: "Badges de integração no detalhe do orçamento"
      contains: "bi-file-pdf"
  key_links:
    - from: "mapQuoteBody"
      to: "GET /api/quotes/:id response"
      via: "campos top-level nfseNumero, nfseLink, paymentConfirmedAt, approved"
      pattern: "nfseNumero"
    - from: "apps/frontend/src/app/orcamento/[id]/page.tsx"
      to: "quote.nfseNumero"
      via: "QuoteDetail type extension"
      pattern: "nfseNumero"
---

<objective>
Expor campos de integração (NFS-e, PIX, aprovação) na resposta da API e renderizar badges de status de integração na página de detalhe.

Purpose: FR-07.3 — operador deve ver status das integrações sem entrar no console ou logs.
Output: `mapQuoteBody` retorna campos de integração; `orcamento/[id]/page.tsx` renderiza badges condicionais.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-ux-do-painel-e-area-do-cliente/05-UI-SPEC.md
@.planning/phases/05-ux-do-painel-e-area-do-cliente/05-RESEARCH.md

<interfaces>
<!-- Extraído de apps/backend/src/modules/quotes/quotes.service.ts -->

A assinatura atual de `mapQuoteBody` (parâmetro do tipo):
```typescript
private mapQuoteBody(quote: {
  id: string;
  internalNumber: number;
  externalQuoteId: bigint | null;
  budgetDate: Date | null;
  sellerExternalId: bigint | null;
  saleExternalId?: bigint | null;
  sellerName: string | null;
  conversationId: bigint | null;
  chatwootContactId: bigint | null;
  status: QuoteStatus;
  notes: string | null;
  validity: string | null;
  deliveryDate: Date | null;
  paymentTerms: string | null;
  createdAt: Date;
  updatedAt: Date;
  editedAt: Date | null;
  discount: Prisma.Decimal;
  surcharge: Prisma.Decimal;
  total: Prisma.Decimal;
  customer: { fullName: string; phone: string | null; email: string | null };
  documents?: Array<{ fileName: string; publicUrl: string | null; generatedAt: Date }>;
  items: Array<...>;
  stamps: Array<...>;
})
```

O objeto `quote` retornado pelo Prisma JÁ inclui os campos escalares da tabela `Quote`.
Os campos que existem no schema (verificado) mas NÃO estão no tipo do parâmetro:
- `nfseNumero: string | null`
- `nfseLink: string | null`
- `nfseEmitidaEm: DateTime | null`
- `paymentConfirmedAt: DateTime | null`
- `approved: boolean`
- `approvedAt: DateTime | null`
- `latestPdfUrl` já está NO return (linha 1186): `latestPdfUrl: latestDocument?.publicUrl ?? null`

O return atual de `mapQuoteBody` inclui campos top-level como `id`, `statusKey`, `statusLabel`, `availableNextStatuses`, `latestPdfUrl`, e um objeto `body` com os dados legados do orçamento.

<!-- Extraído de apps/frontend/src/app/orcamento/[id]/page.tsx -->

Tipo atual:
```typescript
type QuoteDetail = {
  id?: string;
  statusKey?: string;
  availableNextStatuses?: Array<{ value: string; label: string }>;
  body?: {
    idorcamento_interno?: number;
    idorcamento?: number;
    dataorcamento?: string;
    status?: string;
    vendedorNome?: string;
    cliente?: { nome?: string; telefone?: string; email?: string };
    observacoes?: string;
    validade?: string;
    prazoEntrega?: string;
    condicaoPagamento?: string;
    itens?: QuoteItem[];
    carimbos?: { itens?: Array<...> };
    totais?: { valor?: number; desconto?: number; valoracrescimo?: number };
    documentoPdf?: Array<{ publicUrl?: string; generatedAt?: string }>;
  };
};
```

O header do detalhe tem `.orcamento-header` com logo + dados do orçamento. Os badges devem ser inseridos DENTRO do orcamento-header, após o bloco de número/data.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender mapQuoteBody para expor campos de integração no return</name>
  <files>apps/backend/src/modules/quotes/quotes.service.ts</files>
  <action>
Editar `apps/backend/src/modules/quotes/quotes.service.ts`:

**1. Adicionar campos de integração ao tipo do parâmetro de `mapQuoteBody`**:
No tipo do parâmetro (a partir da linha `private mapQuoteBody(quote: {`), adicionar após `editedAt`:
```typescript
nfseNumero?: string | null;
nfseLink?: string | null;
nfseEmitidaEm?: Date | null;
paymentConfirmedAt?: Date | null;
approved?: boolean;
approvedAt?: Date | null;
```

**2. Adicionar campos no return de `mapQuoteBody`**:
O return atual já tem `latestPdfUrl`. Adicionar logo após `latestPdfUrl`:
```typescript
nfseNumero: (quote as any).nfseNumero ?? null,
nfseLink: (quote as any).nfseLink ?? null,
nfseEmitidaEm: (quote as any).nfseEmitidaEm ?? null,
paymentConfirmedAt: (quote as any).paymentConfirmedAt ?? null,
approved: Boolean((quote as any).approved ?? false),
approvedAt: (quote as any).approvedAt ?? null,
```

Usar `(quote as any)` para acessar os campos opcionais — eles existem no Prisma model mas não foram adicionados ao tipo do parâmetro com as modificações acima, então precisamos acessar com cast para garantir compilação. Após os campos estarem no tipo do parâmetro, o TypeScript os reconhecerá diretamente, mas usar `as any` é aceitável para não quebrar o tipo.

**IMPORTANTE**: Não alterar nenhuma outra parte de `mapQuoteBody`. Não alterar as chamadas a `mapQuoteBody`. Não alterar nenhuma outra função do serviço.
  </action>
  <verify>npm run --workspace @bomcusto/backend build 2>&1 | tail -10</verify>
  <done>Build backend passa. `mapQuoteBody` retorna `nfseNumero`, `nfseLink`, `paymentConfirmedAt`, `approved` como campos top-level na resposta.</done>
</task>

<task type="auto">
  <name>Task 2: Estender QuoteDetail type + renderizar badges de integração na página de detalhe</name>
  <files>apps/frontend/src/app/orcamento/[id]/page.tsx</files>
  <action>
Editar `apps/frontend/src/app/orcamento/[id]/page.tsx`:

**1. Estender o tipo `QuoteDetail`** adicionando campos de integração no nível raiz (fora de `body`):
```typescript
type QuoteDetail = {
  id?: string;
  statusKey?: string;
  availableNextStatuses?: Array<{ value: string; label: string }>;
  // --- campos de integração (adicionados em 05-03) ---
  nfseNumero?: string | null;
  nfseLink?: string | null;
  nfseEmitidaEm?: string | null;
  paymentConfirmedAt?: string | null;
  approved?: boolean;
  approvedAt?: string | null;
  latestPdfUrl?: string | null;
  // ---------------------------------------------------
  body?: {
    // ... campos existentes sem alteração
  };
};
```

**2. Adicionar badges** dentro do bloco `.orcamento-header`, após o bloco que contém número do orçamento e data (localizar pelo `idorcamento_interno` ou `idorcamento` para encontrar a posição correta).

Inserir logo após o encerramento desse bloco de informações, mas ainda dentro de `.orcamento-header`:
```tsx
{/* Integration status badges */}
{(quote.latestPdfUrl || quote.nfseNumero || quote.paymentConfirmedAt || quote.approved) && (
  <div className="d-flex flex-wrap gap-2 mt-2">
    {quote.latestPdfUrl && (
      <span className="badge bg-secondary">
        <i className="bi bi-file-pdf me-1" />PDF Gerado
      </span>
    )}
    {quote.nfseNumero && (
      <a
        href={quote.nfseLink ?? "#"}
        target="_blank"
        rel="noreferrer"
        className="badge bg-success text-decoration-none"
      >
        <i className="bi bi-file-check me-1" />NFS-e #{quote.nfseNumero}
      </a>
    )}
    {quote.paymentConfirmedAt && (
      <span className="badge bg-primary">
        <i className="bi bi-check-circle me-1" />PIX Confirmado
      </span>
    )}
    {quote.approved && (
      <span className="badge bg-info text-dark">
        <i className="bi bi-person-check me-1" />Aprovado pelo Cliente
      </span>
    )}
  </div>
)}
```

**IMPORTANTE**: Não alterar a estrutura de dados, handlers de status, lógica de emissão de NFS-e ou geração de PDF. Apenas estender o tipo e adicionar o bloco de badges.
  </action>
  <verify>npm run --workspace @bomcusto/frontend build 2>&1 | tail -5</verify>
  <done>Build passa. Tipo `QuoteDetail` tem os 6 novos campos. Bloco de badges condicional está presente no JSX do header.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend→Frontend | Campos nfseNumero/paymentConfirmedAt/approved expostos na resposta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | nfseLink exposto na response | accept | Link para NFS-e é dado operacional, não dado pessoal sensível; acessível apenas via InternalAuthGuard |
| T-05-03-02 | Tampering | Badge renderizado com `nfseLink` do servidor | mitigate | Link usa `rel="noreferrer"` + `target="_blank"` — não permite inject de script via href pois é atributo HTML, não innerHTML |
</threat_model>

<verification>
- `npm run --workspace @bomcusto/backend build` passa
- `npm run --workspace @bomcusto/frontend build` passa
- GET /api/quotes/:id retorna `nfseNumero`, `paymentConfirmedAt`, `approved` no JSON
</verification>

<success_criteria>
- Build backend + frontend sem erros TypeScript
- `mapQuoteBody` retorna os 6 campos de integração como campos top-level
- Página de detalhe renderiza badges condicionalmente baseado nos dados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-ux-do-painel-e-area-do-cliente/05-03-SUMMARY.md`
</output>
