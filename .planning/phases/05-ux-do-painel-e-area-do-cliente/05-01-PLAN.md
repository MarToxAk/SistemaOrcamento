---
phase: 05-ux-do-painel-e-area-do-cliente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/app/orcamento/page.tsx
autonomous: true
requirements:
  - FR-07.1

must_haves:
  truths:
    - "Operador pode filtrar orçamentos por status usando pills clicáveis"
    - "Lista atualiza sem recarregar a página ao mudar o filtro"
    - "Loading spinner aparece durante o fetch"
    - "Toast verde aparece ao salvar mudança de status com sucesso"
    - "Resposta paginada { total, data } é processada corretamente"
  artifacts:
    - path: "apps/frontend/src/app/orcamento/page.tsx"
      provides: "Página de lista com filtros + toast + fix de resposta"
      contains: "activeFilter"
  key_links:
    - from: "apps/frontend/src/app/orcamento/page.tsx"
      to: "/api/quotes?status=..."
      via: "fetch com URLSearchParams + activeFilter"
      pattern: "activeFilter"
    - from: "handleStatusChange"
      to: "toast-container"
      via: "bootstrap.Toast API"
      pattern: "bootstrap.Toast"
---

<objective>
Adicionar filtros de status, toast de feedback e corrigir o parsing da resposta paginada na tela de lista de orçamentos.

Purpose: FR-07.1 — operador precisa de filtros funcionais e feedback visual claro ao salvar status.
Output: `orcamento/page.tsx` atualizado com filtro por status, toast de sucesso/erro, loading spinner e suporte à resposta `{ total, data }`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-do-painel-e-area-do-cliente/05-UI-SPEC.md
@.planning/phases/05-ux-do-painel-e-area-do-cliente/05-RESEARCH.md

<interfaces>
<!-- Contratos extraídos de apps/frontend/src/app/orcamento/page.tsx -->

```typescript
// Estado atual
const [quotes, setQuotes] = useState<QuoteRow[]>([]);
const [loadingQuotes, setLoadingQuotes] = useState(false);
const [quotesError, setQuotesError] = useState("");
const [statusSavingId, setStatusSavingId] = useState<string | null>(null);
const [conversationId, setConversationId] = useState<number | undefined>(undefined);
const [chatwootContactId, setChatwootContactId] = useState<number | undefined>(undefined);

// useEffect de fetch atual (a corrigir)
useEffect(() => {
  if (validationState !== "valid") return;
  const fetchQuotes = async () => {
    setLoadingQuotes(true);
    const query = new URLSearchParams();
    if (chatwootContactId && chatwootContactId > 0) query.set("chatwootContactId", String(chatwootContactId));
    else if (conversationId && conversationId > 0) query.set("conversationId", String(conversationId));
    const response = await fetch(`/api/quotes?${query.toString()}`, { cache: "no-store" });
    const data = (await response.json()) as unknown;
    setQuotes(Array.isArray(data) ? (data as QuoteRow[]) : []); // ← BUG: resposta agora é { total, data }
  };
  void fetchQuotes();
}, [validationState, chatwootContactId, conversationId]); // ← falta activeFilter

// handleStatusChange — a estender com toast
async function handleStatusChange(quote: QuoteRow, nextStatus: string) {
  setStatusSavingId(quote.id);
  setQuotesError("");
  try {
    const response = await fetch(`/api/quotes/${encodeURIComponent(identifier)}/status`, {
      method: "PATCH",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ newStatus: nextStatus, changedBy: "Lista de orcamentos" }),
    });
    const data = (await response.json().catch(() => ({}))) as QuoteRow & { message?: string; error?: string };
    if (!response.ok) throw new Error(data?.message || data?.error || "Falha ao atualizar status.");
    setQuotes((current) => current.map((q) => (q.id === quote.id ? { ...q, ...data } : q)));
    // ← FALTA: disparar toast de sucesso aqui
  } catch (error) {
    setQuotesError(error instanceof Error ? error.message : "Falha ao atualizar status.");
  } finally {
    setStatusSavingId(null);
  }
}
```

Bootstrap 5.3.2 carregado via `<Script src=".../bootstrap.bundle.min.js" />`.
Bootstrap Icons 1.11.1 carregado via CDN.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar activeFilter state + filtros pills + corrigir parsing de resposta paginada</name>
  <files>apps/frontend/src/app/orcamento/page.tsx</files>
  <action>
Fazer as seguintes mudanças em `apps/frontend/src/app/orcamento/page.tsx`:

**1. Adicionar estado de filtro** (após declaração de `statusSavingId`):
```tsx
const [activeFilter, setActiveFilter] = useState<string>("");
```

**2. Corrigir o useEffect de fetch** (resposta paginada + activeFilter):
- Substituir `setQuotes(Array.isArray(data) ? (data as QuoteRow[]) : [])` por:
  ```tsx
  const parsed = Array.isArray(data)
    ? (data as QuoteRow[])
    : Array.isArray((data as any)?.data)
      ? ((data as any).data as QuoteRow[])
      : [];
  setQuotes(parsed);
  ```
- Adicionar `activeFilter` ao URLSearchParams do fetch:
  ```tsx
  if (activeFilter) query.set("status", activeFilter);
  ```
- Adicionar `activeFilter` ao array de dependências do useEffect: `[validationState, chatwootContactId, conversationId, activeFilter]`

**3. Adicionar loading state** (substituir o `<td>` de "Carregando"):
```tsx
{loadingQuotes ? (
  <tr>
    <td colSpan={7} className="text-center text-muted py-4">
      <span className="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true" />
      Carregando orçamentos do contato...
    </td>
  </tr>
) : ...}
```
Nota: o texto atual é igual — só adicionar o spinner antes do texto.

**4. Adicionar barra de filtros** logo após o início de `.orcamento-section`, antes de `table-responsive`:
```tsx
<nav className="d-flex flex-wrap gap-1 mb-3" aria-label="Filtrar por status">
  {([
    { value: "", label: "Todos" },
    { value: "PENDENTE", label: "Pendente" },
    { value: "ENVIADO", label: "Enviado" },
    { value: "APROVADO", label: "Aprovado" },
    { value: "EM_PRODUCAO", label: "Em Produção" },
    { value: "PRONTO_PARA_ENTREGA", label: "Pronto p/ Entrega" },
    { value: "ENTREGUE", label: "Entregue" },
    { value: "CANCELADO", label: "Cancelado" },
  ] as const).map(({ value, label }) => (
    <button
      key={value}
      type="button"
      className={`btn btn-sm ${activeFilter === value ? "btn-primary" : "btn-outline-secondary"}`}
      onClick={() => setActiveFilter(value)}
    >
      {label}
    </button>
  ))}
</nav>
```

**5. Atualizar empty state** para diferenciar filtro ativo vs sem contato:
```tsx
: quotes.length === 0 ? (
  <tr>
    <td colSpan={7} className="text-center text-muted py-4">
      {activeFilter
        ? `Nenhum orçamento com status "${activeFilter}" para este contato.`
        : "Nenhum orçamento encontrado para este contato."}
    </td>
  </tr>
)
```

**6. Corrigir statusClass** para usar `statusKey` diretamente (em vez de substring matching):
```tsx
const STATUS_CLASS: Record<string, string> = {
  aprovado: "status-aprovado",
  em_producao: "status-em_producao",
  pronto_para_entrega: "status-pronto_para_entrega",
  entregue: "status-entregue",
  cancelado: "status-cancelado",
  pendente: "status-pendente",
  enviado: "status-enviado",
  recusado: "status-recusado",
};
const statusClass = STATUS_CLASS[(quote.statusKey ?? "").toLowerCase()] ?? "";
```
Definir `STATUS_CLASS` fora do componente (constante de módulo).
  </action>
  <verify>npm run --workspace @bomcusto/frontend build 2>&1 | tail -5</verify>
  <done>Build passa sem erros de tipo; filter pills renderizam; fetch inclui `?status=` quando filtro ativo.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar toast container + disparar toast no handleStatusChange</name>
  <files>apps/frontend/src/app/orcamento/page.tsx</files>
  <action>
Continuar editando `apps/frontend/src/app/orcamento/page.tsx`:

**1. Adicionar container de toast** no JSX — após a última `</div>` de fechamento do conteúdo principal, mas ainda dentro do fragment `<>`:
```tsx
<div
  id="toast-container"
  className="toast-container position-fixed bottom-0 end-0 p-3"
  style={{ zIndex: 1100 }}
/>
```

**2. Criar função helper `showToast`** (fora do componente, no módulo):
```tsx
function showToast(message: string, type: "success" | "danger") {
  if (typeof window === "undefined") return;
  const bs = (window as any).bootstrap;
  if (!bs?.Toast) return;
  const container = document.getElementById("toast-container");
  if (!container) return;

  const wrapper = document.createElement("div");
  wrapper.className = `toast align-items-center text-bg-${type} border-0`;
  wrapper.setAttribute("role", "alert");
  wrapper.setAttribute("aria-live", "assertive");
  wrapper.setAttribute("aria-atomic", "true");
  wrapper.innerHTML = `
    <div class="d-flex">
      <div class="toast-body">${message}</div>
      <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Fechar"></button>
    </div>
  `;
  container.appendChild(wrapper);
  const toast = new bs.Toast(wrapper, { delay: 3500 });
  toast.show();
  wrapper.addEventListener("hidden.bs.toast", () => wrapper.remove());
}
```

**3. Disparar toast em `handleStatusChange`**:
- No bloco de sucesso (após `setQuotes(...)`), adicionar:
  ```tsx
  const newLabel = data.statusLabel ?? nextStatus;
  showToast(`Status atualizado para ${newLabel}.`, "success");
  ```
- Remover `setQuotesError(...)` do catch (ou manter como fallback) e adicionar:
  ```tsx
  showToast(error instanceof Error ? error.message : "Falha ao atualizar status.", "danger");
  ```
  Manter o `setQuotesError` também para acessibilidade de screen readers.
  </action>
  <verify>npm run --workspace @bomcusto/frontend build 2>&1 | tail -5</verify>
  <done>Build passa. Toast container presente no DOM. `showToast` definida e chamada em handleStatusChange.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→Next.js API proxy | Parâmetro `status` no filtro vai para o backend via query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | `activeFilter` state → `?status=` query param | accept | Valor controlado por enum de botões no frontend; backend já valida e ignora status inválidos |
| T-05-01-02 | Spoofing | `showToast` com HTML dinâmico | mitigate | Mensagem do toast é de `data.statusLabel` (dado do servidor) ou string hardcoded — nunca HTML do usuário. Não usar innerHTML com input não sanitizado. |
</threat_model>

<verification>
- `npm run --workspace @bomcusto/frontend build` passa sem erros
- Filtro "Aprovado" clicado → fetch inclui `?status=APROVADO` (verificar DevTools)
- Loading spinner aparece enquanto fetch está em voo
- Filtro "Todos" retorna todos os orçamentos do contato
</verification>

<success_criteria>
- Build frontend sem erros TypeScript
- Filter pills renderizam na página de orçamentos
- Resposta paginada `{ total, data: [...] }` é corretamente desestruturada
- Toast aparece após mudança de status com sucesso
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-ux-do-painel-e-area-do-cliente/05-01-SUMMARY.md`
</output>
