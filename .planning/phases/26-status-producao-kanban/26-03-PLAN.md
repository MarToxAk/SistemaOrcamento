---
phase: 26-status-producao-kanban
plan: 03
type: execute
wave: 3
depends_on: [26-01, 26-02]
files_modified:
  - apps/frontend/src/app/status/page.tsx
autonomous: true
requirements: []
tags: [frontend, filter, cleanup, kanban]
must_haves:
  truths:
    - "Barra de filtro acima do kanban com 4 opções: Todos | Pago Caixa | PIX | Aguardando"
    - "Selecionar um filtro reduz cards visíveis em TODAS as colunas (e tabs mobile) para aquele tipo de pagamento"
    - "Filtro reseta para 'Todos' a cada carregamento de página (não persiste)"
    - "Estados mortos selectedStatusFilter, selectedBadgeFilter, onlyWithBadge foram removidos do código"
    - "Contagens nos cabeçalhos de coluna refletem a contagem APÓS o filtro"
  artifacts:
    - path: "apps/frontend/src/app/status/page.tsx"
      provides: "Barra de filtro funcional + visibleQuotes derivado do filtro + remoção de dead state"
      contains: "badgeFilter"
  key_links:
    - from: "badgeFilter state"
      to: "visibleQuotes"
      via: "quotes.filter(getBadgeType match)"
      pattern: "quotes\\.filter.*getBadgeType"
    - from: "visibleQuotes"
      to: "column counts + mobile tab counts"
      via: "visibleQuotes.filter(statusKey===col).length"
      pattern: "visibleQuotes\\.filter"
---

<objective>
Implementar a barra de filtro por tipo de pagamento e conectar ao `visibleQuotes`, garantindo que filtre cards em TODAS as colunas do kanban. Remover os 3 states mortos identificados no CONTEXT. Implementa D-12, D-13, D-14, D-15.

Purpose: Completa o redesign da página. Após este plan, o kanban está 100% funcional conforme decisões.

Output: `page.tsx` com filtro funcional + dead state removido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-status-producao-kanban/26-CONTEXT.md
@.planning/phases/26-status-producao-kanban/26-01-PLAN.md
@.planning/phases/26-status-producao-kanban/26-02-PLAN.md
@apps/frontend/src/app/status/page.tsx

<interfaces>
<!-- Helper já existente que dirige o filtro -->

```typescript
function getBadgeType(quote: QuoteRow): "PAGO_CAIXA" | "PIX_CONFIRMADO" | "AGUARDANDO";
```

Mapeamento filtro UI → valor do badge:
- "TODOS"        → não filtra
- "PAGO_CAIXA"   → getBadgeType(quote) === "PAGO_CAIXA"
- "PIX"          → getBadgeType(quote) === "PIX_CONFIRMADO"
- "AGUARDANDO"   → getBadgeType(quote) === "AGUARDANDO"

State a INTRODUZIR:
```typescript
const [badgeFilter, setBadgeFilter] = useState<"TODOS" | "PAGO_CAIXA" | "PIX" | "AGUARDANDO">("TODOS");
```

State a REMOVER (D-15):
- `selectedStatusFilter` / `setSelectedStatusFilter`
- `selectedBadgeFilter` / `setSelectedBadgeFilter`
- `onlyWithBadge` / `setOnlyWithBadge`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar state badgeFilter + derivar visibleQuotes + remover dead state</name>
  <files>apps/frontend/src/app/status/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/status/page.tsx (estado após Plan 02)
    - .planning/phases/26-status-producao-kanban/26-CONTEXT.md (D-12, D-13, D-14, D-15)
  </read_first>
  <action>
    No bloco de `useState` do componente `StatusPage` (perto do topo da função):

    1. REMOVA estas 3 linhas:
       ```tsx
       const [selectedStatusFilter, setSelectedStatusFilter] = useState<string>("TODOS");
       const [selectedBadgeFilter, setSelectedBadgeFilter] = useState<string>("TODOS");
       const [onlyWithBadge, setOnlyWithBadge] = useState<boolean>(false);
       ```

    2. ADICIONE uma nova linha:
       ```tsx
       const [badgeFilter, setBadgeFilter] = useState<"TODOS" | "PAGO_CAIXA" | "PIX" | "AGUARDANDO">("TODOS");
       ```

    3. SUBSTITUA a linha `const visibleQuotes = quotes;` por:
       ```tsx
       const visibleQuotes = badgeFilter === "TODOS"
         ? quotes
         : quotes.filter((q) => {
             const type = getBadgeType(q);
             if (badgeFilter === "PAGO_CAIXA") return type === "PAGO_CAIXA";
             if (badgeFilter === "PIX") return type === "PIX_CONFIRMADO";
             if (badgeFilter === "AGUARDANDO") return type === "AGUARDANDO";
             return true;
           });
       ```

    NÃO adicione `localStorage` / `sessionStorage` para o filtro — D-14 exige reset por carregamento.

    NÃO toque em SSE / banner / fetchQuotes / handlePdf — preservados.
  </action>
  <verify>
    <automated>grep -c "selectedStatusFilter\|selectedBadgeFilter\|onlyWithBadge" apps/frontend/src/app/status/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "selectedStatusFilter" apps/frontend/src/app/status/page.tsx` retorna 0
    - `grep -c "selectedBadgeFilter" apps/frontend/src/app/status/page.tsx` retorna 0
    - `grep -c "onlyWithBadge" apps/frontend/src/app/status/page.tsx` retorna 0
    - `grep -c "const \\[badgeFilter, setBadgeFilter\\]" apps/frontend/src/app/status/page.tsx` retorna 1
    - `grep -c "quotes.filter" apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c "localStorage.setItem.*badgeFilter\\|sessionStorage.*badgeFilter" apps/frontend/src/app/status/page.tsx` retorna 0 (D-14: não persiste)
    - `cd apps/frontend && npx tsc --noEmit` sem novos erros em page.tsx
  </acceptance_criteria>
  <done>
    State badgeFilter declarado, visibleQuotes derivado dele, dead state eliminado. UI ainda não tem botões — Task 2 conecta.
  </done>
</task>

<task type="auto">
  <name>Task 2: Renderizar barra de filtro acima do kanban</name>
  <files>apps/frontend/src/app/status/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/status/page.tsx (após Task 1 deste plano)
    - .planning/phases/26-status-producao-kanban/26-CONTEXT.md (D-12)
  </read_first>
  <action>
    Localize o `<div className="orcamento-section bg-white rounded-bottom shadow-sm p-4">`. Imediatamente DEPOIS do bloco `{erro ? ... : null}` e ANTES do branch ternário `loading ? ... : visibleQuotes.length === 0 ? ... : (...)`, INSIRA a barra de filtro.

    Conteúdo a inserir:
    ```tsx
    <div className="badge-filter-bar d-flex flex-wrap gap-2 mb-3" role="group" aria-label="Filtrar por tipo de pagamento">
      {([
        { value: "TODOS", label: "Todos", icon: "bi-funnel" },
        { value: "PAGO_CAIXA", label: "Pago Caixa", icon: "bi-cash-coin" },
        { value: "PIX", label: "PIX", icon: "bi-lightning-charge" },
        { value: "AGUARDANDO", label: "Aguardando", icon: "bi-hourglass-split" },
      ] as const).map((opt) => {
        const count = opt.value === "TODOS"
          ? quotes.length
          : quotes.filter((q) => {
              const type = getBadgeType(q);
              if (opt.value === "PAGO_CAIXA") return type === "PAGO_CAIXA";
              if (opt.value === "PIX") return type === "PIX_CONFIRMADO";
              return type === "AGUARDANDO";
            }).length;
        const active = badgeFilter === opt.value;
        return (
          <button
            key={opt.value}
            type="button"
            className={`btn btn-sm ${active ? "btn-primary" : "btn-outline-secondary"}`}
            onClick={() => setBadgeFilter(opt.value)}
            aria-pressed={active}
          >
            <i className={`bi ${opt.icon} me-1`} />{opt.label}
            <span className={`badge ms-2 ${active ? "bg-light text-primary" : "bg-secondary"}`}>{count}</span>
          </button>
        );
      })}
    </div>
    ```

    O contador de cada botão usa `quotes` (universo total), NÃO `visibleQuotes` — assim cada botão sempre mostra quantos cards entrariam se aquela opção fosse selecionada.

    No JSX de cabeçalho (área `orcamento-header`), a linha que mostra `{visibleQuotes.length}/{quotes.length} orçamento(s)` deve PERMANECER inalterada — ela continua útil mostrando o impacto do filtro.
  </action>
  <verify>
    <automated>grep -c "badge-filter-bar" apps/frontend/src/app/status/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "badge-filter-bar" apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c 'setBadgeFilter(opt.value)' apps/frontend/src/app/status/page.tsx` retorna 1
    - `grep -c '"PAGO_CAIXA"' apps/frontend/src/app/status/page.tsx` retorna >= 2 (state type + filtro)
    - `grep -c '"AGUARDANDO"' apps/frontend/src/app/status/page.tsx` retorna >= 2
    - `grep -c 'aria-pressed' apps/frontend/src/app/status/page.tsx` retorna >= 1
    - O label "Todos", "Pago Caixa", "PIX", "Aguardando" cada um aparece pelo menos 1 vez: `grep -c '"Pago Caixa"' page.tsx >= 1`, `grep -c '"Aguardando"' page.tsx >= 1`, `grep -c '"Todos"' page.tsx >= 1`
    - `cd apps/frontend && npx tsc --noEmit` sem novos erros
  </acceptance_criteria>
  <done>
    Barra de filtro visível acima do kanban com 4 botões, contagens, e estado ativo realçado. Clicar muda `badgeFilter` e portanto reduz `visibleQuotes`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Adicionar CSS da barra de filtro e validar build final</name>
  <files>apps/frontend/src/app/status/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/status/page.tsx (após Task 2 deste plano)
  </read_first>
  <action>
    No bloco `<style>{`...`}</style>` no final do componente, adicione:

    ```css
    .badge-filter-bar { align-items: center; }
    .badge-filter-bar .btn { display: inline-flex; align-items: center; }
    .badge-filter-bar .btn .badge { font-weight: 600; }
    @media (max-width: 768px) {
      .badge-filter-bar .btn { flex: 1 1 calc(50% - 0.5rem); justify-content: center; }
    }
    ```

    Em seguida, rode build do frontend e CONFIRME que:
    - Não há warning de unused state.
    - Não há referência remanescente a `production-table`, `row-highlighted`, `kanban-card-placeholder`, `selectedStatusFilter`, `selectedBadgeFilter`, `onlyWithBadge`.

    Execute estas verificações via shell:
    ```bash
    grep -nE "production-table|row-highlighted|kanban-card-placeholder|selectedStatusFilter|selectedBadgeFilter|onlyWithBadge" apps/frontend/src/app/status/page.tsx || echo "CLEAN"
    ```
    A saída esperada é `CLEAN`.
  </action>
  <verify>
    <automated>cd apps/frontend && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ".badge-filter-bar" apps/frontend/src/app/status/page.tsx` retorna >= 2 (declaração + media query)
    - Comando combinado de cleanup retorna saída vazia (apenas "CLEAN"): `grep -nE "production-table|row-highlighted|kanban-card-placeholder|selectedStatusFilter|selectedBadgeFilter|onlyWithBadge" apps/frontend/src/app/status/page.tsx | grep -v '^#' | wc -l` retorna 0
    - `cd apps/frontend && npx next build` (ou equivalente do projeto) completa com exit code 0
    - `cd apps/frontend && npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>
    CSS finalizado. Build passa. Sem referências de código morto. Página pronta para inspeção manual e merge.
  </done>
</task>

</tasks>

<verification>
- Carregar `/status`, conferir barra de filtro acima do kanban.
- Clicar em "PIX": somente cards com badge PIX permanecem visíveis em todas as colunas; contagens das colunas atualizam; contagens nos botões mantêm valores totais.
- Clicar em "Pago Caixa": idem para badge PAGO_CAIXA.
- Clicar em "Todos": volta a mostrar todos os cards.
- Recarregar a página (F5): filtro volta para "Todos" (D-14).
- Mobile: filtro funciona dentro da tab ativa.
- SSE de pagamento: cards continuam atualizando; se filtro = AGUARDANDO e pagamento chega, o card desaparece da view (esperado).
</verification>

<success_criteria>
- 4 botões de filtro renderizam com contagem total estática.
- Filtro reduz `visibleQuotes` corretamente para todas as colunas e tabs mobile.
- Filtro reseta para "Todos" no reload (não persiste).
- Build e tsc passam.
- Zero dead state remanescente.
- Página totalmente funcional conforme D-01 a D-15.
</success_criteria>

<output>
After completion, create `.planning/phases/26-status-producao-kanban/26-03-SUMMARY.md`
</output>
