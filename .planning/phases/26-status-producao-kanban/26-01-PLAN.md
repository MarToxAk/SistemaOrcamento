---
phase: 26-status-producao-kanban
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/app/status/page.tsx
autonomous: true
requirements: []
tags: [frontend, kanban, layout, bootstrap]
must_haves:
  truths:
    - "Página /status renderiza 3 colunas kanban no desktop (APROVADO, EM PRODUÇÃO, PRONTO PARA ENTREGA) — não tabela"
    - "Cada coluna mostra título + contagem dos cards naquele status com cor associada"
    - "Mobile (<768px) mostra Bootstrap nav-tabs — uma coluna visível por vez"
    - "SSE, banner persistente, cabeçalho (logo/título/botões) permanecem inalterados"
  artifacts:
    - path: "apps/frontend/src/app/status/page.tsx"
      provides: "Estrutura kanban 3-colunas substituindo a tabela; tabs mobile"
      contains: "kanban-board"
  key_links:
    - from: "visibleQuotes"
      to: "kanban columns"
      via: "agrupamento por statusKey === coluna"
      pattern: "visibleQuotes\\.filter\\(.*statusKey"
---

<objective>
Substituir a `<table className="production-table">` por uma estrutura kanban com 3 colunas (APROVADO | EM PRODUÇÃO | PRONTO PARA ENTREGA), incluindo cabeçalhos de coluna com contagem colorida e tabs Bootstrap para mobile. Implementa D-01, D-02, D-03 do CONTEXT.

Purpose: Estabelece a estrutura visual base do kanban. Cards reais (com conteúdo completo) vêm no Plan 02.

Output: `page.tsx` com layout kanban (placeholder de cards = `<div>` simples por orçamento mostrando apenas `#numero`) + tabs mobile + CSS atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-status-producao-kanban/26-CONTEXT.md
@apps/frontend/src/app/status/page.tsx

<interfaces>
<!-- Existing types/constants in page.tsx that this plan uses -->

```typescript
type QuoteRow = {
  id: string;
  internalNumber: number;
  updatedAt: string;
  statusKey: string;        // ex: "APROVADO" | "EM_PRODUCAO" | "PRONTO_PARA_ENTREGA"
  statusLabel: string;
  body: {
    idorcamento?: number;
    cliente?: { nome?: string | null; telefone?: string | null };
    totais?: { valor?: number };
  };
  // ... outros campos preservados
};

const PRODUCTION_STATUSES = ["APROVADO", "EM_PRODUCAO", "PRONTO_PARA_ENTREGA"];
```

State preservado:
- `quotes`, `loading`, `erro`, `highlightedId`, `lastPayment`, `bannerDismissed`, `efiStatus`
- `visibleQuotes = quotes` (Plan 03 substitui isso pelo filtro)

Mapeamento coluna → label/cor (consistente com .status-* CSS):
- APROVADO → "APROVADO" → verde (#e9f8ef / #1f7a44)
- EM_PRODUCAO → "EM PRODUÇÃO" → azul (#eef4ff / #2457a6)
- PRONTO_PARA_ENTREGA → "PRONTO PARA ENTREGA" → laranja (#fff5e8 / #a65b12)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir tabela por kanban 3-colunas (desktop)</name>
  <files>apps/frontend/src/app/status/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/status/page.tsx (arquivo inteiro — única source of truth)
    - .planning/phases/26-status-producao-kanban/26-CONTEXT.md (decisões D-01, D-02, D-04)
  </read_first>
  <action>
    Localize o bloco `<div className="table-responsive">...</div>` (linhas ~272-357 do estado atual) e substitua TODO esse bloco por uma estrutura kanban. Mantenha o `loading`, o `visibleQuotes.length === 0` (empty state) e o `erro` inalterados — apenas substitua o branch onde havia a tabela.

    Estrutura nova (desktop, visível em md+):

    ```tsx
    <div className="kanban-board d-none d-md-flex gap-3">
      {PRODUCTION_STATUSES.map((statusKey) => {
        const columnQuotes = visibleQuotes.filter((q) => q.statusKey === statusKey);
        const columnLabel =
          statusKey === "APROVADO" ? "APROVADO" :
          statusKey === "EM_PRODUCAO" ? "EM PRODUÇÃO" :
          "PRONTO PARA ENTREGA";
        return (
          <div key={statusKey} className={`kanban-column kanban-column-${statusKey.toLowerCase()}`}>
            <div className={`kanban-column-header status-${statusKey.toLowerCase()}`}>
              <span className="kanban-column-title">{columnLabel}</span>
              <span className="kanban-column-count">{columnQuotes.length}</span>
            </div>
            <div className="kanban-column-body">
              {columnQuotes.length === 0 ? (
                <div className="kanban-column-empty text-muted small">Sem orçamentos</div>
              ) : (
                columnQuotes.map((quote) => (
                  <div
                    key={quote.id}
                    className={`kanban-card-placeholder ${highlightedId === quote.id ? "card-highlighted" : ""}`}
                  >
                    #{quote.body.idorcamento ?? quote.internalNumber}
                  </div>
                ))
              )}
            </div>
          </div>
        );
      })}
    </div>
    ```

    NOTA: o conteúdo do card está propositalmente como placeholder (`#numero`) — o Plan 02 expandirá para card completo. NÃO adicione cliente/badge/total/telefone/ações aqui.

    Per D-04, troque o class name do highlight de `row-highlighted` para `card-highlighted` (a nova animação será adicionada ao CSS na Task 3).
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit src/app/status/page.tsx 2>&1 | grep -v "^$" || echo "tsc clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "kanban-board" apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c "PRODUCTION_STATUSES.map" apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c "kanban-column-header" apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c "production-table" apps/frontend/src/app/status/page.tsx` retorna 0 (tabela removida)
    - `grep -c '<table' apps/frontend/src/app/status/page.tsx` retorna 0
    - `grep -c "card-highlighted" apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c "row-highlighted" apps/frontend/src/app/status/page.tsx` retorna 0
    - `cd apps/frontend && npx tsc --noEmit` não retorna erros novos em page.tsx
  </acceptance_criteria>
  <done>
    Tabela removida. Kanban com 3 colunas renderiza no desktop. Cards são placeholders simples (#numero) que serão expandidos no Plan 02. Cabeçalho e estado vazio preservados.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar Bootstrap nav-tabs para mobile</name>
  <files>apps/frontend/src/app/status/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/status/page.tsx (após Task 1)
    - .planning/phases/26-status-producao-kanban/26-CONTEXT.md (D-03)
  </read_first>
  <action>
    Adicione, IMEDIATAMENTE ANTES do `<div className="kanban-board d-none d-md-flex gap-3">`, uma versão mobile que usa Bootstrap nav-tabs. A mobile (`d-md-none`) e o desktop (`d-none d-md-flex`) são mutuamente exclusivos via responsive utilities.

    Adicione novo state no topo do componente (junto aos outros `useState`):
    ```tsx
    const [activeMobileTab, setActiveMobileTab] = useState<string>("APROVADO");
    ```

    Estrutura mobile a inserir antes do bloco kanban-board:
    ```tsx
    <div className="kanban-mobile d-md-none">
      <ul className="nav nav-tabs nav-fill mb-3" role="tablist">
        {PRODUCTION_STATUSES.map((statusKey) => {
          const count = visibleQuotes.filter((q) => q.statusKey === statusKey).length;
          const label =
            statusKey === "APROVADO" ? "APROVADO" :
            statusKey === "EM_PRODUCAO" ? "EM PRODUÇÃO" :
            "PRONTO";
          return (
            <li className="nav-item" key={statusKey} role="presentation">
              <button
                type="button"
                className={`nav-link ${activeMobileTab === statusKey ? "active" : ""}`}
                onClick={() => setActiveMobileTab(statusKey)}
                role="tab"
                aria-selected={activeMobileTab === statusKey}
              >
                {label} <span className="badge bg-secondary ms-1">{count}</span>
              </button>
            </li>
          );
        })}
      </ul>
      <div className={`kanban-column kanban-column-${activeMobileTab.toLowerCase()}`}>
        <div className="kanban-column-body">
          {visibleQuotes.filter((q) => q.statusKey === activeMobileTab).length === 0 ? (
            <div className="kanban-column-empty text-muted small">Sem orçamentos</div>
          ) : (
            visibleQuotes
              .filter((q) => q.statusKey === activeMobileTab)
              .map((quote) => (
                <div
                  key={quote.id}
                  className={`kanban-card-placeholder ${highlightedId === quote.id ? "card-highlighted" : ""}`}
                >
                  #{quote.body.idorcamento ?? quote.internalNumber}
                </div>
              ))
          )}
        </div>
      </div>
    </div>
    ```

    Mantenha o mesmo placeholder `#numero` no mobile — Plan 02 substituirá ambos por um componente único de card.
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | grep "page.tsx" || echo "tsc clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nav nav-tabs nav-fill" apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c "activeMobileTab" apps/frontend/src/app/status/page.tsx` retorna >= 4 (declaração + onClick + comparações)
    - `grep -c "d-md-none" apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c "d-none d-md-flex" apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c 'useState<string>("APROVADO")' apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `cd apps/frontend && npx tsc --noEmit` sem erros novos em page.tsx
  </acceptance_criteria>
  <done>
    Mobile renderiza nav-tabs com contagem por status; trocar de aba muda a coluna visível. Desktop preserva 3 colunas lado a lado. Cards continuam placeholders.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atualizar CSS inline para kanban + animação card-highlighted</name>
  <files>apps/frontend/src/app/status/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/status/page.tsx (após Task 2, bloco `<style>` no final)
    - .planning/phases/26-status-producao-kanban/26-CONTEXT.md (D-04 — adaptar highlight para cards)
  </read_first>
  <action>
    Localize o bloco `<style>{`...`}</style>` no final do componente. NÃO remova `.status-aprovado`, `.status-em_producao`, `.status-pronto_para_entrega`, `.status-pill`, `.action-list`, `.orcamento-header`, `.orcamento-section`, `.logo-img`. Esses ainda são usados.

    Remova as regras `.production-table th`, `.production-table th, .production-table td` (referência a uma tabela que não existe mais).

    Renomeie a animação: troque `.row-highlighted` por `.card-highlighted` mantendo a mesma `@keyframes highlight-pulse`. Ajuste o estado final/start para usar `box-shadow` em vez de `background-color` para ficar visualmente correto em card:

    Adicione/atualize estas regras no bloco `<style>`:

    ```css
    .kanban-board { align-items: flex-start; }
    .kanban-column {
      flex: 1 1 0;
      min-width: 0;
      background: #f8f9fa;
      border-radius: 8px;
      padding: 0.75rem;
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }
    .kanban-column-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.5rem 0.75rem;
      border-radius: 6px;
      font-weight: 700;
      font-size: 0.95rem;
    }
    .kanban-column-title { letter-spacing: 0.02em; }
    .kanban-column-count {
      background: rgba(0,0,0,0.08);
      border-radius: 999px;
      padding: 0.1rem 0.6rem;
      font-size: 0.85rem;
      font-weight: 600;
    }
    .kanban-column-body { display: flex; flex-direction: column; gap: 0.5rem; }
    .kanban-column-empty { padding: 1rem 0.5rem; text-align: center; font-style: italic; }
    .kanban-card-placeholder {
      background: #fff;
      border-radius: 6px;
      padding: 0.75rem;
      box-shadow: 0 1px 4px rgba(0,0,0,0.08);
      font-weight: 600;
    }
    .card-highlighted { animation: highlight-pulse 3s ease-out; }
    @keyframes highlight-pulse {
      0%   { box-shadow: 0 0 0 4px #34d399, 0 1px 4px rgba(0,0,0,0.08); background: #d1fae5; }
      60%  { box-shadow: 0 0 0 4px #34d399, 0 1px 4px rgba(0,0,0,0.08); background: #d1fae5; }
      100% { box-shadow: 0 1px 4px rgba(0,0,0,0.08); background: #fff; }
    }
    @media (max-width: 768px) {
      .container { padding-inline: 1rem; }
      .kanban-column { padding: 0.5rem; }
    }
    ```

    A regra antiga `.row-highlighted` DEVE ser removida (foi renomeada para `.card-highlighted`).
  </action>
  <verify>
    <automated>grep -c "kanban-column-header" apps/frontend/src/app/status/page.tsx && grep -c "production-table" apps/frontend/src/app/status/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ".kanban-column " apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c ".kanban-column-header" apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c ".kanban-card-placeholder" apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c ".card-highlighted" apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c ".row-highlighted" apps/frontend/src/app/status/page.tsx` retorna 0
    - `grep -c ".production-table" apps/frontend/src/app/status/page.tsx` retorna 0
    - `grep -c "@keyframes highlight-pulse" apps/frontend/src/app/status/page.tsx` retorna 1 (apenas uma definição)
    - `cd apps/frontend && npm run build` (ou `npx next build` se script ausente) completa sem erro relacionado a page.tsx
  </acceptance_criteria>
  <done>
    CSS reflete o novo layout kanban. Animação de highlight migrada de row para card. Regras obsoletas de tabela removidas. Build do frontend passa.
  </done>
</task>

</tasks>

<verification>
- Inspeção visual desktop: 3 colunas lado a lado, cada uma com header colorido e contagem.
- Inspeção visual mobile (DevTools <768px): nav-tabs no topo, uma coluna visível, troca de tab muda a coluna.
- SSE: simular pagamento (ou disparar fetch manual) → cards aparecem nas colunas certas conforme `statusKey`.
- Banner persistente e cabeçalho (logo, Tempo real, Atualizar, Novo Orçamento, EFI badge) permanecem inalterados.
</verification>

<success_criteria>
- Tabela removida completamente (zero `<table>` na página).
- 3 colunas kanban renderizam por status; mobile usa tabs.
- Cards aparecem como placeholders `#numero` (Plan 02 expandirá).
- TypeScript compila; build do Next passa.
- Nenhuma regressão no SSE, banner, fetch, toast.
</success_criteria>

<output>
After completion, create `.planning/phases/26-status-producao-kanban/26-01-SUMMARY.md`
</output>
