---
phase: 26-status-producao-kanban
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - apps/frontend/src/app/status/page.tsx
autonomous: true
requirements: []
tags: [frontend, kanban, card, badge]
must_haves:
  truths:
    - "Cada card kanban mostra: número do orçamento, nome do cliente, badge de pagamento, valor total em BRL, telefone (texto)"
    - "Cada card oferece ações: Gerar/Abrir PDF, Chatwoot (se URL existir), link Detalhes → /orcamento/[id]"
    - "Card NÃO mostra vendedor, NÃO oferece avançar status, NÃO tem link wa.me"
    - "Animação card-highlighted dispara em cards quando statusKey muda via SSE"
  artifacts:
    - path: "apps/frontend/src/app/status/page.tsx"
      provides: "Componente QuoteCard interno renderizado em desktop e mobile"
      contains: "QuoteCard"
  key_links:
    - from: "QuoteCard"
      to: "handlePdf"
      via: "onClick do botão Gerar PDF"
      pattern: "onClick=\\{.*handlePdf"
    - from: "QuoteCard"
      to: "/orcamento/[id]"
      via: "Link Detalhes (Next Link ou <a>)"
      pattern: "/orcamento/\\$\\{"
---

<objective>
Substituir o placeholder `<div className="kanban-card-placeholder">#numero</div>` por um componente real de card que exibe todas as informações decididas em D-05 a D-10 e oferece as ações de D-10/D-11. Implementa D-05, D-06, D-07, D-08, D-09, D-10, D-11.

Purpose: Conteúdo completo do card. Após este plan, o kanban está visualmente completo — falta apenas o filtro (Plan 03).

Output: `page.tsx` com função interna `QuoteCard` (ou JSX inline reutilizado em mobile e desktop) renderizando cada orçamento.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-status-producao-kanban/26-CONTEXT.md
@.planning/phases/26-status-producao-kanban/26-01-PLAN.md
@apps/frontend/src/app/status/page.tsx

<interfaces>
<!-- Helpers já existentes que o card vai consumir -->

```typescript
function getBadgeType(quote: QuoteRow): "PAGO_CAIXA" | "PIX_CONFIRMADO" | "AGUARDANDO";
function getQuoteIdentifier(quote: QuoteRow): string;        // para link Detalhes
async function handlePdf(quote: QuoteRow): Promise<void>;    // já existente

// State já existente:
// pdfLoadingId, highlightedId
```

Mapeamento badge → label + classe Bootstrap:
- "PAGO_CAIXA" → "Pago no Caixa #{orderNumber}" → `bg-success`
- "PIX_CONFIRMADO" → "PIX Confirmado" → `bg-primary`
- "AGUARDANDO" → "Aguardando pagamento" → `bg-warning text-dark`

Formato BRL (D-08):
```typescript
total.toLocaleString("pt-BR", { style: "currency", currency: "BRL" })
```

Rota Detalhes (D-10): `/orcamento/${getQuoteIdentifier(quote)}` — usar `<a>` simples (não `next/link`) para consistência com o estilo atual da página (que já usa `<a href="/orcamento/novo#condPagamento">`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar componente interno QuoteCard com layout completo</name>
  <files>apps/frontend/src/app/status/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/status/page.tsx (estado após Plan 01)
    - .planning/phases/26-status-producao-kanban/26-CONTEXT.md (D-05 a D-11)
  </read_first>
  <action>
    Defina uma função interna `renderQuoteCard(quote: QuoteRow)` DENTRO do componente `StatusPage` (logo após `handlePdf`, antes do `return`). Ela retorna `ReactNode`. Encapsular como função interna permite reuso entre desktop e mobile.

    Estrutura JSX a retornar:
    ```tsx
    function renderQuoteCard(quote: QuoteRow) {
      const customerName = quote.body.cliente?.nome || "Cliente não informado";
      const phone = quote.body.cliente?.telefone || "Sem telefone";
      const total = quote.body.totais?.valor ?? 0;
      const totalBRL = total.toLocaleString("pt-BR", { style: "currency", currency: "BRL" });
      const quoteNumber = quote.body.idorcamento ?? quote.internalNumber;
      const badgeType = getBadgeType(quote);
      const orderNumber = quote.orderNumber ?? (quote.saleExternalId != null ? String(quote.saleExternalId) : null);
      const canOpenPdf = Boolean(quote.latestPdfUrl);
      const pdfBusy = pdfLoadingId === quote.id;
      const isHighlighted = highlightedId === quote.id;
      const detailsHref = `/orcamento/${getQuoteIdentifier(quote)}`;

      const badgeClass =
        badgeType === "PAGO_CAIXA" ? "bg-success" :
        badgeType === "PIX_CONFIRMADO" ? "bg-primary" :
        "bg-warning text-dark";
      const badgeLabel =
        badgeType === "PAGO_CAIXA" ? `Pago no Caixa${orderNumber ? ` #${orderNumber}` : ""}` :
        badgeType === "PIX_CONFIRMADO" ? "PIX Confirmado" :
        "Aguardando pagamento";
      const badgeIcon = badgeType === "PAGO_CAIXA" ? "bi-cash-coin" : badgeType === "PIX_CONFIRMADO" ? "bi-check-circle" : "bi-hourglass-split";

      return (
        <div
          key={quote.id}
          className={`kanban-card status-border-${quote.statusKey.toLowerCase()} ${isHighlighted ? "card-highlighted" : ""}`}
        >
          <div className="kanban-card-header d-flex align-items-center justify-content-between">
            <span className="kanban-card-number">#{quoteNumber}</span>
            <span className={`badge ${badgeClass}`}>
              <i className={`bi ${badgeIcon} me-1`} />{badgeLabel}
            </span>
          </div>
          <div className="kanban-card-client">{customerName}</div>
          <div className="kanban-card-meta d-flex align-items-center justify-content-between">
            <span className="kanban-card-total">{totalBRL}</span>
            <span className="kanban-card-phone text-muted small">{phone}</span>
          </div>
          <div className="kanban-card-actions d-flex flex-wrap gap-2 mt-2">
            {!canOpenPdf && (
              <button
                type="button"
                className="btn btn-sm btn-outline-primary"
                onClick={() => void handlePdf(quote)}
                disabled={pdfBusy}
              >
                {pdfBusy ? "Gerando..." : "Gerar PDF"}
              </button>
            )}
            {quote.latestPdfUrl && (
              <a className="btn btn-sm btn-outline-dark" href={quote.latestPdfUrl} target="_blank" rel="noreferrer">
                <i className="bi bi-file-earmark-pdf me-1" />PDF
              </a>
            )}
            {quote.chatwootConversationUrl && (
              <a className="btn btn-sm btn-success" href={quote.chatwootConversationUrl} target="_blank" rel="noreferrer">
                <i className="bi bi-chat me-1" />Chatwoot
              </a>
            )}
            <a className="btn btn-sm btn-outline-secondary" href={detailsHref}>
              <i className="bi bi-box-arrow-up-right me-1" />Detalhes
            </a>
          </div>
        </div>
      );
    }
    ```

    Restrições explícitas (D-06, D-09, D-11):
    - NÃO renderize `quote.body.vendedorNome` (D-06).
    - NÃO use `wa.me/` no telefone (D-09) — apenas `<span>` com texto.
    - NÃO renderize botão "Avançar status" / `quote.availableNextStatuses` (D-11).
  </action>
  <verify>
    <automated>grep -c "function renderQuoteCard" apps/frontend/src/app/status/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function renderQuoteCard" apps/frontend/src/app/status/page.tsx` retorna 1
    - `grep -c "getBadgeType(quote)" apps/frontend/src/app/status/page.tsx` retorna >= 1 (usado em renderQuoteCard)
    - `grep -c "vendedorNome" apps/frontend/src/app/status/page.tsx` retorna 0 (omitido por D-06)
    - `grep -c "wa.me" apps/frontend/src/app/status/page.tsx` retorna 0 (D-09)
    - `grep -c "availableNextStatuses" apps/frontend/src/app/status/page.tsx` retorna 1 ou menos (apenas no tipo `QuoteRow`, nunca renderizado)
    - `grep -c 'toLocaleString("pt-BR"' apps/frontend/src/app/status/page.tsx` retorna >= 1 (formato BRL)
    - `grep -c "/orcamento/\\$" apps/frontend/src/app/status/page.tsx` retorna >= 1 (link detalhes)
    - `cd apps/frontend && npx tsc --noEmit` sem novos erros em page.tsx
  </acceptance_criteria>
  <done>
    Função `renderQuoteCard` definida e disponível no escopo do componente. Não usada ainda — Task 2 conecta.
  </done>
</task>

<task type="auto">
  <name>Task 2: Conectar renderQuoteCard em desktop e mobile</name>
  <files>apps/frontend/src/app/status/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/status/page.tsx (estado após Task 1 deste plano)
  </read_first>
  <action>
    Substitua AMBAS as ocorrências de `kanban-card-placeholder` (uma no bloco desktop `<div className="kanban-board d-none d-md-flex">`, outra no bloco mobile `<div className="kanban-mobile d-md-none">`) pela chamada à função.

    Em cada bloco, troque:
    ```tsx
    columnQuotes.map((quote) => (
      <div
        key={quote.id}
        className={`kanban-card-placeholder ${highlightedId === quote.id ? "card-highlighted" : ""}`}
      >
        #{quote.body.idorcamento ?? quote.internalNumber}
      </div>
    ))
    ```
    Por:
    ```tsx
    columnQuotes.map((quote) => renderQuoteCard(quote))
    ```

    E na versão mobile equivalente, troque o `.map((quote) => <div className="kanban-card-placeholder">...</div>)` pelo mesmo `.map((quote) => renderQuoteCard(quote))`.

    NÃO mantenha referências a `kanban-card-placeholder` no JSX (mantenha apenas no CSS para retro-compatibilidade — Task 3 deste plano remove).
  </action>
  <verify>
    <automated>grep -c "renderQuoteCard(quote)" apps/frontend/src/app/status/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "renderQuoteCard(quote)" apps/frontend/src/app/status/page.tsx` retorna 2 (desktop + mobile)
    - `grep -c "kanban-card-placeholder" apps/frontend/src/app/status/page.tsx` retorna no máximo 1 (apenas no CSS, se ainda presente — será removido na Task 3)
    - `grep -E -c '<div[^>]*kanban-card-placeholder' apps/frontend/src/app/status/page.tsx` retorna 0 (zero JSX usando o placeholder)
    - `cd apps/frontend && npx tsc --noEmit` sem erros novos
  </acceptance_criteria>
  <done>
    Desktop e mobile renderizam cards completos via `renderQuoteCard`. Placeholders eliminados do JSX.
  </done>
</task>

<task type="auto">
  <name>Task 3: Adicionar CSS do kanban-card e remover kanban-card-placeholder</name>
  <files>apps/frontend/src/app/status/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/status/page.tsx (após Task 2)
  </read_first>
  <action>
    No bloco `<style>{`...`}</style>` no final do componente:

    1. REMOVA completamente o seletor `.kanban-card-placeholder { ... }` (adicionado no Plan 01 Task 3).
    2. ADICIONE as regras do card real:

    ```css
    .kanban-card {
      background: #fff;
      border-radius: 6px;
      padding: 0.75rem;
      box-shadow: 0 1px 4px rgba(0,0,0,0.08);
      border-top: 3px solid #ccc;
      display: flex;
      flex-direction: column;
      gap: 0.4rem;
    }
    .status-border-aprovado { border-top-color: #1f7a44; }
    .status-border-em_producao { border-top-color: #2457a6; }
    .status-border-pronto_para_entrega { border-top-color: #a65b12; }
    .kanban-card-header { gap: 0.5rem; }
    .kanban-card-number { font-weight: 700; font-size: 1rem; }
    .kanban-card-client { font-weight: 600; font-size: 0.95rem; }
    .kanban-card-meta { font-size: 0.9rem; }
    .kanban-card-total { font-weight: 600; color: #1f7a44; }
    .kanban-card-actions .btn { font-size: 0.82rem; padding: 0.25rem 0.55rem; }
    ```

    A animação `.card-highlighted` definida no Plan 01 Task 3 já é compatível com `.kanban-card` (ambos têm `box-shadow` base). Não duplique a `@keyframes`.
  </action>
  <verify>
    <automated>grep -c ".kanban-card " apps/frontend/src/app/status/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ".kanban-card " apps/frontend/src/app/status/page.tsx` retorna >= 1
    - `grep -c ".status-border-aprovado" apps/frontend/src/app/status/page.tsx` retorna 1
    - `grep -c ".status-border-em_producao" apps/frontend/src/app/status/page.tsx` retorna 1
    - `grep -c ".status-border-pronto_para_entrega" apps/frontend/src/app/status/page.tsx` retorna 1
    - `grep -c ".kanban-card-placeholder" apps/frontend/src/app/status/page.tsx` retorna 0 (totalmente removido)
    - `grep -c "@keyframes highlight-pulse" apps/frontend/src/app/status/page.tsx` retorna 1 (não duplicado)
    - `cd apps/frontend && npx next build` (ou `npm run build`) completa sem erro em page.tsx
  </acceptance_criteria>
  <done>
    CSS do card definido com borda superior colorida por status. Placeholder eliminado. Build passa.
  </done>
</task>

</tasks>

<verification>
- Carregar `/status` localmente; conferir que cards mostram número, cliente, badge (com ícone e cor), valor BRL, telefone, e botões PDF/Chatwoot/Detalhes.
- Conferir que vendedor NÃO aparece (D-06).
- Conferir que telefone é texto, sem link clicável (D-09).
- Conferir que NÃO há botão de avançar status (D-11).
- Clicar em "Detalhes" abre `/orcamento/[id]`.
- Disparar SSE de pagamento → card correspondente recebe animação `card-highlighted`.
</verification>

<success_criteria>
- Cards exibem exatamente os campos definidos em D-05 (sem vendedor, sem wa.me, sem avançar).
- Total formatado em BRL.
- Ações disponíveis: PDF (gerar/abrir), Chatwoot (condicional), Detalhes.
- Highlight de SSE funciona em card.
- Mobile e desktop usam o mesmo render function.
</success_criteria>

<output>
After completion, create `.planning/phases/26-status-producao-kanban/26-02-SUMMARY.md`
</output>
