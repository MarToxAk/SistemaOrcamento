---
status: passed
phase: 999.1-gerenciamento-de-layout-do-pdf-de-orcamento-pela-interface
verified: 2026-06-22
method: goal-backward (UAT manual aprovado + validação Nyquist + suítes automatizadas)
must_haves_verified: 8
must_haves_total: 8
---

# Verification — Fase 999.1

**Goal:** Permitir trocar o layout/template do PDF de orçamento a qualquer momento pelo
próprio sistema (usuário final), sem alterar código/git nem reiniciar o servidor.

## Veredito: PASSED ✓

O goal foi alcançado e validado por três trilhas independentes:

1. **UAT manual (aprovado pelo usuário):** fluxo ponta-a-ponta validado em runtime —
   galeria dos 3 presets, troca de ativo refletindo no PDF de um orçamento real **sem
   reiniciar o servidor**, upload com `<script>` rejeitado, preview, e rollback.
2. **Validação Nyquist (`999.1-VALIDATION.md`, status `validated`):** 8/8 comportamentos
   do contrato verdes, 63 testes, 0 falhas.
3. **Suítes automatizadas:** backend 272 testes verdes + build OK; frontend `tsc --noEmit`
   e `next build` verdes.

## Must-haves

| # | Must-have | Evidência | Status |
|---|-----------|-----------|--------|
| 1 | Trocar template ativo em runtime sem reiniciar | UAT passo 5 + `quotes-pdf-storage.service.test.ts` (D-04/D-05) | ✓ |
| 2 | Galeria com os 3 presets + indicação do ativo | UAT passo 1 + `pdf-templates.presets.test.ts` (D-06) | ✓ |
| 3 | Upload de template customizado | UAT passo 3 + `pdf-templates.service.test.ts` | ✓ |
| 4 | Upload perigoso (`<script>`) rejeitado | UAT passo 4 + `validateUpload` (D-02) | ✓ |
| 5 | Preview server-side antes de ativar | UAT passo 2 + `pdf-templates.controller.test.ts` (D-08) | ✓ |
| 6 | Render seguro (anti-SSRF, Handlebars isolado) | `quotes-pdf-storage.test.ts` (D-02) | ✓ |
| 7 | Acesso admin protegido | `admin-auth.guard.test.ts` (D-03) + gate de senha no painel | ✓ |
| 8 | Swap atômico (1 ativo) | `pdf-templates.service.test.ts` (D-04) | ✓ |

## Observações (não bloqueiam)

- Segurança encerrada com risco aceito documentado — ver `999.1-SECURITY.md`
  (`threats_open: 0`; T-AUTH mitigado com rate-limit, T-SANDBOX aceito p/ deploy interno).
- Dívida do code review registrada em `999.1-REVIEW.md` / `999.1-SECURITY.md`. **CR-01
  (gate fail-open + env de exemplo) exige ação pré-deploy** — definir as 3 variáveis de proteção.
