---
phase: 999.1-gerenciamento-de-layout-do-pdf-de-orcamento-pela-interface
plan: 04
type: execute
wave: 2
depends_on: ["999.1-01"]
files_modified:
  - apps/backend/templates/quote-default.hbs
  - apps/backend/templates/quote-minimal.hbs
  - apps/backend/templates/quote-classic.hbs
  - apps/backend/templates/assets/fonts.css
  - apps/backend/src/modules/pdf-templates/pdf-templates.presets.test.ts
autonomous: true
requirements: [WL-03]
user_setup: []

must_haves:
  truths:
    - "Os 3 presets renderizam com tipografia e icones corretos SEM acesso a rede"
    - "Nenhum dos 3 presets contem telefone/email/instagram BomCusto hardcoded"
    - "Os 3 presets consomem as mesmas variaveis Handlebars documentadas no topo do template"
    - "Com os 3 .hbs presentes, o seed (re-rodado) registra exatamente 3 presets com 1 isActive=true (colorido)"
  artifacts:
    - path: "apps/backend/templates/quote-minimal.hbs"
      provides: "Preset minimalista P&B (D-06)"
      min_lines: 80
    - path: "apps/backend/templates/quote-classic.hbs"
      provides: "Preset classico (D-06)"
      min_lines: 80
    - path: "apps/backend/templates/quote-default.hbs"
      provides: "Preset colorido com fontes/icones inline e contato dehardcoded"
  key_links:
    - from: "apps/backend/templates/quote-default.hbs"
      to: "contexto de render (vars injetadas no Plano 03)"
      via: "os .hbs consomem empresaTelefones/empresaEmail/empresaInstagram (D-07)"
      pattern: "empresaTelefones|empresaEmail|empresaInstagram"
---

<objective>
Entregar os 3 presets iniciais (D-06: colorido, minimalista P&B, classico) com duas
correcoes obrigatorias: (1) inlinar fontes/icones para sobreviver ao bloqueio total de
rede do Puppeteer (regressao critica do RESEARCH — Pitfall 2); (2) dehardcode do
contato BomCusto (telefones, email, instagram) nos arquivos .hbs, trocando os literais
pelas variaveis de empresa `{{empresaTelefones}}/{{empresaEmail}}/{{empresaInstagram}}`
que o Plano 03 ja injeta no contexto de render (D-07). Todos os presets consomem o mesmo
conjunto de variaveis Handlebars para trocar de layout sem perder dados.

ESCOPO: este plano toca SOMENTE os arquivos `.hbs` + assets de fonte/icone. A injecao
das 3 variaveis no servico (ConfigService) e a documentacao no .env.example ficam no
Plano 03, que e o unico dono de quotes-pdf-storage.service.ts (evita colisao de arquivo
na Wave 2).

Purpose: sem fontes/icones inline os presets quebram visualmente quando o Plano 03
bloqueia a rede; sem dehardcode o branding BomCusto vaza para outras empresas.
Output: 3 .hbs (colorido refatorado + minimal + classic) consumindo as vars de empresa,
assets de fonte inline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/999.1-gerenciamento-de-layout-do-pdf-de-orcamento-pela-interface/999.1-CONTEXT.md
@.planning/phases/999.1-gerenciamento-de-layout-do-pdf-de-orcamento-pela-interface/999.1-RESEARCH.md
@apps/backend/templates/quote-default.hbs
@apps/backend/src/modules/quotes/quotes-pdf-storage.service.ts
@.claude/skills/bom-custo-design/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dehardcode contato no quote-default.hbs usando as vars de empresa (D-07)</name>
  <files>apps/backend/templates/quote-default.hbs</files>
  <read_first>
    - apps/backend/templates/quote-default.hbs (bloco contact-list linhas 171-180: telefones, email, instagram hardcoded; cabecalho de variaveis no topo linhas 5-19)
    - .planning/phases/999.1-gerenciamento-de-layout-do-pdf-de-orcamento-pela-interface/999.1-CONTEXT.md (D-07: orcamento@bomcustoilhabela.com.br, @bomcustopapelaria)
    - .planning/phases/999.1-gerenciamento-de-layout-do-pdf-de-orcamento-pela-interface/999.1-03-PLAN.md (Plano 03 injeta empresaTelefones/empresaEmail/empresaInstagram no contexto)
  </read_first>
  <action>
    APENAS no arquivo .hbs (a injecao das variaveis no servico ja foi feita no Plano 03,
    que tambem documenta o .env.example — NAO tocar em quotes-pdf-storage.service.ts nem
    .env.example aqui). Em quote-default.hbs, substituir os valores BomCusto hardcoded do
    bloco contact-list pelas variaveis ja disponiveis no contexto: o telefone vira
    `{{empresaTelefones}}`, o email `{{empresaEmail}}`, o instagram `{{empresaInstagram}}`;
    envolver cada linha em `{{#if}}` para nao renderizar icone/linha quando a variavel
    estiver vazia (D-07 elimina o vazamento de branding da fase 35/36). Atualizar o
    cabecalho de documentacao de variaveis no topo do .hbs para listar as 3 variaveis.
    Confirmar que nenhum valor BomCusto literal (telefone, dominio de email, handle)
    permanece no .hbs.
  </action>
  <verify>
    <automated>cd apps/backend && grep -L "bomcustoilhabela\|bomcustopapelaria\|99648-4918" templates/quote-default.hbs >/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    quote-default.hbs nao contem telefone/email/instagram BomCusto literais; usa
    {{empresaTelefones}}/{{empresaEmail}}/{{empresaInstagram}} com {{#if}}; cabecalho de
    variaveis atualizado.
  </acceptance_criteria>
  <done>Contato dehardcoded no template colorido usando as vars de empresa (D-07).</done>
</task>

<task type="auto">
  <name>Task 2: Inline de fontes/icones no preset colorido (anti-regressao de rede)</name>
  <files>apps/backend/templates/quote-default.hbs, apps/backend/templates/assets/fonts.css</files>
  <read_first>
    - apps/backend/templates/quote-default.hbs (links externos linhas 34-37: Google Fonts + jsDelivr bootstrap-icons; uso de classes bi bi-* nas linhas ~172-179)
    - .planning/phases/999.1-.../999.1-RESEARCH.md (Pitfall 2: bloqueio de rede quebra o colorido; A3: fonts-liberation no Dockerfile; Environment Availability)
    - .claude/skills/bom-custo-design/SKILL.md (Bootstrap Icons 1.11.1; tokens de cor de marca)
  </read_first>
  <action>
    Remover os `<link>` externos para fonts.googleapis.com e cdn.jsdelivr.net
    (linhas ~34-37). Substituir por estrategia que funciona sem rede (Pitfall 2/A3):
    (a) Fontes: usar fontes do sistema/`fonts-liberation` ja presentes no Dockerfile
    como stack primaria no font-family, OU embutir Mulish/JetBrains Mono via @font-face
    base64 em assets/fonts.css inlinado no <style>/<head> do template — escolher a opcao
    que preserva melhor a identidade; se base64, manter o tamanho do template dentro do
    limite de 512KB do upload. (b) Icones: substituir cada `<i class="bi bi-...">` por
    SVG inline correspondente (geo-alt, telephone, envelope, instagram) ou por caractere
    Unicode equivalente — nao depender de bootstrap-icons via CDN. Criar
    assets/fonts.css se optar por base64 (referenciado/inlinado no build do .hbs).
    Confirmar que o template nao referencia mais nenhuma URL http(s) externa.
  </action>
  <verify>
    <automated>cd apps/backend && grep -c "fonts.googleapis.com\|cdn.jsdelivr.net" templates/quote-default.hbs | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    quote-default.hbs nao tem nenhum link/URL externo de fonte ou icone; fontes via
    sistema/base64 e icones via SVG inline/Unicode; renderiza sem rede.
  </acceptance_criteria>
  <done>Preset colorido sobrevive ao bloqueio de rede (Pitfall 2).</done>
</task>

<task type="auto">
  <name>Task 3: Presets minimalista P&B e classico + teste de render sem rede</name>
  <files>apps/backend/templates/quote-minimal.hbs, apps/backend/templates/quote-classic.hbs, apps/backend/src/modules/pdf-templates/pdf-templates.presets.test.ts</files>
  <read_first>
    - apps/backend/templates/quote-default.hbs (apos Tasks 1-2: conjunto canonico de variaveis e secoes — cabecalho, cliente, itens, carimbos, totais)
    - .planning/phases/999.1-.../999.1-RESEARCH.md (D-06 tres layouts; Test Map: 3 presets renderizam sem rede e sem contato hardcoded)
    - .claude/skills/bom-custo-design/SKILL.md (tokens de cor/tipo de marca para o preset colorido; minimal/classic sao layouts alternativos)
  </read_first>
  <action>
    Criar quote-minimal.hbs (D-06 preset 2: minimalista preto-e-branco, sem cores de
    marca, tipografia sobria, linhas finas) e quote-classic.hbs (D-06 preset 3:
    classico, layout mais formal/serifado). Ambos: (a) consomem EXATAMENTE as mesmas
    variaveis Handlebars do colorido (idorcamento, dataorcamento, cliente.*, itens[],
    carimbos.itens[], totais.*, empresaNome/Cnpj/Endereco/LogoUrl/Cor/Telefones/Email/
    Instagram) — para trocar de layout sem perder dados; (b) sem nenhuma URL externa de
    fonte/icone (mesma regra da Task 2); (c) sem contato BomCusto hardcoded (usam as
    variaveis de empresa). Criar pdf-templates.presets.test.ts (integration) que, para
    cada um dos 3 presets, compila com Handlebars (knownHelpersOnly) usando um mock de
    orcamento e asserta: a compilacao nao lanca; o HTML resultante nao contem nenhuma
    URL externa (regex http(s)://fonts/cdn) nem literais BomCusto. Apos criar minimal/
    classic, RE-RODAR o seed do Plano 01 (agora os 3 .hbs existem) e assertar o invariante
    que migra do Plano 01 para ca: exatamente 3 registros PdfTemplate e exatamente 1 com
    isActive=true (o colorido). Esta assercao de D-06 completo pertence a esta onda porque
    so agora os arquivos minimal/classic existem.
  </action>
  <verify>
    <automated>cd apps/backend && npx jest pdf-templates.presets --silent</automated>
  </verify>
  <acceptance_criteria>
    quote-minimal.hbs e quote-classic.hbs existem, usam o mesmo conjunto de variaveis,
    sem URLs externas, sem contato hardcoded; teste de render dos 3 presets verde.
  </acceptance_criteria>
  <done>3 presets (D-06) prontos e validados para render sem rede.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| presets (repo) -> render | presets sao confiaveis (no repo), mas devem respeitar o bloqueio de rede do Plano 03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-12 | Information Disclosure | presets referenciando CDN externa apos bloqueio de rede | mitigate | inline de fontes/icones nos 3 presets (Pitfall 2); teste asserta ausencia de URL externa |
| T-999.1-13 | Information Disclosure | branding BomCusto vazando p/ outras empresas | mitigate | dehardcode do contato para variaveis de empresa (D-07); teste asserta ausencia de literais |
</threat_model>

<verification>
- grep confirma zero URLs externas (fonts.googleapis/jsdelivr) nos 3 .hbs.
- grep confirma zero literais BomCusto (telefone/dominio/handle) nos 3 .hbs.
- pdf-templates.presets.test.ts verde: os 3 compilam e o HTML nao tem URL externa.
- Seed re-rodado: exatamente 3 presets registrados, exatamente 1 isActive=true (colorido) — D-06 completo.
</verification>

<success_criteria>
3 presets (D-06) com fontes/icones inline (sobrevivem ao bloqueio de rede do Plano 03)
e contato dehardcoded em variaveis de empresa (D-07), todos consumindo o mesmo conjunto
de variaveis Handlebars.
</success_criteria>

<output>
Create `.planning/phases/999.1-gerenciamento-de-layout-do-pdf-de-orcamento-pela-interface/999.1-04-SUMMARY.md` when done
</output>
