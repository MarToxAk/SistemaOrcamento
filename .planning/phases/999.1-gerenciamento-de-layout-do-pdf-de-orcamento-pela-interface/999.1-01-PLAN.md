---
phase: 999.1-gerenciamento-de-layout-do-pdf-de-orcamento-pela-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/prisma/schema.prisma
  - apps/backend/prisma/migrations
  - apps/backend/src/modules/pdf-templates/pdf-templates.types.ts
  - apps/backend/scripts/seed-pdf-templates.ts
  - apps/backend/src/modules/security/admin-auth.guard.test.ts
  - apps/backend/src/modules/pdf-templates/pdf-templates.service.test.ts
  - apps/backend/src/modules/quotes/quotes-pdf-storage.test.ts
  - apps/backend/src/modules/pdf-templates/pdf-templates.controller.test.ts
autonomous: true
requirements: [WL-03]
user_setup: []

must_haves:
  truths:
    - "Existe a tabela PdfTemplate no banco com no maximo um registro isActive=true"
    - "Apos o seed, ha 3 presets (colorido, minimalista, classico) e o colorido esta ativo"
    - "Os scaffolds de teste (Wave 0) existem e falham ate as implementacoes das ondas seguintes"
  artifacts:
    - path: "apps/backend/prisma/schema.prisma"
      provides: "Modelo PdfTemplate com isActive"
      contains: "model PdfTemplate"
    - path: "apps/backend/scripts/seed-pdf-templates.ts"
      provides: "Seed idempotente dos 3 presets"
    - path: "apps/backend/src/modules/pdf-templates/pdf-templates.types.ts"
      provides: "Tipos compartilhados do dominio de templates"
  key_links:
    - from: "apps/backend/scripts/seed-pdf-templates.ts"
      to: "apps/backend/prisma/schema.prisma"
      via: "prisma.pdfTemplate.upsert para cada preset"
      pattern: "pdfTemplate\\.(upsert|create|updateMany)"
---

<objective>
Criar a fundacao de armazenamento de templates de PDF: modelo Prisma `PdfTemplate`
com flag `isActive`, migration, seed idempotente dos 3 presets (D-06) e tipos
compartilhados do dominio. Tambem cria os scaffolds de teste (Wave 0) que as ondas
seguintes vao satisfazer (Nyquist).

Implementa D-04 (storage em banco/Prisma, single-tenant com um ativo por deploy) e
prepara D-06 (3 presets seedados). Sem esta base, render, upload e ativacao nao tem
onde persistir.

Purpose: dar a `renderHtml()` (Plano 03) e ao modulo de gerenciamento (Plano 05)
um storage editavel em runtime com swap atomico de ativo.
Output: tabela PdfTemplate + migration + seed + tipos + 4 scaffolds de teste.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.1-gerenciamento-de-layout-do-pdf-de-orcamento-pela-interface/999.1-CONTEXT.md
@.planning/phases/999.1-gerenciamento-de-layout-do-pdf-de-orcamento-pela-interface/999.1-RESEARCH.md
@apps/backend/prisma/schema.prisma
@apps/backend/templates/quote-default.hbs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modelo PdfTemplate + migration</name>
  <files>apps/backend/prisma/schema.prisma, apps/backend/prisma/migrations/</files>
  <read_first>
    - apps/backend/prisma/schema.prisma (datasource postgresql, convencoes de model/enum existentes)
    - .planning/STATE.md (Decisions Log 2026-05-22: migrations aplicadas via DATABASE_URL do .env)
  </read_first>
  <action>
    Adicionar ao schema.prisma o modelo `PdfTemplate` implementando D-04. Campos:
    `id` String @id @default(cuid()); `name` String (rotulo exibido na galeria, ex:
    "Colorido"); `slug` String @unique (identificador estavel: colorful, minimal,
    classic, ou slug do upload); `source` String @db.Text (conteudo Handlebars/HTML
    bruto do template); `kind` enum PdfTemplateKind { PRESET CUSTOM } (presets seedados
    vs uploads de usuario, D-01); `isActive` Boolean @default(false); `createdAt`
    DateTime @default(now()); `updatedAt` DateTime @updatedAt. Adicionar
    `@@index([isActive])`. Single-tenant: a unicidade do ativo (no maximo um
    isActive=true) e garantida por transacao no Plano 05, nao por constraint de banco
    (Postgres nao tem partial unique trivial via Prisma) — documentar isso em comentario
    no model. Gerar a migration com `npm --workspace @bomcusto/backend exec prisma
    migrate dev --name add_pdf_template --create-only` (apenas criar o SQL; nao aplicar
    em ambiente compartilhado automaticamente). Rodar `prisma generate` para atualizar o
    client. NAO criar tabela empresa/config nova — apenas PdfTemplate.
  </action>
  <verify>
    <automated>cd apps/backend && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    `prisma validate` passa; existe `model PdfTemplate` com `isActive` e enum
    `PdfTemplateKind`; ha um diretorio de migration novo com o CREATE TABLE.
  </acceptance_criteria>
  <done>Schema com PdfTemplate validado e migration gerada; client regenerado.</done>
</task>

<task type="auto">
  <name>Task 2: Tipos compartilhados + seed idempotente dos 3 presets</name>
  <files>apps/backend/src/modules/pdf-templates/pdf-templates.types.ts, apps/backend/scripts/seed-pdf-templates.ts</files>
  <read_first>
    - apps/backend/templates/quote-default.hbs (fonte do preset colorido — sera lido pelo seed)
    - .planning/phases/999.1-.../999.1-RESEARCH.md (Pitfall 4: seed idempotente, colorido ativo por padrao)
  </read_first>
  <action>
    Criar `pdf-templates.types.ts` exportando: tipo `PdfTemplateSummary`
    (id, name, slug, kind, isActive, createdAt) usado pela listagem; tipo
    `ActiveTemplate` (slug, source) usado por renderHtml; constante `PRESET_SLUGS`
    = ["colorful","minimal","classic"] e `DEFAULT_ACTIVE_SLUG` = "colorful".
    Criar `scripts/seed-pdf-templates.ts`: script idempotente (executavel via ts-node/
    node) que faz `prisma.pdfTemplate.upsert` por slug para os 3 presets. O source de
    cada preset e lido de `apps/backend/templates/quote-default.hbs`, `quote-minimal.hbs`
    e `quote-classic.hbs` via readFileSync (os arquivos minimal/classic sao criados no
    Plano 04 — o seed deve tolerar ausencia: se o arquivo nao existir, pular aquele
    preset com aviso, sem falhar, implementando D-06 incrementalmente). Marcar
    `colorful` como `isActive: true` no upsert e garantir os demais `isActive: false`.
    O seed deve preservar comportamento atual (Pitfall 4: colorido ativo => fallback nao
    e necessario). Registrar o seed no `bootstrap-runtime.sh` (mesmo ponto onde rodam as
    migrations) se o arquivo existir; caso contrario documentar no SUMMARY que o operador
    deve chamar o seed apos migrate.
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -c "pdf-templates" | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    `pdf-templates.types.ts` compila e exporta os tipos/constantes; o seed usa
    `prisma.pdfTemplate.upsert` por slug, marca colorful ativo, tolera presets
    ausentes sem lancar erro.
  </acceptance_criteria>
  <done>Tipos compartilhados e seed idempotente prontos; seed integra ao bootstrap.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffolds de teste Wave 0 (Nyquist)</name>
  <files>apps/backend/src/modules/security/admin-auth.guard.test.ts, apps/backend/src/modules/pdf-templates/pdf-templates.service.test.ts, apps/backend/src/modules/quotes/quotes-pdf-storage.test.ts, apps/backend/src/modules/pdf-templates/pdf-templates.controller.test.ts</files>
  <read_first>
    - apps/backend/jest.config.js (testRegex .*\.test\.ts$, rootDir src)
    - .planning/phases/999.1-.../999.1-RESEARCH.md (Validation Architecture: Phase Requirements -> Test Map, Wave 0 Gaps)
    - apps/backend/src/modules/security/internal-auth.guard.ts (padrao a espelhar nos testes do guard)
  </read_first>
  <action>
    Criar os 4 arquivos de teste listados nos Wave 0 Gaps do RESEARCH.md. Cada arquivo
    contem `describe` + `it` com `it.todo` ou um `expect` que falha intencionalmente,
    documentando o comportamento que as ondas seguintes implementarao, conforme o Test
    Map do RESEARCH.md: admin-auth.guard (rejeita sem x-admin-api-key — D-03);
    pdf-templates.service (rejeita upload com script/eventos; compile com helper
    desconhecido lanca; ativar zera os demais — D-02/D-04); quotes-pdf-storage
    (renderHtml usa ativo, sem ativo cai no fallback; renderPdfBuffer aborta http
    externo — D-02/D-05); pdf-templates.controller (preview retorna PDF sem persistir —
    D-08). Os testes devem ser placeholders honestos: usar `it.todo("...")` para os que
    dependem de codigo ainda inexistente, garantindo que a suite roda sem erro de
    importacao de modulos ausentes (nao importar arquivos que ainda nao existem; descrever
    o comportamento em texto). NAO inserir nos `<action>` ou comentarios os literais que
    gates negativos buscarem nos planos seguintes.
  </action>
  <verify>
    <automated>cd apps/backend && npx jest --listTests 2>&1 | grep -c "pdf-templates\|admin-auth\|quotes-pdf-storage" | grep -qE '^[3-9]$|^[1-9][0-9]+$'</automated>
  </verify>
  <acceptance_criteria>
    Os 4 arquivos de teste existem, Jest os reconhece, e a suite roda sem erro de
    importacao (testes pendentes via it.todo sao aceitaveis nesta onda).
  </acceptance_criteria>
  <done>Scaffolds Wave 0 criados e reconhecidos pelo Jest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed/migration -> banco | dados de seed sao controlados pelo deploy (confiavel), mas presets futuros vem de upload (tratado nos planos 03/05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-01 | Tampering | seed-pdf-templates.ts | accept | seed le apenas arquivos do repo (confiaveis); presets de upload sao validados no Plano 05 |
| T-999.1-02 | Denial of Service | migration em producao | mitigate | migration gerada com --create-only; aplicacao manual via DATABASE_URL (padrao do projeto), seed idempotente (Pitfall 4) |
</threat_model>

<verification>
- `prisma validate` passa e a migration cria a tabela PdfTemplate.
- Seed idempotente: rodar duas vezes nao duplica registros (upsert por slug).
- Apos seed com os 3 .hbs presentes, exatamente 1 registro tem isActive=true (colorful).
- Jest reconhece os 4 scaffolds de teste.
</verification>

<success_criteria>
Tabela PdfTemplate criada, seed idempotente dos presets pronto (colorido ativo),
tipos compartilhados exportados, scaffolds Wave 0 presentes — base pronta para os
Planos 03 (render) e 05 (gerenciamento).
</success_criteria>

<output>
Create `.planning/phases/999.1-gerenciamento-de-layout-do-pdf-de-orcamento-pela-interface/999.1-01-SUMMARY.md` when done
</output>
