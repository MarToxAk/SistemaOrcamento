---
phase: 22-efi-webhook-pix-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/efi/efi.service.ts
  - apps/backend/src/modules/integrations/nfse/nfse.service.ts
autonomous: true
requirements: [EFIWH-01, EFIWH-02]

must_haves:
  truths:
    - "getWebhookUrl() retorna URL terminada em /webhook/payment/pix"
    - "NfseService usa endpoint padrão quando NFSE_SOAP_URL é string vazia"
  artifacts:
    - path: "apps/backend/src/modules/integrations/efi/efi.service.ts"
      provides: "getWebhookUrl() corrigido"
      contains: "webhook/payment/pix"
    - path: "apps/backend/src/modules/integrations/nfse/nfse.service.ts"
      provides: "getters de URL com fallback para string vazia"
  key_links:
    - from: "efi.service.ts getWebhookUrl()"
      to: "EFI Pay webhook registration"
      via: "configureWebhook()"
      pattern: "webhook/payment/pix"
---

<objective>
Corrigir dois bugs de URL de integração:
1. `getWebhookUrl()` no EFI Service retorna URL sem sufixo `/pix`, fazendo a EFI enviar eventos para o endpoint errado.
2. `NfseService` usa `??` para fallback de URL SOAP, que não trata string vazia — causando erro `ENOENT '?wsdl'` quando a variável está definida mas vazia.

Purpose: Garantir que webhooks EFI cheguem ao endpoint correto e que NFS-e funcione com variável vazia no .env.
Output: efi.service.ts e nfse.service.ts corrigidos; build passa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<interfaces>
<!-- Trecho relevante de efi.service.ts (linha ~486-492) -->
```typescript
private getWebhookUrl(): string {
  const base = this.config.get<string>("BACKEND_URL") ?? this.config.get<string>("APP_BASE_URL") ?? "http://localhost:4000/api";
  const baseNoTrailing = base.replace(/\/$/, "");
  return `${baseNoTrailing}/integrations/efi/webhook/payment`;  // BUG: falta /pix
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir getWebhookUrl() no EFI Service</name>
  <files>apps/backend/src/modules/integrations/efi/efi.service.ts</files>
  <action>
    Localizar o método privado `getWebhookUrl()` (linha ~489) e alterar o `return` de:
    ```
    return `${baseNoTrailing}/integrations/efi/webhook/payment`;
    ```
    para:
    ```
    return `${baseNoTrailing}/integrations/efi/webhook/payment/pix`;
    ```
    Não alterar mais nada no arquivo.
    URL de produção resultante: `https://orcamentoapi.bomcustoilhabela.com.br/api/integrations/efi/webhook/payment/pix`
  </action>
  <verify>grep -n "webhook/payment/pix" apps/backend/src/modules/integrations/efi/efi.service.ts</verify>
  <done>grep retorna a linha com `/pix` dentro de getWebhookUrl()</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir fallback de URL vazia no NfseService</name>
  <files>apps/backend/src/modules/integrations/nfse/nfse.service.ts</files>
  <action>
    Localizar as linhas onde `NFSE_SOAP_URL` e `NFSE_AUX_URL` são lidos via `config.get()` e usam `??` para fallback.
    Substituir o padrão:
    ```typescript
    config.get<string>("NFSE_SOAP_URL") ?? DEFAULT_ENDPOINT
    ```
    por:
    ```typescript
    config.get<string>("NFSE_SOAP_URL")?.trim() || DEFAULT_ENDPOINT
    ```
    Aplicar o mesmo para `NFSE_AUX_URL` / `NFSE_AUX_URL` se existir padrão similar.
    Não alterar lógica de negócio, apenas os getters de URL que usam `??` como fallback de string.
  </action>
  <verify>grep -n "trim.*||" apps/backend/src/modules/integrations/nfse/nfse.service.ts</verify>
  <done>grep retorna ao menos uma linha com `.trim() ||` para as URLs NFSE</done>
</task>

<task type="auto">
  <name>Task 3: Build e verificação</name>
  <files></files>
  <action>
    Executar o build do backend para garantir que não há erros de TypeScript:
    ```
    npm --workspace @bomcusto/backend run build
    ```
    O build deve terminar sem erros de compilação.
  </action>
  <verify>npm --workspace @bomcusto/backend run build 2>&1 | tail -5</verify>
  <done>Build completa sem erros (exit code 0)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EFI Pay → backend | Payload de webhook de PIX recebido sem autenticação obrigatória |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Spoofing | /webhook/payment/pix | accept | Endpoint @Public() por design; EFI não usa assinatura no plano atual; guard opcional permanece disponível |
| T-22-02 | Tampering | getWebhookUrl() | mitigate | URL registrada na EFI é gerada via BACKEND_URL (env var); não exposta a input de usuário |
</threat_model>

<verification>
1. `grep -n "webhook/payment/pix" apps/backend/src/modules/integrations/efi/efi.service.ts` → retorna linha dentro de `getWebhookUrl()`
2. `grep -n "trim.*||" apps/backend/src/modules/integrations/nfse/nfse.service.ts` → retorna linhas de URL NFSE
3. Build backend sem erros
</verification>

<success_criteria>
- getWebhookUrl() retorna `…/integrations/efi/webhook/payment/pix`
- NfseService não usa `??` como fallback para variáveis de URL (usa `?.trim() ||`)
- Build do backend passa sem erros TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/22-efi-webhook-pix-fix/22-01-SUMMARY.md`
</output>
