---
phase: 22-efi-webhook-pix-fix
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - apps/backend/src/modules/integrations/efi/efi.service.spec.ts
autonomous: true
requirements: [EFIWH-03]

must_haves:
  truths:
    - "Teste unitário verifica que getWebhookUrl() retorna URL com /pix"
    - "Suite de testes do webhook EFI passa sem erros"
  artifacts:
    - path: "apps/backend/src/modules/integrations/efi/efi.service.spec.ts"
      provides: "teste de getWebhookUrl() e endpoint /pix"
      contains: "/pix"
  key_links:
    - from: "efi.service.spec.ts"
      to: "efi.service.ts getWebhookUrl()"
      via: "jest unit test"
      pattern: "webhook/payment/pix"
---

<objective>
Adicionar teste unitário que verifica que `getWebhookUrl()` retorna a URL com sufixo `/pix`.
Se o spec do EFI já existir, adicionar o caso dentro dele; se não existir, criar com scaffold mínimo.

Purpose: Garantir regressão — futuras mudanças na função serão detectadas automaticamente.
Output: spec com ao menos um `it()` cobrindo a URL com `/pix`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/22-efi-webhook-pix-fix/22-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teste unitário de getWebhookUrl()</name>
  <files>apps/backend/src/modules/integrations/efi/efi.service.spec.ts</files>
  <behavior>
    - Test: getWebhookUrl() com BACKEND_URL configurado retorna URL terminada em /webhook/payment/pix
    - Test: getWebhookUrl() sem BACKEND_URL usa fallback e ainda termina em /webhook/payment/pix
  </behavior>
  <action>
    Verificar se o arquivo `efi.service.spec.ts` já existe.
    Se sim: adicionar `describe('getWebhookUrl', () => { ... })` com os dois casos acima usando acesso via `(service as any).getWebhookUrl()` (método privado).
    Se não: criar scaffold mínimo com `TestingModule` mockando `ConfigService` e adicionar os dois testes.
    
    Padrão de mock para ConfigService:
    ```typescript
    const mockConfig = { get: jest.fn() };
    mockConfig.get.mockImplementation((key: string) => {
      if (key === 'BACKEND_URL') return 'https://example.com/api';
      return undefined;
    });
    ```
    
    Assertion esperada:
    ```typescript
    expect((service as any).getWebhookUrl()).toContain('/webhook/payment/pix');
    ```
  </action>
  <verify>npm --workspace @bomcusto/backend run test -- --testPathPattern=efi.service 2>&1 | tail -20</verify>
  <done>Testes de efi.service passam; grep '/pix' no spec retorna ao menos um it()</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Service | Testes internos sem exposição externa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | — | test suite | accept | Plano de teste não introduz superfície de ataque |
</threat_model>

<verification>
1. `npm --workspace @bomcusto/backend run test -- --testPathPattern=efi.service` passa
2. `grep -n "/pix" apps/backend/src/modules/integrations/efi/efi.service.spec.ts` retorna resultado
</verification>

<success_criteria>
- Spec de efi.service inclui teste de getWebhookUrl() com `/pix`
- npm test passa para o arquivo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/22-efi-webhook-pix-fix/22-02-SUMMARY.md`
</output>
