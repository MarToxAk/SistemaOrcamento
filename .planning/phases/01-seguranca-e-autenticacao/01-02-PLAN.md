---
phase: 01-seguranca-e-autenticacao
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/backend/package.json
  - apps/backend/src/modules/integrations/efi/efi.controller.ts
  - apps/backend/src/modules/integrations/nfse/nfse.controller.ts
  - apps/backend/src/modules/quotes/quotes.controller.ts
  - apps/backend/src/modules/integrations/chatwoot/chatwoot.controller.ts
  - apps/backend/src/modules/integrations/pdv/pdv.controller.ts
  - apps/backend/src/modules/security/efi-webhook.guard.ts
  - apps/backend/src/modules/security/throttle.config.ts
  - apps/backend/src/modules/app.module.ts
autonomous: true
requirements:
  - FR-01.3
  - FR-01.5
  - NFR-01
must_haves:
  truths:
    - "Webhook EFI sem assinatura valida retorna 401"
    - "Webhook EFI com assinatura valida processa normalmente"
    - "Endpoints sensiveis possuem limite de taxa"
    - "Rotas publicas da fase estao explicitamente marcadas"
  artifacts:
    - path: "apps/backend/src/modules/security/efi-webhook.guard.ts"
      provides: "Validacao HMAC do webhook EFI"
    - path: "apps/backend/src/modules/integrations/efi/efi.controller.ts"
      provides: "Uso de guard de assinatura nos webhooks"
    - path: "apps/backend/src/modules/app.module.ts"
      provides: "ThrottlerModule configurado"
    - path: "apps/backend/src/modules/quotes/quotes.controller.ts"
      provides: "Anotacoes de rotas publicas"
  key_links:
    - from: "apps/backend/src/modules/security/efi-webhook.guard.ts"
      to: "apps/backend/src/modules/integrations/efi/efi.controller.ts"
      via: "@UseGuards(EfiWebhookGuard)"
      pattern: "UseGuards\(EfiWebhookGuard\)"
    - from: "apps/backend/src/modules/app.module.ts"
      to: "controllers sensiveis"
      via: "ThrottlerModule + decorators"
      pattern: "ThrottlerModule"
---

<objective>
Blindar endpoints de pagamento e operacoes sensiveis com assinatura webhook e rate limiting, completando o hardening operacional da fase 1.

Purpose: reduzir spoofing/tampering em eventos financeiros e proteger API contra abuso.
Output: webhook EFI autenticado e limites de request aplicados em rotas de risco.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-seguranca-e-autenticacao/01-CONTEXT.md
@.planning/phases/01-seguranca-e-autenticacao/01-RESEARCH.md
@apps/backend/src/modules/integrations/efi/efi.controller.ts
@apps/backend/src/modules/quotes/quotes.controller.ts
@apps/backend/src/modules/integrations/nfse/nfse.controller.ts
@apps/backend/src/modules/integrations/chatwoot/chatwoot.controller.ts
@apps/backend/src/modules/integrations/pdv/pdv.controller.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar validacao HMAC no webhook EFI (D-03)</name>
  <files>apps/backend/src/modules/security/efi-webhook.guard.ts, apps/backend/src/modules/integrations/efi/efi.controller.ts</files>
  <behavior>
    - Requisicao para webhook sem `x-gn-signature` deve retornar 401.
    - Requisicao com assinatura invalida deve retornar 401.
    - Requisicao com assinatura valida deve chegar ao service.
  </behavior>
  <action>Criar guard `EfiWebhookGuard` que calcule HMAC-SHA256 do payload bruto usando `EFI_WEBHOOK_SECRET` e compare com `x-gn-signature` em tempo constante (per D-03). Aplicar `@UseGuards(EfiWebhookGuard)` nos dois endpoints webhook no `efi.controller.ts`. Manter resposta 200 apenas para assinaturas validas.</action>
  <verify>
    <automated>npm --workspace @bomcusto/backend run build</automated>
  </verify>
  <done>Webhook rejeita chamadas nao assinadas/forjadas e aceita payload assinado corretamente.</done>
</task>

<task type="auto">
  <name>Task 2: Configurar throttling para endpoints sensiveis (D-05)</name>
  <files>apps/backend/package.json, apps/backend/src/modules/security/throttle.config.ts, apps/backend/src/modules/app.module.ts, apps/backend/src/modules/integrations/efi/efi.controller.ts, apps/backend/src/modules/integrations/nfse/nfse.controller.ts, apps/backend/src/modules/quotes/quotes.controller.ts</files>
  <action>Adicionar `@nestjs/throttler` e registrar `ThrottlerModule` no `AppModule` com perfil padrao e perfil estrito para rotas sensiveis (per D-05). Aplicar decorators de throttle para webhook EFI, emissao NFS-e e geracao PDF. Definir limites iniciais conservadores e centralizar constantes em `throttle.config.ts`.</action>
  <verify>
    <automated>npm --workspace @bomcusto/backend run build</automated>
  </verify>
  <done>Throttling ativo globalmente e rotas criticas com limites mais restritos.</done>
</task>

<task type="auto">
  <name>Task 3: Marcar rotas publicas e garantir protecao nas demais (D-02)</name>
  <files>apps/backend/src/modules/quotes/quotes.controller.ts, apps/backend/src/modules/integrations/nfse/nfse.controller.ts, apps/backend/src/modules/integrations/chatwoot/chatwoot.controller.ts, apps/backend/src/modules/integrations/pdv/pdv.controller.ts</files>
  <action>Anotar explicitamente com `@Public()` apenas as rotas publicas previstas pela fase (status/aprovacao do cliente, se aplicavel no backend) e manter endpoints internos protegidos por padrao (per D-02). Evitar expandir escopo para RBAC completo nesta fase.</action>
  <verify>
    <automated>npm --workspace @bomcusto/backend run build</automated>
  </verify>
  <done>Excecoes publicas ficam documentadas no codigo e todos os endpoints internos permanecem sob guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| efi webhook -> backend | evento financeiro externo nao confiavel |
| internet/clientes -> endpoints quote/nfse/pdf | superficie de ataque sujeita a abuso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | S | webhook EFI | mitigate | HMAC-SHA256 em guard dedicado com rejeicao 401 |
| T-01-06 | T | webhook payload | mitigate | validar assinatura antes de processar no service |
| T-01-07 | D | endpoints PDF/NFS-e | mitigate | throttling estrito por rota |
| T-01-08 | E | exposicao de rota interna | mitigate | `@Public` apenas em excecoes explicitas |
</threat_model>

<verification>
- `npm --workspace @bomcusto/backend run build`
- Requisicao webhook sem assinatura retorna 401
- Requisicao webhook com assinatura valida retorna 200
- Burst de requests em rota sensivel retorna 429 apos limite
</verification>

<success_criteria>
- Webhooks EFI so processam eventos autenticados.
- Endpoints de alto risco ficam protegidos por throttling.
- Rotas publicas sao minimas e explicitas; restante protegido por default.
</success_criteria>

<output>
After completion, create `.planning/phases/01-seguranca-e-autenticacao/01-02-SUMMARY.md`
</output>
