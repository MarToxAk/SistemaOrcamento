---
phase: 19-aprovacao-associada-caixa-athos
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01-PLAN.md
files_modified:
  - apps/backend/src/modules/quotes/quotes.service.ts
  - apps/backend/src/modules/quotes/quotes.service.unit.test.ts
autonomous: true
requirements:
  - APR-01
  - APR-02
  - APR-03
  - ATHC-01
  - ATHC-02
  - ATHC-03
  - TRG-01
  - TRG-02
  - TRG-03
  - TXT-01
  - TXT-02

must_haves:
  truths:
    - "changeStatus para EM_PRODUCAO bloqueia se customer.isAssociated=false, independente de approved"
    - "changeStatus distingue: 'sem associacao' vs 'sem pagamento' nas mensagens de erro (ASCII, sem mojibake)"
    - "changeStatus permite EM_PRODUCAO quando isAssociated=true AND (approved=true OR saleExternalId!=null)"
    - "getById dispara conciliarViaCaixaAthos quando quote tem externalQuoteId"
    - "conciliarViaCaixaAthos persiste saleExternalId a partir de relacao_orcamento_venda se ainda nao definido"
    - "conciliarViaCaixaAthos chama checkPaymentStatus para PENDENTE/ENVIADO (mesmo pipeline PIX/cartao)"
    - "Falha em conciliarViaCaixaAthos nao quebra resposta do getById"
  artifacts:
    - path: "apps/backend/src/modules/quotes/quotes.service.ts"
      provides: "guard corrigido em changeStatus + metodo conciliarViaCaixaAthos + getById atualizado"
      contains: "conciliarViaCaixaAthos"
    - path: "apps/backend/src/modules/quotes/quotes.service.unit.test.ts"
      provides: "testes do novo guard e da conciliacao"
      contains: "sem associacao"
  key_links:
    - from: "quotes.service.ts getById"
      to: "athosService.buscarRelacaoOrcamentoVenda"
      via: "conciliarViaCaixaAthos"
      pattern: "buscarRelacaoOrcamentoVenda"
    - from: "quotes.service.ts changeStatus"
      to: "customer.isAssociated + quote.saleExternalId"
      via: "guard EM_PRODUCAO"
      pattern: "isAssociated"
---

<objective>
Corrigir o guard de EM_PRODUCAO em QuotesService para exigir associacao Athos valida e pagamento confirmado, com mensagens ASCII claras; adicionar `conciliarViaCaixaAthos` para que `getById` use `relacao_orcamento_venda` como fonte de conciliacao.

Purpose: Resolver o bug do 400 incorreto, implementar verificacao de pagamento por relacao_orcamento_venda, e garantir mensagens de erro sem mojibake.

Output: QuotesService atualizado + testes de unidade atualizados cobrindo todos os cenarios.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-aprovacao-associada-caixa-athos/19-01-SUMMARY.md

<interfaces>
De apps/backend/src/modules/quotes/quotes.service.ts (linhas relevantes):

```typescript
// Guard ATUAL (linha ~656-661) — a ser substituido:
if (newStatus === "EM_PRODUCAO") {
  const isAssociated = Boolean((quote as any).customer?.isAssociated ?? false);
  if (!quote.approved && !isAssociated) {
    throw new BadRequestException("Orçamento precisa ser aprovado pelo cliente antes de entrar em produção");
  }
}

// getById (linha ~205-213) — trecho a ser alterado:
if (["PENDENTE", "ENVIADO"].includes(resolvedQuote.status)) {
  void this.checkPaymentStatus(resolvedQuote.id).catch((err: unknown) => {
    this.logger.warn(`Disparo de checagem de pagamento falhou para orcamento ${resolvedQuote.id}: ...`);
  });
}

// checkPaymentStatus (linha ~239): metodo existente — NAO modificar
async checkPaymentStatus(identifier: string) { ... }

// toBigInt (ja existe como metodo privado)
private toBigInt(value: number): bigint | null { ... }

// this.athosService.buscarRelacaoOrcamentoVenda (adicionado no plano 19-01):
async buscarRelacaoOrcamentoVenda(idorcamento: number): Promise<{ idvenda: number | null }>
```

Schema do Quote (campos relevantes):
```typescript
quote.approved: boolean         // true apenas via approveByToken
quote.saleExternalId: bigint | null  // idvenda Athos, persistido por checkPaymentStatus
quote.externalQuoteId: bigint | null // idorcamento Athos
quote.status: QuoteStatus
(quote as any).customer?.isAssociated: boolean
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Corrigir guard EM_PRODUCAO em changeStatus + mensagens ASCII</name>
  <files>
    apps/backend/src/modules/quotes/quotes.service.ts
    apps/backend/src/modules/quotes/quotes.service.unit.test.ts
  </files>
  <behavior>
    - isAssociated=false → BadRequestException "sem associacao" (APR-01, APR-02)
    - isAssociated=true, approved=false, saleExternalId=null → BadRequestException "sem pagamento" (APR-03)
    - isAssociated=true, approved=true → permite EM_PRODUCAO
    - isAssociated=true, saleExternalId=123n → permite EM_PRODUCAO
    - isAssociated=false, approved=false → mensagem menciona AMBOS ("sem associacao" — primeiro check)
  </behavior>
  <action>
Em quotes.service.ts, substituir o guard de EM_PRODUCAO (linhas ~656-661) por:

```typescript
// APR-01/02/03: Bloqueia EM_PRODUCAO sem associacao Athos valida e sem pagamento confirmado
if (newStatus === "EM_PRODUCAO") {
  const isAssociated = Boolean((quote as any).customer?.isAssociated ?? false);
  const hasSaleId = quote.saleExternalId != null;
  const hasApproval = Boolean(quote.approved);

  if (!isAssociated && !hasApproval && !hasSaleId) {
    throw new BadRequestException(
      "Orcamento sem associacao com cliente Athos e sem pagamento confirmado. Associe o orcamento e verifique o pagamento antes de entrar em producao.",
    );
  }
  if (!isAssociated) {
    throw new BadRequestException(
      "Orcamento sem associacao com cliente Athos valido. Associe o orcamento a um idcliente antes de entrar em producao.",
    );
  }
  if (!hasApproval && !hasSaleId) {
    throw new BadRequestException(
      "Orcamento sem pagamento confirmado. Verifique o pagamento no Caixa Athos antes de entrar em producao.",
    );
  }
}
```

IMPORTANTE:
- Usar apenas ASCII (sem acentos, cedilha, til) nas mensagens de BadRequestException novas — isso e TXT-01/TXT-02.
- Nao alterar nenhuma outra parte de changeStatus.

Em quotes.service.unit.test.ts, atualizar/adicionar os seguintes cases no describe "transicoes invalidas":

1. Manter "deve lancar BadRequestException ao tentar EM_PRODUCAO sem aprovacao e sem associado" mas atualizar a mensagem esperada para "sem associacao" (ou "sem associacao...e sem pagamento" para o caso ambos).
2. Adicionar: "deve lancar BadRequestException com mensagem clara quando associado mas sem pagamento confirmado"
   - makeQuote({ status: "APROVADO", approved: false, saleExternalId: null, customer: { isAssociated: true } })
   - expect throw BadRequestException com "sem pagamento"
3. Adicionar: "deve permitir EM_PRODUCAO quando associado e aprovado"
   - makeQuote({ status: "APROVADO", approved: true, customer: { isAssociated: true } })
   - deve resolver sem throw
4. Adicionar: "deve permitir EM_PRODUCAO quando associado e tem saleExternalId"
   - makeQuote({ status: "APROVADO", approved: false, saleExternalId: BigInt(42), customer: { isAssociated: true } })
   - deve resolver sem throw
  </action>
  <verify>
    <automated>cd apps/backend ; npx jest quotes.service.unit --no-coverage 2>&1 | tail -25</automated>
  </verify>
  <done>
    Todos os testes de changeStatus passam. Guard novo com 3 condicionais em EM_PRODUCAO presente.
    Mensagens de erro sem caracteres especiais (grep -c "Orçamento" apps/backend/src/modules/quotes/quotes.service.ts deve ser 0 para linhas de throw).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar conciliarViaCaixaAthos e atualizar getById</name>
  <files>
    apps/backend/src/modules/quotes/quotes.service.ts
    apps/backend/src/modules/quotes/quotes.service.unit.test.ts
  </files>
  <behavior>
    - getById com quote.externalQuoteId!=null → dispara conciliarViaCaixaAthos (fire-and-forget)
    - getById com quote.externalQuoteId==null e status PENDENTE → continua usando checkPaymentStatus (fallback)
    - conciliarViaCaixaAthos com idvenda encontrado e saleExternalId=null → persiste saleExternalId (idempotente: nao atualiza se ja preenchido)
    - conciliarViaCaixaAthos com status PENDENTE → chama checkPaymentStatus (mesmo pipeline)
    - conciliarViaCaixaAthos com status APROVADO → nao chama checkPaymentStatus
    - falha em conciliarViaCaixaAthos → logger.warn, sem throw, getById responde normalmente
  </behavior>
  <action>
Em quotes.service.ts:

1. Substituir o bloco de `checkPaymentStatus` fire-and-forget em `getById` (linhas ~205-213):

```typescript
// ATHC-01: verificar pagamento via relacao_orcamento_venda quando orcamento tem ID Athos
if (resolvedQuote.externalQuoteId) {
  void this.conciliarViaCaixaAthos(resolvedQuote).catch((err: unknown) => {
    this.logger.warn(
      `Conciliacao Caixa Athos falhou para orcamento ${resolvedQuote.id}: ${err instanceof Error ? err.message : String(err)}`,
    );
  });
} else if (["PENDENTE", "ENVIADO"].includes(resolvedQuote.status)) {
  void this.checkPaymentStatus(resolvedQuote.id).catch((err: unknown) => {
    this.logger.warn(
      `Disparo de checagem de pagamento falhou para orcamento ${resolvedQuote.id}: ${err instanceof Error ? err.message : String(err)}`,
    );
  });
}
```

2. Adicionar o metodo privado `conciliarViaCaixaAthos` apos `checkPaymentStatus`:

```typescript
private async conciliarViaCaixaAthos(quote: any): Promise<void> {
  const idOrcamento = Number(quote.externalQuoteId);
  if (!Number.isFinite(idOrcamento) || idOrcamento <= 0) return;

  // ATHC-02: Buscar vinculo idorcamento → idvenda em relacao_orcamento_venda
  const relacao = await this.athosService.buscarRelacaoOrcamentoVenda(idOrcamento);

  // TRG-02: Idempotencia — persistir saleExternalId apenas se ainda nao definido
  if (relacao.idvenda != null && !quote.saleExternalId) {
    try {
      await this.prisma.quote.update({
        where: { id: quote.id },
        data: { saleExternalId: this.toBigInt(relacao.idvenda) },
      });
      this.logger.log(
        `conciliarViaCaixaAthos: saleExternalId=${relacao.idvenda} persistido para orcamento ${quote.id}`,
      );
    } catch (err) {
      this.logger.warn(
        `conciliarViaCaixaAthos: falha ao persistir saleExternalId para ${quote.id}: ${err instanceof Error ? err.message : String(err)}`,
      );
    }
  }

  // TRG-01/TRG-03: Acionar mesma rotina de conciliacao de PIX/cartao para status pendentes
  if (["PENDENTE", "ENVIADO"].includes(quote.status)) {
    await this.checkPaymentStatus(quote.id);
  }
}
```

NOTAS IMPORTANTES:
- `conciliarViaCaixaAthos` e PRIVADO (`private async`).
- ATHC-03: falhas ja sao capturadas pelo `.catch()` no caller em getById.
- Nao alterar a assinatura de checkPaymentStatus.
- TRG-02: o check `!quote.saleExternalId` garante idempotencia baseada no estado do quote no momento de getById (race conditions sao aceitaveis dado o contexto de leitura).

Em quotes.service.unit.test.ts, adicionar describe "conciliarViaCaixaAthos (via getById)" com mocks para:
- athosService.buscarRelacaoOrcamentoVenda mockado
- prisma.quote.update mockado
- Verificar que update e chamado quando idvenda encontrado e saleExternalId=null
- Verificar que update NAO e chamado quando saleExternalId ja preenchido
- Verificar que checkPaymentStatus e chamado quando status=PENDENTE (via getById → conciliarViaCaixaAthos → checkPaymentStatus)

NOTA: Como `conciliarViaCaixaAthos` e privado, testa via `getById` com mocks adequados.
  </action>
  <verify>
    <automated>cd apps/backend ; npx jest quotes.service.unit --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <done>
    Todos os testes de quotes.service.unit passam.
    `conciliarViaCaixaAthos` presente em quotes.service.ts.
    `getById` usa `externalQuoteId` como branching para a nova conciliacao.
    `buscarRelacaoOrcamentoVenda` mockado nos novos testes de getById.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin API → changeStatus | Input: identifier (string), newStatusInput (string) — validados antes de chegar aqui |
| getById → Athos pool | conciliarViaCaixaAthos usa query parametrizada via buscarRelacaoOrcamentoVenda |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | changeStatus guard bypass | mitigate | Guard verifica isAssociated no objeto customer incluido via Prisma — nao vem do request |
| T-19-04 | Elevation of Privilege | EM_PRODUCAO sem pagamento | mitigate | Guard duplo: isAssociated E (approved OR saleExternalId) antes de permitir transicao |
| T-19-05 | Denial of Service | conciliarViaCaixaAthos fire-and-forget lento | accept | void + catch — nao bloqueia response do getById |
| T-19-06 | Information Disclosure | mensagens de erro revealam schema interno | accept | Mensagens descrevem logica de negocio, nao detalhes de infra |
</threat_model>

<verification>
- `npx jest quotes.service.unit --no-coverage` passa (todos os testes verdes)
- `grep -c "conciliarViaCaixaAthos" apps/backend/src/modules/quotes/quotes.service.ts` >= 2 (declaracao + chamada)
- `grep -rn "sem associacao" apps/backend/src/modules/quotes/quotes.service.ts` retorna linha com throw
- `grep -rn "sem pagamento" apps/backend/src/modules/quotes/quotes.service.ts` retorna linha com throw
- `grep -v "^\\s*//" apps/backend/src/modules/quotes/quotes.service.ts | grep -c "Orçamento precisa ser aprovado"` == 0 (mensagem antiga removida)
</verification>

<success_criteria>
1. changeStatus para EM_PRODUCAO: bloqueia se !isAssociated (msg "sem associacao"), bloqueia se !approved && !saleExternalId (msg "sem pagamento"), permite quando ambos satisfeitos.
2. getById com externalQuoteId: dispara conciliarViaCaixaAthos em vez do checkPaymentStatus direto.
3. conciliarViaCaixaAthos: consulta relacao_orcamento_venda, persiste saleExternalId de forma idempotente, aciona checkPaymentStatus para status pendentes.
4. Todas as mensagens de erro novas em ASCII (sem acentos/cedilha nas strings de throw).
5. Suite de testes de unidade passa sem erros.
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/19-aprovacao-associada-caixa-athos/19-02-SUMMARY.md`
</output>
