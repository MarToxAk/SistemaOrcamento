---
phase: 19-aprovacao-associada-caixa-athos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/athos/athos.service.ts
  - apps/backend/src/modules/integrations/athos/athos.service.test.ts
autonomous: true
requirements:
  - ATHC-02
  - ATHC-03
  - TRG-01

must_haves:
  truths:
    - "AthosService expoe metodo buscarRelacaoOrcamentoVenda(idorcamento) que retorna { idvenda: number | null }"
    - "Falha na query Athos retorna { idvenda: null } sem lancar excecao"
    - "Metodo usa pool existente e libera conexao no finally"
  artifacts:
    - path: "apps/backend/src/modules/integrations/athos/athos.service.ts"
      provides: "metodo buscarRelacaoOrcamentoVenda"
      contains: "buscarRelacaoOrcamentoVenda"
    - path: "apps/backend/src/modules/integrations/athos/athos.service.test.ts"
      provides: "testes do novo metodo"
      contains: "buscarRelacaoOrcamentoVenda"
  key_links:
    - from: "quotes.service.ts (plan 19-02)"
      to: "athos.service.ts"
      via: "this.athosService.buscarRelacaoOrcamentoVenda()"
      pattern: "buscarRelacaoOrcamentoVenda"
---

<objective>
Adicionar `AthosService.buscarRelacaoOrcamentoVenda()` — metodo que consulta a tabela `relacao_orcamento_venda` no Athos ERP para encontrar o `idvenda` vinculado a um orcamento.

Purpose: Prover o contrato de acesso a dados que o plano 19-02 vai usar para implementar a conciliacao via Caixa Athos e o pipeline de trigger equivalente a PIX/cartao.

Output: Metodo publico em AthosService + testes cobrindo sucesso, nao encontrado, e erro de conexao.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<!-- INTERFACES RELEVANTES (extraidas do codebase) -->
<interfaces>
De apps/backend/src/modules/integrations/athos/athos.service.ts:
```typescript
// Pool e helpers ja importados: Pool, PoolClient, isSafeIdentifier, findExistingTable
// Metodo de acesso ao pool existente:
private getPool(): Pool { ... }
// Logger: this.logger (Logger do NestJS)
// Padrao de metodos existentes (referencia para novo metodo):
async verificarPagamentoPorOrcamento(
  orcamentoId: string,
  vendaId?: number | string | null,
): Promise<{ paid: boolean; idVenda: number | string | null; valor: number }>
// Padrao: client = await pool.connect() ... finally { client?.release() }
```

Estrutura da tabela no Athos ERP (conhecida, nao precisa discovery):
```sql
CREATE TABLE relacao_orcamento_venda (
  idrelataocaorcamentovenda serial PRIMARY KEY,
  idorcamento integer,
  idvenda integer
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar buscarRelacaoOrcamentoVenda no AthosService</name>
  <files>
    apps/backend/src/modules/integrations/athos/athos.service.ts
    apps/backend/src/modules/integrations/athos/athos.service.test.ts
  </files>
  <behavior>
    - buscarRelacaoOrcamentoVenda(5) quando tabela tem row {idorcamento:5, idvenda:42} → { idvenda: 42 }
    - buscarRelacaoOrcamentoVenda(99) quando tabela nao tem row para 99 → { idvenda: null }
    - buscarRelacaoOrcamentoVenda(1) quando pool.connect() rejeita → { idvenda: null } (sem throw)
  </behavior>
  <action>
Em athos.service.ts, adicionar apos o metodo verificarPagamentoPorOrcamento:

```typescript
async buscarRelacaoOrcamentoVenda(idorcamento: number): Promise<{ idvenda: number | null }> {
  this.logger.log(`buscarRelacaoOrcamentoVenda: idorcamento=${idorcamento}`);
  const pool = this.getPool();
  let client: PoolClient | null = null;
  try {
    client = await pool.connect();
    const result = await client.query(
      'SELECT idvenda FROM relacao_orcamento_venda WHERE idorcamento = $1 ORDER BY idrelataocaorcamentovenda DESC LIMIT 1',
      [idorcamento],
    );
    if (result.rows.length === 0) {
      this.logger.log(`buscarRelacaoOrcamentoVenda: nenhuma venda encontrada para idorcamento=${idorcamento}`);
      return { idvenda: null };
    }
    const raw = result.rows[0].idvenda;
    const idvenda = typeof raw === 'number' && Number.isFinite(raw)
      ? raw
      : Number.isFinite(Number(raw)) ? Number(raw) : null;
    this.logger.log(`buscarRelacaoOrcamentoVenda: idorcamento=${idorcamento} → idvenda=${idvenda}`);
    return { idvenda };
  } catch (error) {
    this.logger.warn(
      `buscarRelacaoOrcamentoVenda: erro para idorcamento=${idorcamento}: ${error instanceof Error ? error.message : String(error)}`,
    );
    return { idvenda: null };
  } finally {
    client?.release();
  }
}
```

Em athos.service.test.ts, adicionar describe block `"AthosService - buscarRelacaoOrcamentoVenda"` com os tres cenarios behavior acima. Mockar `pool.connect()` retornando um objeto com metodo `query` e `release`. Seguir o padrao dos describes existentes no arquivo (instanciar servico, mockar pool, etc.).

IMPORTANTE: Nao modificar nenhum metodo existente em athos.service.ts. Apenas adicionar o novo metodo.
  </action>
  <verify>
    <automated>cd apps/backend ; npx jest athos.service.test --no-coverage --testPathPattern=athos.service.test 2>&1 | tail -20</automated>
  </verify>
  <done>Todos os testes passam. buscarRelacaoOrcamentoVenda aparece no arquivo. Os tres cenarios (found, not-found, error) cobertos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → Athos ERP (read-only pg pool) | Query parametrizada com $1 — sem interpolacao de string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Injection | buscarRelacaoOrcamentoVenda query | mitigate | Usa query parametrizada ($1), idorcamento e number — sem interpolacao |
| T-19-02 | Denial of Service | pool.connect() sem timeout | accept | Pool ja tem connectionTimeoutMillis configurado globalmente, max:5 conexoes |
</threat_model>

<verification>
- `npx jest athos.service.test --no-coverage` passa sem erros
- `grep -c "buscarRelacaoOrcamentoVenda" apps/backend/src/modules/integrations/athos/athos.service.ts` >= 1
- `grep -c "buscarRelacaoOrcamentoVenda" apps/backend/src/modules/integrations/athos/athos.service.test.ts` >= 1
</verification>

<success_criteria>
Metodo publico `buscarRelacaoOrcamentoVenda` existe em AthosService, retorna `{ idvenda: number | null }`, captura erros de pool sem throw, e tem testes cobrindo os tres comportamentos.
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/19-aprovacao-associada-caixa-athos/19-01-SUMMARY.md`
</output>
