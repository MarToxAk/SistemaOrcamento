---
phase: "08"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/app/orcamento/[id]/status/page.tsx
autonomous: true
requirements:
  - UX-02
  - UX-03

must_haves:
  truths:
    - "Página /status exibe badge com emoji inline para cada status (ex: '🎨 Em Produção')"
    - "Abaixo do badge, aparece descrição em português amigável correspondente ao status"
    - "Status PAGAMENTO_PARCIAL tem mapeamento e é exibido corretamente"
    - "Cor do badge é semântica por status (não depende de classes Bootstrap genéricas)"
  artifacts:
    - path: "apps/frontend/src/app/orcamento/[id]/status/page.tsx"
      provides: "Página de status reformulada com STATUS_INFO map"
      contains: "STATUS_INFO"
  key_links:
    - from: "STATUS_INFO map"
      to: "statusKey (vindo do backend)"
      via: "lookup STATUS_INFO[statusKey]"
      pattern: "STATUS_INFO\\[statusKey\\]"
---

<objective>
Reformular a página `/orcamento/[id]/status` para exibir o status do pedido com emoji inline no badge e descrição amigável em português abaixo, cumprindo UX-02 e UX-03.

Purpose: Cliente vê o estado do serviço de forma clara, com ícone visual e descrição que explica o que significa cada status.
Output: `status/page.tsx` atualizado com STATUS_INFO map completo substituindo STATUS_CLASS.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-ux-paginas-publicas-do-cliente/08-CONTEXT.md
@.planning/phases/08-ux-paginas-publicas-do-cliente/08-RESEARCH.md

<interfaces>
<!-- Contrato de dados da API (extraído de apps/backend/src/modules/quotes/quotes.service.ts) -->

statusKey chega em lowercase do backend:
```
"pendente" | "enviado" | "aprovado" | "pagamento_parcial" |
"em_producao" | "pronto_para_entrega" | "entregue" | "cancelado"
```

statusLabel chega como string do backend (ex: "Em producao") — usar como fallback se statusKey não estiver no mapa.

Padrão visual existente nas duas páginas:
- Fundo: `#f9f7ed`
- Gradiente header: `linear-gradient(135deg,#c5f2e8 0%,#cbe1f9 25%,#e7d8f9 50%,#f9e7f5 75%,#f0cacb 100%)`
- Card: `border-radius: 16px`, `box-shadow: 0 4px 24px rgba(0,0,0,0.10)`, `max-width: 480px`
- Logo: `/media/logo_new.svg`
- CSS existente `.status-pill`: `font-size: 1.1rem; padding: 10px 24px; border-radius: 50px; display: inline-block;`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir STATUS_CLASS por STATUS_INFO com emoji, descrição e cor por status</name>
  <files>apps/frontend/src/app/orcamento/[id]/status/page.tsx</files>
  <action>
Substituir o objeto `STATUS_CLASS` (que mapeia para classes Bootstrap) por `STATUS_INFO` com a estrutura:
```typescript
const STATUS_INFO: Record<string, { emoji: string; label: string; description: string; color: string }> = {
  pendente:             { emoji: "🕐", label: "Pendente",             description: "Seu orçamento foi recebido e está aguardando confirmação.",                       color: "#6c757d" },
  enviado:              { emoji: "📤", label: "Enviado",              description: "O orçamento foi enviado para análise. Aguardando sua aprovação.",                  color: "#0d6efd" },
  aprovado:             { emoji: "✅", label: "Aprovado",             description: "Orçamento aprovado! Em breve seu pedido entra em produção.",                      color: "#198754" },
  pagamento_parcial:    { emoji: "💰", label: "Pagamento Parcial",    description: "Recebemos parte do pagamento. Aguardando quitação para iniciar a produção.",      color: "#fd7e14" },
  em_producao:          { emoji: "🎨", label: "Em Produção",          description: "Seu pedido está sendo produzido pela nossa equipe.",                              color: "#ffc107" },
  pronto_para_entrega:  { emoji: "📦", label: "Pronto para Retirada", description: "Seu pedido está pronto! Pode passar na loja quando quiser.",                     color: "#0dcaf0" },
  entregue:             { emoji: "🎉", label: "Entregue",             description: "Pedido entregue. Obrigado pela preferência!",                                    color: "#6f42c1" },
  cancelado:            { emoji: "❌", label: "Cancelado",            description: "Este orçamento foi cancelado. Dúvidas? Fale conosco.",                            color: "#dc3545" },
};
```

Lookup no render:
```typescript
const info = STATUS_INFO[statusKey];
const badgeEmoji = info?.emoji ?? "";
const badgeLabel = info?.label ?? statusLabel;
const badgeDescription = info?.description ?? "";
const badgeColor = info?.color ?? "#6c757d";
```

Substituir o JSX do badge de:
```tsx
<span className={`${badgeClass} status-pill`}>{statusLabel}</span>
```
Para:
```tsx
<span
  className="status-pill"
  style={{ backgroundColor: badgeColor, color: "#fff" }}
>
  {badgeEmoji} {badgeLabel}
</span>
{badgeDescription && (
  <p className="text-muted mt-3 mb-0 small px-2">{badgeDescription}</p>
)}
```

Remover a variável `badgeClass` e o import/uso de `STATUS_CLASS`. Manter todo o restante da página sem alteração (header, logo, loading state, error state, updatedAt, contato WhatsApp).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/frontend/tsconfig.json 2>&1 | Select-String "status" | Select-Object -First 5</automated>
  </verify>
  <done>
    - STATUS_INFO definido com 8 entradas (incluindo pagamento_parcial)
    - Badge exibe emoji + label inline (ex: "🎨 Em Produção")
    - Descrição aparece abaixo do badge quando status é conhecido
    - Nenhum erro TypeScript no arquivo
    - STATUS_CLASS removido
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → frontend | statusKey e statusLabel vêm do backend via /api/quotes/[id] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information | status/page.tsx | accept | Página é pública por design — exibe apenas status do pedido, sem dados sensíveis como valores ou endereços |
| T-08-02 | Tampering | STATUS_INFO lookup | accept | statusKey vem do backend autenticado; lookup com fallback gracioso se chave desconhecida |
</threat_model>

<verification>
- Abrir `/orcamento/[id]/status` no browser com um orçamento real
- Badge exibe emoji + label: "🎨 Em Produção"
- Descrição aparece abaixo: "Seu pedido está sendo produzido pela nossa equipe."
- Layout mantém card branco, gradiente e logo
- TypeScript compila sem erros: `npx tsc --noEmit -p apps/frontend/tsconfig.json`
</verification>

<success_criteria>
- STATUS_INFO implementado com 8 entradas completas
- Badge com emoji inline funcionando em todos os status
- Descrição em português exibida abaixo do badge
- Zero erros de TypeScript
- Visual coerente com a identidade da marca (gradiente, card, logo)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-ux-paginas-publicas-do-cliente/08-01-SUMMARY.md`
</output>
