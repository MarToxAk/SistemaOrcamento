---
phase: "08"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/app/orcamento/[id]/approve/page.tsx
autonomous: true
requirements:
  - UX-01
  - UX-03

must_haves:
  truths:
    - "Estado 'success' (pós-clique) exibe texto informando que equipe foi notificada e pedido entra em produção"
    - "Estado 'already-approved' (acesso ao link após já aprovado) exibe mensagem específica diferenciada"
    - "Estado 'already-approved' é distinguível visualmente do estado 'success'"
    - "Número do orçamento visível em ambos os estados de confirmação"
  artifacts:
    - path: "apps/frontend/src/app/orcamento/[id]/approve/page.tsx"
      provides: "Página de aprovação com estados success e already-approved distintos"
      contains: "already-approved"
  key_links:
    - from: "useEffect (load)"
      to: "state already-approved"
      via: "data?.approved === true → setState('already-approved')"
      pattern: "already-approved"
---

<objective>
Melhorar os estados de confirmação da página `/orcamento/[id]/approve`: ajustar texto do `success` (pós-clique) e adicionar state `already-approved` (acesso ao link após aprovação prévia), cumprindo D-03 e D-04 do CONTEXT.md.

Purpose: Cliente recebe feedback claro e diferenciado — seja após aprovar agora, seja ao revisitar o link de um orçamento já aprovado.
Output: `approve/page.tsx` com estado `already-approved` adicionado e texto do `success` atualizado.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-ux-paginas-publicas-do-cliente/08-CONTEXT.md
@.planning/phases/08-ux-paginas-publicas-do-cliente/08-RESEARCH.md

<interfaces>
<!-- Estado atual relevante de apps/frontend/src/app/orcamento/[id]/approve/page.tsx -->

```typescript
// Type atual (linha ~6):
type ApproveState = "loading-quote" | "idle" | "submitting" | "success" | "error" | "no-token";

// No useEffect — hoje seta success diretamente quando já aprovado:
if (data?.approved) {
  setState("success");   // ← mudar para "already-approved"
} else {
  setState("idle");
}

// Estado success atual (já tem círculo verde + bi-check-lg):
{state === "success" && (
  <div className="py-2">
    <div className="rounded-circle d-flex align-items-center justify-content-center mx-auto mb-3"
      style={{ width: 64, height: 64, background: "#c5f2e8" }}>
      <i className="bi bi-check-lg" style={{ fontSize: 32, color: "#2e7d62" }} />
    </div>
    <h5 className="mb-2">Orçamento Aprovado!</h5>
    <p className="text-muted">
      Recebemos sua aprovação. Em breve nossa equipe entrará em contato.  {/* ← atualizar texto */}
    </p>
    <div className="mt-3 border-top pt-3 small text-muted">
      Dúvidas? Fale conosco:{" "}
      <a href="https://wa.me/5512996484918" ...>(12) 99648-4918</a>
    </div>
  </div>
)}
```

Bootstrap Icons já disponível via CDN global — `bi bi-check-lg` e `bi bi-check-circle` sem imports.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar state already-approved e atualizar texto do success</name>
  <files>apps/frontend/src/app/orcamento/[id]/approve/page.tsx</files>
  <action>
Fazer três mudanças cirúrgicas no arquivo:

**1. Expandir o type ApproveState** (per D-04):
```typescript
// Antes:
type ApproveState = "loading-quote" | "idle" | "submitting" | "success" | "error" | "no-token";
// Depois:
type ApproveState = "loading-quote" | "idle" | "submitting" | "success" | "already-approved" | "error" | "no-token";
```

**2. No useEffect, mudar o branch `data?.approved` de** `setState("success")` **para** `setState("already-approved")` (per D-04):
```typescript
// Antes:
if (data?.approved) {
  setState("success");
}
// Depois:
if (data?.approved) {
  setState("already-approved");
}
```

**3. Atualizar o texto do estado `success`** (per D-03) — manter o círculo verde e ícone existentes, apenas trocar o parágrafo:
```tsx
// Antes:
<p className="text-muted">
  Recebemos sua aprovação. Em breve nossa equipe entrará em contato.
</p>
// Depois:
<p className="text-muted">
  Nossa equipe já foi notificada e em breve seu pedido entra em produção. Avisaremos assim que estiver pronto.
</p>
```

**4. Adicionar JSX para o novo estado `already-approved`** — inserir após o bloco `{state === "success" && ...}`:
```tsx
{/* Already approved — revisit */}
{state === "already-approved" && (
  <div className="py-2">
    <div
      className="rounded-circle d-flex align-items-center justify-content-center mx-auto mb-3"
      style={{ width: 64, height: 64, background: "#c5f2e8" }}
    >
      <i className="bi bi-check-lg" style={{ fontSize: 32, color: "#2e7d62" }} />
    </div>
    <h5 className="mb-2">Orçamento já aprovado</h5>
    {quoteNumber && (
      <div className="text-muted mb-2 small">Orçamento <strong>#{quoteNumber}</strong></div>
    )}
    <p className="text-muted">
      Você já aprovou este orçamento anteriormente. Nossa equipe está cuidando do seu pedido.
    </p>
    <div className="mt-3 border-top pt-3 small text-muted">
      Dúvidas? Fale conosco:{" "}
      <a href="https://wa.me/5512996484918" className="text-decoration-none fw-semibold">
        (12) 99648-4918
      </a>
    </div>
  </div>
)}
```

Não alterar nenhum outro bloco da página (tabela de itens, botão, header, estados loading/error/no-token/submitting).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/frontend/tsconfig.json 2>&1 | Select-String "approve" | Select-Object -First 5</automated>
  </verify>
  <done>
    - Type ApproveState inclui "already-approved"
    - useEffect seta "already-approved" quando data?.approved === true no carregamento inicial
    - Estado "success" (pós-clique) exibe texto D-03
    - Estado "already-approved" exibe título "Orçamento já aprovado" + texto D-04 + número do orçamento
    - Zero erros TypeScript
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → frontend | data.approved vem do backend via /api/quotes/[id] |
| URL param | token vem de query string — já validado pelo backend no POST de aprovação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Spoofing | approve/page.tsx | accept | Token validado no backend (POST /approve?token=); frontend apenas exibe resultado |
| T-08-04 | Information | estado already-approved | accept | Confirma que orçamento foi aprovado — informação que o próprio cliente aprovou, sem dados de terceiros |
</threat_model>

<verification>
- Acessar link de aprovação de orçamento ainda não aprovado → deve exibir tabela de itens + botão
- Clicar em "Aprovar" → estado success: "Nossa equipe já foi notificada e em breve seu pedido entra em produção..."
- Acessar link de aprovação de orçamento JÁ aprovado → estado already-approved: "Orçamento já aprovado" + "Você já aprovou este orçamento anteriormente..."
- TypeScript: `npx tsc --noEmit -p apps/frontend/tsconfig.json` sem erros
</verification>

<success_criteria>
- Dois estados de confirmação distintos: "success" (aprovação agora) e "already-approved" (revisita)
- Texto do success alinhado com D-03
- Texto do already-approved alinhado com D-04
- Número do orçamento visível em ambos
- Zero erros TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-ux-paginas-publicas-do-cliente/08-02-SUMMARY.md`
</output>
