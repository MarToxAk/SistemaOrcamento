---
phase: 29-boleto-consolidado-efi
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - apps/frontend/src/app/api/cobranca/boleto/route.ts
  - apps/frontend/src/app/contas-receber/[idcliente]/page.tsx
autonomous: true
requirements:
  - BOL-01
  - BOL-02
  - BOL-03

must_haves:
  truths:
    - "Clicar em 'Gerar Boleto' abre modal — Estado 1 (Confirmação) — com resumo dos títulos selecionados"
    - "Se todos os títulos têm a mesma datavencimento → campo de data pré-preenchido e readonly com badge 'Preenchido automaticamente'"
    - "Se títulos têm datas diferentes → campo de data vazio + alert-danger com mensagem de datas divergentes"
    - "Botão 'Confirmar Geração' desabilitado quando campo de data vazio ou data no passado"
    - "Confirmar abre Estado 2 (Loading) com spinner e oculta botões"
    - "Resposta 2xx abre Estado 3 (Sucesso) com linkBoleto (abre nova aba), barcodeLinhaDigitavel (copiável), valor e expireAt"
    - "Modal Estado 3 exibe barcodeLinhaDigitavel copiável (input readOnly + botão Copiar) e botão 'Abrir Boleto' que abre linkBoleto em nova aba"
    - "Botão 'Copiar' muda para 'Copiado! ✔' por 2s após cópia via navigator.clipboard"
    - "Resposta não-2xx abre Estado 4 (Erro) com mensagem + botão 'Tentar Novamente' que volta ao Estado 1"
    - "Route Handler POST /api/cobranca/boleto usa backendFetch com x-internal-api-key via INTERNAL_API_KEY"
    - "Nenhum uso de .fw-bold ou font-weight: 700 no modal — apenas fw-semibold (weight 600)"
  artifacts:
    - path: "apps/frontend/src/app/api/cobranca/boleto/route.ts"
      provides: "Proxy POST para backend /cobranca/boleto com x-internal-api-key"
    - path: "apps/frontend/src/app/contas-receber/[idcliente]/page.tsx"
      provides: "Modal de 4 estados + onClick do botão Gerar Boleto conectado"
  key_links:
    - from: "page.tsx onClick 'Gerar Boleto'"
      to: "abreBoletoModal()"
      via: "useState<'idle'|'confirm'|'loading'|'success'|'error'>"
      pattern: "boletoModalState"
    - from: "Estado 1 Confirmar"
      to: "POST /api/cobranca/boleto"
      via: "fetch('/api/cobranca/boleto', { method: 'POST', body: JSON.stringify(...) })"
      pattern: "api/cobranca/boleto"
    - from: "Estado 3 Sucesso"
      to: "linkBoleto"
      via: "target='_blank' rel='noopener noreferrer'"
      pattern: "target.*_blank"
---

<objective>
Criar o Route Handler proxy Next.js para POST /api/cobranca/boleto e implementar
o modal de 4 estados na página /contas-receber/[idcliente]/page.tsx, conectando
o botão "Gerar Boleto" ao fluxo completo de geração.

Purpose: Fechar o ciclo frontend — operador seleciona títulos, confirma data,
gera boleto e recebe link + linha digitável para compartilhar com o cliente.

Output:
- apps/frontend/src/app/api/cobranca/boleto/route.ts  (novo)
- apps/frontend/src/app/contas-receber/[idcliente]/page.tsx  (modificado)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-boleto-consolidado-efi/29-CONTEXT.md
@.planning/phases/29-boleto-consolidado-efi/29-UI-SPEC.md
@.planning/phases/29-boleto-consolidado-efi/29-01-SUMMARY.md

<interfaces>
<!-- Route Handler proxy existente como modelo -->
<!-- apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/route.ts -->
import { NextRequest, NextResponse } from "next/server";
import { backendFetch } from "@/lib/backend-client";

// backendFetch já injeta x-internal-api-key via INTERNAL_API_KEY (process.env)
// Implementação: apps/frontend/src/lib/backend-client.ts
// export async function backendFetch(path: string, init?: RequestInit): Promise<Response>

<!-- TituloReceber interface (já no page.tsx) -->
interface TituloReceber {
  idcontareceber: number;
  numerotitulo: string | null;
  datavencimento: string;  // ISO date string "YYYY-MM-DD"
  valor: number;
  observacao: string | null;
  idvenda: number | null;
  dataemissao: string | null;
  numeroordem: string | null;
}

<!-- Backend response shape (D-13) -->
interface CriarBoletoResponse {
  cobrancaId: number;
  chargeId: number;
  linkBoleto: string;
  barcodeLinhaDigitavel: string;
  valor: number;
  expireAt: string;  // "YYYY-MM-DD"
}

<!-- Funções utilitárias já no page.tsx -->
function formatBRL(value: number): string  // toLocaleString pt-BR currency
function formatDate(iso: string): string    // toLocaleDateString pt-BR

<!-- Botão "Gerar Boleto" já existe no page.tsx (onClick vazio — TODO Phase 29) -->
<!-- Linha 297-303: button.btn.btn-warning com bi-receipt -->

<!-- Padrão modal do projeto (referência) -->
<!-- apps/frontend/src/app/orcamento/[id]/page.tsx usa .pdf-modal-backdrop como overlay React state -->
<!-- Seguir EXATAMENTE o mesmo padrão: div fixo com state React, SEM Bootstrap Modal JS -->

<!-- CSS do modal — adicionar ao bloco <style> existente no page.tsx -->
<!-- Definido em 29-UI-SPEC.md — copiar exatamente o bloco de estilos .boleto-modal-* -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route Handler POST /api/cobranca/boleto</name>
  <files>
    apps/frontend/src/app/api/cobranca/boleto/route.ts
  </files>
  <action>
    Criar o diretório apps/frontend/src/app/api/cobranca/boleto/ e o arquivo route.ts.

    O Route Handler é um proxy POST simples para o backend. Seguir o padrão do arquivo
    apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/route.ts mas como POST:

    1. Exportar função `POST(req: NextRequest)`.
    2. Ler o body JSON da requisição: `const body = await req.json().catch(() => null)`. Se null → retornar 400.
    3. Validar que body contém `idclienteAthos` (number), `idcontasReceber` (array não vazio), `expireAt` (string não vazia). Retornar 400 com mensagem clara se inválido.
    4. Chamar `backendFetch('/cobranca/boleto', { method: 'POST', body: JSON.stringify(body), headers: { 'Content-Type': 'application/json' } })`.
    5. `backendFetch` já injeta `x-internal-api-key` automaticamente (per D-21).
    6. Ler resposta: `const data = await res.json().catch(() => ({ error: 'Resposta inválida do backend.' }))`.
    7. Retornar `NextResponse.json(data, { status: res.status })` — propagar status HTTP do backend.
    8. Wrap em try/catch: em caso de exceção de rede retornar 500 com `{ error: 'Falha ao conectar no backend.' }`.

    Nenhum parâmetro dinâmico na rota — rota estática `/api/cobranca/boleto`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    Arquivo route.ts criado. Compila sem erros TypeScript. Exporta função POST.
  </done>
</task>

<task type="auto">
  <name>Task 2: Modal de 4 estados em /contas-receber/[idcliente]/page.tsx</name>
  <files>
    apps/frontend/src/app/contas-receber/[idcliente]/page.tsx
  </files>
  <action>
    Modificar apps/frontend/src/app/contas-receber/[idcliente]/page.tsx para adicionar
    o modal de boleto completo com os 4 estados definidos em 29-UI-SPEC.md.

    **1. Novos estados React** (adicionar após os useState existentes):
    ```typescript
    const [boletoModalState, setBoletoModalState] = useState<'idle' | 'confirm' | 'loading' | 'success' | 'error'>('idle');
    const [expireAt, setExpireAt] = useState('');
    const [expireAtReadonly, setExpireAtReadonly] = useState(false);
    const [erroDatasModal, setErroDatasModal] = useState('');
    const [boletoResult, setBoletoResult] = useState<{
      cobrancaId: number;
      chargeId: number;
      linkBoleto: string;
      barcodeLinhaDigitavel: string;
      valor: number;
      expireAt: string;
    } | null>(null);
    const [boletoErro, setBoletoErro] = useState('');
    const [boletoErroDetalhe, setBoletoErroDetalhe] = useState('');
    const [copiado, setCopiado] = useState(false);
    ```

    **2. Lógica de abertura do modal** (nova função `abreBoletoModal()`):
    ```typescript
    function abreBoletoModal() {
      const titulosSelecionados = titulos.filter(t => selectedIds.has(t.idcontareceber));
      const datas = new Set(titulosSelecionados.map(t => t.datavencimento?.slice(0, 10)));
      if (datas.size === 1) {
        setExpireAt([...datas][0] ?? '');
        setExpireAtReadonly(true);
        setErroDatasModal('');
      } else {
        setExpireAt('');
        setExpireAtReadonly(false);
        setErroDatasModal('Os títulos selecionados possuem datas de vencimento diferentes. Informe a data de vencimento manualmente.');
      }
      setBoletoModalState('confirm');
    }
    ```
    Conectar no botão "Gerar Boleto" existente (linha ~299): substituir o comentário `/* TODO: Phase 29 */` por `onClick={abreBoletoModal}`.

    **3. Função de submissão** (nova função `confirmarGerarBoleto()`):
    ```typescript
    async function confirmarGerarBoleto() {
      setBoletoModalState('loading');
      const titulosSelecionados = titulos.filter(t => selectedIds.has(t.idcontareceber));
      try {
        const res = await fetch('/api/cobranca/boleto', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            idclienteAthos: Number(idcliente),
            idcontasReceber: titulosSelecionados.map(t => t.idcontareceber),
            expireAt,
          }),
        });
        const data = await res.json().catch(() => ({ error: 'Resposta inválida.' }));
        if (!res.ok) {
          setBoletoErro(data?.message ?? data?.error ?? 'Não foi possível gerar o boleto. Verifique a conexão e tente novamente.');
          setBoletoErroDetalhe(`HTTP ${res.status}`);
          setBoletoModalState('error');
        } else {
          setBoletoResult(data);
          setBoletoModalState('success');
        }
      } catch (err) {
        setBoletoErro('Não foi possível gerar o boleto. Verifique a conexão e tente novamente.');
        setBoletoErroDetalhe(err instanceof Error ? err.message : '');
        setBoletoModalState('error');
      }
    }
    ```

    **4. Função de fechar modal** (nova função `fecharBoletoModal()`):
    ```typescript
    function fecharBoletoModal() {
      setBoletoModalState('idle');
      setBoletoResult(null);
      setBoletoErro('');
      setBoletoErroDetalhe('');
      setCopiado(false);
    }
    ```

    **5. useEffect para ESC key** (adicionar após os useEffect existentes):
    ```typescript
    useEffect(() => {
      function handleKeyDown(e: KeyboardEvent) {
        if (e.key === 'Escape' && boletoModalState !== 'loading') {
          fecharBoletoModal();
        }
      }
      if (boletoModalState !== 'idle') {
        document.addEventListener('keydown', handleKeyDown);
        return () => document.removeEventListener('keydown', handleKeyDown);
      }
    }, [boletoModalState]);
    ```

    **6. Calcular variáveis derivadas** para o modal (antes do return):
    ```typescript
    const titulosSelecionadosParaBoleto = titulos.filter(t => selectedIds.has(t.idcontareceber));
    const hoje = new Date().toISOString().slice(0, 10);
    const expireAtInvalido = !expireAt || expireAt < hoje;
    ```

    **7. JSX do modal** — adicionar ANTES do bloco `<style>` (no final do return, após a barra de ações):
    ```jsx
    {boletoModalState !== 'idle' && (
      <div
        className="boleto-modal-backdrop"
        onClick={boletoModalState !== 'loading' ? fecharBoletoModal : undefined}
      >
        <div
          className="boleto-modal-card"
          role="dialog"
          aria-modal="true"
          aria-label="Gerar Boleto Bancário"
          onClick={e => e.stopPropagation()}
        >
          {/* HEADER — visível em todos os estados */}
          <div className="boleto-modal-header">
            <h5 className="mb-0 fw-semibold" style={{ fontSize: 'var(--fs-lg, 1.35rem)' }}>
              <i className="bi bi-receipt me-2" />Gerar Boleto
            </h5>
            {boletoModalState !== 'loading' && (
              <button
                type="button"
                className="btn-close"
                onClick={fecharBoletoModal}
                aria-label="Fechar"
              />
            )}
          </div>

          {/* ESTADO 1 — CONFIRMAÇÃO */}
          {boletoModalState === 'confirm' && (
            <>
              <div className="boleto-modal-body">
                <small className="text-muted d-block mb-3">
                  {titulosSelecionadosParaBoleto.length} título(s) selecionado(s) —{' '}
                  {formatBRL(totalSelecionado)}
                </small>

                {/* Resumo dos títulos */}
                <div className="card border-0 shadow-sm mb-3">
                  <div className="card-body p-3">
                    <div className="mb-2 text-muted small">Valor Total</div>
                    <div className="fw-semibold mb-3" style={{ fontSize: 'var(--fs-xl, 1.7rem)' }}>
                      {formatBRL(totalSelecionado)}
                    </div>
                    <ul className="list-unstyled mb-0">
                      {titulosSelecionadosParaBoleto.map(t => (
                        <li key={t.idcontareceber} className="d-flex justify-content-between small border-bottom pb-1 mb-1">
                          <span className="text-muted">{t.numerotitulo ?? `#${t.idcontareceber}`}</span>
                          <span>{formatBRL(t.valor)}</span>
                        </li>
                      ))}
                    </ul>
                  </div>
                </div>

                {/* Alert datas divergentes */}
                {erroDatasModal && (
                  <div className="alert alert-danger d-flex gap-2 mb-3" role="alert">
                    <i className="bi bi-exclamation-triangle-fill flex-shrink-0" />
                    <span className="small">{erroDatasModal}</span>
                  </div>
                )}

                {/* Campo de vencimento */}
                <div className="mb-3">
                  <label className="form-label fw-semibold small">
                    Data de Vencimento do Boleto
                  </label>
                  <input
                    type="date"
                    className={`form-control${expireAt && expireAt < hoje ? ' is-invalid' : ''}`}
                    value={expireAt}
                    min={hoje}
                    readOnly={expireAtReadonly}
                    onChange={e => !expireAtReadonly && setExpireAt(e.target.value)}
                  />
                  {expireAtReadonly && (
                    <div className="mt-1">
                      <span className="badge bg-info text-dark small">Preenchido automaticamente</span>
                      <small className="text-muted ms-2">Preenchida automaticamente com a data dos títulos.</small>
                    </div>
                  )}
                  {expireAt && expireAt < hoje && (
                    <div className="invalid-feedback">A data de vencimento não pode ser no passado.</div>
                  )}
                </div>
              </div>
              <div className="boleto-modal-footer">
                <button type="button" className="btn btn-outline-secondary" onClick={fecharBoletoModal}>
                  Cancelar
                </button>
                <button
                  type="button"
                  className="btn btn-primary"
                  onClick={confirmarGerarBoleto}
                  disabled={expireAtInvalido}
                >
                  <i className="bi bi-check-lg me-1" />Confirmar Geração
                </button>
              </div>
            </>
          )}

          {/* ESTADO 2 — LOADING */}
          {boletoModalState === 'loading' && (
            <div className="boleto-modal-body d-flex flex-column align-items-center justify-content-center py-5">
              <div className="spinner-border text-primary" role="status">
                <span className="visually-hidden">Gerando boleto...</span>
              </div>
              <p className="mt-3 text-muted mb-0">Gerando boleto junto à EFI Bank…</p>
            </div>
          )}

          {/* ESTADO 3 — SUCESSO */}
          {boletoModalState === 'success' && boletoResult && (
            <>
              <div className="boleto-modal-body" role="status" aria-live="polite">
                <div className="text-center mb-4">
                  <i className="bi bi-check-circle-fill fs-1 text-success" />
                  <h5 className="fw-semibold text-success mt-2">Boleto Gerado com Sucesso</h5>
                </div>
                <div className="card border-0 bg-light mb-3">
                  <div className="card-body p-3">
                    <div className="d-flex justify-content-between mb-2">
                      <span className="text-muted small">Valor</span>
                      <span className="fw-semibold" style={{ fontSize: 'var(--fs-xl, 1.7rem)' }}>
                        {formatBRL(boletoResult.valor)}
                      </span>
                    </div>
                    <div className="d-flex justify-content-between">
                      <span className="text-muted small">Vencimento</span>
                      <span className="small">{formatDate(boletoResult.expireAt)}</span>
                    </div>
                  </div>
                </div>

                <label className="form-label small fw-semibold">Linha Digitável</label>
                <div className="d-flex gap-2 align-items-start mb-3">
                  <input
                    type="text"
                    className="form-control boleto-linha-digitavel"
                    value={boletoResult.barcodeLinhaDigitavel}
                    readOnly
                    aria-label="Linha digitável do boleto"
                  />
                  <button
                    type="button"
                    className="btn btn-outline-secondary btn-sm flex-shrink-0"
                    style={{ minWidth: '44px', minHeight: '44px' }}
                    onClick={() => {
                      navigator.clipboard.writeText(boletoResult!.barcodeLinhaDigitavel);
                      setCopiado(true);
                      setTimeout(() => setCopiado(false), 2000);
                    }}
                  >
                    {copiado ? 'Copiado! ✔' : <><i className="bi bi-clipboard" /> Copiar</>}
                  </button>
                </div>
              </div>
              <div className="boleto-modal-footer">
                <button type="button" className="btn btn-outline-secondary" onClick={fecharBoletoModal}>
                  Fechar
                </button>
                <a
                  href={boletoResult.linkBoleto}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="btn btn-success"
                >
                  <i className="bi bi-box-arrow-up-right me-1" />Abrir Boleto
                </a>
              </div>
            </>
          )}

          {/* ESTADO 4 — ERRO */}
          {boletoModalState === 'error' && (
            <>
              <div className="boleto-modal-body" role="alert" aria-live="assertive">
                <div className="alert alert-danger d-flex gap-2">
                  <i className="bi bi-exclamation-triangle-fill flex-shrink-0" />
                  <div>
                    <div>{boletoErro || 'Não foi possível gerar o boleto. Verifique a conexão e tente novamente.'}</div>
                    {boletoErroDetalhe && (
                      <small className="text-muted d-block mt-1">{boletoErroDetalhe}</small>
                    )}
                  </div>
                </div>
              </div>
              <div className="boleto-modal-footer">
                <button type="button" className="btn btn-outline-secondary" onClick={fecharBoletoModal}>
                  Cancelar
                </button>
                <button
                  type="button"
                  className="btn btn-warning"
                  onClick={() => {
                    setBoletoErro('');
                    setBoletoErroDetalhe('');
                    setBoletoModalState('confirm');
                  }}
                >
                  Tentar Novamente
                </button>
              </div>
            </>
          )}
        </div>
      </div>
    )}
    ```

    **8. CSS do modal** — adicionar ao bloco `<style>` existente no page.tsx (não substituir o CSS existente, apenas acrescentar ao final da string CSS):
    Copiar exatamente o bloco de estilos `.boleto-modal-*` definido em 29-UI-SPEC.md (classes: boleto-modal-backdrop, boleto-modal-card, boleto-modal-header, boleto-modal-body, boleto-modal-footer, boleto-linha-digitavel, @keyframes fadeIn, @keyframes slideUp).

    **Proibições (per UI-SPEC.md):**
    - NUNCA usar `.fw-bold` ou `font-weight: 700` no modal — apenas `fw-semibold` (weight 600)
    - Valor total: `style={{ fontSize: 'var(--fs-xl, 1.7rem)' }}` + className `fw-semibold`
    - Linha digitável: classe `boleto-linha-digitavel` (font-mono via CSS adicionado)
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/frontend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    page.tsx compila sem erros TypeScript. Botão "Gerar Boleto" tem onClick={abreBoletoModal}. Todos os 4 estados do modal implementados. CSS do modal adicionado ao bloco style. Nenhum uso de fw-bold no modal.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Route Handler POST /api/cobranca/boleto criado. Modal de 4 estados implementado
    em /contas-receber/[idcliente]/page.tsx conectado ao botão "Gerar Boleto".
  </what-built>
  <how-to-verify>
    1. Navegar para /contas-receber/[idcliente] (substituir idcliente por um ID válido)
    2. Selecionar 1 ou mais títulos na tabela — barra de ações aparece
    3. Clicar em "Gerar Boleto":
       - Estado 1 deve aparecer com resumo dos títulos e campo de data
       - Se todos os títulos têm mesma datavencimento → campo pré-preenchido + badge "Preenchido automaticamente"
       - Se datas diferentes → campo vazio + alert-danger com mensagem de datas divergentes
    4. Verificar que botão "Confirmar Geração" está desabilitado com campo de data vazio
    5. Informar data futura → botão habilita
    6. Informar data passada → mensagem de validação inline + botão permanece desabilitado
    7. Clicar em "Confirmar Geração":
       - Estado 2 (Loading) aparece com spinner + "Gerando boleto junto à EFI Bank…"
       - Botões devem estar ocultos durante loading
    8. Aguardar resposta da API:
       - Sucesso → Estado 3 com ícone bi-check-circle-fill, valor, vencimento, linha digitável copiável
       - Testar botão "Copiar" → texto muda para "Copiado! ✔" por 2s
       - Testar botão "Abrir Boleto" → abre em nova aba
       - Erro → Estado 4 com alert-danger, botão "Tentar Novamente" volta ao Estado 1
    9. Verificar ESC fecha modal em Estado 1, 3 e 4 (NÃO em Estado 2/loading)
    10. Verificar que nenhuma ocorrência de fw-bold aparece no modal:
        grep -n "fw-bold" apps/frontend/src/app/contas-receber/\\[idcliente\\]/page.tsx
        (deve retornar zero resultados para o modal — apenas o código existente pode ter fw-bold fora do modal)
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Route Handler /api/cobranca/boleto | Requisição do cliente Next.js → Route Handler valida body antes de repassar |
| Route Handler→backend /cobranca/boleto | backendFetch injeta x-internal-api-key via process.env (server-side) — nunca exposta ao browser |
| Estado 3 Sucesso → linkBoleto | URL EFI aberta em nova aba com rel="noopener noreferrer" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29F-01 | Spoofing | Route Handler /api/cobranca/boleto | mitigate | x-internal-api-key injetada server-side via process.env — nunca exposta ao browser |
| T-29F-02 | Tampering | body idcontasReceber | mitigate | Route Handler valida presença e tipo dos campos antes de repassar ao backend |
| T-29F-03 | Information Disclosure | linkBoleto em nova aba | mitigate | target="_blank" com rel="noopener noreferrer" — previne window.opener access |
| T-29F-04 | Denial of Service | Cliques múltiplos em Confirmar | mitigate | Estado 'loading' oculta botões — impossível submeter múltiplas vezes |
| T-29F-SC | Tampering | npm install | mitigate | Nenhum pacote novo — usa apenas Next.js, React e fetch nativos |
</threat_model>

<verification>
1. `npx tsc --noEmit -p apps/frontend/tsconfig.json` — zero erros TypeScript
2. Arquivo apps/frontend/src/app/api/cobranca/boleto/route.ts existe e exporta POST
3. grep -n "abreBoletoModal" apps/frontend/src/app/contas-receber/\\[idcliente\\]/page.tsx — deve aparecer no onClick do botão Gerar Boleto e na função
4. grep -c "boletoModalState" apps/frontend/src/app/contas-receber/\\[idcliente\\]/page.tsx — deve ser >= 8 (múltiplos usos)
5. grep -n "boleto-modal-backdrop" apps/frontend/src/app/contas-receber/\\[idcliente\\]/page.tsx — deve aparecer no JSX e no CSS
6. grep -v "^//" apps/frontend/src/app/contas-receber/\\[idcliente\\]/page.tsx | grep -c "fw-bold" — deve retornar 0 (dentro do modal)
7. grep -n "target.*_blank" apps/frontend/src/app/contas-receber/\\[idcliente\\]/page.tsx — deve aparecer no botão "Abrir Boleto"
8. grep -n "navigator.clipboard" apps/frontend/src/app/contas-receber/\\[idcliente\\]/page.tsx — deve aparecer no botão Copiar
9. grep -n "barcodeLinhaDigitavel" apps/frontend/src/app/contas-receber/\\[idcliente\\]/page.tsx — deve aparecer no input readOnly e no onClick do botão Copiar
</verification>

<success_criteria>
- Route Handler /api/cobranca/boleto proxy funcional com x-internal-api-key server-side
- Botão "Gerar Boleto" abre modal Estado 1 com data pré-preenchida ou alerta de datas divergentes
- Estado 2 (loading) oculta botões e mostra spinner
- Estado 3 (sucesso) exibe barcodeLinhaDigitavel copiável e botão "Abrir Boleto" que abre linkBoleto em nova aba
- Estado 4 (erro) exibe boletoErro como mensagem principal e boletoErroDetalhe (HTTP status / error.message) como detalhe secundário — sem duplicação
- Estado 4 (erro) permite "Tentar Novamente" voltando ao Estado 1
- ESC fecha modal em estados 1, 3, 4 — não fecha em estado 2
- Zero ocorrências de fw-bold no modal
- Compilação TypeScript limpa
</success_criteria>

<output>
Criar `.planning/phases/29-boleto-consolidado-efi/29-02-SUMMARY.md` quando concluído
</output>
