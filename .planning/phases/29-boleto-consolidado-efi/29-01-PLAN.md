---
phase: 29-boleto-consolidado-efi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/cobranca/cobranca.module.ts
  - apps/backend/src/modules/cobranca/cobranca.service.ts
  - apps/backend/src/modules/cobranca/cobranca.controller.ts
  - apps/backend/src/modules/app.module.ts
autonomous: true
requirements:
  - BOL-01
  - BOL-02
  - BOL-03

must_haves:
  truths:
    - "POST /cobranca/boleto cria boleto na API EFI com valor igual à soma dos títulos e retorna cobrancaId, chargeId, linkBoleto, barcodeLinhaDigitavel, valor, expireAt"
    - "CobrancaService valida que todos idcontasReceber existem nos títulos do cliente e têm status AVC/VEN antes de chamar a EFI"
    - "Se expireAt for data passada o endpoint retorna 400 com mensagem clara"
    - "CobrancaBoleto + CobrancaBoletoTitulo[] são criados no Prisma após criação bem-sucedida na EFI"
    - "POST /cobranca/boleto/notificacao é público (sem auth guard), recebe token EFI, consulta GET /v1/notification/{token} e atualiza CobrancaBoleto.status para 'pago' quando paid"
    - "Auth com EFI Cobranças usa EXATAMENTE o padrão de createCardPaymentLink(): POST /v1/authorize com Basic base64(CLIENT_ID:CLIENT_SECRET) + grant_type=client_credentials, depois Bearer token"
  artifacts:
    - path: "apps/backend/src/modules/cobranca/cobranca.module.ts"
      provides: "CobrancaModule com imports EfiModule, AthosModule, DatabaseModule"
    - path: "apps/backend/src/modules/cobranca/cobranca.service.ts"
      provides: "CobrancaService.criarBoleto() + processarNotificacaoEFI()"
    - path: "apps/backend/src/modules/cobranca/cobranca.controller.ts"
      provides: "POST /cobranca/boleto (autenticado) + POST /cobranca/boleto/notificacao (@Public)"
    - path: "apps/backend/src/modules/app.module.ts"
      provides: "AthosModule + CobrancaModule registrados"
  key_links:
    - from: "cobranca.service.ts"
      to: "EFIService via injeção NestJS"
      via: "constructor(private readonly efiService: EfiService)"
      pattern: "EfiService"
    - from: "cobranca.service.ts"
      to: "prisma.cobrancaBoleto.create"
      via: "nested write com titulos.createMany"
      pattern: "cobrancaBoleto\\.create"
    - from: "cobranca.controller.ts"
      to: "@Public() em notificacao"
      via: "import { Public } from '../security/public.decorator'"
      pattern: "@Public"
---

<objective>
Criar CobrancaModule completo com serviço, controlador e registro no AppModule.
O módulo expõe POST /cobranca/boleto (autenticado via x-internal-api-key) e
POST /cobranca/boleto/notificacao (público para webhook EFI).

Purpose: Backend completo de geração de boleto EFI com persistência Prisma. O
frontend do Plano 02 depende deste endpoint estar funcional.

Output:
- apps/backend/src/modules/cobranca/cobranca.module.ts
- apps/backend/src/modules/cobranca/cobranca.service.ts
- apps/backend/src/modules/cobranca/cobranca.controller.ts
- apps/backend/src/modules/app.module.ts (atualizado com CobrancaModule)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-boleto-consolidado-efi/29-CONTEXT.md

<interfaces>
<!-- Padrão de auth EFI — COPIAR EXATAMENTE de createCardPaymentLink() -->
<!-- Arquivo: apps/backend/src/modules/integrations/efi/efi.service.ts linhas 292-373 -->

Auth OAuth2 EFI Cobranças (sem mTLS):
```typescript
const baseUrl = this.config.get<string>("EFI_COBRANCA_BASE_URL") ?? "https://cobrancas-h.api.efipay.com.br";
const clientId = this.getRequiredConfig("EFI_CLIENT_ID");
const clientSecret = this.getRequiredConfig("EFI_CLIENT_SECRET");
const basic = Buffer.from(`${clientId}:${clientSecret}`).toString("base64");
const cobrancaClient = axios.create({ baseURL: baseUrl, timeout: 15_000 });
const authResp = await cobrancaClient.post(
  "/v1/authorize",
  { grant_type: "client_credentials" },
  { headers: { Authorization: `Basic ${basic}`, "Content-Type": "application/json" } },
);
const token = authResp.data?.access_token;
```

Valor em centavos (padrão do projeto):
```typescript
const valorCentavos = Math.round(Number(totalValor.toFixed(2)) * 100);
```

<!-- Modelos Prisma (Phase 28 — já migrados) -->
<!-- Arquivo: apps/backend/prisma/schema.prisma -->
model CobrancaBoleto {
  id             Int                    @id @default(autoincrement())
  txidEfi        String?                // chargeId.toString()
  idclienteAthos Int
  valor          Decimal                @db.Decimal(12, 2)
  status         String                 @default("pendente")
  linkBoleto     String?
  pixPayload     String?
  criadoEm       DateTime               @default(now())
  atualizadoEm   DateTime               @updatedAt
  titulos        CobrancaBoletoTitulo[]
}

model CobrancaBoletoTitulo {
  id               Int            @id @default(autoincrement())
  cobrancaBoletoId Int
  idcontareceber   Int
  valor            Decimal        @db.Decimal(12, 2)
  cobrancaBoleto   CobrancaBoleto @relation(fields: [cobrancaBoletoId], references: [id])
}

<!-- AthosService exports -->
<!-- Arquivo: apps/backend/src/modules/integrations/athos/athos.service.ts -->
async buscarTitulosClienteContasReceber(idcliente: number): Promise<Array<{
  idcontareceber: number;
  numerotitulo: string | null;
  datavencimento: string;
  valor: number;
  observacao: string | null;
  idvenda: number | null;
  dataemissao: string | null;
  numeroordem: string | null;
}>>

async buscarDadosClienteContasReceber(idcliente: number): Promise<{
  idcliente: number;
  nome_cliente: string;
  telefone_completo: string | null;
  emailcliente: string | null;
  emailcobrancacliente: string | null;
  limitecredito: number;
  bloqueaprazo: string | null;
} | null>

async buscarClientePorId(clienteId: string | number): Promise<{
  id: string;
  name: string;
  type: "juridico" | "fisico";
  documento: string | null;
  endereco?: {...} | null;
} | null>

<!-- Segurança: @Public() decorator -->
<!-- Arquivo: apps/backend/src/modules/security/public.decorator.ts -->
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);

<!-- InternalAuthGuard já registrado globalmente em app.module.ts via APP_GUARD -->
<!-- Rotas @Public() são isentas automaticamente -->

<!-- EfiModule exports EfiService -->
<!-- AthosModule exports AthosService -->
<!-- DatabaseModule é @Global() — PrismaService injetável em qualquer módulo sem import -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cobranca.module.ts + cobranca.controller.ts (esqueleto com endpoints)</name>
  <files>
    apps/backend/src/modules/cobranca/cobranca.module.ts
    apps/backend/src/modules/cobranca/cobranca.controller.ts
    apps/backend/src/modules/app.module.ts
  </files>
  <action>
    Criar o diretório apps/backend/src/modules/cobranca/ com os seguintes arquivos:

    **cobranca.module.ts** — importa EfiModule e AthosModule. DatabaseModule é @Global() e não precisa ser importado explicitamente. Declara CobrancaController e CobrancaService.

    **cobranca.controller.ts** — dois endpoints:

    1. `@Post('boleto')` — protegido pelo InternalAuthGuard global (sem decorator extra necessário, é o padrão). Recebe body `CriarBoletoDto` e retorna o resultado de `cobrancaService.criarBoleto()`.

    2. `@Post('boleto/notificacao')` — decorado com `@Public()` importado de `'../../security/public.decorator'` (dois níveis acima de cobranca/). Recebe body com `token: string` (EFI envia como POST body ou query param — aceitar ambos via `@Body() body` e `@Query('token') tokenQuery`). Chama `cobrancaService.processarNotificacaoEFI(token)` e SEMPRE retorna HTTP 200 independentemente do resultado (per D-18). Usar try/catch e retornar `{ ok: true }` mesmo em caso de erro (logar o erro mas não relançar).

    **CriarBoletoDto** — definir inline no controller ou em arquivo separado dto/criar-boleto.dto.ts (a critério, por ser simples):
    ```
    idclienteAthos: number  (validar > 0)
    idcontasReceber: number[]  (validar array não vazio)
    expireAt: string  (formato YYYY-MM-DD)
    ```
    Usar class-validator se já usado no projeto (verificar imports existentes nos outros controllers). Se não, validar manualmente no service.

    **app.module.ts** — adicionar `import { AthosModule } from './integrations/athos/athos.module'` e `import { CobrancaModule } from './cobranca/cobranca.module'` ao array `imports`. AthosModule não estava no AppModule (apenas em módulos que precisavam via forwardRef) — verificar se já está antes de adicionar para evitar duplicata. CobrancaModule é novo. Não remover nenhum import existente.

    Caminho de importação do public.decorator no controller: `'../../security/public.decorator'` (cobranca/ está em modules/cobranca/, security/ está em modules/security/).
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc --noEmit 2>&1 | head -30 && grep -n "CobrancaModule" apps/backend/src/modules/app.module.ts</automated>
  </verify>
  <done>
    Três arquivos criados sem erros TypeScript. app.module.ts contém CobrancaModule no array imports (grep retorna ao menos 2 ocorrências: import statement + imports array). CobrancaController tem dois endpoints com rota base @Controller('cobranca').
  </done>
</task>

<task type="auto">
  <name>Task 2: cobranca.service.ts — criarBoleto() + processarNotificacaoEFI()</name>
  <files>
    apps/backend/src/modules/cobranca/cobranca.service.ts
  </files>
  <action>
    Criar CobrancaService com dois métodos públicos. Injetar EfiService, AthosService, PrismaService via constructor.

    **criarBoleto(dto: CriarBoletoDto): Promise&lt;CriarBoletoResponseDto&gt;**

    Implementar os 7 passos de D-12 na ordem exata:

    1. Buscar todos os títulos do cliente via `athosService.buscarTitulosClienteContasReceber(dto.idclienteAthos)`. Filtrar pelos `dto.idcontasReceber`. Se algum ID não for encontrado nos resultados → lançar `BadRequestException('Título {id} não encontrado para este cliente.')`.

    2. Validar status: todos os títulos filtrados devem ser AVC (datavencimento >= hoje) ou VEN (datavencimento < hoje). Como a query do Athos já filtra por status AVC/VEN, esta validação é implícita — mas conferir se a lista filtrada tem length === dto.idcontasReceber.length.

    3. Calcular `totalValor = titulos.reduce((acc, t) => acc + Number(t.valor), 0)`. Arredondar com `Number(totalValor.toFixed(2))`.

    4. Validar expireAt: `if (new Date(dto.expireAt) < new Date(new Date().toISOString().slice(0, 10))) throw new BadRequestException('A data de vencimento informada já passou. Informe uma data futura.')`.

    5. Buscar dados do cliente via `athosService.buscarDadosClienteContasReceber(dto.idclienteAthos)`. Usar `nome_cliente` como `customer.name`. Para CPF/CNPJ, chamar `athosService.buscarClientePorId(dto.idclienteAthos)` e extrair `documento`. Se `documento` disponível e tiver 11 dígitos → incluir como `customer.cpf`. Se tiver 14 dígitos → incluir como `customer.cnpj`. Se null → omitir.

    6. Criar boleto na EFI usando o PADRÃO IDÊNTICO ao de `createCardPaymentLink()` (per D-03):
       - Criar `cobrancaClient = axios.create({ baseURL, timeout: 15_000 })`
       - POST /v1/authorize com Basic base64(CLIENT_ID:CLIENT_SECRET) e body `{ grant_type: "client_credentials" }`
       - Obter Bearer token da resposta
       - Montar body EFI conforme D-14:
         ```
         items: [{ name: `Cobrança #${dto.idclienteAthos}`.slice(0, 255), value: Math.round(Number(totalValor.toFixed(2)) * 100), amount: 1 }]
         customer: { name: nomeCliente.slice(0, 80), ...cpfOuCnpj }
         settings: { payment_method: "banking_billet", expire_at: dto.expireAt, request_delivery_address: false }
         metadata: { custom_id: `cr-${dto.idclienteAthos}-${Date.now()}`.slice(0, 50), notification_url: <webhookUrl> }
         ```
       - Para `notification_url`: usar `process.env.WEBHOOK_BASE_URL ?? process.env.APP_URL ?? ''` + `/api/cobranca/boleto/notificacao`. Se a URL contiver localhost ou 127.0.0.1, omitir `notification_url` (mesmo padrão de isPublicUrl de createCardPaymentLink).
       - POST /v1/charge/one-step (NÃO /link — boleto não é link de pagamento). Endpoint para boleto bancário é `/v1/charge/one-step`.
       - Extrair da resposta: `charge_id` (número inteiro), `link` (URL do PDF do boleto), `barcode` (linha digitável). Campos na resposta EFI one-step: `resp.data.data.charge_id`, `resp.data.data.link`, `resp.data.data.barcode`.
       - Wrap com try/catch → em caso de erro logar e lançar `InternalServerErrorException('Não foi possível gerar o boleto na EFI.')`.

    7. Salvar no Prisma com nested write:
       ```
       prisma.cobrancaBoleto.create({
         data: {
           txidEfi: String(chargeId),
           idclienteAthos: dto.idclienteAthos,
           valor: totalValor,
           status: 'pendente',
           linkBoleto: linkBoleto,
           pixPayload: null,
           titulos: {
             createMany: {
               data: titulosFiltrados.map(t => ({
                 idcontareceber: t.idcontareceber,
                 valor: t.valor,
               })),
             },
           },
         },
       })
       ```

    8. Retornar shape de D-13:
       ```
       { cobrancaId: boleto.id, chargeId, linkBoleto, barcodeLinhaDigitavel: barcode, valor: totalValor, expireAt: dto.expireAt }
       ```

    **processarNotificacaoEFI(token: string): Promise&lt;void&gt;**

    Implementar fluxo de D-17:
    1. Autenticar na API EFI Cobranças (mesmo padrão Basic Auth acima).
    2. GET /v1/notification/{token} com Bearer token.
    3. Extrair `charge_id = resp.data.data.charge.charge_id` e `status = resp.data.data.charge.status`.
    4. Buscar `prisma.cobrancaBoleto.findFirst({ where: { txidEfi: String(charge_id) } })`.
    5. Se `status === 'paid'` e registro encontrado → `prisma.cobrancaBoleto.update({ where: { id: boleto.id }, data: { status: 'pago' } })`.
    6. Idempotente: se já `status === 'pago'` → não fazer update, apenas logar.
    7. Nunca lançar exceção que chegue ao controller (per D-18) — capturar tudo internamente e logar.

    Usar `this.logger = new Logger(CobrancaService.name)` para logs estruturados.
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    CobrancaService compila sem erros TypeScript. Ambos os métodos implementados com os 7 passos de criarBoleto() e o fluxo de webhook processarNotificacaoEFI(). Compilação limpa.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    CobrancaModule completo: controller com POST /cobranca/boleto (autenticado) e
    POST /cobranca/boleto/notificacao (@Public), serviço com criarBoleto() e
    processarNotificacaoEFI(), registrado em AppModule.
  </what-built>
  <how-to-verify>
    1. Build do backend:
       `cd apps/backend && npx tsc --noEmit` — deve passar sem erros
    2. Rodar backend em modo dev (se disponível):
       `cd apps/backend && npm run start:dev`
       Verificar no log de startup que CobrancaModule carregou sem erro de injeção
    3. Testar endpoint (sandbox EFI ou mock):
       ```
       curl -X POST http://localhost:4000/api/cobranca/boleto \
         -H "Content-Type: application/json" \
         -H "x-internal-api-key: $INTERNAL_API_KEY" \
         -d '{"idclienteAthos": 1, "idcontasReceber": [1], "expireAt": "2026-12-31"}'
       ```
       Resultado esperado: 200 com JSON contendo cobrancaId, chargeId, linkBoleto, barcodeLinhaDigitavel, valor, expireAt
       (Aceita 500/EFI error em ambiente sem credenciais EFI — o importante é que a rota existe e a autenticação funciona)
    4. Testar data no passado → 400:
       `-d '{"idclienteAthos": 1, "idcontasReceber": [1], "expireAt": "2020-01-01"}'`
       Resultado esperado: status 400
    5. Testar webhook (deve retornar 200 sem auth):
       ```
       curl -X POST http://localhost:4000/api/cobranca/boleto/notificacao \
         -H "Content-Type: application/json" \
         -d '{"token": "token_teste_invalido"}'
       ```
       Resultado esperado: HTTP 200 com `{"ok":true}` (mesmo com token inválido — por design D-18)
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend→POST /cobranca/boleto | Header x-internal-api-key obrigatório — InternalAuthGuard global cobre |
| EFI→POST /cobranca/boleto/notificacao | Endpoint público — EFI envia token opaco; validação ocorre ao consultar /v1/notification/{token} na EFI |
| CobrancaService→EFI API | Credenciais EFI_CLIENT_ID/EFI_CLIENT_SECRET via ConfigService, nunca expostos na resposta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Spoofing | POST /cobranca/boleto | mitigate | InternalAuthGuard global valida x-internal-api-key com timingSafeEqual — já implementado |
| T-29-02 | Tampering | idcontasReceber[] | mitigate | Service valida que todos os IDs existem nos títulos do cliente via Athos antes de criar cobrança |
| T-29-03 | Repudiation | CobrancaBoleto | mitigate | Registro Prisma com criadoEm/atualizadoEm + txidEfi para rastreabilidade |
| T-29-04 | Information Disclosure | EFI credentials | mitigate | Credenciais via ConfigService/env vars, nunca incluídas na resposta ao frontend |
| T-29-05 | Elevation of Privilege | POST /cobranca/boleto/notificacao | accept | Endpoint público por requisito EFI; token opaco validado pela própria EFI API em GET /v1/notification/{token} |
| T-29-SC | Tampering | npm install | mitigate | Nenhum pacote novo — reutiliza axios e @nestjs/* já no projeto |
</threat_model>

<verification>
1. `cd apps/backend && npx tsc --noEmit` — zero erros TypeScript
2. Arquivo apps/backend/src/modules/cobranca/cobranca.module.ts existe com CobrancaModule
3. Arquivo apps/backend/src/modules/cobranca/cobranca.service.ts existe com criarBoleto() e processarNotificacaoEFI()
4. Arquivo apps/backend/src/modules/cobranca/cobranca.controller.ts existe com dois endpoints
5. grep -n "CobrancaModule" apps/backend/src/modules/app.module.ts — deve retornar ao menos 2 matches (import statement + imports array)
6. grep -n "@Public" apps/backend/src/modules/cobranca/cobranca.controller.ts — deve aparecer antes de notificacao
7. grep -v "^//" apps/backend/src/modules/cobranca/cobranca.service.ts | grep -c "criarBoleto\|processarNotificacaoEFI" — deve retornar 2
</verification>

<success_criteria>
- CobrancaModule registrado e carregado pelo AppModule sem erro de injeção
- POST /cobranca/boleto retorna 400 para data no passado (validação D-07)
- POST /cobranca/boleto retorna 200 com shape correto quando EFI responde com sucesso
- POST /cobranca/boleto/notificacao retorna 200 sempre (sem auth, per D-18)
- CobrancaBoleto criado no banco após boleto gerado com sucesso (txidEfi, linkBoleto preenchidos)
- Zero erros de compilação TypeScript
</success_criteria>

<output>
Criar `.planning/phases/29-boleto-consolidado-efi/29-01-SUMMARY.md` quando concluído
</output>
