---
phase: 38-aplica-o-de-defaults-na-cria-o-de-produto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/athos/dto/create-produto.dto.ts
  - apps/backend/src/modules/integrations/athos/athos-produto.service.ts
  - apps/backend/src/modules/integrations/athos/athos-produto.service.test.ts
autonomous: true
requirements: [DOPR-01, DOPR-02, DFIS-01, DFIS-02, DFIS-03, OVRD-01, OVRD-02, OVRD-03, OBSV-01]

must_haves:
  truths:
    - "Criar produto sem statusproduto/vendeproduto -> INSERT inclui ambos = true (DOPR-01, D-03/D-04)"
    - "Criar produto sem controlaestoque/baixarestoque/estoqueloja -> INSERT inclui true/true/'10' (DOPR-02, D-05/D-06/D-07)"
    - "Campos fiscais omitidos preenchidos com a moda de getDefaults(); fiscal sem moda fica omitido do INSERT (DFIS-01/02/03, D-08/D-13)"
    - "Valor explicito do operador (mesmo igual ao default) chega intacto ao INSERT; default nunca sobrescreve (OVRD-01/03, D-01/D-02)"
    - "editarProduto nunca chama getDefaults() nem injeta defaults (OVRD-02, D-11)"
    - "Cada criacao gera uma linha de log campo->valor aplicado; 'nenhum default necessario' quando nada foi aplicado (OBSV-01, D-12)"
  prohibitions:
    - "editarProduto NAO pode chamar AthosDefaultsService.getDefaults() nem aplicar OPERATIONAL_DEFAULTS (D-11)"
    - "Nenhuma escrita em tabela Athos alem de produto (OVRD-03)"
    - "Default NAO pode sobrescrever valor nao-nulo enviado pelo operador (D-02)"
    - "NAO criar segunda lista de campos fiscais — reusar FISCAL_FIELDS da Fase 37 (D-10)"
  artifacts:
    - apps/backend/src/modules/integrations/athos/dto/create-produto.dto.ts
    - apps/backend/src/modules/integrations/athos/athos-produto.service.ts
    - apps/backend/src/modules/integrations/athos/athos-produto.service.test.ts
  key_links:
    - "AthosProdutoService injeta AthosDefaultsService via construtor (ja exportado por AthosModule)"
    - "FISCAL_FIELDS importado de athos-defaults.util dirige tanto o merge fiscal quanto a lista de campos (D-10)"
    - "O loop de optionalFields do INSERT deve iterar sobre o objeto merged (nao o dto original) para incluir defaults aplicados"
---

<objective>
Ligar o motor de defaults da Fase 37 (`AthosDefaultsService`) ao fluxo de **criacao** de produto (`criarProduto`), preenchendo campos omitidos com defaults operacionais fixos (DOPR) e fiscais por moda (DFIS), preservando integralmente qualquer valor enviado pelo operador (OVRD), sem tocar na edicao (D-11) e registrando em log os defaults aplicados (OBSV-01).

Cobre todas as 13 decisoes do CONTEXT (D-01..D-13). A logica vive **exclusivamente** em `criarProduto`.

Purpose: Produto recem-criado nasce com configuracao operacional e fiscal sensata sem trabalho manual do operador, mantendo override total e a edicao intacta.
Output: `CreateProdutoDto` estendido, `criarProduto` com merge de defaults + log, e suite de testes estendida cobrindo DOPR/DFIS/OVRD/OBSV.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/38-aplica-o-de-defaults-na-cria-o-de-produto/38-CONTEXT.md
@.planning/phases/38-aplica-o-de-defaults-na-cria-o-de-produto/38-PATTERNS.md
@.planning/phases/38-aplica-o-de-defaults-na-cria-o-de-produto/38-VALIDATION.md

# Codigo a estender (analogos exatos — ver 38-PATTERNS.md)
@apps/backend/src/modules/integrations/athos/dto/create-produto.dto.ts
@apps/backend/src/modules/integrations/athos/athos-produto.service.ts
@apps/backend/src/modules/integrations/athos/athos-produto.service.test.ts

# Dependencia direta (Fase 37, read-only — fonte de FISCAL_FIELDS e getDefaults)
@apps/backend/src/modules/integrations/athos/athos-defaults.util.ts
@apps/backend/src/modules/integrations/athos/athos-defaults.service.ts
@apps/backend/src/modules/integrations/athos/produto.types.ts
</context>

<artifacts_produced>
## Artifacts this phase produces

**Novos campos opcionais em `CreateProdutoDto`** (D-09):
- Booleanos: `statusproduto?: boolean`, `vendeproduto?: boolean`, `baixarestoque?: boolean`
- Strings: `icms?`, `icmsnfe?`, `tributacao?`, `tributacaonfe?`, `codigocsosn?`, `codigocsosnnfe?`, `tipoitem?`, `piscst?`, `cofinscst?`, `idcfopsaida?`, `estoqueloja?` (todos `string`)
- Inteiros (`@Type(() => Number) @IsInt`): `origem?: number`, `origemnfe?: number`
- (`ncm` e `controlaestoque` ja existem — nao recriar)

**Novos simbolos em `athos-produto.service.ts`**:
- Constante nomeada `OPERATIONAL_DEFAULTS` (mapa proprio da Fase 38 — D-10): `{ statusproduto: true, vendeproduto: true, controlaestoque: true, baixarestoque: true, estoqueloja: "10" }`
- Construtor alterado: `constructor(private readonly defaultsService: AthosDefaultsService)`
- `criarProduto` estendido: merge de defaults (operacionais + fiscais), allowlist `optionalFields` ampliada, linha de log D-12
- Import novo: `FISCAL_FIELDS` de `./athos-defaults.util` e `AthosDefaultsService` de `./athos-defaults.service`

**`athos-produto.service.test.ts`**: setup do modulo passa a prover `AthosDefaultsService` mockado (`useValue`); 8 casos novos cobrindo DOPR/DFIS/OVRD/OBSV.

Nenhuma assinatura publica removida; `editarProduto` e `alterarStatusProduto` permanecem inalterados em comportamento.
</artifacts_produced>

<source_audit>
## Multi-Source Coverage Audit

| Item | Tipo | Coberto por | Status |
|------|------|-------------|--------|
| Goal ROADMAP §Phase 38 (criacao preenche defaults, preserva operador, edicao intacta, log) | GOAL | Tasks 1-3 | COVERED |
| DOPR-01 (nasce ativo/vendavel) | REQ | Task 3 (OPERATIONAL_DEFAULTS) + Task 1 (teste) | COVERED |
| DOPR-02 (controlaestoque/baixarestoque default sensato) | REQ | Task 3 + Task 1 | COVERED |
| DFIS-01 (ICMS pela moda) | REQ | Task 3 (merge FISCAL_FIELDS) + Task 1 | COVERED |
| DFIS-02 (tributacao/CSOSN/origem pela moda) | REQ | Task 3 + Task 1 | COVERED |
| DFIS-03 (PIS/COFINS/tipoitem/idcfopsaida/ncm pela moda) | REQ | Task 3 + Task 1 | COVERED |
| OVRD-01 (valor do DTO prevalece) | REQ | Task 3 (== null) + Task 1 | COVERED |
| OVRD-02 (edicao nunca forca defaults) | REQ | Task 3 (D-11, nao tocar editarProduto) + Task 1 | COVERED |
| OVRD-03 (escrita restrita a produto) | REQ | Task 3 (allowlist parametrizada) + testes existentes de tabela | COVERED |
| OBSV-01 (log dos defaults aplicados) | REQ | Task 3 (linha D-12) + Task 1 | COVERED |
| D-01..D-13 (decisoes) | CONTEXT | Tasks 1-3 (citadas nas actions) | COVERED |
| RESEARCH | — | N/A (research pulado; CONTEXT+PATTERNS carregam a base tecnica) | N/A |

Itens deferidos (nao gaps): remover STOCK_FIELDS da Fase 37; demais campos de estoque; preview/dry-run (DEFV-01/02); frontend. Todos explicitamente fora de escopo no CONTEXT.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — estender suite de testes com AthosDefaultsService mockado e casos de defaults (RED)</name>
  <files>apps/backend/src/modules/integrations/athos/athos-produto.service.test.ts</files>
  <read_first>
    - apps/backend/src/modules/integrations/athos/athos-produto.service.test.ts (suite existente — padrao de mock pg e casos)
    - .planning/phases/38-aplica-o-de-defaults-na-cria-o-de-produto/38-PATTERNS.md (secao "athos-produto.service.test.ts" — padrao de setup com mockDefaultsService e os 8 casos)
    - apps/backend/src/modules/integrations/athos/athos-defaults.util.ts (FISCAL_FIELDS, formato de ProductDefaults)
  </read_first>
  <behavior>
    Setup: o `beforeEach` passa a prover `AthosDefaultsService` mockado via `{ provide: AthosDefaultsService, useValue: { getDefaults: jest.fn().mockResolvedValue({}) } }`, e o mock fica acessivel ao teste para reconfigurar o retorno e espionar chamadas. Casos novos (todos devem FALHAR antes do wiring da Task 3):
    - DOPR-01: criarProduto sem statusproduto/vendeproduto -> SQL do INSERT contem colunas statusproduto e vendeproduto, params correspondentes = true.
    - DOPR-02: criarProduto sem controlaestoque/baixarestoque/estoqueloja -> INSERT contem as 3 colunas com params true, true, "10".
    - DFIS aplicado: getDefaults retorna `{ icms: "NAO", tributacao: "60" }`; DTO sem esses campos -> INSERT contem icms e tributacao com os valores da moda.
    - DFIS omitido sem moda: getDefaults retorna `{}`; DTO sem icms -> INSERT NAO contem coluna icms.
    - D-13 robustez: getDefaults retorna `{}` -> criarProduto resolve sem lancar e retorna idproduto.
    - OVRD-01/03 override a prova de coincidencia: DTO com statusproduto=false (e outro campo com valor igual ao default) -> param gravado = false (e o valor enviado), nunca o default.
    - OVRD-02/D-11: editarProduto chamado -> mockDefaultsService.getDefaults NAO foi chamado (toHaveBeenCalledTimes(0)).
    - OBSV-01: spy em logger.log -> uma chamada contendo os campos aplicados (ex.: statusproduto); e, com DTO preenchendo todos os campos default, uma chamada contendo "nenhum default necessario".
  </behavior>
  <action>
    Estender a suite existente (NAO reescrever). No topo, importar `AthosDefaultsService` de "./athos-defaults.service". Refatorar o `beforeEach` para criar um `mockDefaultsService = { getDefaults: jest.fn().mockResolvedValue({}) }` e incluir `{ provide: AthosDefaultsService, useValue: mockDefaultsService }` nos providers do TestingModule; manter o mock em escopo (variavel do describe) para os casos reconfigurarem `getDefaults.mockResolvedValueOnce(...)` e asserirem chamadas. Adicionar os 8 casos descritos em <behavior> dentro de `describe("criarProduto")` (exceto o caso D-11 que vai em `describe("editarProduto")`), seguindo o padrao de inspecao de SQL ja usado (`client.query.mock.calls.map(...)`, localizar o INSERT por "INSERT INTO", separar colunas via split("VALUES"), e cruzar coluna->placeholder->params para checar valores). Para asserts de valor de coluna, derive o indice do placeholder `$N` a partir da posicao da coluna na lista. Para OBSV-01, usar `jest.spyOn(service["logger"], "log")` ou spy no Logger e checar a string. Implementa D-01 (omitido = `== null`), D-02 (override), D-03..D-07 (operacionais), D-08/D-13 (fiscal omitido sem moda), D-11 (edicao sem getDefaults), D-12 (log). NAO usar flags de watch. Rodar a suite e confirmar que os casos novos FALHAM (RED) porque o wiring ainda nao existe; os casos pre-existentes devem continuar passando (prover um provider extra nao injetado e inofensivo).
  </action>
  <verify>
    <automated>cd apps/backend && npx jest athos-produto --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    Os 8 casos novos existem e estao em estado RED (falham por ausencia da logica de defaults). Os casos pre-existentes de criarProduto/editarProduto/alterarStatusProduto continuam verdes. Nenhum flag de watch. Setup provê AthosDefaultsService mockado.
  </acceptance_criteria>
  <done>8 casos novos (DOPR/DFIS/OVRD/OBSV) existem em estado RED; casos pre-existentes verdes; AthosDefaultsService mockado no setup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estender CreateProdutoDto com os campos de default opcionais (D-09)</name>
  <files>apps/backend/src/modules/integrations/athos/dto/create-produto.dto.ts</files>
  <read_first>
    - apps/backend/src/modules/integrations/athos/dto/create-produto.dto.ts (padroes de campo: boolean como tipoproduto L149-152, string como ncm L42-45, int com @Type como idunidade L57-61)
    - apps/backend/src/modules/integrations/athos/produto.types.ts (tipos das colunas: estoqueloja string|null L67; origem/origemnfe number|null L43/L130; demais fiscais string)
    - .planning/phases/38-aplica-o-de-defaults-na-cria-o-de-produto/38-PATTERNS.md (resumo D-09 dos campos a adicionar)
  </read_first>
  <behavior>
    Apos a extensao, `class-validator` aceita um DTO que inclua qualquer um dos novos campos com o tipo correto e rejeita tipos errados (ex.: statusproduto deve ser boolean, estoqueloja string, origem number). Todos sao opcionais (`@IsOptional`) — DTO sem eles continua valido.
  </behavior>
  <action>
    Adicionar a `CreateProdutoDto` os campos opcionais (D-09), reusando os padroes de decorators ja presentes no arquivo:
    - Booleanos (`@ApiPropertyOptional` + `@IsOptional` + `@IsBoolean`): `statusproduto?: boolean` (exemplo true), `vendeproduto?: boolean` (exemplo true), `baixarestoque?: boolean` (exemplo true).
    - Strings (`@ApiPropertyOptional` + `@IsOptional` + `@IsString`): `icms?`, `icmsnfe?`, `tributacao?`, `tributacaonfe?`, `codigocsosn?`, `codigocsosnnfe?`, `tipoitem?`, `piscst?`, `cofinscst?`, `idcfopsaida?`, `estoqueloja?` (exemplo "10", descricao "Estoque inicial na loja").
    - Inteiros (`@ApiPropertyOptional` + `@IsOptional` + `@Type(() => Number)` + `@IsInt`): `origem?: number`, `origemnfe?: number` (number|null no schema).
    NAO recriar `ncm` nem `controlaestoque` (ja existem). Manter a ordem/estilo do arquivo. Esses campos sao a fonte que o operador usa para sobrescrever defaults (D-02) — a deteccao de omissao (`== null`) acontece na Task 3, nao aqui.
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "^#" | grep -i "create-produto" | head -20; echo "tsc-done"</automated>
  </verify>
  <acceptance_criteria>
    Os 16 novos campos opcionais existem com os tipos e decorators corretos (3 boolean, 11 string incl. estoqueloja, 2 int). `ncm`/`controlaestoque` nao duplicados. Compila sem erro de tipo no DTO.
  </acceptance_criteria>
  <done>16 campos opcionais adicionados (3 boolean, 11 string, 2 int) com decorators corretos; sem duplicar ncm/controlaestoque; tsc limpo no DTO.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Aplicar defaults (operacionais + fiscais) em criarProduto, com override e log (GREEN)</name>
  <files>apps/backend/src/modules/integrations/athos/athos-produto.service.ts</files>
  <read_first>
    - apps/backend/src/modules/integrations/athos/athos-produto.service.ts (criarProduto L61-158: ponto de injecao apos FK pre-validacao L75 e antes de columns L79; allowlist optionalFields L85-110; log L124-127; editarProduto L160-254 — NAO tocar)
    - apps/backend/src/modules/integrations/athos/athos-defaults.util.ts (FISCAL_FIELDS — fonte unica, D-10; tipos fiscais)
    - apps/backend/src/modules/integrations/athos/athos-defaults.service.ts (getDefaults() — retorno ProductDefaults; nunca lanca por logica de moda)
    - .planning/phases/38-aplica-o-de-defaults-na-cria-o-de-produto/38-PATTERNS.md (secao criarProduto: ponto de injecao, merge, log D-12, injecao de dependencia)
  </read_first>
  <behavior>
    Coberto pelos testes da Task 1. Resumo: defaults operacionais e fiscais preenchem apenas campos `== null` no DTO; valor nao-nulo do operador prevalece sempre; fiscal sem moda fica fora do INSERT; getDefaults vazio nao quebra a criacao; cada criacao loga campo->valor (ou "nenhum default necessario"); editarProduto permanece sem qualquer chamada a getDefaults.
  </behavior>
  <action>
    Injetar a dependencia: importar `AthosDefaultsService` de "./athos-defaults.service" e `FISCAL_FIELDS` de "./athos-defaults.util"; adicionar `constructor(private readonly defaultsService: AthosDefaultsService) {}` (AthosModule ja provê e exporta o serviço — sem mudanca no modulo).

    Em `criarProduto`, no ponto de injecao (apos a pre-validacao de FK e antes de montar `columns`):
    1. Buscar fiscais: `const fiscalDefaults = await this.defaultsService.getDefaults();` (dentro do try existente — erro do pg cai no catch atual; por D-13 a logica de moda nunca lanca).
    2. Definir a constante nomeada `OPERATIONAL_DEFAULTS` (D-03..D-07): statusproduto=true, vendeproduto=true, controlaestoque=true, baixarestoque=true, estoqueloja="10" (string, pois a coluna e string|null).
    3. Construir `const merged = { ...dto }` e um acumulador `appliedDefaults: Record<string, unknown> = {}`. Aplicar operacionais: para cada par de OPERATIONAL_DEFAULTS, se `merged[campo] == null` (D-01 — captura undefined E null), atribuir o default e registrar em appliedDefaults. Depois aplicar fiscais: para cada `field` de `FISCAL_FIELDS` (D-10), pegar `fiscalDefaults[field]`; se ele for `!== undefined` E `merged[field] == null`, atribuir e registrar. O operador SEMPRE prevalece (D-02) — nunca sobrescrever valor nao-nulo, mesmo igual ao default (OVRD-03).
    4. Ampliar a allowlist `optionalFields` com os campos novos: statusproduto, vendeproduto, baixarestoque, estoqueloja, icms, icmsnfe, tributacao, tributacaonfe, codigocsosn, codigocsosnnfe, origem, origemnfe, tipoitem, piscst, cofinscst, idcfopsaida. (controlaestoque e ncm ja estao na lista — nao duplicar.)
    5. **Alterar o loop do INSERT para iterar sobre `merged`** (nao `dto`): `if (merged[field] !== undefined) { columns.push(field); ...; params.push(merged[field]); }` — caso contrario os defaults aplicados nao seriam gravados.
    6. Manter a pre-validacao de FK lendo de `dto` (defaults nao tocam FK). Manter o bloco catch/finally inalterado.
    7. Log D-12 (OBSV-01): apos obter `idproduto`, adicionar uma linha estruturada listando campo=valor para cada entrada de appliedDefaults; se vazio, logar "nenhum default necessario". Manter a linha de log existente. Serializar valores de forma legivel (ex.: JSON.stringify do valor).

    NAO criar helper compartilhado alcancavel por `editarProduto`; NAO chamar getDefaults em editarProduto/alterarStatusProduto (D-11/OVRD-02). NAO escrever em nenhuma tabela alem de produto (OVRD-03).
  </action>
  <verify>
    <automated>cd apps/backend && npx jest athos-produto --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    Todos os casos da Task 1 ficam VERDES. criarProduto injeta AthosDefaultsService, aplica OPERATIONAL_DEFAULTS + fiscais via FISCAL_FIELDS somente em campos `== null`, itera o INSERT sobre `merged`, e emite a linha de log D-12. editarProduto/alterarStatusProduto permanecem sem chamada a getDefaults. Suite athos-produto inteira verde.
  </acceptance_criteria>
  <done>Suite athos-produto inteira verde; defaults aplicados so em campos == null com override do operador; INSERT itera sobre merged; log D-12 emitido; editarProduto sem getDefaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador HTTP -> ProdutoController -> AthosProdutoService | DTO do operador (input nao confiavel) cruza para a montagem do INSERT |
| AthosProdutoService -> tabela `produto` (Athos) | escrita na unica tabela permitida (excecao controlada v2.2) |
| AthosDefaultsService -> SELECT read-only do catalogo Athos | leitura para calculo de moda (Fase 37, fora de escopo de escrita) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Tampering | montagem do INSERT em criarProduto | medium | mitigate | Allowlist `optionalFields` hardcoded + placeholders `$N` parametrizados; os novos campos default entram na allowlist estatica (sem nomes de coluna vindos de input). Valores validados por class-validator no DTO (V5 Input Validation). |
| T-38-02 | Elevation of Privilege | caminho de edicao (editarProduto) | medium | mitigate | D-11: logica de defaults vive SO em criarProduto; sem helper compartilhado; teste assert que getDefaults nao e chamado por editarProduto (toHaveBeenCalledTimes(0)). |
| T-38-03 | Information Disclosure | linha de log D-12 | low | accept | Loga apenas os valores aplicados ao produto unico em criacao (aceitavel por D-12), nunca a moda do catalogo inteiro (regra preservada da Fase 37). |
| T-38-04 | Denial of Service | getDefaults() durante criacao | low | mitigate | getDefaults usa cache de 24h e nunca lanca por logica de moda (Fase 37 D-13); falha de DB cai no catch existente -> 500 sem escrita parcial. |
| T-38-SC | Tampering | dependencias npm | low | accept | Nenhuma instalacao de pacote nesta fase — sem nova superficie de supply chain; reusa apenas modulos internos ja entregues. |

Nenhuma ameaca high/critical sem mitigacao. Controle ASVS L1 principal: V5 (Input Validation) — Parcial, satisfeito por DTO class-validator + allowlist de colunas hardcoded.
</threat_model>

<verification>
## Checks da fase

Automatizado:
- `cd apps/backend && npx jest athos-produto --no-coverage` — suite verde (todos os 8 casos novos + pre-existentes).
- `cd apps/backend && npx jest --no-coverage` — suite completa verde antes de `/gsd-verify-work`.
- `cd apps/backend && npx tsc --noEmit` — sem erro de tipo.

Manual (UAT — exige escrita real no Athos, fora dos testes unitarios):
- Criar produto via API sem status/estoque e confirmar no Athos: statusproduto=true, vendeproduto=true, controlaestoque=true, baixarestoque=true, estoqueloja=10 (DOPR-01/02, criterio 1 do roadmap).
- Criar produto sem campos fiscais e confirmar que vieram coerentes com a moda do catalogo (DFIS, criterio 2).
</verification>

<success_criteria>
- Os 5 criterios de sucesso da ROADMAP §Phase 38 satisfeitos (defaults na criacao, override do operador a prova de coincidencia, edicao intacta, log por cadastro).
- Os 9 requisitos (DOPR-01/02, DFIS-01/02/03, OVRD-01/02/03, OBSV-01) verificaveis pela suite athos-produto.
- D-01..D-13 implementadas; nenhuma logica de defaults alcancavel por editarProduto.
- Escrita restrita a tabela produto; suite completa verde.
</success_criteria>

<output>
Create `.planning/phases/38-aplica-o-de-defaults-na-cria-o-de-produto/38-01-SUMMARY.md` when done
</output>
