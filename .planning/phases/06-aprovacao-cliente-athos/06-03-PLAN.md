---
phase: 06-aprovacao-cliente-athos
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified: []
autonomous: false
requirements:
  - FR-07.1
  - FR-07.2
  - FR-07.3

must_haves:
  truths:
    - "Ao criar orçamento com número Athos, mensagem Chatwoot é enviada automaticamente ao cliente"
    - "O link enviado na mensagem abre a página /orcamento/{id}/approve (não /api/quotes/)"
    - "A página de aprovação exibe itens e total do orçamento"
    - "Clicar em Aprovar muda o status para APROVADO no sistema"
  artifacts:
    - path: "apps/backend/src/modules/quotes/quotes.service.ts"
      provides: "Hook verificado em produção/dev"
      contains: "void this.enviarParaCliente"
    - path: "apps/frontend/src/app/orcamento/[id]/approve/page.tsx"
      provides: "Página com itens e total verificados visualmente"
      contains: "quoteItems"
  key_links:
    - from: "POST /api/quotes (com idorcamento)"
      to: "Chatwoot — mensagem com link"
      via: "enviarParaCliente fire-and-forget"
      pattern: "void this\\.enviarParaCliente"
---

<objective>
Verificar o fluxo completo de aprovação do cliente após a implementação dos Planos 01 e 02: desde a criação do orçamento com idcliente Athos até a aprovação via página do cliente.

Purpose: Os Planos 01 e 02 são cirúrgicos e independentes — este checkpoint confirma que funcionam corretamente de ponta a ponta no ambiente real, não apenas nos testes unitários.
Output: Confirmação humana de que o fluxo completo funciona.
</objective>

<execution_context>
@C:/Users/Use/Documents/Projeto/SistemaOrcamento-main/.planning/phases/06-aprovacao-cliente-athos/06-CONTEXT.md
</execution_context>

<context>
@C:/Users/Use/Documents/Projeto/SistemaOrcamento-main/.planning/STATE.md
@C:/Users/Use/Documents/Projeto/SistemaOrcamento-main/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Verificar compilação e testes automatizados antes do smoke test</name>
  <files>apps/backend/src/modules/quotes/quotes.service.ts apps/frontend/src/app/orcamento/[id]/approve/page.tsx apps/backend/src/modules/quotes/quotes.service.test.ts</files>
  <read_first>
    - .planning/phases/06-aprovacao-cliente-athos/06-01-SUMMARY.md
    - .planning/phases/06-aprovacao-cliente-athos/06-02-SUMMARY.md
  </read_first>
  <action>
Executar as verificações automatizadas para confirmar que os planos 01 e 02 foram aplicados corretamente:

1. Verificar compilação TypeScript do backend:
   ```bash
   cd /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend && npx tsc --noEmit 2>&1 | grep "error TS"
   ```
   Esperado: nenhuma saída.

2. Executar testes unitários do Phase 06:
   ```bash
   cd /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend && npx jest --testPathPattern quotes.service.test --no-coverage
   ```
   Esperado: PASS — todos os testes verdes.

3. Verificar compilação TypeScript do frontend:
   ```bash
   cd /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend && npx tsc --noEmit 2>&1 | grep "error TS"
   ```
   Esperado: nenhuma saída.

4. Verificar grep gates críticos:
   ```bash
   # Hook presente:
   grep -c "void this.enviarParaCliente" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend/src/modules/quotes/quotes.service.ts

   # Link corrigido (não deve conter padrão errado):
   grep -v "^[[:space:]]*//" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend/src/modules/quotes/quotes.service.ts | grep "approvalLink.*api/quotes" | wc -l

   # Itens na página de aprovação:
   grep -c "quoteItems" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend/src/app/orcamento/[id]/approve/page.tsx
   ```
   Esperado: 1, 0, >=4 respectivamente.

Se qualquer verificação falhar, corrigir antes de prosseguir para o checkpoint humano.
  </action>
  <verify>
    <automated>cd /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend && npx jest --testPathPattern quotes.service.test --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Saída do jest termina com "Tests: N passed" sem failures
    - grep de "void this.enviarParaCliente" no quotes.service.ts retorna 1
    - grep de approvalLink com api/quotes (fora de comentários) retorna 0
    - grep de "quoteItems" no approve/page.tsx retorna >= 4
    - npx tsc --noEmit no backend retorna 0 erros
    - npx tsc --noEmit no frontend retorna 0 erros
  </acceptance_criteria>
  <done>Todas as verificações automatizadas passam — ambiente pronto para smoke test manual.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Backend: hook fire-and-forget no create() de quotes.service.ts (D-01/D-05/D-06) e correção do link de aprovação para /orcamento/{id}/approve (D-03).
Frontend: página /orcamento/{id}/approve agora exibe lista de itens (nome, qtd, preço unit., subtotal) e total do orçamento acima do botão Aprovar (D-04).
  </what-built>
  <how-to-verify>
**Cenário A — Disparo automático ao criar orçamento (D-01):**
1. Fazer um POST para criar/reimportar um orçamento com número do Athos que tenha `idcliente` e `conversationId` associado.
   Usar o n8n ou curl: `POST /api/quotes` com `{ idorcamento: <numero_athos_valido> }` nos headers de autenticação.
2. Verificar no log do backend (Docker ou console): deve aparecer uma das mensagens:
   - (sucesso) Nenhum log de warning sobre enviarParaCliente — significa que foi disparado silenciosamente.
   - (falha esperada em dev sem Chatwoot real) Log: `[create] Falha no disparo automatico enviarParaCliente para orcamento ...`
3. Verificar que a resposta HTTP do POST retornou 200/201 (não 500) mesmo se Chatwoot falhou.

**Cenário B — Link de aprovação corrigido (D-03):**
4. No Chatwoot, verificar a última mensagem enviada para um cliente com orçamento recente.
   O link deve ser no formato: `https://seu-dominio.com/orcamento/{uuid}/approve?token=...`
   NÃO deve ser: `https://seu-dominio.com/api/quotes/{uuid}/approve?token=...`

**Cenário C — Página com itens e total (D-04):**
5. Acessar diretamente no browser: `http://localhost:3000/orcamento/{uuid}/approve?token={token_valido}`
   (Usar um UUID e token de um orçamento não aprovado com itens)
6. Verificar que a página exibe:
   - Nome do cliente e número do orçamento (já existia)
   - Tabela de itens com colunas: Item | Qtd | Unit. | Total
   - Linha de total formatada em R$ abaixo da tabela
   - Botão "Aprovar Orçamento" abaixo dos itens

**Cenário D — Aprovação funcional (regressão):**
7. Clicar em "Aprovar Orçamento" na página.
8. Verificar que o status muda para "Aprovado!" na página.
9. No painel interno (/dashboard ou equivalente), verificar que o orçamento aparece com status APROVADO.
  </how-to-verify>
  <resume-signal>
Digite "aprovado" se todos os cenários passaram.
Digite "parcial: [descreva qual cenário falhou]" se algum cenário não funcionou como esperado.
Digite "bloqueado: [motivo]" se não foi possível testar (ex: ambiente dev sem Chatwoot configurado).
  </resume-signal>
  <acceptance_criteria>
    - Cenário A: POST retorna HTTP 200/201 mesmo com Chatwoot offline/sem conversationId
    - Cenário B: Link no Chatwoot contém /orcamento/ e não /api/quotes/
    - Cenário C: Página exibe tabela de itens e total formatado em BRL
    - Cenário D: Aprovação muda status para APROVADO no sistema
  </acceptance_criteria>
  <done>Fluxo completo de aprovação do cliente verificado por humano em ambiente real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Browser → /orcamento/{id}/approve | URL pública com token — verificar que token inválido retorna erro, não dados do orçamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Elevation of Privilege | Smoke test com token real | accept | Usar token de orçamento de teste, não de cliente real. Invalidar o token de teste após verificação. |
</threat_model>

<verification>
Este plano é principalmente um checkpoint de verificação humana. As verificações automatizadas estão na Tarefa 1.

UAT mínimo aceitável:
- [ ] Ao criar orçamento com idorcamento Athos → POST retorna 200 (D-06 verificado)
- [ ] Link no Chatwoot usa /orcamento/ (D-03 verificado)
- [ ] Página de aprovação exibe itens e total (D-04 verificado)
- [ ] Aprovação via página muda status para APROVADO (regressão D-04/FR-07.3)
</verification>

<success_criteria>
Smoke test confirma o fluxo completo:
- Criação de orçamento com Athos não retorna 500 mesmo com falha no envio Chatwoot
- Link de aprovação correto (página visual, não endpoint de API)
- Página exibe itens e total do orçamento
- Aprovação pelo cliente atualiza status no sistema
- Todos os requisitos FR-07.1, FR-07.2, FR-07.3 verificados em ambiente real
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-aprovacao-cliente-athos/06-03-SUMMARY.md` com:
- Resultado do checkpoint humano
- Quais cenários foram testados
- Qualquer comportamento inesperado observado
- Status final da fase 6
</output>
