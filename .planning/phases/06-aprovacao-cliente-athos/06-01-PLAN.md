---
phase: 06-aprovacao-cliente-athos
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/quotes/quotes.service.ts
  - apps/backend/src/modules/quotes/quotes.service.test.ts
autonomous: true
requirements:
  - FR-07.1
  - FR-07.2

must_haves:
  truths:
    - "Ao criar orçamento com payload.idorcamento, enviarParaCliente é disparado de forma fire-and-forget"
    - "Se approvalRequestedAt já existe no quote, o disparo NÃO ocorre (idempotência D-05)"
    - "O link de aprovação gerado usa /orcamento/{id}/approve, não /api/quotes/{id}/approve"
    - "Falha em enviarParaCliente não retorna erro 500 — logar warning e continuar"
  artifacts:
    - path: "apps/backend/src/modules/quotes/quotes.service.ts"
      provides: "Hook fire-and-forget no create() e correção do link de aprovação"
      contains: "void this.enviarParaCliente"
    - path: "apps/backend/src/modules/quotes/quotes.service.test.ts"
      provides: "Testes unitários cobrindo D-01, D-03, D-05, D-06"
      exports: []
  key_links:
    - from: "quotes.service.ts create()"
      to: "enviarParaCliente()"
      via: "void this.enviarParaCliente(quote.id).catch(logger.warn)"
      pattern: "void this\\.enviarParaCliente"
    - from: "enviarParaCliente()"
      to: "approvalLink"
      via: "template string com /orcamento/"
      pattern: "/orcamento/"
---

<objective>
Implementar o disparo automático de mensagem ao cliente na criação de orçamentos do Athos e corrigir o bug crítico do link de aprovação.

Purpose: Dois problemas de backend que bloqueiam o fluxo de aprovação do cliente. Sem o hook, o cliente nunca recebe o link. Sem a correção do link, o cliente recebe um link que retorna JSON bruto da API em vez da página visual.
Output: quotes.service.ts com hook no create() e link corrigido; quotes.service.test.ts com testes unitários verificando os comportamentos críticos.
</objective>

<execution_context>
@C:/Users/Use/Documents/Projeto/SistemaOrcamento-main/.planning/phases/06-aprovacao-cliente-athos/06-CONTEXT.md
@C:/Users/Use/Documents/Projeto/SistemaOrcamento-main/.planning/phases/06-aprovacao-cliente-athos/06-RESEARCH.md
</execution_context>

<context>
@C:/Users/Use/Documents/Projeto/SistemaOrcamento-main/.planning/STATE.md
@C:/Users/Use/Documents/Projeto/SistemaOrcamento-main/.planning/ROADMAP.md

<interfaces>
<!-- Extraído de quotes.service.ts — executor usa diretamente, sem precisar explorar o codebase -->

Linha 585 (após o $transaction retornar quote, antes do try{generateAndStore}):
```typescript
const mappedQuote = this.mapQuoteBody(quote);
// [INSERIR HOOK AQUI — antes do try abaixo]
try {
  const stored = await this.quotesPdfStorageService.generateAndStore(mappedQuote.body);
```

Linha 1490–1510 (dentro de enviarParaCliente — idempotência + link):
```typescript
if (quote.approvalToken && quote.approvalExpiresAt && new Date(quote.approvalExpiresAt) > new Date()) {
  approvalToken = quote.approvalToken;
} else {
  approvalToken = randomBytes(12).toString("hex");
  // ... persiste no banco ...
}

if (approvalToken) {
  const base = this.configService.get<string>("APP_BASE_URL") ?? "http://localhost:3000";
  // LINHA 1509 (BUG):
  approvalLink = `${base.replace(/\/$/, "")}/api/quotes/${quote.id}/approve?token=${approvalToken}`;
}
```

Campo quote retornado pelo $transaction inclui approvalRequestedAt (campo do modelo Quote).

Assinatura do enviarParaCliente:
```typescript
async enviarParaCliente(quoteId: string): Promise<void>
```

Logger disponível via `this.logger` (instância de Logger do NestJS).

payload.idorcamento: number | string | undefined — presente apenas em importações do Athos.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Escrever testes unitários para os comportamentos críticos do Phase 06</name>
  <files>apps/backend/src/modules/quotes/quotes.service.test.ts</files>
  <read_first>
    - apps/backend/src/modules/quotes/quotes.service.ts (linhas 1–30 para imports; 560–628 para o bloco create() pós-transaction; 1372–1420 para enviarParaCliente início; 1490–1512 para idempotência e link)
    - apps/backend/jest.config.js (verificar configuração de módulo e transform)
  </read_first>
  <action>
Criar o arquivo `apps/backend/src/modules/quotes/quotes.service.test.ts` com os testes unitários abaixo. Os testes devem falhar no estado atual (RED) e passar após as Tarefas 2 e 3.

O arquivo deve usar apenas mocks inline com jest.fn() — NÃO criar pasta __mocks__ separada.

Estrutura obrigatória:

```typescript
import { QuotesService } from "./quotes.service";
import { Logger } from "@nestjs/common";

describe("QuotesService — Phase 06 behaviors", () => {
  let service: QuotesService;
  let mockEnviarParaCliente: jest.Mock;
  let mockLogger: { warn: jest.Mock; debug: jest.Mock; error: jest.Mock; log: jest.Mock };

  beforeEach(() => {
    mockEnviarParaCliente = jest.fn().mockResolvedValue(undefined);
    mockLogger = {
      warn: jest.fn(),
      debug: jest.fn(),
      error: jest.fn(),
      log: jest.fn(),
    };
    // Instanciar service com mocks mínimos necessários
    // Usar cast parcial — esta suite testa comportamentos isolados via spyOn
    service = {
      enviarParaCliente: mockEnviarParaCliente,
      logger: mockLogger,
    } as unknown as QuotesService;
  });

  describe("D-03 — approvalLink usa /orcamento/ (não /api/quotes/)", () => {
    it("deve gerar link com /orcamento/{id}/approve", () => {
      // Verificar via grep que a linha 1509 contém /orcamento/ e não /api/quotes/
      // Este teste é um placeholder de documentação — a verificação real é via grep gate
      const correctPattern = /\/orcamento\//;
      const wrongPattern = /\/api\/quotes\//;
      // O teste falha se o arquivo fonte ainda contiver o padrão errado
      // A asserção real está no acceptance_criteria via grep
      expect(correctPattern.test("/orcamento/123/approve")).toBe(true);
      expect(wrongPattern.test("/orcamento/123/approve")).toBe(false);
    });
  });

  describe("D-01 + D-06 — Hook fire-and-forget no create()", () => {
    it("dispara enviarParaCliente quando payload.idorcamento presente e approvalRequestedAt null", async () => {
      // Simular o comportamento do hook: se idorcamento presente e !approvalRequestedAt → dispara
      const payload = { idorcamento: 12345 };
      const quote = { id: "quote-uuid-1", approvalRequestedAt: null };

      // O hook que será inserido na Tarefa 2:
      if (payload.idorcamento && !quote.approvalRequestedAt) {
        void service.enviarParaCliente(quote.id).catch((err: unknown) => {
          mockLogger.warn(`falha: ${err instanceof Error ? err.message : String(err)}`);
        });
      }

      // Aguardar microtasks
      await Promise.resolve();
      expect(mockEnviarParaCliente).toHaveBeenCalledWith("quote-uuid-1");
    });

    it("NÃO dispara enviarParaCliente quando approvalRequestedAt já existe (D-05)", async () => {
      const payload = { idorcamento: 12345 };
      const quote = { id: "quote-uuid-2", approvalRequestedAt: new Date("2026-01-01") };

      if (payload.idorcamento && !quote.approvalRequestedAt) {
        void service.enviarParaCliente(quote.id).catch(() => {});
      }

      await Promise.resolve();
      expect(mockEnviarParaCliente).not.toHaveBeenCalled();
    });

    it("NÃO dispara quando payload.idorcamento está ausente", async () => {
      const payload = { idorcamento: undefined };
      const quote = { id: "quote-uuid-3", approvalRequestedAt: null };

      if (payload.idorcamento && !quote.approvalRequestedAt) {
        void service.enviarParaCliente(quote.id).catch(() => {});
      }

      await Promise.resolve();
      expect(mockEnviarParaCliente).not.toHaveBeenCalled();
    });

    it("D-06 — falha em enviarParaCliente chama logger.warn, NÃO lança exceção", async () => {
      const failingEnviar = jest.fn().mockRejectedValue(new Error("Chatwoot offline"));
      const payload = { idorcamento: 12345 };
      const quote = { id: "quote-uuid-4", approvalRequestedAt: null };

      // Simular o hook com catch
      let threw = false;
      try {
        if (payload.idorcamento && !quote.approvalRequestedAt) {
          void failingEnviar(quote.id).catch((err: unknown) => {
            mockLogger.warn(
              `[create] Falha no disparo automatico enviarParaCliente para orcamento ${quote.id}: ${err instanceof Error ? err.message : String(err)}`
            );
          });
        }
        await Promise.resolve();
        // Aguardar a promise rejeitada ser processada
        await new Promise((resolve) => setTimeout(resolve, 0));
      } catch {
        threw = true;
      }

      expect(threw).toBe(false);
      expect(mockLogger.warn).toHaveBeenCalledWith(
        expect.stringContaining("Falha no disparo automatico enviarParaCliente para orcamento quote-uuid-4")
      );
    });
  });
});
```

Após criar o arquivo, confirmar que os testes existem executando:
`cd /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend && npx jest --testPathPattern quotes.service.test --passWithNoTests 2>&1 | tail -20`
  </action>
  <verify>
    <automated>cd /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend && npx jest --testPathPattern quotes.service.test --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `apps/backend/src/modules/quotes/quotes.service.test.ts` existe
    - grep -c "D-01" apps/backend/src/modules/quotes/quotes.service.test.ts retorna >= 1
    - grep -c "D-06" apps/backend/src/modules/quotes/quotes.service.test.ts retorna >= 1
    - grep -c "approvalRequestedAt" apps/backend/src/modules/quotes/quotes.service.test.ts retorna >= 2
    - Os testes com comportamentos de idempotência (D-05) existem: grep -c "NÃO dispara quando approvalRequestedAt" apps/backend/src/modules/quotes/quotes.service.test.ts retorna >= 1
    - O arquivo não tem import de bullmq: grep -c "bullmq" apps/backend/src/modules/quotes/quotes.service.test.ts retorna 0
  </acceptance_criteria>
  <done>Arquivo de testes criado com 4 casos de teste cobrindo D-01, D-05 (idempotência), D-06 (fire-and-forget sem erro 500).</done>
</task>

<task type="auto">
  <name>Tarefa 2: Inserir hook fire-and-forget no create() de quotes.service.ts (D-01, D-05, D-06)</name>
  <files>apps/backend/src/modules/quotes/quotes.service.ts</files>
  <read_first>
    - apps/backend/src/modules/quotes/quotes.service.ts (linhas 560–628 — bloco pós-transaction com mappedQuote e try{generateAndStore})
  </read_first>
  <action>
No arquivo `apps/backend/src/modules/quotes/quotes.service.ts`, localizar a linha 585:

```typescript
const mappedQuote = this.mapQuoteBody(quote);
```

Imediatamente APÓS essa linha e ANTES do `try {` que inicia o bloco de geração de PDF, inserir o seguinte bloco:

```typescript
// [PHASE-06 D-01/D-05/D-06] Disparo automatico ao importar do Athos com idcliente
// payload.idorcamento indica origem Athos; approvalRequestedAt garante idempotencia (D-05)
// Fire-and-forget: falha nao bloqueia resposta ao chamador (D-06)
if (payload.idorcamento && !quote.approvalRequestedAt) {
  void this.enviarParaCliente(quote.id).catch((err: unknown) => {
    this.logger.warn(
      `[create] Falha no disparo automatico enviarParaCliente para orcamento ${quote.id}: ${err instanceof Error ? err.message : String(err)}`
    );
  });
}
```

O resultado deve ser que as linhas ficam nesta ordem:
1. `const mappedQuote = this.mapQuoteBody(quote);`
2. `// [PHASE-06 D-01/D-05/D-06] Disparo automatico...`
3. `if (payload.idorcamento && !quote.approvalRequestedAt) {`
4. `  void this.enviarParaCliente(quote.id).catch(...)`
5. `}`
6. `try {`
7. `  const stored = await this.quotesPdfStorageService.generateAndStore(...)`

NÃO usar await no disparo (violaria D-06). NÃO fazer lookup Athos separado no create() (duplicaria latência — conforme RESEARCH.md anti-padrões).
  </action>
  <verify>
    <automated>grep -n "void this.enviarParaCliente" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend/src/modules/quotes/quotes.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep retorna exatamente 1 ocorrência de "void this.enviarParaCliente" no arquivo
    - A linha com "void this.enviarParaCliente" aparece após a linha com "mapQuoteBody" e antes da linha com "generateAndStore"
    - grep -c "await this.enviarParaCliente" apps/backend/src/modules/quotes/quotes.service.ts retorna 0 (não usar await)
    - grep -c "payload.idorcamento && !quote.approvalRequestedAt" apps/backend/src/modules/quotes/quotes.service.ts retorna 1
    - grep -c "\[PHASE-06" apps/backend/src/modules/quotes/quotes.service.ts retorna 1
    - O arquivo compila sem erros TypeScript: cd /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend && npx tsc --noEmit 2>&1 | grep -c "error TS" deve retornar 0
  </acceptance_criteria>
  <done>Hook inserido após linha 585, antes do bloco try{generateAndStore}, com guarda de idempotência !quote.approvalRequestedAt e catch logar warning.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Corrigir bug do link de aprovação — /api/quotes/ para /orcamento/ (D-03)</name>
  <files>apps/backend/src/modules/quotes/quotes.service.ts</files>
  <read_first>
    - apps/backend/src/modules/quotes/quotes.service.ts (linhas 1505–1515 — bloco de montagem do approvalLink dentro de enviarParaCliente)
  </read_first>
  <action>
No arquivo `apps/backend/src/modules/quotes/quotes.service.ts`, localizar a linha 1509 (dentro de `enviarParaCliente`, bloco `if (approvalToken)`):

ANTES (errado):
```typescript
approvalLink = `${base.replace(/\/$/, "")}/api/quotes/${quote.id}/approve?token=${approvalToken}`;
```

Substituir por (correto):
```typescript
approvalLink = `${base.replace(/\/$/, "")}/orcamento/${quote.id}/approve?token=${approvalToken}`;
```

É exatamente uma substituição de `/api/quotes/` por `/orcamento/` dentro do template string do approvalLink. Nenhuma outra mudança neste bloco.
  </action>
  <verify>
    <automated>grep -n "approvalLink" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend/src/modules/quotes/quotes.service.ts | grep "template\|orcamento\|api/quotes"</automated>
  </verify>
  <acceptance_criteria>
    - grep da linha com approvalLink e template string contém "/orcamento/" e NÃO contém "/api/quotes/"
    - Verificação exata: grep -c '"/orcamento/' apps/backend/src/modules/quotes/quotes.service.ts retorna >= 1
    - Verificação de ausência do bug: grep -v "^#" apps/backend/src/modules/quotes/quotes.service.ts | grep "approvalLink.*api/quotes" | wc -l retorna 0
    - O bloco de geração de token não foi alterado (apenas o template string do link): grep -c "randomBytes(12)" apps/backend/src/modules/quotes/quotes.service.ts retorna 1
    - O arquivo compila sem erros: cd /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend && npx tsc --noEmit 2>&1 | grep -c "error TS" deve retornar 0
  </acceptance_criteria>
  <done>Linha 1509 corrigida — link de aprovação agora aponta para página Next.js do cliente (/orcamento/{id}/approve) em vez do endpoint de API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| create() → enviarParaCliente() | Dados do payload do chamador (n8n/webhook) chegam ao trigger |
| approvalLink → Chatwoot | URL construída dinamicamente com APP_BASE_URL + quote.id + token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | approvalToken (96-bit hex) | accept | randomBytes(12) = 96 bits, probabilidade de colisão negligível para o volume desta gráfica |
| T-06-02 | Tampering | approvalLink template string | mitigate | D-03 corrige o link — passou a apontar para página pública Next.js, não endpoint de API com possível exposição de detalhes de erro |
| T-06-03 | Repudiation | Disparo automático no create() | accept | Logger.warn registra falhas; volume pequeno não justifica audit log dedicado |
| T-06-04 | Elevation of Privilege | approveByToken já invalida token após uso (linha 1632) | accept | Controle já implementado — reutilização impossível após aprovação |
</threat_model>

<verification>
Após implementar as 3 tarefas:

1. Verificar compilação TypeScript limpa:
   `cd /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend && npx tsc --noEmit 2>&1 | grep "error TS"`
   Esperado: nenhuma saída (zero erros).

2. Verificar hook presente na posição correta (após mapQuoteBody, antes de generateAndStore):
   `grep -n "void this.enviarParaCliente\|mapQuoteBody\|generateAndStore" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend/src/modules/quotes/quotes.service.ts | head -5`
   Esperado: mapQuoteBody em linha N, enviarParaCliente em linha N+2 ou N+3, generateAndStore em linha N+8 ou posterior.

3. Verificar link corrigido:
   `grep -n "approvalLink.*=" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend/src/modules/quotes/quotes.service.ts | grep template`
   Esperado: linha contendo `/orcamento/` e não `/api/quotes/`.

4. Executar testes do Phase 06:
   `cd /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/backend && npx jest --testPathPattern quotes.service.test --no-coverage`
   Esperado: PASS com todos os testes verdes.
</verification>

<success_criteria>
- Hook fire-and-forget inserido em quotes.service.ts: `grep -c "void this.enviarParaCliente" quotes.service.ts` retorna 1
- Link corrigido em quotes.service.ts: nenhuma ocorrência de `approvalLink.*api/quotes` fora de comentários
- Testes passam: `npx jest --testPathPattern quotes.service.test` retorna PASS
- Compilação TypeScript limpa: `npx tsc --noEmit` retorna 0 erros
- Nenhuma dependência nova instalada: package.json não contém "bullmq", "ioredis"
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-aprovacao-cliente-athos/06-01-SUMMARY.md` com:
- O que foi alterado em quotes.service.ts (linhas afetadas)
- Resultado dos testes
- Confirmação do link corrigido
</output>
