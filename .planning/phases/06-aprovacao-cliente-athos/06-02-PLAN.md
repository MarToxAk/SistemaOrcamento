---
phase: 06-aprovacao-cliente-athos
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/app/orcamento/[id]/approve/page.tsx
autonomous: true
requirements:
  - FR-07.3

must_haves:
  truths:
    - "A página de aprovação exibe a lista de itens do orçamento (nome, quantidade, preço unitário, subtotal)"
    - "A página de aprovação exibe o total do orçamento formatado em BRL"
    - "Os itens ficam acima do botão Aprovar Orçamento"
    - "Se não houver itens, a página continua funcionando sem quebrar"
    - "Campos da API Athos (quantidadeitem, valoritem, etc.) são mapeados corretamente para o estado React"
  artifacts:
    - path: "apps/frontend/src/app/orcamento/[id]/approve/page.tsx"
      provides: "Página de aprovação enriquecida com itens e total"
      contains: "quoteItems"
  key_links:
    - from: "approve/page.tsx useEffect load()"
      to: "data?.body?.itens"
      via: "setQuoteItems com mapeamento de campos Athos"
      pattern: "quantidadeitem"
    - from: "approve/page.tsx idle state"
      to: "tabela de itens"
      via: "quoteItems.length > 0 && <table>"
      pattern: "quoteItems\\.length"
---

<objective>
Enriquecer a página pública de aprovação do cliente para exibir a lista de itens do orçamento e o valor total, permitindo que o cliente saiba exatamente o que está aprovando antes de clicar no botão.

Purpose: Atualmente a página mostra apenas o número do orçamento e o nome do cliente. O cliente não consegue verificar o que está aprovando. Os dados (itens e total) já estão disponíveis no response do endpoint /api/quotes/{id} — é trabalho só de frontend.
Output: approve/page.tsx com 2 novos estados (quoteTotal, quoteItems), mapeamento dos campos Athos, e tabela de itens renderizada acima do botão de aprovação.
</objective>

<execution_context>
@C:/Users/Use/Documents/Projeto/SistemaOrcamento-main/.planning/phases/06-aprovacao-cliente-athos/06-CONTEXT.md
@C:/Users/Use/Documents/Projeto/SistemaOrcamento-main/.planning/phases/06-aprovacao-cliente-athos/06-RESEARCH.md
</execution_context>

<context>
@C:/Users/Use/Documents/Projeto/SistemaOrcamento-main/.planning/STATE.md

<interfaces>
<!-- Estrutura atual de approve/page.tsx — executor não precisa re-ler o arquivo para entender o estado -->

Estado atual (linhas 13–16):
```typescript
const [state, setState] = useState<ApproveState>("loading-quote");
const [quoteNumber, setQuoteNumber] = useState<number | null>(null);
const [clientName, setClientName] = useState<string>("");
const [errorMessage, setErrorMessage] = useState<string>("");
```

Função load() atual (linhas 24–44):
```typescript
const load = async () => {
  try {
    const res = await fetch(`/api/quotes/${encodeURIComponent(id)}`);
    if (!res.ok) throw new Error("Orçamento não encontrado.");
    const data = await res.json();
    setQuoteNumber(data?.body?.idorcamento_interno ?? null);
    setClientName(data?.body?.cliente?.nome ?? "");
    if (data?.approved) { setState("success"); } else { setState("idle"); }
  } catch {
    setErrorMessage("Não foi possível carregar o orçamento.");
    setState("error");
  }
};
```

Bloco idle atual (linhas 141–157) — itens e total DEVEM ser inseridos entre clientName e o parágrafo de instrução:
```tsx
{state === "idle" && (
  <>
    <h5 className="mb-1">Aprovação de Orçamento</h5>
    {quoteNumber && <div className="text-muted mb-1">Orçamento <strong>#{quoteNumber}</strong></div>}
    {clientName && <div className="text-muted mb-4 small">{clientName}</div>}
    {/* [INSERIR TABELA DE ITENS AQUI] */}
    <p className="mb-4">
      Ao clicar no botão abaixo, você confirma a aprovação...
    </p>
    <button className="btn btn-approve w-100" onClick={handleApprove}>Aprovar Orçamento</button>
  </>
)}
```

Estrutura dos itens no response da API (campos Athos — nomenclatura legada, NÃO camelCase):
```typescript
data.body.itens[n] = {
  quantidadeitem: number,       // quantidade do item
  valoritem: number,            // preço unitário
  orcamentovalorfinalitem: number, // subtotal do item
  produto: {
    descricaoproduto: string,   // nome completo
    descricaocurta: string,     // nome curto (preferir este)
    referencia: string | null,
  }
}
data.body.totais.valor: number  // total do orçamento
```

Padrão de formatação de moeda do projeto (extraído de quotes.service.ts):
```typescript
valor.toLocaleString("pt-BR", { style: "currency", currency: "BRL" })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Adicionar estados e popular dados de itens/total na página de aprovação</name>
  <files>apps/frontend/src/app/orcamento/[id]/approve/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/orcamento/[id]/approve/page.tsx (arquivo completo — 209 linhas)
  </read_first>
  <action>
Editar `apps/frontend/src/app/orcamento/[id]/approve/page.tsx` com as seguintes mudanças precisas:

**Mudança 1 — Adicionar 2 novos estados após a linha 16 (`const [errorMessage, ...]`):**

```typescript
const [quoteTotal, setQuoteTotal] = useState<number | null>(null);
const [quoteItems, setQuoteItems] = useState<Array<{
  descricao: string;
  quantidade: number;
  valorUnitario: number;
  subtotal: number;
}>>([]);
```

**Mudança 2 — No load(), após a linha `setClientName(data?.body?.cliente?.nome ?? "")` (linha 30), adicionar:**

```typescript
setQuoteTotal(data?.body?.totais?.valor ?? null);
setQuoteItems(
  (data?.body?.itens ?? []).map((item: Record<string, unknown>) => ({
    descricao:
      String((item.produto as Record<string, unknown>)?.descricaocurta ?? "") ||
      String((item.produto as Record<string, unknown>)?.descricaoproduto ?? ""),
    quantidade: Number(item.quantidadeitem ?? 0),
    valorUnitario: Number(item.valoritem ?? 0),
    subtotal: Number(item.orcamentovalorfinalitem ?? 0),
  }))
);
```

NOTA: usar `Record<string, unknown>` em vez de `any` para satisfazer regras de linting do projeto.

**Mudança 3 — No bloco `{state === "idle"}`, APÓS o `{clientName && ...}` e ANTES do `<p className="mb-4">`, adicionar o bloco de tabela:**

```tsx
{quoteItems.length > 0 && (
  <div className="mb-4 text-start">
    <table className="table table-sm table-borderless mb-1">
      <thead>
        <tr className="small text-muted border-bottom">
          <th>Item</th>
          <th className="text-end">Qtd</th>
          <th className="text-end">Unit.</th>
          <th className="text-end">Total</th>
        </tr>
      </thead>
      <tbody>
        {quoteItems.map((item, idx) => (
          <tr key={idx} className="small">
            <td>{item.descricao}</td>
            <td className="text-end">{item.quantidade}</td>
            <td className="text-end">
              {item.valorUnitario.toLocaleString("pt-BR", { style: "currency", currency: "BRL" })}
            </td>
            <td className="text-end">
              {item.subtotal.toLocaleString("pt-BR", { style: "currency", currency: "BRL" })}
            </td>
          </tr>
        ))}
      </tbody>
    </table>
    {quoteTotal !== null && (
      <div className="text-end fw-semibold small border-top pt-1">
        Total:{" "}
        {quoteTotal.toLocaleString("pt-BR", { style: "currency", currency: "BRL" })}
      </div>
    )}
  </div>
)}
```

O `{clientName && <div className="text-muted mb-4 small">...}` deve ter a classe `mb-4` reduzida para `mb-2` quando há itens abaixo — mas como esta condição é difícil de expressar em JSX sem duplicação, manter `mb-4` no clientName e usar `mb-4` também no container de itens. O espaçamento visual ficará adequado.

Não alterar nenhuma outra parte do arquivo.
  </action>
  <verify>
    <automated>grep -n "quoteItems\|quoteTotal\|quantidadeitem\|orcamentovalorfinalitem" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend/src/app/orcamento/[id]/approve/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "quoteItems" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend/src/app/orcamento/[id]/approve/page.tsx retorna >= 4 (declaração, setQuoteItems, uso no JSX, length check)
    - grep -c "quoteTotal" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend/src/app/orcamento/[id]/approve/page.tsx retorna >= 3 (declaração, setQuoteTotal, uso no JSX)
    - grep -c "quantidadeitem" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend/src/app/orcamento/[id]/approve/page.tsx retorna 1 (dentro do mapeamento)
    - grep -c "orcamentovalorfinalitem" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend/src/app/orcamento/[id]/approve/page.tsx retorna 1
    - grep -c "pt-BR.*currency.*BRL" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend/src/app/orcamento/[id]/approve/page.tsx retorna >= 2 (valorUnitario e subtotal, mais total)
    - grep -c "table table-sm" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend/src/app/orcamento/[id]/approve/page.tsx retorna 1
    - grep -c "quoteItems.length > 0" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend/src/app/orcamento/[id]/approve/page.tsx retorna 1
    - O arquivo não contém "item: any" — grep -c "item: any" /c/.../approve/page.tsx retorna 0
    - O arquivo mantém o botão "Aprovar Orçamento" intacto: grep -c "Aprovar Orçamento" /c/.../approve/page.tsx retorna 1
  </acceptance_criteria>
  <done>approve/page.tsx atualizado com: 2 novos estados (quoteTotal, quoteItems), mapeamento dos campos Athos no load(), tabela de itens renderizada no bloco idle acima do botão de aprovação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| BFF proxy → approve/page.tsx | Dados do orçamento chegam via fetch no cliente — renderizados sem escape adicional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | approve/page.tsx renderiza itens/total | accept | Página pública com token de 96 bits no URL — apenas quem tem o link vê os dados. Token expira em 7 dias. |
| T-06-06 | Tampering | descricaocurta/descricaoproduto rendereizado como texto | accept | React escapa automaticamente texto em JSX — sem risco de XSS via nomes de produto |
</threat_model>

<verification>
Após implementar a tarefa:

1. Verificar presença dos novos estados:
   `grep -n "quoteTotal\|quoteItems" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend/src/app/orcamento/[id]/approve/page.tsx`
   Esperado: pelo menos 6 linhas (2 declarações, 2 setters, 2 usos no JSX).

2. Verificar mapeamento dos campos Athos:
   `grep -n "quantidadeitem\|valoritem\|orcamentovalorfinalitem\|descricaocurta" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend/src/app/orcamento/[id]/approve/page.tsx`
   Esperado: 4 linhas, uma para cada campo Athos.

3. Verificar tabela de itens no JSX:
   `grep -n "table table-sm\|quoteItems.map\|quoteItems.length" /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend/src/app/orcamento/[id]/approve/page.tsx`
   Esperado: 3 linhas.

4. Build do frontend sem erros TypeScript:
   `cd /c/Users/Use/Documents/Projeto/SistemaOrcamento-main/apps/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"`
   Esperado: 0.
</verification>

<success_criteria>
- quoteItems e quoteTotal declarados como useState no componente
- Mapeamento de campos Athos (quantidadeitem, valoritem, orcamentovalorfinalitem) presente no load()
- Tabela de itens renderizada condicionalmente no bloco idle, ACIMA do botão Aprovar
- Total formatado como BRL via toLocaleString("pt-BR", { style: "currency", currency: "BRL" })
- Sem uso de `any` — campos Athos mapeados via Record<string, unknown>
- Build TypeScript sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-aprovacao-cliente-athos/06-02-SUMMARY.md` com:
- Mudanças realizadas em approve/page.tsx (linhas afetadas)
- Campos Athos mapeados e seus nomes React correspondentes
- Resultado da compilação TypeScript
</output>
