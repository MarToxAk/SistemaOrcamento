---
phase: 06-aprovacao-cliente-athos
plan: "04"
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/quotes/quotes.service.ts
  - apps/backend/src/modules/quotes/quotes.service.test.ts
autonomous: true
requirements:
  - FR-07.1
must_haves:
  truths:
    - "Criar orçamento com idorcamento do Athos NÃO dispara enviarParaCliente automaticamente."
    - "O envio da mensagem ao cliente continua funcionando normalmente via botão 'Enviar' (POST /api/quotes/:id/enviar)."
    - "npm test passa sem erros após a remoção do hook."
  artifacts:
    - path: "apps/backend/src/modules/quotes/quotes.service.ts"
      provides: "create() sem o bloco fire-and-forget do disparo automático (linhas ~587-596)"
    - path: "apps/backend/src/modules/quotes/quotes.service.test.ts"
      provides: "Testes D-01/D-05/D-06 removidos ou adaptados para refletir ausência do hook"
  key_links:
    - from: "apps/backend/src/modules/quotes/quotes.service.ts"
      to: "create()"
      via: "bloco if (payload.idorcamento && !quote.approvalRequestedAt) removido"
---

<objective>
Fechar gap de UAT da fase 6 (teste 1): remover o hook de disparo automático de
enviarParaCliente no método create() de quotes.service.ts.

O operador relatou que o sistema não deve enviar mensagem automaticamente ao criar
o orçamento — o envio deve continuar sendo manual via botão "Enviar" no painel.
</objective>

<context>
- Gap origem: .planning/phases/06-aprovacao-cliente-athos/06-UAT.md (teste 1)
- O hook foi adicionado em plan 06-01 (commit 137fd82), linhas 587-596 de quotes.service.ts
- O endpoint POST /api/quotes/:id/enviar (quotes.controller.ts) já implementa o envio manual — permanece intacto
- O approvalLink fix (D-03) e as melhorias da página de aprovação (D-04) devem permanecer
</context>

<tasks>

<task>
  <name>Remover bloco fire-and-forget do create()</name>
  <read_first>
    - apps/backend/src/modules/quotes/quotes.service.ts (linhas 585-600 — ver bloco exato)
  </read_first>
  <files>
    - apps/backend/src/modules/quotes/quotes.service.ts
  </files>
  <action>
Remover as linhas 587-596 de quotes.service.ts (o comentário e o bloco if):

```typescript
// REMOVER estas linhas:
// [PHASE-06 D-01/D-05/D-06] Disparo automatico ao importar do Athos com idcliente
// payload.idorcamento indica origem Athos; approvalRequestedAt garante idempotencia (D-05)
// Fire-and-forget: falha nao bloqueia resposta ao chamador (D-06)
if (payload.idorcamento && !quote.approvalRequestedAt) {
  void this.enviarParaCliente(quote.id).catch((err: unknown) => {
    this.logger.warn(
      `[create] Falha no disparo automatico enviarParaCliente para orcamento ${quote.id}: ${err instanceof Error ? err.message : String(err)}`,
    );
  });
}
```

O código antes (linha 585) e depois (linha 598) devem permanecer:
- Antes: `const mappedQuote = this.mapQuoteBody(quote);`
- Depois: `try { const stored = await this.quotesPdfStorageService.generateAndStore(...)`
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc -p tsconfig.build.json --noEmit</automated>
    <automated>grep -n "enviarParaCliente" apps/backend/src/modules/quotes/quotes.service.ts | grep -v "async enviarParaCliente\|await this.enviarParaCliente\|enviarParaCliente(quoteId" | grep "create\|disparo\|idorcamento.*approvalRequestedAt"</automated>
  </verify>
  <acceptance_criteria>
    - grep "if (payload.idorcamento && !quote.approvalRequestedAt)" apps/backend/src/modules/quotes/quotes.service.ts retorna vazio
    - grep "disparo automatico" apps/backend/src/modules/quotes/quotes.service.ts retorna vazio (ou só nos testes, se mantidos)
    - npx tsc -p tsconfig.build.json --noEmit exit 0
  </acceptance_criteria>
  <done>
Bloco fire-and-forget removido do create(). O método create() não chama mais enviarParaCliente automaticamente.
  </done>
</task>

<task>
  <name>Remover testes do hook automático (D-01/D-05/D-06)</name>
  <read_first>
    - apps/backend/src/modules/quotes/quotes.service.test.ts (bloco describe "D-01 + D-06")
  </read_first>
  <files>
    - apps/backend/src/modules/quotes/quotes.service.test.ts
  </files>
  <action>
Remover o bloco describe inteiro "D-01 + D-06 — Hook fire-and-forget no create()" e seus
3 testes (linhas ~31-96 do arquivo de teste).

Remover também o mock mockEnviarParaCliente e sua referência no jest.spyOn, se ficar
sem uso após a remoção (verificar se algum outro teste ainda o usa).

Se mockEnviarParaCliente não for usado em nenhum outro describe, remover:
- A declaração `let mockEnviarParaCliente: jest.SpyInstance;`
- O `mockEnviarParaCliente = jest.spyOn(service, 'enviarParaCliente')...`
  </action>
  <verify>
    <automated>cd apps/backend && npm test -- --testPathPattern=quotes.service.test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npm test passa sem erros
    - grep "D-01\|D-05\|D-06\|fire-and-forget" apps/backend/src/modules/quotes/quotes.service.test.ts retorna vazio
    - Contagem de testes no output é >= 33 (37 testes anteriores - 4 removidos = ~33, podendo variar)
  </acceptance_criteria>
  <done>
Testes do hook automático removidos. Suite de testes passa completamente.
  </done>
</task>

<task>
  <name>Build e verificação final</name>
  <files>
    - apps/backend/src/modules/quotes/quotes.service.ts
    - apps/backend/src/modules/quotes/quotes.service.test.ts
  </files>
  <action>
1. Rodar build completo do backend: npm --workspace @bomcusto/backend run build
2. Rodar testes: npm test
3. Confirmar que POST /api/quotes/:id/enviar ainda existe e funciona (grep no controller)
  </action>
  <verify>
    <automated>npm --workspace @bomcusto/backend run build</automated>
    <automated>npm test</automated>
    <automated>grep -n "enviar" apps/backend/src/modules/quotes/quotes.controller.ts | grep "Post\|route\|enviar"</automated>
  </verify>
  <acceptance_criteria>
    - npm --workspace @bomcusto/backend run build exit 0
    - npm test exit 0 (todos os testes passam)
    - grep retorna o endpoint POST :id/enviar no controller (envio manual intacto)
  </acceptance_criteria>
  <done>
Build passa. Testes passam. Endpoint de envio manual intacto.
  </done>
</task>

</tasks>

<success_criteria>
- Hook automático removido do create() — criar orçamento não dispara envio ao cliente.
- Endpoint POST /api/quotes/:id/enviar (envio manual) permanece intacto e funcional.
- Build e testes passam sem erros.
- Gap registrado em 06-UAT.md fica endereçado.
</success_criteria>

<output>
Após execução, atualizar:
- .planning/phases/06-aprovacao-cliente-athos/06-UAT.md (gap status: resolved)
- .planning/phases/06-aprovacao-cliente-athos/06-04-SUMMARY.md
</output>
