---
phase: 27-dashboard-contas-receber
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/athos/athos.service.ts
  - apps/backend/src/modules/integrations/athos/athos.controller.ts
autonomous: true
requirements:
  - CR-01
  - CR-02
  - CR-03

must_haves:
  truths:
    - "GET /api/athos/contas-receber/dashboard retorna 200 com shape { summary, clientes } quando x-api-token válido"
    - "summary contém total_a_receber, total_atrasado e total_clientes_devedores"
    - "clientes é array com até 100 itens ordenados por total_atrasado DESC NULLS LAST, total_devido DESC"
    - "Cada item de clientes inclui idcliente, nome_cliente, telefone_completo, emailcliente, emailcobrancacliente, limitecredito, bloqueaprazo, total_devido, total_atrasado, titulos_pendentes, maior_atraso_dias"
    - "GET /api/athos/contas-receber/cliente/:idcliente/titulos retorna array de títulos com numerotitulo, datavencimento, valor, observacao, idvenda, numeroordem"
    - "Ambos os endpoints retornam 401 sem x-api-token"
  artifacts:
    - path: "apps/backend/src/modules/integrations/athos/athos.service.ts"
      provides: "Métodos buscarDashboardContasReceber() e buscarTitulosClienteContasReceber()"
      contains: "buscarDashboardContasReceber"
    - path: "apps/backend/src/modules/integrations/athos/athos.controller.ts"
      provides: "Rotas GET contas-receber/dashboard e GET contas-receber/cliente/:idcliente/titulos"
      contains: "contas-receber/dashboard"
  key_links:
    - from: "athos.controller.ts"
      to: "athos.service.ts"
      via: "this.athosService.buscarDashboardContasReceber()"
      pattern: "buscarDashboardContasReceber"
    - from: "athos.service.ts"
      to: "conta_receber (Athos DB)"
      via: "pool.connect() + client.query(SQL)"
      pattern: "conta_receber"
---

<objective>
Implementar dois endpoints NestJS em AthosController/AthosService para o dashboard de contas a receber:

1. GET /athos/contas-receber/dashboard — retorna summary global + array de clientes devedores agrupados (top 100 por total_atrasado)
2. GET /athos/contas-receber/cliente/:idcliente/titulos — retorna títulos individuais do cliente (lazy load para drawer)

Purpose: Fornecer os dados que o frontend do dashboard consumirá. Sem esses endpoints, a Fase 27 não tem dados.
Output: Dois novos métodos em AthosService + duas novas rotas em AthosController, com autenticação via x-api-token.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-dashboard-contas-receber/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar buscarDashboardContasReceber() em AthosService</name>
  <files>apps/backend/src/modules/integrations/athos/athos.service.ts</files>

  <read_first>
    - apps/backend/src/modules/integrations/athos/athos.service.ts (arquivo completo — padrões getPool(), client.query(), client.release(), estrutura de try/finally, tipo Row, pickString())
    - .planning/phases/27-dashboard-contas-receber/27-CONTEXT.md (SQL exato em seção specifics, decisões D-07 a D-16)
  </read_first>

  <action>
Adicionar dois métodos públicos ao final da classe AthosService (antes do fechamento da classe), seguindo o padrão estabelecido em listarContasPagar() e buscarClientes():

MÉTODO 1 — buscarDashboardContasReceber():

Tipo de retorno: Promise com objeto contendo:
- summary: { total_a_receber: number; total_atrasado: number; total_clientes_devedores: number }
- clientes: Array de objetos com campos idcliente (number), nome_cliente (string), telefone_completo (string | null), emailcliente (string | null), emailcobrancacliente (string | null), limitecredito (number), bloqueaprazo (string | null), total_devido (number), total_atrasado (number), titulos_pendentes (number), maior_atraso_dias (number | null)

Implementação:
1. Chamar this.getPool() e pool.connect() para obter PoolClient
2. Executar a seguinte query SQL agregada no try/finally (com client.release() no finally):

SELECT
    c.idcliente,
    COALESCE(cf.nome, cj.nomefantasia, cj.razaosocial, 'Cliente #' || c.idcliente::text) AS nome_cliente,
    c.dddtelefoneempresa || c.telefoneempresa AS telefone_completo,
    c.emailcliente,
    c.emailcobrancacliente,
    c.limitecredito,
    c.bloqueaprazo,
    SUM(cr.valor) FILTER (WHERE cr.statusconta = 'ABE') AS total_devido,
    SUM(cr.valor) FILTER (WHERE cr.statusconta = 'ABE' AND cr.datavencimento < CURRENT_DATE) AS total_atrasado,
    COUNT(cr.idcontareceber) FILTER (WHERE cr.statusconta = 'ABE') AS titulos_pendentes,
    MAX(CURRENT_DATE - cr.datavencimento::date) FILTER (WHERE cr.statusconta = 'ABE' AND cr.datavencimento < CURRENT_DATE) AS maior_atraso_dias
FROM cliente c
INNER JOIN conta_receber cr ON c.idcliente = cr.idcliente
LEFT JOIN cliente_fisico cf ON cf.idcliente = c.idcliente
LEFT JOIN cliente_juridico cj ON cj.idcliente = c.idcliente
WHERE cr.statusconta = 'ABE'
GROUP BY c.idcliente, cf.nome, cj.nomefantasia, cj.razaosocial,
         c.dddtelefoneempresa, c.telefoneempresa, c.emailcliente,
         c.emailcobrancacliente, c.limitecredito, c.bloqueaprazo
ORDER BY total_atrasado DESC NULLS LAST, total_devido DESC
LIMIT 100

3. Mapear result.rows para o tipo de clientes, convertendo todos os campos numéricos com Number() (os valores vêm como string do driver pg: valor, limitecredito, total_devido, total_atrasado, titulos_pendentes, maior_atraso_dias). Usar null para campos ausentes.
4. Calcular summary a partir do array mapeado:
   - total_a_receber = soma de todos total_devido
   - total_atrasado = soma de todos total_atrasado (tratar null como 0)
   - total_clientes_devedores = clientes.length
5. Retornar { summary, clientes }

MÉTODO 2 — buscarTitulosClienteContasReceber(idcliente: number):

Tipo de retorno: Promise de array com campos: idcontareceber (number), numerotitulo (string | null), datavencimento (string), valor (number), observacao (string | null), idvenda (number | null), dataemissao (string | null), numeroordem (string | null)

Implementação:
1. Chamar this.getPool() e pool.connect()
2. Executar no try/finally:

SELECT
    cr.idcontareceber, cr.numerotitulo, cr.datavencimento, cr.valor,
    cr.observacao, cr.idvenda, cr.dataemissao,
    v.numeroordem
FROM conta_receber cr
LEFT JOIN venda v ON v.idvenda = cr.idvenda
WHERE cr.idcliente = $1 AND cr.statusconta = 'ABE'
ORDER BY cr.datavencimento ASC

Com parâmetro [$1 = idcliente].
3. Mapear linhas: Number() para idcontareceber, valor; String() ou null para campos texto; datavencimento e dataemissao como string ISO (já vêm como Date do pg — usar .toISOString() se for Date, ou String() se já string).

Logging: usar this.logger.log() no início de cada método com o nome do método, padrão idêntico a buscarRelacaoOrcamentoVenda().
  </action>

  <verify>
    <automated>cd /d/Projetos/cloudeproject/SistemaOrcamento && grep -n "buscarDashboardContasReceber\|buscarTitulosClienteContasReceber" apps/backend/src/modules/integrations/athos/athos.service.ts</automated>
  </verify>

  <acceptance_criteria>
    - athos.service.ts contém método buscarDashboardContasReceber com retorno Promise tipado com { summary, clientes }
    - athos.service.ts contém método buscarTitulosClienteContasReceber com parâmetro idcliente: number
    - SQL em buscarDashboardContasReceber contém "INNER JOIN conta_receber cr ON c.idcliente = cr.idcliente"
    - SQL contém "LIMIT 100" e "ORDER BY total_atrasado DESC NULLS LAST"
    - SQL em buscarTitulosClienteContasReceber contém "LEFT JOIN venda v ON v.idvenda = cr.idvenda"
    - Ambos os métodos usam try/finally com client.release() no finally
    - grep -c "buscarDashboardContasReceber" apps/backend/src/modules/integrations/athos/athos.service.ts retorna 2 (declaração + chamada interna se houver) ou 1 (só declaração)
  </acceptance_criteria>

  <done>AthosService expõe buscarDashboardContasReceber() e buscarTitulosClienteContasReceber(idcliente) com as queries SQL corretas, tipagem TypeScript completa e padrão de pool idêntico aos métodos existentes.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar rotas GET contas-receber/dashboard e contas-receber/cliente/:idcliente/titulos em AthosController</name>
  <files>apps/backend/src/modules/integrations/athos/athos.controller.ts</files>

  <read_first>
    - apps/backend/src/modules/integrations/athos/athos.controller.ts (arquivo completo — padrões @Get, @Param, @Headers, validateAthosToken, @ApiOperation, @ApiOkResponse, @ApiUnauthorizedResponse)
    - apps/backend/src/modules/integrations/athos/athos.service.ts (assinaturas dos métodos criados na Task 1)
    - .planning/phases/27-dashboard-contas-receber/27-CONTEXT.md (decisões D-13, D-14, D-15, D-16, D-22, D-23)
  </read_first>

  <action>
Adicionar dois novos métodos ao AthosController, seguindo exatamente o padrão de listarContasPagar() e buscarClientes():

ROTA 1 — GET contas-receber/dashboard:

Decorators (nesta ordem, conforme padrão do controller existente):
- @ApiOperation({ summary: "Dashboard de contas a receber", description: "Retorna summary global e top 100 clientes devedores com títulos em aberto (statusconta=ABE)." })
- @ApiOkResponse({ description: "Dashboard com summary e lista de clientes devedores" })
- @ApiUnauthorizedResponse({ description: "Token ausente ou inválido" })
- @Get("contas-receber/dashboard")

Assinatura do método:
async dashboardContasReceber(
  @Headers("authorization") authorization?: string,
  @Headers("x-api-token") xApiToken?: string,
)

Corpo: chamar this.validateAthosToken(authorization, xApiToken) e retornar this.athosService.buscarDashboardContasReceber().

ROTA 2 — GET contas-receber/cliente/:idcliente/titulos:

Decorators (nesta ordem):
- @ApiOperation({ summary: "Títulos individuais de um cliente (contas a receber)", description: "Retorna títulos em aberto (ABE) do cliente para exibição no drawer. Lazy load." })
- @ApiParam({ name: "idcliente", example: "123", description: "ID do cliente no Athos" })
- @ApiOkResponse({ description: "Array de títulos em aberto do cliente" })
- @ApiUnauthorizedResponse({ description: "Token ausente ou inválido" })
- @Get("contas-receber/cliente/:idcliente/titulos")

Assinatura do método:
async titulosClienteContasReceber(
  @Param("idcliente") idcliente: string,
  @Headers("authorization") authorization?: string,
  @Headers("x-api-token") xApiToken?: string,
)

Corpo:
1. this.validateAthosToken(authorization, xApiToken)
2. const id = Number(idcliente); if (!Number.isFinite(id) || id <= 0) throw new BadRequestException("idcliente inválido");
3. return this.athosService.buscarTitulosClienteContasReceber(id);

Nota: BadRequestException já está disponível no import existente de @nestjs/common. Verificar e adicionar ao import destrutivo se não estiver presente.
  </action>

  <verify>
    <automated>cd /d/Projetos/cloudeproject/SistemaOrcamento && grep -n "contas-receber/dashboard\|contas-receber/cliente" apps/backend/src/modules/integrations/athos/athos.controller.ts</automated>
  </verify>

  <acceptance_criteria>
    - athos.controller.ts contém @Get("contas-receber/dashboard")
    - athos.controller.ts contém @Get("contas-receber/cliente/:idcliente/titulos")
    - Ambas as rotas chamam this.validateAthosToken(authorization, xApiToken) antes de qualquer lógica
    - Rota de títulos converte idcliente para Number e lança BadRequestException se inválido
    - Ambas as rotas têm @ApiOperation, @ApiOkResponse, @ApiUnauthorizedResponse
    - Build do backend não apresenta erros de TypeScript: cd apps/backend && npx tsc --noEmit retorna exit code 0
  </acceptance_criteria>

  <done>AthosController expõe GET /athos/contas-receber/dashboard e GET /athos/contas-receber/cliente/:idcliente/titulos, ambos protegidos por validateAthosToken, delegando para AthosService. Build TypeScript limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → NestJS API | x-api-token enviado pelo frontend interno; não exposto ao público |
| NestJS → Athos DB (PostgreSQL) | Parâmetro idcliente vindo da URL — deve ser validado antes do query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Spoofing | GET /athos/contas-receber/* | mitigate | validateAthosToken com timingSafeEqual já presente no controller — aplicar em ambas as rotas (per D-14) |
| T-27-02 | Tampering | idcliente param em /cliente/:idcliente/titulos | mitigate | Number(idcliente) + isFinite + > 0 antes do query; SQL usa $1 parametrizado (sem interpolação) |
| T-27-03 | Information Disclosure | dados de inadimplência de clientes | accept | Endpoint interno (x-api-token); sem exposição pública; dados não saem do sistema |
| T-27-04 | Denial of Service | query agregada sem LIMIT sobre conta_receber | mitigate | LIMIT 100 hardcoded na SQL; sem parâmetro de paginação nesta fase (per D-08) |
| T-27-SC | Tampering | npm/pip installs | accept | Este plano não instala pacotes novos — sem risco de supply chain |
</threat_model>

<verification>
Após execução dos dois tasks:

1. grep -n "contas-receber/dashboard" apps/backend/src/modules/integrations/athos/athos.controller.ts — deve retornar ao menos 1 linha
2. grep -n "buscarDashboardContasReceber\|buscarTitulosClienteContasReceber" apps/backend/src/modules/integrations/athos/athos.service.ts — deve retornar ao menos 2 linhas
3. cd apps/backend && npx tsc --noEmit — deve retornar exit code 0 (build limpo)
4. Smoke test manual (com servidor rodando): curl -H "x-api-token: $ATHOS_API_TOKEN" http://localhost:3001/api/athos/contas-receber/dashboard — deve retornar JSON com chaves "summary" e "clientes"
5. curl sem header deve retornar 401
</verification>

<success_criteria>
- GET /athos/contas-receber/dashboard retorna { summary: { total_a_receber, total_atrasado, total_clientes_devedores }, clientes: [...] } com status 200
- GET /athos/contas-receber/cliente/123/titulos retorna array de títulos com campo numeroordem (null se sem venda)
- Requisição sem x-api-token retorna 401 em ambas as rotas
- TypeScript build (npx tsc --noEmit) sem erros
</success_criteria>

<output>
Criar `.planning/phases/27-dashboard-contas-receber/27-01-SUMMARY.md` ao concluir
</output>
