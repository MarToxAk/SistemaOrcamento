---
phase: 40-write-crud-post-patch-delete-flag-usaprodutocomposto
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - apps/backend/src/modules/integrations/athos/athos-produto-composto.service.ts
  - apps/backend/src/modules/integrations/athos/athos-produto-composto.controller.ts
  - apps/backend/src/modules/integrations/athos/athos-produto-composto.service.test.ts
  - apps/backend/src/modules/integrations/athos/dto/update-produto-composto.dto.ts
autonomous: true
requirements: [COMP-03, COMP-04, COMP-05]
must_haves:
  truths:
    - "PATCH /athos/produtos/:idprodutomaster/composicao/:idprodutodetail atualiza a quantidade do componente — COMP-03"
    - "PATCH retorna 404 quando o par (idprodutomaster, idprodutodetail) nao existe em produto_composto — COMP-03"
    - "Quantidade fora do dominio numeric(9,3) retorna 422 (pg 22003) — COMP-03"
    - "DELETE /athos/produtos/:idprodutomaster/composicao/:idprodutodetail remove fisicamente o componente — COMP-04"
    - "DELETE retorna 404 quando o par nao existe — COMP-04"
    - "Ao remover o ULTIMO componente, usaprodutocomposto do master vira false na MESMA transacao BEGIN/COMMIT do DELETE — COMP-05"
  artifacts:
    - "apps/backend/src/modules/integrations/athos/athos-produto-composto.service.ts (metodos atualizarQuantidade e removerComponente)"
    - "apps/backend/src/modules/integrations/athos/athos-produto-composto.controller.ts (endpoints PATCH e DELETE)"
    - "apps/backend/src/modules/integrations/athos/dto/update-produto-composto.dto.ts (finalizado, sem comentario SCAFFOLD)"
    - "apps/backend/src/modules/integrations/athos/athos-produto-composto.service.test.ts (testes de PATCH e DELETE)"
  key_links:
    - "DELETE + UPDATE produto SET usaprodutocomposto=false ocorrem no MESMO PoolClient dentro de BEGIN/COMMIT; flag-off so quando a contagem APOS o delete == 0"
    - "atualizarQuantidade e removerComponente reusam o helper mapPgWriteError criado em 40-01"
  prohibitions:
    - "NAO usar soft-delete em produto_composto — DELETE e FISICO (tabela de composicao, distinta da regra de soft-delete de produto)"
    - "NAO interpolar idprodutomaster/idprodutodetail/quantidade na string SQL — sempre parametros $N"
    - "NAO desligar usaprodutocomposto enquanto ainda restarem componentes do master — flag-off so quando a contagem apos o DELETE == 0"
---

<objective>
Implementar os caminhos de ESCRITA de edicao e remocao de componente de kit: PATCH e DELETE em /athos/produtos/:idprodutomaster/composicao/:idprodutodetail. Finaliza o DTO update, adiciona atualizarQuantidade (com checagem de existencia / 404) e removerComponente (DELETE fisico com flag-off transacional no ultimo componente), reusando o helper mapPgWriteError e o padrao transacional do plano 40-01. Cobre COMP-03, COMP-04 e COMP-05 (flag-off).

Purpose: Completa o CRUD de composicao iniciado em 40-01, fechando o gerenciamento transacional do flag usaprodutocomposto (liga no primeiro add, desliga no ultimo remove).
Output: DTO update finalizado, metodos atualizarQuantidade e removerComponente, endpoints PATCH/DELETE e testes Jest verdes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-SPIKES.md

# Codigo existente a estender / espelhar (apos 40-01)
@apps/backend/src/modules/integrations/athos/athos-produto-composto.service.ts
@apps/backend/src/modules/integrations/athos/athos-produto-composto.controller.ts
@apps/backend/src/modules/integrations/athos/athos-produto-composto.service.test.ts
@apps/backend/src/modules/integrations/athos/dto/update-produto-composto.dto.ts
@apps/backend/src/modules/integrations/athos/athos-produto.service.ts
</context>

<dependency_note>
Este plano DEPENDE de 40-01 (wave 1): reusa o helper privado mapPgWriteError e o padrao transacional BEGIN/COMMIT/ROLLBACK criados la. Como ambos os planos editam os MESMOS arquivos (service, controller, service.test, e o DTO correspondente), este plano executa em wave 2, sequencialmente apos 40-01.
</dependency_note>

<constraints_from_phase_39_spikes>
1. Dominio quantidade = numeric(9,3) SEM CHECK. UpdateProdutoCompostoDto: @IsNumber() + @Min(0.001) (ja no scaffold — apenas remover o comentario SCAFFOLD). Valor fora de dominio gera pg 22003 (overflow) -> mapeado para 422 pelo helper de 40-01. NAO esperar 23514.
2. NAO existe UNIQUE em (idprodutomaster, idprodutodetail); a checagem de existencia do par (para 404) e feita por SELECT na propria tabela produto_composto.
3. PostgreSQL 9.0.5 — sem pg_sequence_last_value; nada de introspeccao necessaria aqui.
4. DELETE e FISICO (produto_composto e tabela de composicao). Soft-delete vale so para produto.
</constraints_from_phase_39_spikes>

<external_gate_note>
GATE EXTERNO (responsabilidade do usuario): o GRANT UPDATE/DELETE em produto_composto deve estar confirmado no banco Athos antes da verificacao live. Sem o GRANT, PATCH/DELETE retornam 500 (pg 42501) em runtime; o helper mapPgWriteError (de 40-01) ja mapeia 42501 para 500 acionavel. NAO criar task que tente rodar o GRANT.
</external_gate_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalizar UpdateProdutoCompostoDto e implementar atualizarQuantidade + removerComponente no service</name>
  <files>apps/backend/src/modules/integrations/athos/dto/update-produto-composto.dto.ts, apps/backend/src/modules/integrations/athos/athos-produto-composto.service.ts</files>
  <read_first>
    - apps/backend/src/modules/integrations/athos/athos-produto-composto.service.ts (helper mapPgWriteError e padrao BEGIN/COMMIT/ROLLBACK ja criados em 40-01 — reusar)
    - apps/backend/src/modules/integrations/athos/athos-produto.service.ts (padrao de checagem de existencia com SELECT 1 + NotFoundException; UPDATE parametrizado)
    - apps/backend/src/modules/integrations/athos/dto/update-produto-composto.dto.ts (scaffold com @IsNumber/@Min(0.001) — apenas remover comentario SCAFFOLD)
  </read_first>
  <behavior>
    - DTO: quantidade valida com @IsNumber() + @Min(0.001); comentario SCAFFOLD removido.
    - atualizarQuantidade(master, detail, dto): par inexistente em produto_composto -> NotFoundException (404). Sucesso -> UPDATE produto_composto SET quantidade=$1 WHERE idprodutomaster=$2 AND idprodutodetail=$3; retorna { idprodutocomposto } do par. Quantidade fora do dominio -> 422 (22003 via mapPgWriteError).
    - removerComponente(master, detail): par inexistente -> NotFoundException (404). Sucesso -> DELETE fisico do par. Apos o delete, se a contagem de componentes restantes do master == 0, UPDATE produto SET usaprodutocomposto=false na mesma transacao. Retorna { removed: true } (ou shape equivalente).
    - removerComponente nao desliga o flag se ainda restarem componentes.
    - client.release em todos os caminhos; ROLLBACK no caminho de erro do DELETE.
  </behavior>
  <action>
    Finalizar update-produto-composto.dto.ts: manter @IsNumber() + @Min(0.001) + @Type(() => Number) em quantidade; remover o bloco de comentario SCAFFOLD.

    No athos-produto-composto.service.ts, importar UpdateProdutoCompostoDto de ./dto/update-produto-composto.dto (e garantir que NotFoundException ja esteja importado de @nestjs/common — ja esta).

    Adicionar metodo async atualizarQuantidade(idprodutomaster: number, idprodutodetail: number, dto: UpdateProdutoCompostoDto): Promise<{ idprodutocomposto: number }>. Fluxo: obter client; try: SELECT idprodutocomposto FROM produto_composto WHERE idprodutomaster=$1 AND idprodutodetail=$2 LIMIT 1; se vazio, lancar NotFoundException citando o par. Senao UPDATE produto_composto SET quantidade=$1 WHERE idprodutomaster=$2 AND idprodutodetail=$3 (todos parametrizados). Logar e retornar { idprodutocomposto } da linha encontrada. catch: chamar this.mapPgWriteError(err) (re-lanca NotFound; mapeia 22003->422 etc.). finally: client.release(). Esta operacao nao altera flag e nao requer transacao multi-statement, mas e aceitavel envolver em try/finally simples sem BEGIN.

    Adicionar metodo async removerComponente(idprodutomaster: number, idprodutodetail: number): Promise<{ removed: true }>. Fluxo transacional: obter client; try: client.query('BEGIN'); SELECT 1 FROM produto_composto WHERE idprodutomaster=$1 AND idprodutodetail=$2 LIMIT 1; se vazio, lancar NotFoundException (404). DELETE FROM produto_composto WHERE idprodutomaster=$1 AND idprodutodetail=$2 (DELETE FISICO — sem soft-delete). Contar restantes: SELECT count(*)::int AS total FROM produto_composto WHERE idprodutomaster=$1; se total === 0, UPDATE produto SET usaprodutocomposto=false WHERE idproduto=$1 (master) no mesmo client. client.query('COMMIT'). Retornar { removed: true }. catch: ROLLBACK protegido + this.mapPgWriteError(err). finally: client.release(). Logar master/detail/flagDesligado.

    Todos os valores sempre via parametros $N; nunca interpolar input na string SQL.
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc -p tsconfig.build.json --noEmit</automated>
  </verify>
  <done>
    DTO update sem comentario SCAFFOLD; atualizarQuantidade e removerComponente implementados; flag-off transacional no ultimo componente; DELETE fisico; projeto compila.
  </done>
  <acceptance_criteria>
    - update-produto-composto.dto.ts nao contem a palavra SCAFFOLD.
    - atualizarQuantidade lanca NotFoundException quando o SELECT do par retorna vazio.
    - removerComponente usa DELETE FROM produto_composto (fisico) e nao faz UPDATE de soft-delete.
    - Existe UPDATE produto SET usaprodutocomposto = false condicionado a total === 0 dentro da transacao do DELETE.
    - Ambos os metodos reusam this.mapPgWriteError no catch.
    - tsc compila sem erro.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Adicionar endpoints PATCH e DELETE no ProdutoCompostoController</name>
  <files>apps/backend/src/modules/integrations/athos/athos-produto-composto.controller.ts</files>
  <read_first>
    - apps/backend/src/modules/integrations/athos/athos-produto-composto.controller.ts (controller apos 40-01 com GET e POST; espelhar padrao de rota aninhada e guard de classe)
    - apps/backend/src/modules/integrations/athos/athos-produto.controller.ts (padrao de @Patch/@Delete/@Param ParseIntPipe/@Body e decorators Swagger)
    - apps/backend/src/modules/integrations/athos/dto/update-produto-composto.dto.ts (DTO finalizado na Task 1)
  </read_first>
  <action>
    No athos-produto-composto.controller.ts, importar Patch, Delete e Body de @nestjs/common, ApiBody de @nestjs/swagger e UpdateProdutoCompostoDto de ./dto/update-produto-composto.dto. Adicionar handler @Patch(":idprodutomaster/composicao/:idprodutodetail") chamado atualizarQuantidade, recebendo @Param("idprodutomaster", ParseIntPipe), @Param("idprodutodetail", ParseIntPipe) e @Body() dto: UpdateProdutoCompostoDto, delegando para this.athosProdutoCompostoService.atualizarQuantidade(master, detail, dto). Adicionar handler @Delete(":idprodutomaster/composicao/:idprodutodetail") chamado removerComponente, recebendo os dois @Param ParseIntPipe, delegando para this.athosProdutoCompostoService.removerComponente(master, detail). Decorar ambos com @ApiOperation, @ApiParam (para os dois params) e @ApiOkResponse; o PATCH tambem com @ApiBody({ type: UpdateProdutoCompostoDto }). Logar via this.logger.log em cada handler. Guard x-internal-api-key herdado do @ApiSecurity("InternalApiKey") de classe — sem guard inline.
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc -p tsconfig.build.json --noEmit</automated>
  </verify>
  <done>
    PATCH e DELETE em :idprodutomaster/composicao/:idprodutodetail registrados e tipados; projeto compila.
  </done>
  <acceptance_criteria>
    - Existe handler @Patch(":idprodutomaster/composicao/:idprodutodetail") com @Body UpdateProdutoCompostoDto.
    - Existe handler @Delete(":idprodutomaster/composicao/:idprodutodetail").
    - Ambos usam ParseIntPipe nos dois params e nenhum guard inline.
    - tsc compila sem erro.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes Jest de PATCH (atualizarQuantidade) e DELETE (removerComponente) com flag-off</name>
  <files>apps/backend/src/modules/integrations/athos/athos-produto-composto.service.test.ts</files>
  <read_first>
    - apps/backend/src/modules/integrations/athos/athos-produto-composto.service.test.ts (padrao de mock do pg e dos describes de 40-01)
    - apps/backend/src/modules/integrations/athos/athos-produto-composto.service.ts (ordem das queries em atualizarQuantidade e removerComponente para sequenciar os mocks)
  </read_first>
  <behavior>
    - atualizarQuantidade sucesso: SELECT do par retorna { idprodutocomposto }, UPDATE ok -> retorna { idprodutocomposto }.
    - atualizarQuantidade par inexistente: SELECT vazio -> NotFoundException.
    - atualizarQuantidade quantidade invalida: UPDATE lanca { code: '22003' } -> UnprocessableEntityException.
    - removerComponente ultimo componente: SELECT do par [linha], DELETE ok, count apos == 0 -> UPDATE usaprodutocomposto=false (assertar SQL contem usaprodutocomposto e o valor false), COMMIT, retorna { removed: true }.
    - removerComponente nao-ultimo: count apos > 0 -> NENHUM UPDATE de flag (assertar que nenhum SQL chamado contem usaprodutocomposto).
    - removerComponente par inexistente: SELECT vazio -> NotFoundException; assertar ROLLBACK e release.
    - DELETE e fisico: assertar que algum SQL chamado contem "DELETE FROM" e nenhum faz soft-delete (sem UPDATE ... SET status... na tabela produto_composto).
    - client.release chamado em todos os caminhos.
  </behavior>
  <action>
    Estender athos-produto-composto.service.test.ts com describe("atualizarQuantidade") e describe("removerComponente"), seguindo o padrao de mock do arquivo. Para atualizarQuantidade: sequenciar client.query com SELECT do par e UPDATE; cobrir sucesso, par inexistente (SELECT vazio -> NotFoundException) e quantidade invalida (UPDATE mockRejectedValueOnce { code: '22003' } -> UnprocessableEntityException). Para removerComponente: sequenciar BEGIN, SELECT do par, DELETE, SELECT count, [UPDATE flag], COMMIT. Caso ultimo componente: count retorna { total: 0 } e assertar que uma das chamadas de query contem "usaprodutocomposto" com valor false; caso nao-ultimo: count retorna { total: 3 } e assertar que NENHUM SQL chamado contem "usaprodutocomposto". Caso par inexistente: SELECT do par retorna { rows: [] } -> esperar NotFoundException e assertar que houve chamada 'ROLLBACK' e client.release. Assertar tambem que ha "DELETE FROM" no caminho de sucesso e que nenhum UPDATE de soft-delete e emitido contra produto_composto.
  </action>
  <verify>
    <automated>cd apps/backend && npx jest athos-produto-composto.service.test --runInBand</automated>
  </verify>
  <done>
    Suite athos-produto-composto.service.test verde, cobrindo PATCH (sucesso, 404, 422) e DELETE (ultimo com flag-off, nao-ultimo sem flag-off, 404), sem regressao nos describes de GET e POST.
  </done>
  <acceptance_criteria>
    - Os testes de GET (listarPorMaster) e POST (adicionarComponente) continuam passando.
    - Ha teste de PATCH 404 (par inexistente) e PATCH 422 (22003).
    - Ha teste de DELETE que asserta flag-off (usaprodutocomposto=false) apenas quando count apos == 0.
    - Ha teste de DELETE nao-ultimo que asserta ausencia de UPDATE de flag.
    - Ha teste de DELETE 404 com ROLLBACK e release.
    - jest sai com codigo 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP -> ProdutoCompostoController | input nao confiavel (idprodutomaster, idprodutodetail, quantidade) cruza aqui |
| AthosProdutoCompostoService -> Postgres Athos | SQL parametrizado; privilegio de UPDATE/DELETE controlado por GRANT externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-06 | Tampering | atualizarQuantidade / removerComponente (UPDATE/DELETE) | high | mitigate | Todos os valores via parametros $N; nomes de coluna sao literais fixos; WHERE sempre parametrizado. Nunca interpolar input em SQL. |
| T-40-07 | Spoofing | endpoints PATCH/DELETE | high | mitigate | @ApiSecurity("InternalApiKey") de classe aplica o guard x-internal-api-key, igual ao GET/POST. Sem guard inline. |
| T-40-08 | Tampering | flag usaprodutocomposto desincronizado no ultimo remove | high | mitigate | DELETE + UPDATE do flag dentro de BEGIN/COMMIT; ROLLBACK em qualquer erro; flag-off so quando contagem apos o delete == 0. |
| T-40-09 | Information Disclosure | least-privilege / pg 42501 (GRANT ausente) | medium | mitigate | 42501 mapeado para 500 acionavel pelo helper mapPgWriteError (de 40-01), sem vazar credenciais. |
| T-40-10 | Denial of Service | DELETE fisico de componente ainda em uso | low | accept | DELETE fisico e a regra correta para tabela de composicao (produto_composto distinta de produto); Athos resolve explosao no PDV. Sem caso pratico de necessidade de retencao. |
| T-40-SC | Tampering | dependencias npm | low | accept | Zero novas dependencias nesta fase (RESEARCH / Phase 39: do-not-add). Nenhum install a auditar. |

block_on=high — todas as ameacas high estao mitigadas (T-40-06, T-40-07, T-40-08).
</threat_model>

<verification>
- tsc compila o backend sem erros de tipo.
- Suite athos-produto-composto.service.test verde, sem regressao em GET/POST.
- DELETE fisico (DELETE FROM), nunca soft-delete em produto_composto.
- flag-off transacional coberto por teste (ultimo componente liga false; nao-ultimo nao altera).
- PATCH 404 e PATCH 422 (22003) cobertos por teste.
- DTOs update finalizado sem comentario SCAFFOLD.
</verification>

<success_criteria>
- COMP-03: PATCH atualiza quantidade; 404 quando o par master/detail nao existe; quantidade invalida -> 422.
- COMP-04: DELETE fisico remove componente; 404 quando o par nao existe.
- COMP-05 (parte flag-off): usaprodutocomposto do master vira false ao remover o ultimo componente, na mesma transacao.
</success_criteria>

<output>
Create `.planning/phases/40-write-crud-post-patch-delete-flag-usaprodutocomposto/40-02-SUMMARY.md` when done
</output>
