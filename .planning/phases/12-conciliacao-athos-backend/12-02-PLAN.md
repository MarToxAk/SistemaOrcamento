---
phase: "12"
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - apps/backend/src/modules/integrations/athos/athos.service.test.ts
autonomous: true
requirements:
  - ATHP-01
  - ATHP-02
  - ATHP-03

must_haves:
  truths:
    - "Suite de testes cobre cenario pago (paid=true) e nao pago (paid=false)"
    - "Teste valida prioridade de busca por vendaId quando fornecido"
    - "Teste valida fallback sem excecao quando tabela nao encontrada"
    - "ATHP-03 validado: quando paid=true, caller ja atualiza status (verificado via checkPaymentStatus)"
  artifacts:
    - path: "apps/backend/src/modules/integrations/athos/athos.service.test.ts"
      provides: "Cobertura unitaria de verificarPagamentoPorOrcamento"
      contains: "paid: true"
    - path: "apps/backend/src/modules/integrations/athos/athos.service.test.ts"
      provides: "Teste de degradacao graciosa"
      contains: "paid: false"
  key_links:
    - from: "apps/backend/src/modules/integrations/athos/athos.service.test.ts"
      to: "apps/backend/src/modules/integrations/athos/athos.service.ts"
      via: "testes unitarios do metodo real"
      pattern: "verificarPagamentoPorOrcamento"
---

<objective>
Criar testes unitários para `verificarPagamentoPorOrcamento` cobrindo os cenários principais e validando que ATHP-03 funciona corretamente via caller existente.

Purpose: Atender ATHP-01, ATHP-02, ATHP-03 com evidências automatizadas.
Output: Arquivo de testes `athos.service.test.ts` com suite passando no backend.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-conciliacao-athos-backend/12-CONTEXT.md
@.planning/phases/12-conciliacao-athos-backend/12-RESEARCH.md
@apps/backend/src/modules/integrations/athos/athos.service.ts
@apps/backend/src/modules/integrations/efi/efi.webhook.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar testes unitários para verificarPagamentoPorOrcamento</name>
  <read_first>apps/backend/src/modules/integrations/athos/athos.service.ts, apps/backend/src/modules/integrations/efi/efi.webhook.test.ts</read_first>
  <files>apps/backend/src/modules/integrations/athos/athos.service.test.ts</files>
  <action>
Criar o arquivo `apps/backend/src/modules/integrations/athos/athos.service.test.ts` com testes unitários para `verificarPagamentoPorOrcamento`.

Usar Jest com mock do módulo `pg`:
- Mock de `Pool` que retorna um `PoolClient` fake com método `query` e `release`
- O mock de `query` deve ser configurável por cenário para simular respostas diferentes da tabela `venda`

Cobrir os seguintes cenários:
1. **Pago por orcamentoId**: query para `information_schema.columns` retorna colunas com `"idorcamento"` e `"situacaovenda"`; query para tabela `venda` retorna row com `situacaovenda: "PAGO"` e `idvenda: 42` → resultado `{paid: true, idVenda: 42}`
2. **Nao pago**: mesma estrutura mas `situacaovenda: "ABERTO"` → resultado `{paid: false}`
3. **Busca por vendaId fornecido**: quando `vendaId=99` passado, deve buscar por `idvenda = 99` na tabela venda → `{paid: true, idVenda: 99}`
4. **Tabela nao encontrada (degradacao graciosa)**: `information_schema.columns` retorna rows vazios → resultado `{paid: false}` sem lancamento de excecao
5. **Erro de conexao (degradacao graciosa)**: `pool.connect()` rejeita com Error → resultado `{paid: false}` sem re-throw

Usar o mesmo padrão do arquivo `efi.webhook.test.ts` existente (jest.mock, beforeEach, describe/it).
Para mockar o Pool do pg, usar `jest.mock("pg", ...)` com factory que retorna `{ Pool: jest.fn().mockImplementation(...) }`.
  </action>
  <acceptance_criteria>
    - Arquivo `athos.service.test.ts` criado
    - Contém teste que resulta em `paid: true`
    - Contém teste que resulta em `paid: false` sem excecao (degradacao graciosa)
    - Contém teste de busca por vendaId
    - Suite executa sem erros: `npm --workspace @bomcusto/backend run test -- athos.service.test.ts --runInBand`
  </acceptance_criteria>
  <verify>
    <automated>
      npm --workspace @bomcusto/backend run test -- athos.service.test.ts --runInBand
    </automated>
  </verify>
</task>

</tasks>
