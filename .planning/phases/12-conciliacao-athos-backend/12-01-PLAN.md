---
phase: "12"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/athos/athos.service.ts
autonomous: true
requirements:
  - ATHP-01
  - ATHP-02

must_haves:
  truths:
    - "verificarPagamentoPorOrcamento faz consulta real ao banco Athos e nao retorna sempre paid=false"
    - "Busca por vendaId tem prioridade quando fornecido (ATHP-02)"
    - "Fallback para busca por orcamentoId quando vendaId nao disponivel"
    - "Erro de conexao ou tabela nao encontrada retorna {paid:false} sem re-throw"
  artifacts:
    - path: "apps/backend/src/modules/integrations/athos/athos.service.ts"
      provides: "Implementacao real de verificarPagamentoPorOrcamento"
      contains: "information_schema.columns"
    - path: "apps/backend/src/modules/integrations/athos/athos.service.ts"
      provides: "Sem stub - metodo executa query via pool"
      contains: "situacaovenda"
  key_links:
    - from: "apps/backend/src/modules/integrations/athos/athos.service.ts"
      to: "apps/backend/src/modules/quotes/quotes.service.ts"
      via: "checkPaymentStatus chama verificarPagamentoPorOrcamento"
      pattern: "verificarPagamentoPorOrcamento"
---

<objective>
Substituir o stub `verificarPagamentoPorOrcamento` por implementação real com consulta ao banco Athos usando o padrão de dynamic column discovery já estabelecido no serviço.

Purpose: Atender ATHP-01 e ATHP-02 — consulta real que considera `orcamentoId` e `vendaId`.
Output: AthosService com método implementado que retorna `{paid, idVenda, valor}` com base em dados reais do Athos.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-conciliacao-athos-backend/12-CONTEXT.md
@.planning/phases/12-conciliacao-athos-backend/12-RESEARCH.md
@apps/backend/src/modules/integrations/athos/athos.service.ts
@apps/backend/src/modules/quotes/quotes.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar verificarPagamentoPorOrcamento com consulta real ao Athos</name>
  <read_first>apps/backend/src/modules/integrations/athos/athos.service.ts</read_first>
  <files>apps/backend/src/modules/integrations/athos/athos.service.ts</files>
  <action>
Substituir o stub `verificarPagamentoPorOrcamento` pela implementação real seguindo o padrão já estabelecido no arquivo (dynamic column discovery, Pool, PoolClient, pickNumber, pickString, findExistingTable, isSafeIdentifier).

A implementação deve:
1. Usar `this.getPool()` para obter a conexão
2. **Se `vendaId` fornecido**: buscar direto na tabela venda por vendaId (candidatos: `"idvenda"`, `"vendaid"`, `"id_venda"`, `"id"`) e verificar coluna de situação
3. **Se `vendaId` ausente ou não encontrou**: buscar na tabela venda pelos candidatos `["venda", "vendas", "orcamento_venda", "movimento_venda"]` usando coluna de ligação ao orçamento (candidatos: `"idorcamento"`, `"orcamentoid"`, `"id_orcamento"`)
4. Extrair `idVenda` a partir dos candidatos `"idvenda"`, `"vendaid"`, `"id_venda"`, `"id"` da row da venda
5. Verificar pagamento pelo valor de colunas de situação (candidatos: `"situacaovenda"`, `"situacao"`, `"statuspagamento"`, `"status"`, `"statussituacao"`) — considerar pago se o valor (uppercased) contiver qualquer de: `"PAGO"`, `"QUITADO"`, `"RECEBIDO"`, `"LIQUIDADO"`, `"FINALIZADO"`
6. Tentar buscar valor pago em tabela financeira (candidatos: `"financeiro"`, `"conta_receber"`, `"contasreceber"`, `"parcela_receber"`, `"receber"`) por `idVenda` usando colunas `"valorpago"`, `"valor_pago"`, `"valorquitado"`, `"totalrecebido"` — se não encontrar, usar o campo de valor da própria venda com candidatos `"valortotal"`, `"valor"`, `"totalvenda"`
7. Em qualquer erro (tabela não encontrada, coluna não encontrada, erro de query): logar com `this.logger.warn(...)` e retornar `{paid: false, idVenda: vendaId ?? null, valor: 0}` sem re-throw
8. Retornar `{paid: boolean, idVenda: number|string|null, valor: number}`
9. Sempre liberar a conexão no `finally` com `client.release()`

Manter a assinatura existente: `async verificarPagamentoPorOrcamento(orcamentoId: string, vendaId?: number | string | null)`
  </action>
  <acceptance_criteria>
    - `athos.service.ts` contém `information_schema.columns` dentro de `verificarPagamentoPorOrcamento`
    - `athos.service.ts` contém verificação de situação com `PAGO` ou `QUITADO`
    - `athos.service.ts` contém `client.release()` no finally de `verificarPagamentoPorOrcamento`
    - O método não contém apenas `return { paid: false, idVenda: vendaId ?? null, valor: 0 }` (stub)
  </acceptance_criteria>
  <verify>
    <automated>
      Select-String -Path "apps\backend\src\modules\integrations\athos\athos.service.ts" -Pattern "information_schema|PAGO|QUITADO|client.release"
    </automated>
  </verify>
</task>

</tasks>
