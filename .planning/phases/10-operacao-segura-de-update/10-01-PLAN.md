---
phase: "10"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/UPDATE_RUNBOOK.md
  - README.md
autonomous: true
requirements:
  - MIG-04
  - OPS-01

must_haves:
  truths:
    - "Operador tem roteiro unico e ordenado para update da stack em VPS"
    - "Fluxo inclui criterio explicito de sucesso e falha em cada etapa"
    - "Runbook define acao imediata para restart loop do backend"
  artifacts:
    - path: "deploy/UPDATE_RUNBOOK.md"
      provides: "Procedimento operacional de update"
      contains: "Pre-check"
    - path: "deploy/UPDATE_RUNBOOK.md"
      provides: "Procedimento de contingencia"
      contains: "Rollback tatico"
    - path: "README.md"
      provides: "Link para runbook oficial"
      contains: "UPDATE_RUNBOOK.md"
  key_links:
    - from: "README.md"
      to: "deploy/UPDATE_RUNBOOK.md"
      via: "secao de deploy"
      pattern: "UPDATE_RUNBOOK\.md"
    - from: "deploy/UPDATE_RUNBOOK.md"
      to: "deploy/docker-compose.vps.yml"
      via: "comandos de update"
      pattern: "docker-compose\.vps\.yml"
---

<objective>
Definir e versionar um runbook operacional de update para VPS que reduza risco de falha em producao e padronize resposta a restart loop.

Purpose: Atender MIG-04 e OPS-01 com um fluxo operacional reproduzivel, auditavel e facil de executar.
Output: `deploy/UPDATE_RUNBOOK.md` completo e README referenciando o documento.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-operacao-segura-de-update/10-CONTEXT.md
@.planning/phases/10-operacao-segura-de-update/10-RESEARCH.md
@deploy/docker-compose.vps.yml
@deploy/stack.env.example
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar runbook de update com pre-check, execucao e rollback tatico</name>
  <read_first>deploy/docker-compose.vps.yml, deploy/stack.env.example, README.md</read_first>
  <files>deploy/UPDATE_RUNBOOK.md</files>
  <action>Criar `deploy/UPDATE_RUNBOOK.md` com secoes obrigatorias: `Pre-check`, `Update passo a passo`, `Validacao imediata`, `Rollback tatico`, `Troubleshooting rapido`. Em `Update passo a passo`, incluir comandos concretos com compose da VPS: `docker compose -f deploy/docker-compose.vps.yml pull`, `docker compose -f deploy/docker-compose.vps.yml up -d`, `docker compose -f deploy/docker-compose.vps.yml ps`, `docker compose -f deploy/docker-compose.vps.yml logs backend --tail=120`. Em `Rollback tatico`, documentar retorno para imagem anterior usando tags conhecidas e re-subida da stack.</action>
  <acceptance_criteria>
    - `deploy/UPDATE_RUNBOOK.md` existe
    - arquivo contem `## Pre-check`
    - arquivo contem `## Update passo a passo`
    - arquivo contem `## Rollback tatico`
    - arquivo contem `docker compose -f deploy/docker-compose.vps.yml up -d`
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path deploy/UPDATE_RUNBOOK.md -Pattern '## Pre-check|## Update passo a passo|## Rollback tatico|docker compose -f deploy/docker-compose.vps.yml up -d'</automated>
  </verify>
  <done>Runbook operacional versionado com fluxo principal e contingencia para falhas de startup.</done>
</task>

<task type="auto">
  <name>Task 2: Referenciar runbook no README como fonte oficial de operacao</name>
  <read_first>README.md, deploy/UPDATE_RUNBOOK.md</read_first>
  <files>README.md</files>
  <action>Atualizar a secao de deploy do `README.md` para incluir link explicito para `deploy/UPDATE_RUNBOOK.md` como procedimento oficial de update/rollback. Manter o restante do README sem reformatacao ampla.</action>
  <acceptance_criteria>
    - `README.md` contem `deploy/UPDATE_RUNBOOK.md`
    - referencia aparece no contexto de deploy/atualizacao
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path README.md -Pattern 'deploy/UPDATE_RUNBOOK.md|runbook'</automated>
  </verify>
  <done>Leitor do README encontra rapidamente o procedimento padrao de update e contingencia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador -> host VPS | Execucao manual de comandos de deploy com impacto direto na disponibilidade |
| Host VPS -> stack Docker | Estado de containers e env vars define sucesso de startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Denial | procedimento de update | mitigate | Runbook com pre-check e validacao imediata reduz downtime por erro operacional |
| T-10-02 | Tampering | variaveis de ambiente | mitigate | Runbook exige validacao do arquivo de env antes de aplicar update |
| T-10-03 | Repudiation | execucao manual | mitigate | Passos e comandos padronizados permitem auditoria e repetibilidade |
</threat_model>

<verification>
- Runbook existe com seções e comandos essenciais
- README referencia o runbook na área de deploy
- Rollback tático está documentado com comandos concretos
</verification>

<success_criteria>
- Procedimento operacional de update foi padronizado
- Fluxo cobre cenário de restart loop com ação imediata
- Time consegue executar update com baixa ambiguidade
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/10-operacao-segura-de-update/10-01-SUMMARY.md`
</output>
