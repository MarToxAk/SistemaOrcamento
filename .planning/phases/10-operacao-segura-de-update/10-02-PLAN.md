---
phase: "10"
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - scripts/verify-deploy-health.ps1
  - deploy/UPDATE_RUNBOOK.md
  - .planning/STATE.md
autonomous: true
requirements:
  - OPS-02
  - MIG-04

must_haves:
  truths:
    - "Pos-deploy existe checklist objetivo para confirmar saude da aplicacao"
    - "Checklist valida startup sem loop e estado de migration/schema"
    - "Estado do projeto registra o processo operacional adotado"
  artifacts:
    - path: "scripts/verify-deploy-health.ps1"
      provides: "Checklist automatizavel de verificacao pos-deploy"
      contains: "backend healthy"
    - path: "deploy/UPDATE_RUNBOOK.md"
      provides: "Secao de checklist pós-deploy"
      contains: "Checklist pos-deploy"
    - path: ".planning/STATE.md"
      provides: "Contexto operacional atualizado"
      contains: "fase 10"
  key_links:
    - from: "deploy/UPDATE_RUNBOOK.md"
      to: "scripts/verify-deploy-health.ps1"
      via: "instrucoes de verificacao"
      pattern: "verify-deploy-health\.ps1"
    - from: "scripts/verify-deploy-health.ps1"
      to: "deploy/docker-compose.vps.yml"
      via: "comandos compose/health"
      pattern: "docker-compose\.vps\.yml"
---

<objective>
Implementar checklist de verificacao pos-deploy com comandos repetiveis para confirmar saude da API e estabilidade de startup apos update.

Purpose: Atender OPS-02 e reforcar MIG-04 com verificacao padronizada apos cada release.
Output: Script de verificacao pos-deploy + runbook atualizado + estado operacional registrado.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-operacao-segura-de-update/10-CONTEXT.md
@.planning/phases/10-operacao-segura-de-update/10-RESEARCH.md
@deploy/UPDATE_RUNBOOK.md
@deploy/docker-compose.vps.yml
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar script de verificacao pos-deploy com checks de compose, logs e health</name>
  <read_first>deploy/docker-compose.vps.yml, deploy/UPDATE_RUNBOOK.md</read_first>
  <files>scripts/verify-deploy-health.ps1</files>
  <action>Criar `scripts/verify-deploy-health.ps1` com verificacoes em sequencia: (1) `docker compose -f deploy/docker-compose.vps.yml ps`; (2) confirmar backend em estado running; (3) coletar ultimas linhas de log do backend e falhar se houver `DB_READINESS_FAILED` ou `MIGRATION_DEPLOY_FAILED`; (4) realizar chamada de health endpoint (`/health` ou endpoint equivalente configuravel via parametro) e retornar sucesso apenas com HTTP 200. Incluir retorno de codigo de saida nao-zero quando qualquer check falhar.</action>
  <acceptance_criteria>
    - `scripts/verify-deploy-health.ps1` existe
    - arquivo contem `docker compose -f deploy/docker-compose.vps.yml ps`
    - arquivo contem busca por `DB_READINESS_FAILED`
    - arquivo contem validacao de status HTTP 200
    - script usa `exit 1` em falha
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path scripts/verify-deploy-health.ps1 -Pattern 'docker compose -f deploy/docker-compose.vps.yml ps|DB_READINESS_FAILED|MIGRATION_DEPLOY_FAILED|200|exit 1'</automated>
  </verify>
  <done>Checklist automatizavel de health pos-deploy criado com criterio binario de sucesso/falha.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar checklist ao runbook e atualizar contexto do projeto</name>
  <read_first>deploy/UPDATE_RUNBOOK.md, scripts/verify-deploy-health.ps1, .planning/STATE.md</read_first>
  <files>deploy/UPDATE_RUNBOOK.md, .planning/STATE.md</files>
  <action>Atualizar `deploy/UPDATE_RUNBOOK.md` com secao `Checklist pos-deploy` apontando para execucao de `scripts/verify-deploy-health.ps1` e com saidas esperadas (containers running, sem erros criticos de startup, endpoint health 200). Atualizar `.planning/STATE.md` em `Notes` com nota de que fase 10 padronizou runbook + checklist pos-deploy.</action>
  <acceptance_criteria>
    - `deploy/UPDATE_RUNBOOK.md` contem `## Checklist pos-deploy`
    - `deploy/UPDATE_RUNBOOK.md` contem `verify-deploy-health.ps1`
    - `.planning/STATE.md` contem referencia a fase 10 e checklist pos-deploy
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path deploy/UPDATE_RUNBOOK.md,.planning/STATE.md -Pattern 'Checklist pos-deploy|verify-deploy-health.ps1|fase 10|checklist pos-deploy'</automated>
  </verify>
  <done>Runbook e estado do projeto refletem verificacao operacional padronizada de pos-deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script local -> host com Docker | Execucao de verificacoes depende de permissao e binarios disponiveis |
| Logs/container state -> decisao de go/no-go | Operacao toma decisao de continuidade com base em sinais de health |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Denial | script de verificacao | mitigate | Falha binaria com `exit 1` evita seguir deploy com estado degradado |
| T-10-05 | Repudiation | checklist manual | mitigate | Script e runbook versionados geram trilha reproduzivel de verificacao |
| T-10-06 | Tampering | leitura de logs | accept | Verificacao usa sinais de erro explicitos e endpoint de health para reduzir falso positivo |
</threat_model>

<verification>
- Script de verificacao pos-deploy criado com checks de compose/logs/health
- Runbook incorpora checklist com comando e resultados esperados
- STATE registra padronizacao operacional da fase 10
</verification>

<success_criteria>
- Existe procedimento objetivo de validacao após update
- Time identifica rapidamente loop de startup ou falha de migration
- Decisao go/no-go do deploy fica baseada em sinais mensuraveis
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/10-operacao-segura-de-update/10-02-SUMMARY.md`
</output>
