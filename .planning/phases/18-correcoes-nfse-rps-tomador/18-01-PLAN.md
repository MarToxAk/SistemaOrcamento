---
phase: 18-correcoes-nfse-rps-tomador
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/nfse/nfse.service.ts
  - apps/backend/src/modules/integrations/athos/athos.service.ts
autonomous: true
requirements: [RPS-01, RPS-02, TOM-01, TOM-02, TOM-03, REG-01, REG-02]

must_haves:
  truths:
    - "RPS numero no XML da NFS-e e sempre (ultimo emitido + 1)"
    - "Quando externalQuoteId esta preenchido, nome/CPF ou CNPJ do cliente aparecem no XML"
    - "Logs mostram: lookupId usado, identifierColumn detectado, idcliente retornado pelo Athos"
    - "NotFoundException de buscarOrcamentoPorNumero e capturado separadamente com mensagem clara"
    - "Emissao sem externalQuoteId (orcamento nao associado ao Athos) continua funcionando"
    - "Emissao com desconto nao regride (NFSC-01..05)"
  artifacts:
    - path: "apps/backend/src/modules/integrations/nfse/nfse.service.ts"
      provides: "NFS-e service com bug RPS e tomador corrigidos"
      contains: "proximoRps + 1"
    - path: "apps/backend/src/modules/integrations/athos/athos.service.ts"
      provides: "Log de identifierColumn detectado em buscarOrcamentoPorNumero"
      contains: "identifierColumn"
  key_links:
    - from: "nfse.service.ts emitir()"
      to: "getInfoNfse()"
      via: "rpsNumero = infoNfse.proximoRps + 1"
      pattern: "proximoRps \\+ 1"
    - from: "nfse.service.ts buscarTomador()"
      to: "athosService.buscarOrcamentoPorNumero()"
      via: "lookupId derivado de externalQuoteId"
      pattern: "buscarOrcamentoPorNumero"
---

<objective>
Corrigir dois bugs na emissão de NFS-e via iiBrasil:

1. **RPS off-by-one**: `getInfoNfse()` retorna o último número RPS emitido; o código usa
   esse valor diretamente em vez de incrementar. Resultado: nota emitida com RPS duplicado.
   Fix: `rpsNumero = infoNfse.proximoRps + 1`

2. **Tomador ausente quando associado ao Athos**: `buscarTomador()` falha silenciosamente
   quando o orcamento está associado ao Athos (`externalQuoteId` preenchido). Root causes:
   - `NotFoundException` de `buscarOrcamentoPorNumero` é capturada pelo catch genérico sem log útil
   - `idcliente = 0` passa por `|| undefined` → falsy → `if (clienteId)` skipa a busca
   - Falta de diagnóstico sobre `identifierColumn` e `idcliente` retornados
   Fix: logs diagnósticos + catch separado de NotFoundException + check `> 0` em idcliente

Purpose: Ambos os bugs causam NFS-e inválida/incompleta que precisa de reemissão manual.
Output: `nfse.service.ts` e `athos.service.ts` com correções e logs de diagnóstico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Contratos relevantes extraídos do codebase para o executor. -->

De apps/backend/src/modules/integrations/nfse/nfse.service.ts:
```typescript
// Imports atuais (linha 1) — adicionar NotFoundException:
import { BadRequestException, Injectable, Logger } from "@nestjs/common";
// → deve virar:
import { BadRequestException, Injectable, Logger, NotFoundException } from "@nestjs/common";

// getInfoNfse() retorna:
interface InfoNfse { proximoRps: number; serieRps: string; }
// Retorna o ÚLTIMO RPS emitido (não o próximo). Fix: usar proximoRps + 1.

// buscarTomador() — estado atual com bugs:
private async buscarTomador(quote: any): Promise<{
  cnpj: string | null; cpf: string | null;
  nome: string | null; endereco: TomadorEndereco | null;
}> {
  try {
    const lookupId  = String(quote.externalQuoteId ?? quote.internalNumber ?? "");
    const athosData = await this.athosService.buscarOrcamentoPorNumero(lookupId);
    const clienteId = (athosData as any)?.mapped?.idcliente;
    if (clienteId) {                  // BUG: 0 é falsy → skipa busca
      const info = await this.athosService.buscarClientePorId(clienteId);
      ...
    }
  } catch (err) {
    this.logger.warn(`Falha ao buscar tomador no Athos: ...`); // BUG: captura NotFoundException sem detalhe
  }
}

// Bloco RPS atual (dentro de emitir()) — linhas ~456-464:
let rpsNumero = Number(quote.internalNumber);
let rpsSerie  = this.SERIE_RPS;
const infoNfse = await this.getInfoNfse();
if (infoNfse) {
  rpsNumero = infoNfse.proximoRps;        // BUG: deve ser + 1
  rpsSerie  = infoNfse.serieRps || this.SERIE_RPS;
  this.logger.log(`ProximoRPS=${rpsNumero} SerieRPS=${rpsSerie}`);
} else {
  this.logger.warn(`API Auxiliar indisponível, usando internalNumber=${rpsNumero} como RPS`);
}
```

De apps/backend/src/modules/integrations/athos/athos.service.ts:
```typescript
// pickNumber — fallback = 0 (não null):
function pickNumber(row: Row, keys: string[], fallback = 0): number
// → pickNumber retorna 0 quando campo não existe ou é inválido.
// → idcliente = 0 em mapeamento = "sem cliente" (não é ID válido).
// → No mapped: idcliente: pickNumber(...) || undefined
//   0 || undefined = undefined → correto semanticamente (0 = sem cliente)
//   Porém sem log → falha silenciosa.

// buscarOrcamentoPorNumero() — detecta identifierColumn dinamicamente:
//   candidates: ["numero", "idorcamento", "codorcamento", "orcamento", "codigo"]
//   Lança NotFoundException se rows.length === 0
//   Logger já existe: this.logger (Logger do NestJS)
//   Adicionar log após detectar identifierColumn:
//   this.logger.log(`buscarOrcamentoPorNumero: numero="${numero}" identifierColumn="${identifierColumn}"`)

// NotFoundException já importado em athos.service.ts linha 1:
import { Injectable, InternalServerErrorException, Logger, NotFoundException } from "@nestjs/common";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir RPS off-by-one em nfse.service.ts</name>
  <files>apps/backend/src/modules/integrations/nfse/nfse.service.ts</files>
  <action>
No bloco de resolução do RPS dentro de `emitir()` (buscar pelo padrão `ProximoRPS=`), fazer dois ajustes:

**Ajuste A — incrementar rpsNumero:**
Substituir:
```typescript
  rpsNumero = infoNfse.proximoRps;
```
Por:
```typescript
  rpsNumero = infoNfse.proximoRps + 1;
```

**Ajuste B — atualizar a mensagem de log** para refletir o valor correto e o raciocínio:
Substituir:
```typescript
  this.logger.log(`ProximoRPS=${rpsNumero} SerieRPS=${rpsSerie}`);
```
Por:
```typescript
  this.logger.log(`[RPS] API retornou ultimoRPS=${infoNfse.proximoRps} → emitindo rpsNumero=${rpsNumero} serie=${rpsSerie}`);
```

Nenhuma outra mudança neste arquivo nesta tarefa. O bloco `else` (fallback para internalNumber) NÃO deve ser alterado — comportamento de fallback continua correto (RPS-02).
  </action>
  <verify>
    <automated>cd SistemaOrcamento/apps/backend ; grep -n "proximoRps + 1" src/modules/integrations/nfse/nfse.service.ts</automated>
  </verify>
  <done>
    - `grep "proximoRps + 1"` retorna pelo menos 1 resultado em nfse.service.ts
    - Log message contém "ultimoRPS" e "rpsNumero"
    - Bloco else com `internalNumber` não foi alterado
  </done>
</task>

<task type="auto">
  <name>Task 2: Corrigir buscarTomador — logs diagnósticos + NotFoundException separado</name>
  <files>
    apps/backend/src/modules/integrations/nfse/nfse.service.ts
    apps/backend/src/modules/integrations/athos/athos.service.ts
  </files>
  <action>
**A. nfse.service.ts — adicionar NotFoundException ao import:**
Substituir linha 1:
```typescript
import { BadRequestException, Injectable, Logger } from "@nestjs/common";
```
Por:
```typescript
import { BadRequestException, Injectable, Logger, NotFoundException } from "@nestjs/common";
```

**B. nfse.service.ts — reescrever o método `buscarTomador()`:**
Localizar o método `private async buscarTomador(quote: any)` e substituir o bloco `try/catch` interno por:

```typescript
  try {
    const lookupId = String(quote.externalQuoteId ?? quote.internalNumber ?? "");
    this.logger.log(
      `[Tomador] buscando: lookupId="${lookupId}" externalQuoteId=${quote.externalQuoteId} internalNumber=${quote.internalNumber}`,
    );

    let athosData: Awaited<ReturnType<typeof this.athosService.buscarOrcamentoPorNumero>> | null = null;
    try {
      athosData = await this.athosService.buscarOrcamentoPorNumero(lookupId);
    } catch (err) {
      if (err instanceof NotFoundException) {
        this.logger.warn(
          `[Tomador] orcamento "${lookupId}" nao encontrado no Athos (NotFoundException) — sem dados do tomador`,
        );
      } else {
        this.logger.warn(
          `[Tomador] erro ao buscar orcamento "${lookupId}" no Athos: ${err instanceof Error ? err.message : String(err)}`,
        );
      }
    }

    if (athosData) {
      const clienteId = (athosData as any)?.mapped?.idcliente;
      this.logger.log(`[Tomador] orcamento encontrado — idcliente=${clienteId}`);

      if (clienteId != null && clienteId > 0) {
        const info = await this.athosService.buscarClientePorId(clienteId);
        if (info) {
          nome     = info.name || quote.customer?.fullName || null;
          endereco = (info as any).endereco ?? null;
          if (info.type === "juridico" && info.documento?.length === 14) cnpj = info.documento;
          else if (info.type === "fisico"   && info.documento?.length === 11) cpf  = info.documento;
          this.logger.log(
            `[Tomador] cliente encontrado — tipo=${info.type} nome="${nome}" documento=${info.documento ?? "null"}`,
          );
        } else {
          this.logger.warn(`[Tomador] buscarClientePorId(${clienteId}) retornou null`);
        }
      } else {
        this.logger.warn(
          `[Tomador] idcliente=${clienteId} invalido ou ausente no mapeamento do orcamento "${lookupId}"`,
        );
      }
    }
  } catch (err) {
    this.logger.warn(
      `[Tomador] falha inesperada ao buscar tomador no Athos: ${err instanceof Error ? err.message : String(err)}`,
    );
  }
```

As variáveis `cnpj`, `cpf`, `nome`, `endereco` já foram declaradas antes deste bloco — não redeclará-las.

**C. athos.service.ts — adicionar log de identifierColumn em `buscarOrcamentoPorNumero()`:**
Localizar o bloco onde `identifierColumn` é determinado (após o `find()`). Imediatamente após o throw de `InternalServerErrorException` (quando não encontrado), adicionar um log de sucesso. Localizar a linha:
```typescript
      if (!identifierColumn) {
        throw new InternalServerErrorException(
          "Tabela orcamento sem coluna identificadora conhecida (numero/idorcamento/codorcamento).",
        );
      }
```
E logo após o fechamento do `if`, adicionar:
```typescript
      this.logger.log(`[Athos] buscarOrcamentoPorNumero: numero="${numero}" identifierColumn="${identifierColumn}"`);
```
  </action>
  <verify>
    <automated>
cd SistemaOrcamento/apps/backend
grep -n "NotFoundException" src/modules/integrations/nfse/nfse.service.ts
grep -n "Tomador.*lookupId\|lookupId.*Tomador" src/modules/integrations/nfse/nfse.service.ts
grep -n "identifierColumn" src/modules/integrations/athos/athos.service.ts | grep "logger"
    </automated>
  </verify>
  <done>
    - `NotFoundException` importado em nfse.service.ts (grep retorna linha 1)
    - buscarTomador loga `lookupId` antes de chamar Athos
    - NotFoundException capturado separadamente com mensagem "[Tomador] orcamento ... nao encontrado"
    - `clienteId > 0` na checagem (não mais só truthy)
    - athos.service.ts loga `identifierColumn` detectado
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nfse.service.ts → API Auxiliar iiBrasil | Resposta da API não é validada estruturalmente — `proximoRps` pode ser zero ou negativo |
| nfse.service.ts → Athos PostgreSQL | Dado de `idcliente` vem de DB externo read-only sem esquema garantido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | getInfoNfse() proximoRps | accept | API Auxiliar é sistema interno da prefeitura; resposta validada com `> 0` check existente em getInfoNfse() |
| T-18-02 | Information Disclosure | Logs de diagnóstico [Tomador] | accept | Logs internos de servidor NestJS, não expostos ao cliente; contêm apenas IDs e nomes |
| T-18-03 | Denial of Service | buscarOrcamentoPorNumero timeout | accept | Pool PostgreSQL já tem connectionTimeoutMillis=5000; NotFoundException já tratado |
</threat_model>

<verification>
Após execução, verificar manualmente (ou via logs):

1. **RPS fix**: No log ao emitir, procurar `[RPS] API retornou ultimoRPS=X → emitindo rpsNumero=X+1`
2. **Tomador fix**: No log ao emitir com orcamento associado, procurar:
   - `[Tomador] buscando: lookupId="..."` 
   - `[Tomador] orcamento encontrado — idcliente=N`
   - `[Tomador] cliente encontrado — tipo=... nome="..." documento=...`
3. **Regressão**: Emitir NFS-e sem externalQuoteId — deve completar sem erro, tomador pode ser null
4. **Build**: `cd apps/backend && npx tsc --noEmit` passa sem erros de tipo
</verification>

<success_criteria>
- `grep "proximoRps + 1" apps/backend/src/modules/integrations/nfse/nfse.service.ts` retorna match
- `grep "NotFoundException" apps/backend/src/modules/integrations/nfse/nfse.service.ts | head -3` mostra import + 2 usos no catch
- `grep "logger.*identifierColumn\|identifierColumn.*logger" apps/backend/src/modules/integrations/athos/athos.service.ts` retorna match
- `cd apps/backend && npx tsc --noEmit` exit 0
- Nenhuma alteração em schema.prisma, rotas HTTP ou DTOs (escopo cirúrgico)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/18-correcoes-nfse-rps-tomador/18-01-SUMMARY.md`
</output>
