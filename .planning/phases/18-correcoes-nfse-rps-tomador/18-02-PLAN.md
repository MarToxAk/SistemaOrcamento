---
phase: 18-correcoes-nfse-rps-tomador
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/nfse/nfse.service.ts
autonomous: true
requirements: [RPS-01, TOM-01, TOM-02, TOM-03]

must_haves:
  truths:
    - "rpsNumero = infoNfse.proximoRps + 1 em emitir()"
    - "buscarTomador faz busca por nome como fallback quando orcamento lookup falha ou idcliente=0"
    - "Log [Tomador] fallback aparece quando fallback e acionado"
    - "npx tsc --noEmit passa sem erros"
  artifacts:
    - path: "apps/backend/src/modules/integrations/nfse/nfse.service.ts"
      provides: "RPS off-by-one fix e fallback de tomador por nome"
      contains: "proximoRps + 1"
  key_links:
    - from: "nfse.service.ts emitir()"
      to: "infoNfse.proximoRps"
      via: "rpsNumero = infoNfse.proximoRps + 1"
      pattern: "proximoRps \\+ 1"
    - from: "nfse.service.ts buscarTomador()"
      to: "athosService.buscarClientes"
      via: "fallback por nome quando orcamento lookup falha"
      pattern: "\\[Tomador\\] fallback nome"
---

<objective>
Fecha dois gaps de UAT identificados na fase 18: (1) o numero RPS emitido e identico ao ultimo emitido pela prefeitura em vez de ser proximo+1, causando rejeicao; (2) o modal NFS-e abre com tomador em branco quando o orcamento nao e encontrado no Athos ou nao tem idcliente valido.

Purpose: Corrigir rejeicoes de NFS-e por RPS duplicado e falha silenciosa de populacao de tomador que bloqueia a emissao.
Output: nfse.service.ts com +1 no RPS e fallback de busca por nome em buscarTomador().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Assinatura real de buscarClientes em athos.service.ts (linha ~682) -->
```typescript
async buscarClientes(params: {
  nome?: string;
  documento?: string;
  idcliente?: number | string;
  page?: number;
  take?: number;
}): Promise<{
  total: number;
  page: number;
  take: number;
  items: Array<{
    idcliente: number;
    tipoPessoa: "fisico" | "juridico";
    nome: string;
    documento: string | null;
    endereco: {
      logradouro: string;
      numero: string;
      bairro: string;
      cep: string;
      codigoMunicipio: string;
      uf: string;
    } | null;
  }>;
}>
```

<!-- Bloco RPS atual em emitir() — linhas 491-500 -->
```typescript
let rpsNumero = Number(quote.internalNumber);
let rpsSerie  = this.SERIE_RPS;
const infoNfse = await this.getInfoNfse();
if (infoNfse) {
  rpsNumero = infoNfse.proximoRps;                                       // LINHA 495 — corrigir para + 1
  rpsSerie  = infoNfse.serieRps || this.SERIE_RPS;
  this.logger.log(`[RPS] ProximoRPS=${rpsNumero} SerieRPS=${rpsSerie}`); // LINHA 497 — atualizar mensagem
} else {
  this.logger.warn(`API Auxiliar indisponível, usando internalNumber=${rpsNumero} como RPS`);
}
```

<!-- buscarTomador() atual — linhas 345-410 -->
```typescript
private async buscarTomador(quote: any): Promise<{
  cnpj: string | null; cpf: string | null;
  nome: string | null; endereco: TomadorEndereco | null;
}> {
  let cnpj = null, cpf = null, nome = null, endereco = null;
  try {
    const lookupId = String(quote.externalQuoteId ?? quote.internalNumber ?? "");
    let athosData = null;
    try {
      athosData = await this.athosService.buscarOrcamentoPorNumero(lookupId);
    } catch (err) {
      if (err instanceof NotFoundException) {
        this.logger.warn(`[Tomador] orcamento "${lookupId}" nao encontrado ...`);
        // <-- AQUI: inserir fallback por nome quando NotFoundException
      } else { ... }
    }
    if (athosData) {
      const clienteId = (athosData as any)?.mapped?.idcliente;
      if (clienteId != null && clienteId > 0) {
        // buscarClientePorId ...
      } else {
        this.logger.warn(`[Tomador] idcliente=${clienteId} invalido ...`);
        // <-- AQUI: inserir fallback por nome quando idcliente=0
      }
    }
  } catch (err) { ... }
  if (!nome) nome = quote.customer?.fullName ?? null;
  return { cnpj, cpf, nome, endereco };
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir RPS off-by-one em emitir()</name>
  <read_first>
    Read apps/backend/src/modules/integrations/nfse/nfse.service.ts lines 488-502 to confirm the exact current text before editing.
  </read_first>
  <files>apps/backend/src/modules/integrations/nfse/nfse.service.ts</files>
  <action>
    Na linha ~495, substituir:

    ```typescript
    rpsNumero = infoNfse.proximoRps;
    rpsSerie  = infoNfse.serieRps || this.SERIE_RPS;
    this.logger.log(`[RPS] ProximoRPS=${rpsNumero} SerieRPS=${rpsSerie}`);
    ```

    Por:

    ```typescript
    rpsNumero = infoNfse.proximoRps + 1;
    rpsSerie  = infoNfse.serieRps || this.SERIE_RPS;
    this.logger.log(`[RPS] ultimoRPS=${infoNfse.proximoRps} → emitindo rpsNumero=${rpsNumero} serie=${rpsSerie}`);
    ```

    Racional: o campo `ProximoRPS` da API iiBrasil representa o ultimo RPS emitido, nao o proximo. Incrementar em 1 evita rejeicao por numero duplicado.

    Use Edit tool para substituir apenas o bloco if (infoNfse) { ... } — nao altere nada fora dessas 3 linhas.
  </action>
  <verify>
    <automated>grep -n "proximoRps + 1" apps/backend/src/modules/integrations/nfse/nfse.service.ts</automated>
    <automated>grep -n "ultimoRPS" apps/backend/src/modules/integrations/nfse/nfse.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "proximoRps + 1"` retorna pelo menos 1 linha com o novo calculo.
    - `grep "ultimoRPS"` retorna a nova mensagem de log.
    - Nenhuma outra linha foi alterada no bloco RPS.
  </acceptance_criteria>
  <done>rpsNumero recebe infoNfse.proximoRps + 1 e o log exibe "ultimoRPS" e "rpsNumero" distintos.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar fallback por nome em buscarTomador()</name>
  <read_first>
    1. Read apps/backend/src/modules/integrations/nfse/nfse.service.ts lines 345-410 (metodo completo buscarTomador) para confirmar estrutura exata.
    2. Read apps/backend/src/modules/integrations/athos/athos.service.ts lines 682-725 (assinatura buscarClientes) para confirmar nome do metodo e campos retornados.
  </read_first>
  <files>apps/backend/src/modules/integrations/nfse/nfse.service.ts</files>
  <action>
    Sao necessarias duas insercoes dentro de buscarTomador(). Ambas chamam o mesmo helper inline descrito abaixo.

    **Helper inline (nao extrair para metodo separado — inserir como bloco inline em cada local):**

    ```typescript
    const nomeBusca = /* definido em cada local conforme abaixo */.trim();
    if (nomeBusca.length >= 3) {
      try {
        const resultado = await this.athosService.buscarClientes({ nome: nomeBusca, take: 1 });
        if (resultado.items.length === 1) {
          const cli = resultado.items[0];
          nome     = cli.nome || nome;
          endereco = cli.endereco ?? endereco;
          if (cli.tipoPessoa === "juridico" && cli.documento?.replace(/\D/g,"").length === 14)
            cnpj = cli.documento!.replace(/\D/g,"");
          else if (cli.tipoPessoa === "fisico" && cli.documento?.replace(/\D/g,"").length === 11)
            cpf = cli.documento!.replace(/\D/g,"");
          this.logger.log(
            `[Tomador] fallback nome="${nomeBusca}" → encontrado: tipo=${cli.tipoPessoa} doc=${cli.documento ?? "null"}`,
          );
        } else {
          this.logger.warn(
            `[Tomador] fallback nome="${nomeBusca}" → ${resultado.total} resultados (ambiguo ou ausente) — sem dados`,
          );
        }
      } catch (fbErr) {
        this.logger.warn(
          `[Tomador] fallback nome="${nomeBusca}" → erro: ${fbErr instanceof Error ? fbErr.message : String(fbErr)}`,
        );
      }
    }
    ```

    **Insercao 1 — apos o warn de NotFoundException (linha ~368-374):**

    Depois da linha:
    ```typescript
    this.logger.warn(`[Tomador] orcamento "${lookupId}" nao encontrado no Athos (NotFoundException) - sem dados do tomador`);
    ```

    Inserir (onde `nomeBusca` e definido como):
    ```typescript
    const nomeBusca = (quote.customer?.fullName ?? "").trim();
    ```
    ...seguido pelo bloco helper acima.

    **Insercao 2 — dentro do bloco else de idcliente invalido (linha ~394-398):**

    Dentro do bloco:
    ```typescript
    } else {
      this.logger.warn(`[Tomador] idcliente=${clienteId} invalido ou ausente ...`);
      // INSERIR AQUI
    }
    ```

    Inserir (onde `nomeBusca` e definido como):
    ```typescript
    const nomeBusca = ((athosData as any)?.mapped?.cliente ?? quote.customer?.fullName ?? "").trim();
    ```
    ...seguido pelo bloco helper acima.

    **Restricoes:**
    - NAO alterar o bloco final `if (!nome) nome = quote.customer?.fullName ?? null;` (linha 407).
    - NAO alterar o catch externo (linha 400).
    - NAO criar metodo privado separado — o bloco inline e intencional para manter a logica visivel no fluxo.
    - O campo de nome no item retornado por buscarClientes e `cli.nome` (nao `cli.name`).
    - O campo de tipo e `cli.tipoPessoa` (nao `cli.type`).
  </action>
  <verify>
    <automated>grep -n "fallback nome" apps/backend/src/modules/integrations/nfse/nfse.service.ts</automated>
    <automated>grep -n "buscarClientes" apps/backend/src/modules/integrations/nfse/nfse.service.ts</automated>
    <automated>cd apps/backend && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep "fallback nome"` retorna pelo menos 2 ocorrencias (uma para cada caminho de fallback: NotFoundException e idcliente=0).
    - `grep "buscarClientes"` retorna pelo menos 2 ocorrencias dentro de buscarTomador.
    - `npx tsc --noEmit` sai com codigo 0 (sem erros de tipo).
  </acceptance_criteria>
  <done>buscarTomador() tenta buscarClientes por nome quando o orcamento nao e encontrado no Athos OU quando o orcamento existe mas idcliente=0. Log "[Tomador] fallback nome=..." aparece em ambos os caminhos. TypeScript compila sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nfse.service → iiBrasil API | ProximoRPS e um campo externo; assumir que pode estar desatualizado em caso de concorrencia |
| nfse.service → Athos DB | buscarClientes aceita nome livre; resultado ambiguo (total > 1) e descartado sem populacao de CPF/CNPJ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | RPS numero | accept | Incremento +1 e deterministico; colisao de RPS em emissao concorrente e rara e rejeitada pela prefeitura com erro legivel |
| T-18-02-02 | Information Disclosure | buscarClientes fallback | mitigate | Fallback so popula dados quando exatamente 1 resultado retorna; resultado ambiguo (>1) e descartado com log de aviso sem expor dados |
| T-18-02-03 | Spoofing | tomador por nome | accept | O nome vem do proprio orcamento/chat do sistema; nao e input externo direto do usuario anonimo |
</threat_model>

<verification>
Executar na raiz do repositorio apos aplicar as duas tasks:

```bash
# 1. RPS fix
grep -n "proximoRps + 1" apps/backend/src/modules/integrations/nfse/nfse.service.ts

# 2. Log RPS atualizado
grep -n "ultimoRPS" apps/backend/src/modules/integrations/nfse/nfse.service.ts

# 3. Fallback de tomador presente
grep -n "fallback nome" apps/backend/src/modules/integrations/nfse/nfse.service.ts

# 4. Chamada buscarClientes presente
grep -n "buscarClientes" apps/backend/src/modules/integrations/nfse/nfse.service.ts

# 5. TypeScript sem erros
cd apps/backend && npx tsc --noEmit
```

Todos os greps devem retornar matches. tsc deve sair com codigo 0.
</verification>

<success_criteria>
- grep "proximoRps + 1" retorna match em nfse.service.ts
- grep "ultimoRPS" retorna match em nfse.service.ts
- grep "fallback nome" retorna >= 2 matches em nfse.service.ts
- grep "buscarClientes" retorna >= 2 matches em nfse.service.ts
- npx tsc --noEmit (em apps/backend) sai com codigo 0
</success_criteria>

<output>
Apos execucao, criar `.planning/phases/18-correcoes-nfse-rps-tomador/18-02-SUMMARY.md` com:
- Tasks executadas e resultado de cada verificacao
- Linhas exatas alteradas em nfse.service.ts
- Resultado do tsc --noEmit
</output>
