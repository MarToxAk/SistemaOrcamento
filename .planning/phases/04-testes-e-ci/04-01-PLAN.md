---
phase: 04-testes-e-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/quotes/quotes.service.unit.test.ts
  - apps/backend/src/modules/integrations/efi/efi.webhook.test.ts
  - apps/backend/src/modules/quotes/quotes.service.chatwoot.test.ts
  - apps/backend/jest.config.js
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - FR-06.1
  - FR-06.2
  - FR-06.3

must_haves:
  truths:
    - "npm test passes with 0 failures"
    - "changeStatus invalid transitions throw BadRequestException"
    - "approveByToken validates token and expiry"
    - "EFI webhook processes pix payload correctly"
    - "CI runs on push/PR to main and dev"
  artifacts:
    - path: "apps/backend/jest.config.js"
      provides: "Jest configuration with ts-jest"
    - path: "apps/backend/src/modules/quotes/quotes.service.unit.test.ts"
      provides: "changeStatus + approveByToken + normalizeStatus tests"
    - path: "apps/backend/src/modules/integrations/efi/efi.webhook.test.ts"
      provides: "EFI webhook processWebhook tests"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI on push/PR to main and dev"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "npm --workspace @bomcusto/backend run test"
      via: "Run tests step"
      pattern: "workspace.*backend.*test"
---

<objective>
Setup Jest, write critical unit/integration tests, and configure GitHub Actions CI.

Purpose: FR-06.1, FR-06.2, FR-06.3 — test coverage for critical flows and CI on every PR.
Output: 3 test suites passing, CI workflow triggering on push/PR.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jest config + unit tests (changeStatus, approveByToken, normalizeStatus)</name>
  <files>apps/backend/jest.config.js, apps/backend/src/modules/quotes/quotes.service.unit.test.ts</files>
  <action>Configure ts-jest. Write tests for all valid/invalid status transitions, approveByToken token validation, normalizeStatus aliases.</action>
  <verify>npm run --workspace @bomcusto/backend test passes</verify>
  <done>3 test suites, 32 tests, all passing</done>
</task>

<task type="auto">
  <name>Task 2: EFI webhook + Chatwoot integration tests</name>
  <files>apps/backend/src/modules/integrations/efi/efi.webhook.test.ts, apps/backend/src/modules/quotes/quotes.service.chatwoot.test.ts</files>
  <action>Test EFI processWebhook with valid/invalid PIX payloads and idempotency. Test Chatwoot context validation.</action>
  <verify>npm run --workspace @bomcusto/backend test passes</verify>
  <done>All integration mock tests pass</done>
</task>

<task type="auto">
  <name>Task 3: GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>Create CI workflow: checkout, Node 20, npm ci, prisma:generate, build, test on push/PR to main and dev.</action>
  <verify>File exists with correct trigger branches and test step</verify>
  <done>CI triggers on push/PR to main and dev, runs tests</done>
</task>

</tasks>

<verification>
npm run --workspace @bomcusto/backend test → 3 suites, 32 tests, all passing
.github/workflows/ci.yml exists with correct configuration
</verification>

<success_criteria>
- npm test passes (0 failures)
- CI workflow exists and configured for main/dev branches
- changeStatus, approveByToken, normalizeStatus, EFI webhook, Chatwoot validation all covered
</success_criteria>

<output>
After completion, create `.planning/phases/04-testes-e-ci/04-01-SUMMARY.md`
</output>
