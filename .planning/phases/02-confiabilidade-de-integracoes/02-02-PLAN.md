---
phase: 02-confiabilidade-de-integracoes
plan: "02"
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/nfse/nfse.service.ts
  - apps/backend/src/modules/integrations/nfse/nfse.controller.ts
  - apps/frontend/src/app/orcamento/[id]/page.tsx
autonomous: true
requirements:
  - FR-02.4
must_haves:
  truths:
    - "Usuário consegue informar endereço manual do tomador no modal de NFS-e quando não há associado no Athos."
    - "Backend aceita endereço manual e o utiliza na geração do XML da NFS-e."
    - "Sem endereço mínimo no cenário manual, API retorna 400 com mensagem clara em português."
  artifacts:
    - path: "apps/backend/src/modules/integrations/nfse/nfse.service.ts"
      provides: "Contrato e validação de endereço manual + retorno de tomador.endereco no consultar()"
    - path: "apps/backend/src/modules/integrations/nfse/nfse.controller.ts"
      provides: "Entrada do body compatível com endereço manual"
    - path: "apps/frontend/src/app/orcamento/[id]/page.tsx"
      provides: "UI para editar/enviar endereço e exibir erro da API"
  key_links:
    - from: "apps/frontend/src/app/orcamento/[id]/page.tsx"
      to: "/api/quotes/:id/nfse"
      via: "GET para pré-preenchimento e POST com campos de endereço"
---

<objective>
Fechar gap de UAT da fase 2: permitir preencher endereço do tomador na emissão de NFS-e quando não houver cliente associado com dados completos no Athos.

Output: fluxo de NFS-e com endereço visível/ editável no modal, payload backend aceitando endereço manual e validação com mensagem clara quando faltarem campos obrigatórios.
</objective>

<context>
- Gap origem: .planning/phases/02-confiabilidade-de-integracoes/02-UAT.md (teste 4)
- Backend já possui estrutura de tomadorEndereco em buildRpsXml, mas o fluxo consultar/emissão não expõe nem recebe endereço manual no contrato da API.
- Frontend atual envia apenas documento e nome no modal de NFS-e.
</context>

<tasks>

<task>
  <name>Backend: estender contrato de emissão/consulta com endereço do tomador</name>
  <files>
    - apps/backend/src/modules/integrations/nfse/nfse.service.ts
    - apps/backend/src/modules/integrations/nfse/nfse.controller.ts
  </files>
  <action>
1. Estender EmitirNfseInput para aceitar endereço manual:
   - tomadorEnderecoLogradouro
   - tomadorEnderecoNumero
   - tomadorEnderecoBairro
   - tomadorEnderecoCep
   - tomadorEnderecoCodigoMunicipio
   - tomadorEnderecoUf
2. Em emitir(), montar tomadorEndereco a partir do body quando informado; se ausente, usar fallback Athos.
3. Em consultar(), incluir objeto tomador.endereco no payload para pré-preenchimento do frontend.
4. Adicionar validação: quando emissão for com documento manual e sem endereço mínimo, retornar BadRequestException com mensagem clara em pt-BR.
  </action>
  <verify>
    <automated>npm --workspace @bomcusto/backend run build</automated>
    <manual>GET /api/quotes/:id/nfse retorna tomador.endereco com logradouro, numero, bairro, cep, codigoMunicipio e uf.</manual>
    <manual>POST /api/quotes/:id/nfse com documento manual e endereço completo segue emissão.</manual>
    <manual>POST /api/quotes/:id/nfse sem endereço mínimo no cenário manual retorna 400 com mensagem clara.</manual>
  </verify>
  <done>
GET /api/quotes/:id/nfse retorna tomador.endereco completo quando houver fallback Athos.
POST /api/quotes/:id/nfse aceita endereço manual no payload.
Sem endereço mínimo no cenário manual, API retorna 400 com mensagem em português.
  </done>
</task>

<task>
  <name>Frontend: coletar e enviar endereço no modal de NFS-e</name>
  <files>
    - apps/frontend/src/app/orcamento/[id]/page.tsx
  </files>
  <action>
1. Adicionar estados para campos de endereço do tomador.
2. Pré-preencher campos a partir de GET /api/quotes/:id/nfse (tomador.endereco).
3. Incluir inputs no modal de NFS-e: logradouro, número, bairro, CEP, município (código IBGE), UF.
4. Enviar os campos no body do POST /api/quotes/:id/nfse.
5. Exibir erro retornado pela API de forma legível para usuário.
  </action>
  <verify>
    <automated>npm --workspace @bomcusto/frontend run build</automated>
    <automated>cd apps/frontend && npx tsc --noEmit</automated>
    <manual>Modal mostra campos de endereço e permite edição manual.</manual>
    <manual>Payload do POST /api/quotes/:id/nfse contém campos de endereço.</manual>
  </verify>
  <done>
Modal de NFS-e exibe e permite editar logradouro, numero, bairro, cep, codigoMunicipio e uf.
POST de emissão envia os campos de endereço no body.
Erro de validação do backend é exibido de forma legível para o usuário.
  </done>
</task>

<task>
  <name>Validação e regressão</name>
  <files>
    - apps/backend/src/modules/integrations/nfse/nfse.service.ts
    - apps/frontend/src/app/orcamento/[id]/page.tsx
  </files>
  <action>
1. Rodar TypeScript backend e frontend sem erros.
2. Executar fluxo manual:
   - orçamento sem associado no Athos
   - abrir modal NFS-e
   - preencher documento + endereço
   - emitir com sucesso
3. Revalidar cenário de erro sem documento para manter mensagem já esperada.
  </action>
  <verify>
    <automated>npm --workspace @bomcusto/backend run build</automated>
    <automated>cd apps/frontend && npx tsc --noEmit</automated>
    <manual>Executar cenário completo do UAT teste 4 e registrar resultado em 02-UAT.md.</manual>
    <manual>Revalidar cenário sem CPF/CNPJ para manter mensagem clara já existente.</manual>
  </verify>
  <done>
Gap do teste 4 do UAT fica fechado com evidência de execução.
Regressão de documento obrigatório permanece com a mesma mensagem clara de erro.
  </done>
</task>

</tasks>

<success_criteria>
- Endereço do tomador pode ser informado manualmente no fluxo de emissão.
- Backend aceita e utiliza endereço manual no XML da NFS-e.
- Usuário recebe erro claro quando faltar dado obrigatório.
- Gap registrado em 02-UAT.md fica endereçado por plano executável.
</success_criteria>

<output>
Após execução, atualizar:
- .planning/phases/02-confiabilidade-de-integracoes/02-UAT.md (status do gap)
- .planning/phases/02-confiabilidade-de-integracoes/02-02-SUMMARY.md
</output>
