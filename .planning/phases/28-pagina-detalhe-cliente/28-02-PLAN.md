---
phase: 28-pagina-detalhe-cliente
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - apps/frontend/src/app/contas-receber/page.tsx
  - apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/route.ts
  - apps/frontend/src/app/contas-receber/[idcliente]/page.tsx
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03

must_haves:
  truths:
    - "Clique em 'Ver Detalhe' em /contas-receber navega para /contas-receber/[idcliente]"
    - "Página de detalhe exibe nome, telefone, email e limite de crédito do cliente via Athos"
    - "Tabela de títulos lista todos AVC+VEN com checkbox individual, numerotitulo, datavencimento, valor e status badge"
    - "Checkbox 'Selecionar todos' no thead marca/desmarca todos; estado indeterminado quando parcial"
    - "Rodapé exibe N título(s) selecionado(s) — Total: R$ X.XXX,XX atualizado em tempo real"
    - "Barra de ações sticky aparece somente quando selectedIds.size > 0; oculta (removida do DOM) quando zero"
    - "Botões 'Gerar Boleto' (btn-warning) e 'Emitir NFS-e' (btn-primary) existem na barra com onClick vazio (TODO Phase 29/30)"
  artifacts:
    - path: "apps/frontend/src/app/contas-receber/page.tsx"
      provides: "Página /contas-receber sem accordion inline; botão 'Ver Detalhe' no card-footer"
      contains: "Ver Detalhe"
    - path: "apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/route.ts"
      provides: "Route Handler proxy GET → backend /athos/contas-receber/cliente/:id/dados com await params e x-internal-api-key"
      contains: "await params"
    - path: "apps/frontend/src/app/contas-receber/[idcliente]/page.tsx"
      provides: "Client Component com dados do cliente, tabela de títulos com checkboxes, barra de ações condicional"
      contains: "selectedIds"
  key_links:
    - from: "apps/frontend/src/app/contas-receber/[idcliente]/page.tsx"
      to: "apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/route.ts"
      via: "fetch /api/athos/contas-receber/cliente/${idcliente}"
      pattern: "fetch.*api/athos/contas-receber/cliente"
    - from: "apps/frontend/src/app/contas-receber/[idcliente]/page.tsx"
      to: "apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/titulos/route.ts"
      via: "fetch /api/athos/contas-receber/cliente/${idcliente}/titulos"
      pattern: "fetch.*titulos"
    - from: "apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/route.ts"
      to: "apps/backend /athos/contas-receber/cliente/:idcliente/dados"
      via: "backendFetch com x-api-token"
      pattern: "backendFetch.*dados"
---

<objective>
Implementar a navegação da página de listagem para a página de detalhe do cliente, o proxy de API de dados cadastrais e a própria página de detalhe com tabela de títulos interativa e barra de ações.

Purpose: Com o backend de Wave 1 disponível, o frontend pode agora exibir os dados do cliente em rota dedicada, permitindo ao operador selecionar títulos e acionar (em Phases futuras) geração de boleto e NFS-e.

Output: Três arquivos frontend — página de listagem simplificada, proxy Route Handler de dados cadastrais, e página de detalhe Client Component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-pagina-detalhe-cliente/28-CONTEXT.md
@.planning/phases/28-pagina-detalhe-cliente/28-01-SUMMARY.md
</context>

<interfaces>
Route Handler existente (padrão a replicar):
apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/titulos/route.ts

```typescript
import { NextRequest, NextResponse } from "next/server";
import { backendFetch } from "@/lib/backend-client";

export async function GET(
  _req: NextRequest,
  { params }: { params: Promise<{ idcliente: string }> },
) {
  const { idcliente } = await params;  // await obrigatório — Next.js 15
  const id = Number(idcliente);
  if (!Number.isFinite(id) || id <= 0) {
    return NextResponse.json({ error: "idcliente inválido." }, { status: 400 });
  }
  const athosToken = process.env.INTERNAL_API_KEY ?? "";
  const extraHeaders: Record<string, string> = athosToken ? { "x-api-token": athosToken } : {};
  try {
    const res = await backendFetch(`/athos/contas-receber/cliente/${id}/titulos`, {
      method: "GET",
      headers: extraHeaders,
    });
    const data = await res.json().catch(() => ({ error: "Resposta inválida do backend." }));
    if (!res.ok) return NextResponse.json(data, { status: res.status });
    return NextResponse.json(data);
  } catch {
    return NextResponse.json({ error: "Falha ao conectar no backend." }, { status: 500 });
  }
}
```

Interface ClienteDevedor em /contas-receber/page.tsx (campo idcliente disponível):
```typescript
interface ClienteDevedor {
  idcliente: number;
  nome_cliente: string;
  telefone_completo: string | null;
  emailcliente: string | null;
  emailcobrancacliente: string | null;
  limitecredito: number;
  bloqueaprazo: string | null;
  total_devido: number;
  total_atrasado: number;
  titulos_pendentes: number;
  maior_atraso_dias: number | null;
}
```

Interface TituloReceber (existente, reaproveitada na página de detalhe):
```typescript
interface TituloReceber {
  idcontareceber: number;
  numerotitulo: string | null;
  datavencimento: string;
  valor: number;
  observacao: string | null;
  idvenda: number | null;
  dataemissao: string | null;
  numeroordem: string | null;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Tarefa 1: Modificar /contas-receber/page.tsx — remover accordion e adicionar botão "Ver Detalhe"</name>
  <files>
    apps/frontend/src/app/contas-receber/page.tsx
  </files>

  <read_first>
    - apps/frontend/src/app/contas-receber/page.tsx — arquivo inteiro: identificar exatamente os estados titulosMap, expandedId, handleToggleCliente e o bloco de accordion inline ({isExpanded && ...}) para remover; identificar o card-footer com botão "Títulos" para substituir
  </read_first>

  <action>
  Modificar apps/frontend/src/app/contas-receber/page.tsx com as seguintes alterações (per D-01, D-02, D-03):

  1. Remover das declarações de estado (useState):
     - expandedId: number | null (e setExpandedId)
     - titulosMap: Record de TituloReceber[] | "loading" | "error" (e setTitulosMap)

  2. Remover a função handleToggleCliente inteira (linhas ~97-116).

  3. Remover a interface TituloReceber (não será mais usada nesta página).

  4. No useEffect e na função fetchDashboard, remover as chamadas setExpandedId(null) e setTitulosMap({}).

  5. Na variável isExpanded (dentro do .map dos clientes), remover essa declaração — ela não existe mais.

  6. No card-footer de cada cliente (substituir completamente o bloco div.card-footer), trocar o botão "Títulos" por:
     <div className="card-footer bg-transparent border-top">
       <a
         href={`/contas-receber/${cliente.idcliente}`}
         className="btn btn-sm btn-outline-primary"
       >
         <i className="bi bi-person-lines-fill me-1" />Ver Detalhe
       </a>
     </div>

  7. Remover completamente o bloco {isExpanded && (...)} que renderiza o accordion inline (tudo que está dentro do card após o card-footer, incluindo o div.card-body com a tabela de títulos).

  8. Manter todos os demais estados (summary, clientes, loading, erro, statusFiltro), a função fetchDashboard, os Top Cards, a lógica do filtro e os cards dos clientes inalterados.

  A página resultante não deve importar nem referenciar titulosMap, expandedId ou handleToggleCliente.
  </action>

  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <done>
    - grep -c "titulosMap\|expandedId\|handleToggleCliente" apps/frontend/src/app/contas-receber/page.tsx retorna 0
    - grep -c "Ver Detalhe" apps/frontend/src/app/contas-receber/page.tsx retorna 1
    - grep -c "contas-receber/\${cliente.idcliente}" apps/frontend/src/app/contas-receber/page.tsx retorna 1
    - npx tsc --noEmit sem erros de tipo
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar proxy Route Handler /api/athos/contas-receber/cliente/[idcliente]/route.ts + página de detalhe /contas-receber/[idcliente]/page.tsx</name>
  <files>
    apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/route.ts
    apps/frontend/src/app/contas-receber/[idcliente]/page.tsx
  </files>

  <read_first>
    - apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/titulos/route.ts — padrão completo de Route Handler com await params (Next.js 15), backendFetch, INTERNAL_API_KEY, validação de id — replicar estrutura exata
    - apps/frontend/src/app/contas-receber/page.tsx — após a Tarefa 1: funções formatBRL, formatDate, getBadgeClass, getBadgeLabel e as classes CSS do style tag — reutilizar na página de detalhe (copiar as funções necessárias)
  </read_first>

  <action>
  ARQUIVO 1: Criar apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/route.ts (per D-05, D-22)

  Este arquivo coexiste com o arquivo titulos/route.ts — a pasta [idcliente] já existe. O novo arquivo route.ts fica DIRETAMENTE em [idcliente]/, não em [idcliente]/titulos/.

  Conteúdo: estrutura idêntica ao titulos/route.ts, trocando apenas o path da chamada backendFetch de .../titulos para .../dados. Importar NextRequest, NextResponse de "next/server" e backendFetch de "@/lib/backend-client". Exportar função GET assíncrona com assinatura: GET(_req: NextRequest, { params }: { params: Promise<{ idcliente: string }> }). Aplicar await params. Validar id com Number.isFinite(id) && id > 0. Adicionar x-api-token com process.env.INTERNAL_API_KEY. Chamar backendFetch("/athos/contas-receber/cliente/${id}/dados", { method: "GET", headers: extraHeaders }). Tratar erro com catch retornando 500.

  ARQUIVO 2: Criar apps/frontend/src/app/contas-receber/[idcliente]/page.tsx (per D-04, D-07 a D-15)

  Diretiva "use client" no topo. Importar useEffect, useState, use de "react". Importar Script de "next/script".

  Interfaces necessárias neste arquivo:
  - DadosCliente: { idcliente: number; nome_cliente: string; telefone_completo: string | null; emailcliente: string | null; emailcobrancacliente: string | null; limitecredito: number; bloqueaprazo: string | null }
  - TituloReceber: { idcontareceber: number; numerotitulo: string | null; datavencimento: string; valor: number; observacao: string | null; idvenda: number | null; dataemissao: string | null; numeroordem: string | null }

  Funções utilitárias: formatBRL(value: number) usando toLocaleString("pt-BR", { style: "currency", currency: "BRL" }). formatDate(iso: string) usando toLocaleDateString("pt-BR").

  Assinatura do componente: export default function ClienteDetalhePage({ params }: { params: Promise<{ idcliente: string }> }). Resolver com const { idcliente } = use(params) — React.use() para params em Next.js 15 Client Components.

  Estados:
  - dadosCliente: DadosCliente | null — para dados cadastrais
  - titulos: TituloReceber[] — para a tabela
  - loadingCliente: boolean — inicia true
  - loadingTitulos: boolean — inicia true
  - erroCliente: string — inicia ""
  - erroTitulos: string — inicia ""
  - selectedIds: Set<number> — inicia new Set() (per D-10)

  useEffect: ao montar, disparar dois fetch em paralelo:
  1. fetch /api/athos/contas-receber/cliente/${idcliente} → setDadosCliente
  2. fetch /api/athos/contas-receber/cliente/${idcliente}/titulos → setTitulos
  Tratar erros individualmente em cada bloco catch.

  Lógica de seleção (per D-10, D-11):
  - handleToggle(id: number): cria novo Set, se id está → delete, senão → add, chama setSelectedIds
  - handleToggleAll(): se selectedIds.size === titulos.length → setSelectedIds(new Set()), senão → setSelectedIds(new Set(titulos.map(t => t.idcontareceber)))
  - allSelected: boolean = titulos.length > 0 && selectedIds.size === titulos.length
  - someSelected: boolean = selectedIds.size > 0 && selectedIds.size < titulos.length

  Cálculo do total selecionado:
  - totalSelecionado: number = titulos.filter(t => selectedIds.has(t.idcontareceber)).reduce((acc, t) => acc + t.valor, 0)

  Renderização JSX:
  Incluir mesmos Script CDN (bootstrap.bundle.min.js, bootstrap-icons.js) e link CDN (bootstrap CSS, bootstrap-icons CSS) da página pai.

  Estrutura visual (per D-07): div.container.my-4 com:

  HEADER: div.orcamento-header com logo /media/logo-primary.png + título "Detalhe do Cliente". Breadcrumb: a href="/contas-receber" className="btn btn-sm btn-outline-secondary me-3" com ← Contas a Receber.

  SEÇÃO DADOS DO CLIENTE: dentro de div.orcamento-section (após header), exibir card com 4 linhas: Nome (nome_cliente), Telefone (telefone_completo || "—"), E-mail (emailcliente || "—"), Limite de Crédito (formatBRL(limitecredito)). Se bloqueaprazo === "S", exibir badge bg-danger "Bloqueado". Spinner enquanto loadingCliente. Alert danger se erroCliente.

  SEÇÃO TABELA DE TÍTULOS (per D-09):
  div.table-responsive > table.table.table-sm.table-hover.table-bordered
  THEAD: tr com 5 th:
    1. Checkbox input type="checkbox" checked={allSelected} ref aplicando indeterminate via useRef+useEffect: criar ref checkboxRef = useRef(null); useEffect(() => { if (checkboxRef.current) checkboxRef.current.indeterminate = someSelected; }, [someSelected]). onChange={handleToggleAll}
    2. Título
    3. Vencimento
    4. Valor
    5. Status

  TBODY: para cada titulo em titulos:
    tr com key={titulo.idcontareceber}:
    td: input type="checkbox" checked={selectedIds.has(titulo.idcontareceber)} onChange={() => handleToggle(titulo.idcontareceber)}
    td: titulo.numerotitulo ?? "—"
    td: formatDate(titulo.datavencimento) — vermelho se new Date(titulo.datavencimento) < new Date()
    td: formatBRL(titulo.valor)
    td: badge — se datavencimento < hoje: span.badge.bg-danger "VEN", senão span.badge.bg-info.text-dark "AVC"

  RODAPÉ DA TABELA (per D-12): tfoot > tr > td colSpan={5} com texto: "${selectedIds.size} título(s) selecionado(s) — Total: ${formatBRL(totalSelecionado)}"

  BARRA DE AÇÕES (per D-13, D-14, D-15): Renderizar SOMENTE SE selectedIds.size > 0 (não disabled — remover do DOM quando zero). Implementar como div fixo na bottom da viewport com position sticky: style={{ position: "sticky", bottom: 0, background: "white", borderTop: "1px solid #dee2e6", padding: "12px 16px", zIndex: 10 }} contendo:
  - Texto: "${selectedIds.size} título(s) selecionado(s) — ${formatBRL(totalSelecionado)}"
  - Botão: className="btn btn-warning me-2" onClick={() => { /* TODO: Phase 29 — Gerar Boleto */ }} com texto "Gerar Boleto" e ícone bi-receipt me-1
  - Botão: className="btn btn-primary" onClick={() => { /* TODO: Phase 30 — Emitir NFS-e */ }} com texto "Emitir NFS-e" e ícone bi-file-earmark-text me-1

  Style tag ao final com as mesmas regras CSS da página pai (body, .orcamento-header, .orcamento-section, .logo-img, .bg-orange).

  Nota: Se loadingTitulos for true, exibir spinner no lugar da tabela. Se erroTitulos, exibir alert-danger. Se titulos.length === 0 após carregar, exibir alert-info "Nenhum título encontrado para este cliente."
  </action>

  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>

  <done>
    - grep -c "await params" "apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/route.ts" retorna 1
    - grep -c "selectedIds" "apps/frontend/src/app/contas-receber/[idcliente]/page.tsx" retorna resultado >= 5 (múltiplos usos do estado)
    - grep -c "TODO: Phase 29" "apps/frontend/src/app/contas-receber/[idcliente]/page.tsx" retorna 1
    - grep -c "TODO: Phase 30" "apps/frontend/src/app/contas-receber/[idcliente]/page.tsx" retorna 1
    - grep -c "Contas a Receber" "apps/frontend/src/app/contas-receber/[idcliente]/page.tsx" retorna >= 1 (breadcrumb)
    - npx tsc --noEmit sem erros de tipo
    - cd apps/frontend && npm run build 2>&1 | tail -20 — build concluído sem erros de compilação
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Browser → Next.js Route Handler | Chamada client-side fetch para /api/*; Route Handler adiciona token server-side |
| Next.js Route Handler → NestJS backend | backendFetch com x-api-token = INTERNAL_API_KEY |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Spoofing | /api/athos/contas-receber/cliente/[idcliente]/route.ts | mitigate | Token INTERNAL_API_KEY adicionado server-side; nunca exposto ao browser |
| T-28-06 | Information Disclosure | DadosCliente (email, limite de crédito) | accept | Página interna de operação (não pública); sem autenticação de usuário neste MVP — mesmo nível que /contas-receber/page.tsx existente |
| T-28-07 | Tampering | onClick vazio nos botões de ação | accept | Botões sem implementação são intencionais (D-15); sem side effects possíveis |
| T-28-08 | Elevation of Privilege | idcliente como parâmetro de rota | mitigate | Validação Number.isFinite(id) && id > 0 no Route Handler; backend valida novamente antes de consultar Athos |
</threat_model>

<verification>
1. npm run build (apps/frontend) sem erros
2. npx tsc --noEmit (apps/frontend) sem erros de tipo
3. Acessar /contas-receber → cards exibem botão "Ver Detalhe" (sem botão "Títulos"), sem accordion inline
4. Clicar "Ver Detalhe" navega para /contas-receber/[idcliente] com breadcrumb "← Contas a Receber"
5. Página de detalhe exibe nome do cliente, telefone, email e limite de crédito
6. Tabela de títulos lista AVC+VEN com checkbox por linha
7. Marcar 1 título: barra de ações aparece com total correto e botões "Gerar Boleto" / "Emitir NFS-e"
8. Desmarcar todos: barra de ações desaparece do DOM (inspecionar elemento — elemento não presente)
9. Clicar checkbox "Selecionar todos" → todos marcados; estado indeterminate quando parcial
10. Clicar "Gerar Boleto" ou "Emitir NFS-e" → nada acontece (onClick vazio — esperado)
</verification>

<success_criteria>
- /contas-receber não tem mais accordion inline nem titulosMap/expandedId no código
- /contas-receber/[idcliente] renderiza dados do cliente e tabela de títulos sem erro
- Seleção por checkbox funciona; totalSelecionado atualiza em tempo real
- Barra de ações presente no DOM somente quando selectedIds.size > 0
- Build do frontend sem erros
</success_criteria>

<output>
Criar .planning/phases/28-pagina-detalhe-cliente/28-02-SUMMARY.md quando concluído
</output>
