---
phase: 28-pagina-detalhe-cliente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/athos/athos.service.ts
  - apps/backend/src/modules/integrations/athos/athos.controller.ts
  - apps/backend/prisma/schema.prisma
autonomous: true
requirements:
  - CLI-01

must_haves:
  truths:
    - "GET /athos/contas-receber/cliente/:idcliente/dados retorna nome, telefone, email, limitecredito e bloqueaprazo do cliente"
    - "Tabelas CobrancaBoleto, CobrancaBoletoTitulo, NfseEmitida e NfseEmitidaTitulo existem no banco sem conflito"
    - "Migration aplicada com npx prisma migrate dev sem erro"
  artifacts:
    - path: "apps/backend/src/modules/integrations/athos/athos.service.ts"
      provides: "Método buscarDadosClienteContasReceber(idcliente: number) com query SQL de JOIN cliente+cliente_fisico+cliente_juridico"
      contains: "buscarDadosClienteContasReceber"
    - path: "apps/backend/src/modules/integrations/athos/athos.controller.ts"
      provides: "Rota GET contas-receber/cliente/:idcliente/dados protegida por validateAthosToken"
      contains: "contas-receber/cliente/:idcliente/dados"
    - path: "apps/backend/prisma/schema.prisma"
      provides: "4 novos modelos Prisma: CobrancaBoleto, CobrancaBoletoTitulo, NfseEmitida, NfseEmitidaTitulo"
      contains: "model CobrancaBoleto"
  key_links:
    - from: "athos.controller.ts GET contas-receber/cliente/:idcliente/dados"
      to: "athos.service.ts buscarDadosClienteContasReceber"
      via: "injeção de dependência NestJS"
      pattern: "buscarDadosClienteContasReceber"
    - from: "prisma/schema.prisma CobrancaBoletoTitulo"
      to: "prisma/schema.prisma CobrancaBoleto"
      via: "relation cobrancaBoletoId"
      pattern: "cobrancaBoleto.*CobrancaBoleto @relation"
---

<objective>
Implementar o backend de dados cadastrais do cliente e as migrations Prisma necessárias para as Fases 29 e 30.

Purpose: A página de detalhe /contas-receber/[idcliente] (Wave 2) precisa de um endpoint próprio para dados cadastrais do cliente, separado do endpoint de títulos já existente. As tabelas CobrancaBoleto e NfseEmitida são pré-requisitos para Phases 29 e 30 e devem ser criadas nesta fase.

Output: Novo método AthosService, nova rota REST, schema Prisma com 4 modelos e migration aplicada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-pagina-detalhe-cliente/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Adicionar buscarDadosClienteContasReceber ao AthosService + rota no AthosController</name>
  <files>
    apps/backend/src/modules/integrations/athos/athos.service.ts
    apps/backend/src/modules/integrations/athos/athos.controller.ts
  </files>

  <read_first>
    - apps/backend/src/modules/integrations/athos/athos.service.ts — linhas ~882-950: padrão completo de buscarClientePorId() (PoolClient, pool.connect(), try/finally client.release(), pickString helper) para replicar estrutura
    - apps/backend/src/modules/integrations/athos/athos.controller.ts — arquivo inteiro: estrutura de imports, validateAthosToken, padrão @ApiOperation/@ApiParam/@Get existente em contas-receber/cliente/:idcliente/titulos (linhas ~251-270) para replicar
  </read_first>

  <action>
  Em apps/backend/src/modules/integrations/athos/athos.service.ts, adicionar o método buscarDadosClienteContasReceber logo após buscarClientePorId() (após linha ~950). O método aceita idcliente: number e retorna Promise com os campos: idcliente (number), nome_cliente (string), telefone_completo (string | null), emailcliente (string | null), emailcobrancacliente (string | null), limitecredito (number), bloqueaprazo (string | null). Retorna null se não encontrado.

  Implementação: adquirir PoolClient via this.getPool().connect(), executar query parametrizada com $1 = idcliente:

    SELECT c.idcliente,
      COALESCE(cf.nome, cj.nomefantasia, cj.razaosocial, 'Cliente #' || c.idcliente::text) AS nome_cliente,
      c.dddtelefoneempresa || c.telefoneempresa AS telefone_completo,
      c.emailcliente, c.emailcobrancacliente, c.limitecredito, c.bloqueaprazo
    FROM cliente c
    LEFT JOIN cliente_fisico cf ON cf.idcliente = c.idcliente
    LEFT JOIN cliente_juridico cj ON cj.idcliente = c.idcliente
    WHERE c.idcliente = $1

  Tratar result.rows[0] com a interface Row existente. Nos campos numéricos (limitecredito), converter com Number(). No bloco catch, usar this.logger.warn igual a buscarClientePorId(). Obrigatoriamente usar try/finally com client.release() no finally.

  Em apps/backend/src/modules/integrations/athos/athos.controller.ts, adicionar nova rota IMEDIATAMENTE ANTES do fechamento da classe (antes do último `}`). Seguir exatamente o mesmo padrão de titulosClienteContasReceber:
  - Decorators: @ApiOperation({ summary: "Dados cadastrais do cliente (contas a receber)" }), @ApiParam({ name: "idcliente", example: "123" }), @ApiOkResponse({ description: "Dados cadastrais: nome, telefone, email, limitecredito, bloqueaprazo" }), @ApiUnauthorizedResponse({ description: "Token ausente ou inválido" }), @Get("contas-receber/cliente/:idcliente/dados")
  - Parâmetros de método: @Param("idcliente") idcliente: string, @Headers("authorization") authorization?: string, @Headers("x-api-token") xApiToken?: string
  - Corpo: this.validateAthosToken(authorization, xApiToken); const id = Number(idcliente); if (!Number.isFinite(id) || id <= 0) throw new BadRequestException("idcliente inválido"); const dados = await this.athosService.buscarDadosClienteContasReceber(id); if (!dados) throw new NotFoundException(\`Cliente ${id} não encontrado no Athos\`); return dados;
  - Importar NotFoundException de @nestjs/common se não estiver nos imports.
  </action>

  <verify>
    <automated>cd apps/backend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <done>
    - grep -c "buscarDadosClienteContasReceber" apps/backend/src/modules/integrations/athos/athos.service.ts retorna 2 (definição do método + chamada da query)
    - grep -c "contas-receber/cliente/:idcliente/dados" apps/backend/src/modules/integrations/athos/athos.controller.ts retorna 1
    - npx tsc --noEmit sem erros de tipo
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Adicionar 4 modelos ao schema.prisma e executar migration</name>
  <files>
    apps/backend/prisma/schema.prisma
  </files>

  <read_first>
    - apps/backend/prisma/schema.prisma — arquivo inteiro: indentação, convenção de nomes dos campos, uso de @db.Decimal(12,2), @updatedAt, @@index em FKs — para manter consistência com modelos existentes (PaymentTransaction, QuoteItem, etc.)
  </read_first>

  <action>
  Adicionar os 4 novos modelos ao FINAL de apps/backend/prisma/schema.prisma, após o último modelo existente (ExternalReference). Não modificar nenhum modelo existente.

  Modelo CobrancaBoleto:
    id              Int      @id @default(autoincrement())
    txidEfi         String?
    idclienteAthos  Int
    valor           Decimal  @db.Decimal(12,2)
    status          String   @default("pendente")
    linkBoleto      String?
    pixPayload      String?
    criadoEm        DateTime @default(now())
    atualizadoEm    DateTime @updatedAt
    titulos         CobrancaBoletoTitulo[]

  Modelo CobrancaBoletoTitulo:
    id               Int            @id @default(autoincrement())
    cobrancaBoletoId Int
    idcontareceber   Int
    valor            Decimal        @db.Decimal(12,2)
    cobrancaBoleto   CobrancaBoleto @relation(fields: [cobrancaBoletoId], references: [id])
    @@index([cobrancaBoletoId])
    @@index([idcontareceber])

  Modelo NfseEmitida:
    id             Int      @id @default(autoincrement())
    numeroNfse     String?
    numeroRps      Int
    idclienteAthos Int
    valorServico   Decimal  @db.Decimal(12,2)
    dataEmissao    DateTime @default(now())
    criadoEm       DateTime @default(now())
    titulos        NfseEmitidaTitulo[]

  Modelo NfseEmitidaTitulo:
    id            Int         @id @default(autoincrement())
    nfseEmitidaId Int
    idcontareceber Int
    valor         Decimal     @db.Decimal(12,2)
    nfseEmitida   NfseEmitida @relation(fields: [nfseEmitidaId], references: [id])
    @@index([nfseEmitidaId])
    @@index([idcontareceber])

  Após editar o arquivo, executar a migration a partir do diretório apps/backend:
    npx prisma migrate dev --name add_cobranca_boleto_nfse_emitida

  Se o ambiente não tiver DATABASE_URL disponível para migrate dev, executar npx prisma generate apenas para validar o schema e anotar na SUMMARY que migrate dev deve ser executado no ambiente de produção com as credenciais corretas.
  </action>

  <verify>
    <automated>cd apps/backend && npx prisma validate 2>&1</automated>
  </verify>

  <done>
    - grep -c "model CobrancaBoleto" apps/backend/prisma/schema.prisma retorna 1 (não 2 — CobrancaBoletoTitulo não deve disparar este grep)
    - grep -c "model CobrancaBoletoTitulo" apps/backend/prisma/schema.prisma retorna 1
    - grep -c "model NfseEmitida" apps/backend/prisma/schema.prisma retorna 1
    - grep -c "model NfseEmitidaTitulo" apps/backend/prisma/schema.prisma retorna 1
    - npx prisma validate retorna "The schema at ... is valid"
    - Migration aplicada (arquivo em apps/backend/prisma/migrations/ com sufixo add_cobranca_boleto_nfse_emitida) OU npx prisma generate concluído sem erro quando DATABASE_URL indisponível
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| frontend Route Handler → NestJS backend | Chamada server-side via backendFetch com x-internal-api-key |
| NestJS backend → Athos PostgreSQL | Pool de conexão read-only; Athos nunca recebe escritas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Spoofing | GET /athos/contas-receber/cliente/:idcliente/dados | mitigate | validateAthosToken com timingSafeEqual — mesmo padrão de titulos endpoint existente |
| T-28-02 | Tampering | Athos PostgreSQL (read) | accept | Pool somente leitura; nenhum INSERT/UPDATE/DELETE no banco Athos |
| T-28-03 | Information Disclosure | dados cadastrais do cliente (email, limite de crédito) | mitigate | Endpoint protegido por ATHOS_API_TOKEN; Route Handler do frontend adiciona token server-side via INTERNAL_API_KEY |
| T-28-04 | Elevation of Privilege | Prisma migration altera schema | accept | Migration é aditiva (somente novos modelos); nenhum modelo existente é modificado |
</threat_model>

<verification>
1. npx tsc --noEmit no apps/backend sem erros de tipo
2. npx prisma validate retorna schema válido
3. curl -s -H "x-api-token: $ATHOS_API_TOKEN" http://localhost:3001/athos/contas-receber/cliente/1/dados retorna JSON com campos nome_cliente, emailcliente, limitecredito
4. curl -s -H "x-api-token: $ATHOS_API_TOKEN" http://localhost:3001/athos/contas-receber/cliente/0/dados retorna 400 BadRequest
5. curl -s http://localhost:3001/athos/contas-receber/cliente/1/dados retorna 401 Unauthorized
</verification>

<success_criteria>
- GET /athos/contas-receber/cliente/:idcliente/dados retorna 200 com nome_cliente, telefone_completo, emailcliente, emailcobrancacliente, limitecredito, bloqueaprazo para idcliente válido
- GET retorna 404 quando idcliente não existe no Athos
- GET retorna 400 quando idcliente = 0 ou não-numérico
- GET retorna 401 sem token
- schema.prisma contém os 4 novos modelos com relações bidirecionais e @@index nas FKs
- npx prisma validate passa sem erros
- Migration aplicada ou prisma generate executado com sucesso
</success_criteria>

<output>
Criar .planning/phases/28-pagina-detalhe-cliente/28-01-SUMMARY.md quando concluído
</output>
