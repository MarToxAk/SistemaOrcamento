---
phase: 36-frontend-white-label
plan: 03
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - apps/frontend/src/app/orcamento/[id]/approve/page.tsx
  - apps/frontend/src/app/orcamento/[id]/status/page.tsx
autonomous: true
requirements:
  - FRONT-03

must_haves:
  truths:
    - "A página pública /orcamento/[id]/approve exibe logo e nome da empresa vindos de empresa.ts"
    - "A página pública /orcamento/[id]/status exibe logo e nome da empresa vindos de empresa.ts"
    - "Nenhuma referência a 'Bom Custo', 'BomCusto' ou logo-primary.png hardcoded permanece nos 2 arquivos"
    - "alt do img usa EMPRESA_NOME em vez de string fixa"
    - "Texto de nome no header usa {EMPRESA_NOME} em vez de string literal com &amp;"
  artifacts:
    - path: "apps/frontend/src/app/orcamento/[id]/approve/page.tsx"
      provides: "Página pública de aprovação de orçamento com logo/nome white-label"
    - path: "apps/frontend/src/app/orcamento/[id]/status/page.tsx"
      provides: "Página pública de status do orçamento com logo/nome white-label"
  key_links:
    - from: "apps/frontend/src/app/orcamento/[id]/approve/page.tsx"
      to: "apps/frontend/src/lib/empresa.ts"
      via: "import { EMPRESA_NOME, EMPRESA_LOGO_URL } from '@/lib/empresa'"
      pattern: "from.*@/lib/empresa"
    - from: "apps/frontend/src/app/orcamento/[id]/status/page.tsx"
      to: "apps/frontend/src/lib/empresa.ts"
      via: "import { EMPRESA_NOME, EMPRESA_LOGO_URL } from '@/lib/empresa'"
      pattern: "from.*@/lib/empresa"
---

<objective>
Dehardcode as 2 páginas públicas de orçamento (approve e status) substituindo logo hardcoded e nome da empresa por imports do módulo empresa.ts.

Purpose: FRONT-03 — páginas públicas (acessíveis sem autenticação) exibem os dados corretos da empresa mesmo em deploys white-label. SC-4 do ROADMAP: "Páginas públicas de aprovação e status exibem o logo e o nome corretos da empresa mesmo sem autenticação".
Output: 2 arquivos modificados; padrão simplificado (somente logo + nome — sem CNPJ/endereço/email)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-frontend-white-label/36-CONTEXT.md
@.planning/phases/36-frontend-white-label/36-RESEARCH.md
@.planning/phases/36-frontend-white-label/36-UI-SPEC.md
@.planning/phases/36-frontend-white-label/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dehardcode header das 2 páginas públicas (approve e status)</name>
  <files>
    apps/frontend/src/app/orcamento/[id]/approve/page.tsx,
    apps/frontend/src/app/orcamento/[id]/status/page.tsx
  </files>
  <read_first>
    - apps/frontend/src/app/orcamento/[id]/approve/page.tsx (ler linhas 140–155: bloco do header com img e nome; verificar imports existentes no topo do arquivo)
    - apps/frontend/src/app/orcamento/[id]/status/page.tsx (ler linhas 100–115: bloco equivalente)
    - apps/frontend/src/lib/empresa.ts (confirmar que foi criado pelo Plano 01)
    - .planning/phases/36-frontend-white-label/36-RESEARCH.md (seções "approve/page.tsx" e "status/page.tsx" — inventário de linhas; Pitfall 6 — &amp; nos textos hardcoded)
    - .planning/phases/36-frontend-white-label/36-UI-SPEC.md (seção "Logo — Contrato de Renderização" — padrão simplificado para páginas públicas)
  </read_first>
  <action>
    Para cada um dos 2 arquivos, adicionar import de empresa.ts e substituir os valores hardcoded.

    IMPORT a adicionar (início do arquivo, após imports existentes):
    `import { EMPRESA_NOME, EMPRESA_LOGO_URL } from "@/lib/empresa";`

    Substituições JSX para approve/page.tsx (linhas ~144–149):
    - `src="/media/logo-primary.png"` → `src={EMPRESA_LOGO_URL}`
    - `alt="Bom Custo Papelaria & Gráfica Rápida"` → `alt={EMPRESA_NOME}`
    - `<div className="mt-2 small text-muted">Bom Custo Papelaria &amp; Gráfica Rápida</div>` → `<div className="mt-2 small text-muted">{EMPRESA_NOME}</div>`

    Substituições JSX para status/page.tsx (linhas ~105–110):
    - `src="/media/logo-primary.png"` → `src={EMPRESA_LOGO_URL}`
    - `alt="Bom Custo Papelaria &amp; Gráfica Rápida"` → `alt={EMPRESA_NOME}`
    - `<div className="mt-2 small text-muted">Bom Custo Papelaria &amp; Gráfica Rápida</div>` → `<div className="mt-2 small text-muted">{EMPRESA_NOME}</div>`

    IMPORTANTE — Pitfall 6 do RESEARCH.md: os textos hardcoded usam `&amp;` (entidade HTML). Ao substituir por `{EMPRESA_NOME}`, o React renderiza a string diretamente sem encode. Nenhuma ação especial necessária — apenas substituir o elemento inteiro pela expressão JSX.

    Preservar todos os outros atributos do elemento img (className, style com maxWidth/maxHeight, borderRadius, padding).

    PROIBIDO: não parametrizar os telefones hardcoded nessas páginas. O RESEARCH.md (linhas 247, 252, 274, 278 de approve e linhas 128, 189 de status) identifica telefones como fora do escopo de FRONT-03. Telefone permanece como texto estático.

    PROIBIDO: não adicionar CNPJ, endereço ou email nestas páginas — o padrão simplificado das páginas públicas é somente logo + nome (UI-SPEC seção "Logo — Contrato de Renderização" — padrão simplificado).
  </action>
  <verify>
    <automated>cd apps/frontend && grep -rn "logo-primary.png\|Bom Custo Papelaria\|BomCusto" "src/app/orcamento/[id]/approve/page.tsx" "src/app/orcamento/[id]/status/page.tsx" 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Ambos os arquivos importam de "@/lib/empresa" (grep -rn 'from.*@/lib/empresa' deve retornar 2 linhas nestes arquivos)
    - Nenhum dos 2 arquivos contém "/media/logo-primary.png" hardcoded (grep -rn 'logo-primary.png' deve retornar 0)
    - Nenhum dos 2 arquivos contém "Bom Custo Papelaria" como string literal — nem com & nem com &amp; (grep -rn 'Bom Custo Papelaria' nos 2 arquivos deve retornar 0)
    - Ambos os arquivos usam EMPRESA_LOGO_URL no atributo src (grep -rn 'EMPRESA_LOGO_URL' deve retornar 2 linhas)
    - Ambos os arquivos usam EMPRESA_NOME no atributo alt (grep -rn 'alt={EMPRESA_NOME}' deve retornar 2 linhas)
    - Ambos os arquivos usam {EMPRESA_NOME} no texto do header (grep -rn '{EMPRESA_NOME}' nos 2 arquivos deve retornar >= 4 linhas — 2 no alt + 2 no texto)
  </acceptance_criteria>
  <done>As 2 páginas públicas exibem logo e nome de empresa.ts; nenhuma string "Bom Custo" hardcoded permanece; telefones mantidos como texto estático</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NEXT_PUBLIC_EMPRESA_LOGO_URL → img src (página pública) | URL da logo renderizada em página acessível sem autenticação |
| NEXT_PUBLIC_EMPRESA_NOME → texto visível (página pública) | Nome da empresa renderizado em página sem autenticação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Information Disclosure | EMPRESA_NOME/LOGO_URL em páginas públicas | accept | Por design: páginas /approve e /status já são públicas. Nome e logo da empresa são dados públicos de branding. NEXT_PUBLIC_* são baked no bundle — sem diferença de exposição vs texto hardcoded anterior. |
| T-36-06 | Tampering | EMPRESA_LOGO_URL → img src em página pública | accept | Quem controla .env.local já tem acesso total ao servidor. URL vai para atributo src — sem execução de script. Fallback para logo interno garante que header nunca fica vazio. |
</threat_model>

<verification>
Após execução:

1. `cd apps/frontend && grep -rn "logo-primary.png\|Bom Custo\|BomCusto" "src/app/orcamento/[id]/approve/page.tsx" "src/app/orcamento/[id]/status/page.tsx"` deve retornar 0 resultados
2. `cd apps/frontend && grep -rn 'from.*@/lib/empresa' "src/app/orcamento/[id]/approve/page.tsx" "src/app/orcamento/[id]/status/page.tsx"` deve retornar 2 linhas
3. `cd apps/frontend && grep -rn 'EMPRESA_LOGO_URL' "src/app/orcamento/[id]/approve/page.tsx" "src/app/orcamento/[id]/status/page.tsx"` deve retornar 2 linhas
</verification>

<success_criteria>
- 2 páginas públicas importam EMPRESA_NOME e EMPRESA_LOGO_URL de @/lib/empresa
- Nenhum dos 2 arquivos contém /media/logo-primary.png, "Bom Custo" ou "BomCusto" como string literal
- Padrão simplificado: somente logo + nome (sem CNPJ, endereço, email nas páginas públicas)
- Telefones hardcoded mantidos como texto estático (fora do escopo de FRONT-03)
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Type |
|--------|------|------|
| import empresa.ts | apps/frontend/src/app/orcamento/[id]/approve/page.tsx | import added |
| import empresa.ts | apps/frontend/src/app/orcamento/[id]/status/page.tsx | import added |

<output>
Create `.planning/phases/36-frontend-white-label/36-03-SUMMARY.md` when done
</output>
