---
phase: 36-frontend-white-label
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - apps/frontend/src/app/orcamento/page.tsx
  - apps/frontend/src/app/orcamento/novo/page.tsx
  - apps/frontend/src/app/orcamento/[id]/page.tsx
  - apps/frontend/src/app/contas-receber/page.tsx
  - apps/frontend/src/app/contas-receber/[idcliente]/page.tsx
autonomous: true
requirements:
  - FRONT-02

must_haves:
  truths:
    - "As 3 páginas de orçamento exibem logo, nome, CNPJ, endereço e email vindos de empresa.ts"
    - "As 2 páginas de contas-receber exibem logo vindo de empresa.ts"
    - "CNPJ, endereço e email usam renderização condicional — não exibem linha vazia quando string vazia"
    - "Endereço fragmentado em 2 divs foi consolidado em 1 div com EMPRESA_ENDERECO"
    - "Nenhuma referência a BomCusto ou logo-primary.png hardcoded permanece nos 5 arquivos"
  artifacts:
    - path: "apps/frontend/src/app/orcamento/page.tsx"
      provides: "Página de listagem de orçamentos com header white-label"
    - path: "apps/frontend/src/app/orcamento/novo/page.tsx"
      provides: "Página de criação de orçamento com header white-label"
    - path: "apps/frontend/src/app/orcamento/[id]/page.tsx"
      provides: "Página de detalhe de orçamento com header white-label"
    - path: "apps/frontend/src/app/contas-receber/page.tsx"
      provides: "Dashboard de contas a receber com logo white-label"
    - path: "apps/frontend/src/app/contas-receber/[idcliente]/page.tsx"
      provides: "Detalhe de cliente em contas a receber com logo white-label"
  key_links:
    - from: "apps/frontend/src/app/orcamento/page.tsx"
      to: "apps/frontend/src/lib/empresa.ts"
      via: "import { EMPRESA_NOME, EMPRESA_CNPJ, EMPRESA_ENDERECO, EMPRESA_EMAIL, EMPRESA_LOGO_URL } from '@/lib/empresa'"
      pattern: "from.*@/lib/empresa"
    - from: "apps/frontend/src/app/contas-receber/page.tsx"
      to: "apps/frontend/src/lib/empresa.ts"
      via: "import { EMPRESA_NOME, EMPRESA_LOGO_URL } from '@/lib/empresa'"
      pattern: "from.*@/lib/empresa"
---

<objective>
Dehardcode as 5 páginas internas do frontend substituindo valores hardcoded de branding (logo, nome, CNPJ, endereço, email) por imports do módulo empresa.ts criado no Plano 01.

Purpose: FRONT-02 — páginas internas do sistema exibem dados da empresa a partir de env vars. Nenhuma string literal "BomCusto" ou "bomcusto" permanece nos 5 arquivos após esta fase.
Output: 5 arquivos modificados importando de @/lib/empresa
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-frontend-white-label/36-CONTEXT.md
@.planning/phases/36-frontend-white-label/36-RESEARCH.md
@.planning/phases/36-frontend-white-label/36-UI-SPEC.md
@.planning/phases/36-frontend-white-label/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dehardcode header das 3 páginas de orçamento (logo + nome + CNPJ + endereço + email)</name>
  <files>
    apps/frontend/src/app/orcamento/page.tsx,
    apps/frontend/src/app/orcamento/novo/page.tsx,
    apps/frontend/src/app/orcamento/[id]/page.tsx
  </files>
  <read_first>
    - apps/frontend/src/app/orcamento/page.tsx (ler linhas 395–415: bloco do header com img + nome + CNPJ + endereço + email)
    - apps/frontend/src/app/orcamento/novo/page.tsx (ler linhas 583–600: bloco do header equivalente)
    - apps/frontend/src/app/orcamento/[id]/page.tsx (ler linhas 525–540: bloco do header)
    - apps/frontend/src/lib/empresa.ts (confirmar que foi criado pelo Plano 01 com as 6 exports)
    - .planning/phases/36-frontend-white-label/36-RESEARCH.md (seção "Codebase Audit" — inventário linha a linha; Pitfall 3 dimensões diferentes; Pitfall 4 endereço fragmentado)
    - .planning/phases/36-frontend-white-label/36-UI-SPEC.md (seção "Copywriting Contract" — renderização condicional de CNPJ/endereço/email)
  </read_first>
  <action>
    Para cada um dos 3 arquivos, fazer duas operações: (1) adicionar o import de empresa.ts; (2) substituir os valores hardcoded no JSX.

    IMPORT a adicionar (início do arquivo, após os imports existentes de React/hooks):
    `import { EMPRESA_NOME, EMPRESA_CNPJ, EMPRESA_ENDERECO, EMPRESA_EMAIL, EMPRESA_LOGO_URL } from "@/lib/empresa";`

    JSX — substituições para orcamento/page.tsx (linha ~399 e seguintes):
    - `src="/media/logo-primary.png"` → `src={EMPRESA_LOGO_URL}`
    - `alt="Logo Bom Custo"` → `alt={EMPRESA_NOME}`
    - `<h3 className="mb-0">Bom Custo Papelaria & Gráfica Rápida LTDA</h3>` → `<h3 className="mb-0">{EMPRESA_NOME}</h3>`
    - `<div className="small">CNPJ: 62.391.927/0001-57</div>` → `{EMPRESA_CNPJ && <div className="small">CNPJ: {EMPRESA_CNPJ}</div>}`
    - As DUAS divs de endereço (linha 403: "Rua Olímpio..." e linha 404: "Ilhabela - SP...") → consolidar em UMA: `{EMPRESA_ENDERECO && <div className="small">{EMPRESA_ENDERECO}</div>}` (Pitfall 4 — endereço vira string única na env var)
    - O bloco de telefone + email (linhas 405–408 — div com Telefones... e E-mail...): remover a parte do email hardcoded e adicionar `{EMPRESA_EMAIL && <div className="small">E-mail: {EMPRESA_EMAIL}</div>}` separado. O bloco de telefone fixo pode permanecer como texto estático (fora do escopo; sem EMPRESA_TELEFONE no módulo).

    JSX — mesma lógica para orcamento/novo/page.tsx (linha ~587):
    - Dimensões do logo DEVEM ser preservadas como estão nesse arquivo: `maxWidth:140, maxHeight:100` (Pitfall 3 — não uniformizar para 180/120)
    - Aplicar mesmas substituições: logo URL/alt, nome, CNPJ condicional, endereço condicional (2 divs → 1 div), email condicional separado do bloco de telefone

    JSX — orcamento/[id]/page.tsx (linha ~529):
    - Aplicar mesmas substituições: logo URL/alt, nome, CNPJ condicional, endereço (2 divs → 1 div), email condicional
    - Preservar dimensões exatas do logo já existentes neste arquivo

    PROIBIDO: não adicionar EMPRESA_TELEFONE (não está no módulo). Bloco de telefone permanece como texto estático.
    PROIBIDO: não alterar nenhuma dimensão de logo (maxWidth/maxHeight) — preservar valor exato existente em cada arquivo.
    PROIBIDO: não usar renderização condicional para EMPRESA_LOGO_URL — o módulo tem fallback garantido; a img SEMPRE deve ser renderizada (D-09 + Pitfall 3 do RESEARCH).
  </action>
  <verify>
    <automated>cd apps/frontend && grep -rn "logo-primary.png\|Logo Bom Custo\|Bom Custo Papelaria\|62.391.927\|bomcustoilhabela" src/app/orcamento/page.tsx src/app/orcamento/novo/page.tsx "src/app/orcamento/[id]/page.tsx" 2>/dev/null | grep -v "node_modules" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Os 3 arquivos importam de "@/lib/empresa" (grep -rn 'from.*@/lib/empresa' src/app/orcamento/page.tsx src/app/orcamento/novo/page.tsx "src/app/orcamento/[id]/page.tsx" deve retornar 3 linhas)
    - Nenhum dos 3 arquivos contém "/media/logo-primary.png" hardcoded (grep -rn 'logo-primary.png' src/app/orcamento/ deve retornar 0)
    - Nenhum dos 3 arquivos contém "Bom Custo" ou "BomCusto" como string literal (grep -rn '"Bom Custo\|"BomCusto' src/app/orcamento/page.tsx src/app/orcamento/novo/page.tsx "src/app/orcamento/[id]/page.tsx" deve retornar 0)
    - Nenhum dos 3 arquivos contém "62.391.927" hardcoded (grep -rn '62.391.927' src/app/orcamento/ deve retornar 0)
    - Nenhum dos 3 arquivos contém "bomcustoilhabela.com.br" hardcoded (grep -rn 'bomcustoilhabela' src/app/orcamento/ deve retornar 0)
    - Os 3 arquivos usam EMPRESA_CNPJ com renderização condicional — grep '{EMPRESA_CNPJ &&' em cada arquivo deve retornar 1 linha
    - Os 3 arquivos usam EMPRESA_ENDERECO com renderização condicional — grep '{EMPRESA_ENDERECO &&' em cada arquivo deve retornar 1 linha (consolidado — não 2 divs separadas)
  </acceptance_criteria>
  <done>As 3 páginas de orçamento exibem logo, nome, CNPJ, endereço e email de empresa.ts; nenhuma string BomCusto hardcoded permanece</done>
</task>

<task type="auto">
  <name>Task 2: Dehardcode logo nas 2 páginas de contas-receber</name>
  <files>
    apps/frontend/src/app/contas-receber/page.tsx,
    apps/frontend/src/app/contas-receber/[idcliente]/page.tsx
  </files>
  <read_first>
    - apps/frontend/src/app/contas-receber/page.tsx (ler linhas 100–120: bloco com img do logo; verificar imports existentes no topo do arquivo)
    - apps/frontend/src/app/contas-receber/[idcliente]/page.tsx (ler linhas 605–620: bloco com img do logo)
    - apps/frontend/src/lib/empresa.ts (confirmar exports disponíveis)
    - .planning/phases/36-frontend-white-label/36-RESEARCH.md (seção "contas-receber/page.tsx" e "[idcliente]/page.tsx" — somente logo, sem outros dados de empresa)
  </read_first>
  <action>
    Para cada um dos 2 arquivos, adicionar import de empresa.ts e substituir os valores hardcoded.

    Estas páginas têm APENAS o logo hardcoded (sem nome, CNPJ, endereço ou email da empresa — confirmado no RESEARCH.md). A modificação é mínima.

    IMPORT a adicionar (início do arquivo, após imports existentes):
    `import { EMPRESA_NOME, EMPRESA_LOGO_URL } from "@/lib/empresa";`

    Substituições JSX para contas-receber/page.tsx (linha ~108–109):
    - `src="/media/logo-primary.png"` → `src={EMPRESA_LOGO_URL}`
    - `alt="Logo Bom Custo"` → `alt={EMPRESA_NOME}`

    Substituições JSX para contas-receber/[idcliente]/page.tsx (linha ~611–612):
    - `src="/media/logo-primary.png"` → `src={EMPRESA_LOGO_URL}`
    - `alt="Logo Bom Custo"` → `alt={EMPRESA_NOME}`

    Preservar todos os outros atributos do elemento img (className, style, etc.) exatamente como estão.

    PROIBIDO: não adicionar EMPRESA_NOME como texto visível no header destas páginas se não estava lá antes. RESEARCH.md confirma que estas páginas não têm nome/CNPJ/endereço hardcoded — somente o logo. Apenas logo URL e alt text mudam.
  </action>
  <verify>
    <automated>cd apps/frontend && grep -rn "logo-primary.png\|Logo Bom Custo" src/app/contas-receber/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Ambos os arquivos importam de "@/lib/empresa" (grep -rn 'from.*@/lib/empresa' src/app/contas-receber/ deve retornar 2 linhas)
    - Nenhum dos 2 arquivos contém "/media/logo-primary.png" hardcoded (grep -rn 'logo-primary.png' src/app/contas-receber/ deve retornar 0)
    - Nenhum dos 2 arquivos contém 'alt="Logo Bom Custo"' (grep -rn '"Logo Bom Custo"' src/app/contas-receber/ deve retornar 0)
    - Ambos os arquivos usam EMPRESA_LOGO_URL no atributo src (grep -rn 'EMPRESA_LOGO_URL' src/app/contas-receber/ deve retornar 2 linhas)
    - Ambos os arquivos usam EMPRESA_NOME no atributo alt (grep -rn 'alt={EMPRESA_NOME}' src/app/contas-receber/ deve retornar 2 linhas)
  </acceptance_criteria>
  <done>As 2 páginas de contas-receber exibem logo de empresa.ts; nenhuma string hardcoded permanece</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NEXT_PUBLIC_EMPRESA_LOGO_URL → img src | URL da logo vinda de env var é renderizada em atributo src do img |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Information Disclosure | EMPRESA_CNPJ/ENDERECO/EMAIL no bundle JS | accept | Por design: NEXT_PUBLIC_* são públicas. CNPJ é dado público. Email de contato da empresa é público. Nenhum segredo exposto. |
| T-36-04 | Tampering | EMPRESA_LOGO_URL → img src | accept | Quem controla .env.local já tem acesso total ao servidor. URL vai para atributo src — sem execução de script. Fallback para logo interno garante que header nunca fica vazio. |
</threat_model>

<verification>
Após execução de ambas as tarefas:

1. `cd apps/frontend && grep -rn "logo-primary.png" src/app/orcamento/ src/app/contas-receber/` deve retornar 0 resultados
2. `cd apps/frontend && grep -rn "Bom Custo\|BomCusto\|62.391.927\|bomcustoilhabela" src/app/orcamento/ src/app/contas-receber/` deve retornar 0 resultados
3. `cd apps/frontend && grep -rn 'from.*@/lib/empresa' src/app/orcamento/page.tsx src/app/orcamento/novo/page.tsx "src/app/orcamento/[id]/page.tsx" src/app/contas-receber/` deve retornar 5 linhas
</verification>

<success_criteria>
- 5 páginas internas importam EMPRESA_LOGO_URL (e demais vars conforme cada página) de @/lib/empresa
- Nenhum dos 5 arquivos contém /media/logo-primary.png, "Bom Custo", "BomCusto", "62.391.927" ou "bomcustoilhabela" como string literal
- CNPJ, endereço e email usam renderização condicional nos 3 arquivos de orçamento
- Endereço consolidado em 1 div (não 2 divs separadas)
- Dimensões de logo preservadas exatamente como estavam em cada arquivo
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Type |
|--------|------|------|
| import empresa.ts | apps/frontend/src/app/orcamento/page.tsx | import added |
| import empresa.ts | apps/frontend/src/app/orcamento/novo/page.tsx | import added |
| import empresa.ts | apps/frontend/src/app/orcamento/[id]/page.tsx | import added |
| import empresa.ts | apps/frontend/src/app/contas-receber/page.tsx | import added |
| import empresa.ts | apps/frontend/src/app/contas-receber/[idcliente]/page.tsx | import added |

<output>
Create `.planning/phases/36-frontend-white-label/36-02-SUMMARY.md` when done
</output>
