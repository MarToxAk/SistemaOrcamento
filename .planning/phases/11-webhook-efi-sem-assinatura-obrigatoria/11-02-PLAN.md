---
phase: "11"
plan: "02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - apps/backend/src/modules/integrations/efi/efi.webhook.test.ts
  - apps/backend/src/modules/integrations/efi/efi.controller.ts
  - apps/backend/src/modules/integrations/efi/efi.service.ts
autonomous: true
requirements:
  - EFIW-02
  - EFIW-03

must_haves:
  truths:
    - "Processamento sem assinatura continua funcional e auditavel"
    - "Idempotencia por eventId/externalId continua protegendo contra duplicidade"
    - "Metadados de transacao registram assinatura ausente como null sem quebrar fluxo"
  artifacts:
    - path: "apps/backend/src/modules/integrations/efi/efi.webhook.test.ts"
      provides: "Cobertura de cenarios sem assinatura e duplicidade"
      contains: "ignored_duplicate"
    - path: "apps/backend/src/modules/integrations/efi/efi.service.ts"
      provides: "Persistencia de metadata.signature e contadores de processamento"
      contains: "metadata"
    - path: "apps/backend/src/modules/integrations/efi/efi.controller.ts"
      provides: "Encaminhamento de assinatura opcional para processWebhook"
      contains: "processWebhook(payload, signature ?? gnSignature)"
  key_links:
    - from: "apps/backend/src/modules/integrations/efi/efi.controller.ts"
      to: "apps/backend/src/modules/integrations/efi/efi.service.ts"
      via: "assinatura opcional repassada como parametro"
      pattern: "signature \?\? gnSignature"
    - from: "apps/backend/src/modules/integrations/efi/efi.service.ts"
      to: "apps/backend/src/modules/integrations/efi/efi.webhook.test.ts"
      via: "testes validam comportamento de processamento e auditoria"
      pattern: "processWebhook"
---

<objective>
Fortalecer cobertura de testes e validar que a mudanca de assinatura opcional nao degrada idempotencia nem trilha de auditoria do webhook EFI.

Purpose: Atender EFIW-02 e EFIW-03 com evidencias automatizadas de comportamento apos remocao da obrigatoriedade de assinatura.
Output: Suite de testes do webhook atualizada e validada no backend.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-webhook-efi-sem-assinatura-obrigatoria/11-CONTEXT.md
@.planning/phases/11-webhook-efi-sem-assinatura-obrigatoria/11-RESEARCH.md
@apps/backend/src/modules/integrations/efi/efi.service.ts
@apps/backend/src/modules/integrations/efi/efi.controller.ts
@apps/backend/src/modules/integrations/efi/efi.webhook.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar testes para assinatura opcional e trilha de auditoria no webhook EFI</name>
  <read_first>apps/backend/src/modules/integrations/efi/efi.webhook.test.ts, apps/backend/src/modules/integrations/efi/efi.service.ts</read_first>
  <files>apps/backend/src/modules/integrations/efi/efi.webhook.test.ts</files>
  <action>No arquivo `apps/backend/src/modules/integrations/efi/efi.webhook.test.ts`, adicionar testes cobrindo: (1) processamento de pagamento sem assinatura (`processWebhook(payload, undefined)`) com resultado `received=true` sem erro; (2) persistencia de `metadata.signature` como `null` na transacao quando assinatura ausente (mockando `prisma.$transaction` para inspecionar o payload de create); (3) idempotencia mantida para evento duplicado sem assinatura (status `ignored_duplicate`). Reutilizar o padrao de mocks ja presente no arquivo para nao criar infraestrutura nova de testes.</action>
  <acceptance_criteria>
    - `apps/backend/src/modules/integrations/efi/efi.webhook.test.ts` contem ao menos 1 teste com `processWebhook(` e `undefined` como assinatura
    - `apps/backend/src/modules/integrations/efi/efi.webhook.test.ts` contem assercao sobre `metadata.signature` igual a `null`
    - `apps/backend/src/modules/integrations/efi/efi.webhook.test.ts` contem teste de duplicidade com `ignored_duplicate` no cenario sem assinatura
  </acceptance_criteria>
  <verify>
    <automated>rg -n "processWebhook\(.*undefined|metadata\.signature|ignored_duplicate" apps/backend/src/modules/integrations/efi/efi.webhook.test.ts</automated>
  </verify>
  <done>Cobertura automatizada comprova assinatura opcional sem perda de auditoria e idempotencia.</done>
</task>

<task type="auto">
  <name>Task 2: Executar e validar suite de testes do webhook EFI apos mudanca</name>
  <read_first>apps/backend/package.json, apps/backend/jest.config.js, apps/backend/src/modules/integrations/efi/efi.webhook.test.ts</read_first>
  <files>apps/backend/src/modules/integrations/efi/efi.webhook.test.ts</files>
  <action>Executar no workspace backend o comando `npm --workspace @bomcusto/backend run test -- efi.webhook.test.ts --runInBand` e corrigir eventuais falhas relacionadas a mocks/expectativas introduzidas pelos novos testes. Se necessario, ajustar pontualmente assertions no proprio arquivo de teste para alinhar ao contrato atual de `processWebhook` (contadores `processed`, `duplicates`, `notFound`, `results`).</action>
  <acceptance_criteria>
    - comando de teste do arquivo `efi.webhook.test.ts` finaliza com sucesso
    - nenhum teste existente da suite de webhook foi removido para "fazer passar"
    - suite cobre explicitamente cenario sem assinatura
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace @bomcusto/backend run test -- efi.webhook.test.ts --runInBand</automated>
  </verify>
  <done>Suite do webhook verde e aderente aos requisitos de resiliencia/auditoria da fase 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness -> service de webhook | Mocks devem representar corretamente persistencia e idempotencia |
| Endpoint publico -> tabela de transacoes | Auditoria de eventos depende de dados persistidos no metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Repudiation | trilha de auditoria do webhook | mitigate | Teste explicito garante `metadata.signature` mesmo quando null |
| T-11-05 | Denial | regressao de idempotencia | mitigate | Testes de duplicidade cobrem `ignored_duplicate` sem assinatura |
| T-11-06 | Tampering | inconsistencias de contrato em refactor | mitigate | Suite valida campos chave de retorno (`processed`, `duplicates`, `results`) |
</threat_model>

<verification>
- Testes cobrem assinatura ausente, auditoria e idempotencia
- Suite de webhook executa com sucesso apos mudancas
- Contrato de retorno de processWebhook permanece consistente
</verification>

<success_criteria>
- Requisito EFIW-03 atendido com evidencias de persistencia/auditoria
- Requisito EFIW-02 reforcado com cobertura de resiliencia (duplicidade)
- Fase 11 pronta para execucao com risco reduzido de regressao
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/11-webhook-efi-sem-assinatura-obrigatoria/11-02-SUMMARY.md`
</output>
