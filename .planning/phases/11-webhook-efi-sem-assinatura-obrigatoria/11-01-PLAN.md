---
phase: "11"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/efi/efi.controller.ts
  - apps/backend/src/modules/app.module.ts
  - apps/backend/src/modules/security/efi-webhook.guard.ts
autonomous: true
requirements:
  - EFIW-01
  - EFIW-02

must_haves:
  truths:
    - "Endpoints de webhook EFI aceitam requests sem x-signature/x-gn-signature"
    - "Throttle de webhook permanece ativo apos remocao de assinatura obrigatoria"
    - "API nao falha bootstrap por ausencia de EFI_WEBHOOK_SECRET"
  artifacts:
    - path: "apps/backend/src/modules/integrations/efi/efi.controller.ts"
      provides: "Endpoints webhook sem bloqueio de guard HMAC"
      contains: "@Post(\"webhook/payment\")"
    - path: "apps/backend/src/modules/app.module.ts"
      provides: "Lista de env vars obrigatorias sem EFI_WEBHOOK_SECRET"
      contains: "REQUIRED_ENV_VARS"
    - path: "apps/backend/src/modules/security/efi-webhook.guard.ts"
      provides: "Sem rejeicao hard para assinatura ausente"
      contains: "Missing webhook signature"
  key_links:
    - from: "apps/backend/src/modules/integrations/efi/efi.controller.ts"
      to: "apps/backend/src/modules/integrations/efi/efi.service.ts"
      via: "handleWebhook/handleWebhookPix chamam processWebhook"
      pattern: "processWebhook"
    - from: "apps/backend/src/modules/app.module.ts"
      to: "apps/backend/src/modules/integrations/efi/efi.controller.ts"
      via: "bootstrap da API nao bloqueia endpoint publico"
      pattern: "REQUIRED_ENV_VARS"
---

<objective>
Remover o bloqueio de assinatura HMAC obrigatoria nos webhooks EFI sem quebrar resiliencia operacional minima.

Purpose: Atender EFIW-01 e EFIW-02, liberando recebimento de notificacoes de pagamento em cenarios onde assinatura nao e enviada/configurada.
Output: Controller e bootstrap ajustados para aceitar webhook sem assinatura obrigatoria.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-webhook-efi-sem-assinatura-obrigatoria/11-CONTEXT.md
@.planning/phases/11-webhook-efi-sem-assinatura-obrigatoria/11-RESEARCH.md
@apps/backend/src/modules/integrations/efi/efi.controller.ts
@apps/backend/src/modules/security/efi-webhook.guard.ts
@apps/backend/src/modules/app.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover exigencia de guard de assinatura dos endpoints de webhook EFI</name>
  <read_first>apps/backend/src/modules/integrations/efi/efi.controller.ts, apps/backend/src/modules/security/throttle.config.ts</read_first>
  <files>apps/backend/src/modules/integrations/efi/efi.controller.ts</files>
  <action>No arquivo `apps/backend/src/modules/integrations/efi/efi.controller.ts`, remover o import de `EfiWebhookGuard` e remover `@UseGuards(EfiWebhookGuard)` dos metodos `handleWebhook` e `handleWebhookPix`. Manter `@Public()`, `@Throttle({ default: THROTTLE_WEBHOOK })`, `@Post(...)` e `@HttpCode(200)` exatamente como estao para preservar comportamento de endpoint publico com rate limiting.</action>
  <acceptance_criteria>
    - `apps/backend/src/modules/integrations/efi/efi.controller.ts` nao contem `UseGuards`
    - `apps/backend/src/modules/integrations/efi/efi.controller.ts` nao contem `EfiWebhookGuard`
    - `apps/backend/src/modules/integrations/efi/efi.controller.ts` contem `@Throttle({ default: THROTTLE_WEBHOOK })` nos dois handlers de webhook
    - `apps/backend/src/modules/integrations/efi/efi.controller.ts` contem `@Post("webhook/payment")` e `@Post("webhook/payment/pix")`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "UseGuards|EfiWebhookGuard|@Throttle\(\{ default: THROTTLE_WEBHOOK \}\)|@Post\(\"webhook/payment\"\)|@Post\(\"webhook/payment/pix\"\)" apps/backend/src/modules/integrations/efi/efi.controller.ts</automated>
  </verify>
  <done>Webhook EFI deixa de depender de assinatura para passar pelo controller, mantendo throttle ativo.</done>
</task>

<task type="auto">
  <name>Task 2: Ajustar fail-fast de ambiente e comportamento do guard para assinatura opcional</name>
  <read_first>apps/backend/src/modules/app.module.ts, apps/backend/src/modules/security/efi-webhook.guard.ts</read_first>
  <files>apps/backend/src/modules/app.module.ts, apps/backend/src/modules/security/efi-webhook.guard.ts</files>
  <action>No `apps/backend/src/modules/app.module.ts`, remover `EFI_WEBHOOK_SECRET` de `REQUIRED_ENV_VARS` para nao bloquear startup quando segredo nao estiver configurado. No `apps/backend/src/modules/security/efi-webhook.guard.ts`, tornar a assinatura opcional: se header de assinatura estiver ausente, retornar `true` sem `UnauthorizedException`; se assinatura estiver presente e `EFI_WEBHOOK_SECRET` tambem estiver presente, validar HMAC como hoje; se assinatura estiver presente e segredo ausente, nao bloquear request e apenas retornar `true` (sem excecao). Manter mensagens de excecao apenas para caso de assinatura presente e invalida quando segredo existir.</action>
  <acceptance_criteria>
    - `apps/backend/src/modules/app.module.ts` nao contem `EFI_WEBHOOK_SECRET` em `REQUIRED_ENV_VARS`
    - `apps/backend/src/modules/security/efi-webhook.guard.ts` nao lanca `Missing webhook signature`
    - `apps/backend/src/modules/security/efi-webhook.guard.ts` mantem caminho de validacao com `createHmac("sha256", secret)`
    - `apps/backend/src/modules/security/efi-webhook.guard.ts` mantem `Invalid webhook signature` para assinatura invalida com segredo configurado
  </acceptance_criteria>
  <verify>
    <automated>rg -n "EFI_WEBHOOK_SECRET|Missing webhook signature|createHmac\(|Invalid webhook signature" apps/backend/src/modules/app.module.ts apps/backend/src/modules/security/efi-webhook.guard.ts</automated>
  </verify>
  <done>Aplicacao sobe sem segredo EFI obrigatorio e guard fica compativel com assinatura opcional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet publica -> endpoint webhook EFI | Endpoint recebe payload sem autenticacao forte de origem |
| Endpoint webhook -> persistencia de transacoes | Eventos validos e invalidos impactam estado de pagamento e trilha de auditoria |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | webhook EFI sem assinatura obrigatoria | mitigate | Manter throttle e idempotencia para reduzir impacto de requests maliciosos/repetidos |
| T-11-02 | Denial | endpoint publico de webhook | mitigate | Limite de taxa em `THROTTLE_WEBHOOK` permanece ativo |
| T-11-03 | Tampering | payload forjado | accept | Assinatura deixa de ser gate obrigatorio nesta fase; controles complementares serao tratados em hardening futuro |
</threat_model>

<verification>
- Webhook EFI aceita requests sem assinatura no controller
- Bootstrap da API nao exige EFI_WEBHOOK_SECRET
- Guard nao bloqueia requests sem assinatura
- Build de backend permanece valido
</verification>

<success_criteria>
- Requisito EFIW-01 atendido com aceite sem assinatura
- Requisito EFIW-02 atendido com throttle mantido
- Endpoint permanece estavel para processamento de notificacoes reais
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/11-webhook-efi-sem-assinatura-obrigatoria/11-01-SUMMARY.md`
</output>
