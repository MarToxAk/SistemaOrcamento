---
phase: "09"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/Dockerfile
  - apps/backend/package.json
  - apps/backend/scripts/bootstrap-runtime.sh
  - apps/backend/scripts/wait-for-db.js
autonomous: true
requirements:
  - MIG-01
  - MIG-02

must_haves:
  truths:
    - "Atualizacao do backend em container so aplica migration depois de o banco aceitar conexao"
    - "Fluxo de bootstrap nao quebra quando nao ha migrations pendentes"
    - "Erro de indisponibilidade do banco para startup e claramente identificavel"
  artifacts:
    - path: "apps/backend/scripts/wait-for-db.js"
      provides: "Readiness check com timeout para PostgreSQL"
      contains: "WAIT_FOR_DB_TIMEOUT_MS"
    - path: "apps/backend/scripts/bootstrap-runtime.sh"
      provides: "Sequenciamento de wait -> generate -> deploy -> start"
      contains: "prisma migrate deploy"
    - path: "apps/backend/Dockerfile"
      provides: "CMD runtime apontando para bootstrap-runtime.sh"
      contains: "bootstrap-runtime.sh"
  key_links:
    - from: "bootstrap-runtime.sh"
      to: "wait-for-db.js"
      via: "node apps/backend/scripts/wait-for-db.js"
      pattern: "wait-for-db\.js"
    - from: "bootstrap-runtime.sh"
      to: "prisma migrate deploy"
      via: "npm workspace script"
      pattern: "prisma:deploy"
---

<objective>
Implementar bootstrap deterministico no backend para esperar o Postgres antes de rodar `prisma migrate deploy`, garantindo idempotencia no update do Docker Compose.

Purpose: Evitar falhas intermitentes de migration causadas por corrida de startup entre backend e banco.
Output: Script de bootstrap + readiness check ligados ao CMD do container backend.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-fluxo-de-migration-idempotente/09-CONTEXT.md
@.planning/phases/09-fluxo-de-migration-idempotente/09-RESEARCH.md
@apps/backend/Dockerfile
@apps/backend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar readiness check de Postgres com timeout e erro acionavel</name>
  <read_first>apps/backend/package.json, apps/backend/Dockerfile</read_first>
  <files>apps/backend/scripts/wait-for-db.js</files>
  <action>Adicionar `apps/backend/scripts/wait-for-db.js` usando `pg` para tentar conexao em loop ate sucesso ou timeout. Usar variaveis concretas: `WAIT_FOR_DB_TIMEOUT_MS` (default 120000), `WAIT_FOR_DB_INTERVAL_MS` (default 2000), `DATABASE_URL` obrigatoria. Em timeout, imprimir erro com prefixo `DB_READINESS_FAILED` e instruir revisar `DATABASE_URL`, conectividade de rede e logs do servico postgres.</action>
  <acceptance_criteria>
    - Arquivo `apps/backend/scripts/wait-for-db.js` existe
    - Arquivo contem string `DB_READINESS_FAILED`
    - Arquivo contem `WAIT_FOR_DB_TIMEOUT_MS`
    - Script encerra com `process.exit(1)` no timeout
  </acceptance_criteria>
  <verify>
    <automated>rg -n "DB_READINESS_FAILED|WAIT_FOR_DB_TIMEOUT_MS|process\.exit\(1\)" apps/backend/scripts/wait-for-db.js</automated>
  </verify>
  <done>Readiness check criado com timeout configuravel, retry interval e log tecnico acionavel.</done>
</task>

<task type="auto">
  <name>Task 2: Encadear bootstrap runtime para wait, migrate deploy e start</name>
  <read_first>apps/backend/Dockerfile, apps/backend/package.json, apps/backend/scripts/wait-for-db.js</read_first>
  <files>apps/backend/scripts/bootstrap-runtime.sh, apps/backend/Dockerfile, apps/backend/package.json</files>
  <action>Criar `apps/backend/scripts/bootstrap-runtime.sh` com `set -eu` e ordem fixa: `node apps/backend/scripts/wait-for-db.js`, `npm --workspace @bomcusto/backend run prisma:generate -- --schema prisma/schema.prisma`, `npm --workspace @bomcusto/backend run prisma:deploy -- --schema prisma/schema.prisma`, `npm --workspace @bomcusto/backend run start`. Atualizar `apps/backend/Dockerfile` para copiar a pasta `apps/backend/scripts` no stage runtime e trocar `CMD` para executar `sh ./apps/backend/scripts/bootstrap-runtime.sh`. Ajustar permissao de execucao do script no Dockerfile com `chmod +x`.</action>
  <acceptance_criteria>
    - `apps/backend/scripts/bootstrap-runtime.sh` contem `set -eu`
    - `apps/backend/scripts/bootstrap-runtime.sh` contem `wait-for-db.js` antes de `prisma:deploy`
    - `apps/backend/Dockerfile` contem `COPY --from=build /app/apps/backend/scripts ./apps/backend/scripts`
    - `apps/backend/Dockerfile` contem `chmod +x ./apps/backend/scripts/bootstrap-runtime.sh`
    - `apps/backend/Dockerfile` usa CMD apontando para `bootstrap-runtime.sh`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "bootstrap-runtime\.sh|wait-for-db\.js|prisma:deploy|chmod \+x" apps/backend/Dockerfile apps/backend/scripts/bootstrap-runtime.sh</automated>
  </verify>
  <done>Backend passa a iniciar com fluxo deterministico de readiness e migration idempotente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container backend -> PostgreSQL | Conexao depende de credenciais e disponibilidade de rede interna |
| Runtime script -> env vars | DATABASE_URL e variaveis de timeout entram como input nao confiavel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Denial | wait-for-db.js | mitigate | Timeout configuravel com erro explicito evita loop infinito e falha silenciosa |
| T-09-02 | Tampering | bootstrap-runtime.sh | mitigate | Ordem fixa de comandos + `set -eu` reduz estado parcial e comandos ignorados |
| T-09-03 | Information | logs startup | accept | Logs nao devem incluir segredos; exibir apenas causa tecnica generica |
</threat_model>

<verification>
- Build da imagem backend conclui com scripts copiados no runtime
- Startup do backend executa wait-for-db antes de migration
- `prisma migrate deploy` continua sendo o comando de migration em producao
</verification>

<success_criteria>
- Bootstrap runtime foi externalizado e versionado
- Readiness check executa antes de migrations
- Fluxo e idempotente para updates com schema ja aplicado
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/09-fluxo-de-migration-idempotente/09-01-SUMMARY.md`
</output>
