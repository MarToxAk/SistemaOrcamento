---
phase: "09"
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - deploy/docker-compose.vps.yml
  - deploy/stack.env.example
  - .planning/STATE.md
autonomous: true
requirements:
  - MIG-01
  - MIG-02
  - MIG-03

must_haves:
  truths:
    - "Backend em producao so inicia quando o Postgres estiver healthy"
    - "Configuracao de timeout/retry do readiness check e declarada em ambiente"
    - "Falhas de startup por banco indisponivel ficam distinguiveis no fluxo operacional"
  artifacts:
    - path: "deploy/docker-compose.vps.yml"
      provides: "Healthcheck do Postgres e dependencia condicional do backend"
      contains: "healthcheck"
    - path: "deploy/stack.env.example"
      provides: "Variaveis WAIT_FOR_DB_TIMEOUT_MS e WAIT_FOR_DB_INTERVAL_MS"
      contains: "WAIT_FOR_DB_TIMEOUT_MS"
  key_links:
    - from: "deploy/docker-compose.vps.yml"
      to: "backend startup"
      via: "depends_on com condition: service_healthy"
      pattern: "service_healthy"
    - from: "deploy/stack.env.example"
      to: "wait-for-db.js"
      via: "env vars injetadas no container backend"
      pattern: "WAIT_FOR_DB_TIMEOUT_MS"
---

<objective>
Fechar o fluxo de fase 9 no nivel de orquestracao da stack VPS, adicionando healthcheck do Postgres e variaveis de controle do readiness check para reduzir falhas de migration em update.

Purpose: Evitar corrida de startup e tornar diagnostico de falhas mais rapido durante atualizacoes.
Output: Compose VPS com gate de healthcheck + env vars de readiness documentadas.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-fluxo-de-migration-idempotente/09-CONTEXT.md
@.planning/phases/09-fluxo-de-migration-idempotente/09-RESEARCH.md
@deploy/docker-compose.vps.yml
@deploy/stack.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar healthcheck no Postgres e gate de dependencias no backend</name>
  <read_first>deploy/docker-compose.vps.yml</read_first>
  <files>deploy/docker-compose.vps.yml</files>
  <action>No servico `postgres` de `deploy/docker-compose.vps.yml`, adicionar bloco `healthcheck` com comando `pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}`, `interval: 10s`, `timeout: 5s`, `retries: 6`, `start_period: 15s`. No servico `backend`, trocar `depends_on` para formato com condition: `postgres: { condition: service_healthy }` e manter dependencia de `ts-webserver2` como `service_started`.</action>
  <acceptance_criteria>
    - `deploy/docker-compose.vps.yml` contem `healthcheck:` no servico postgres
    - `deploy/docker-compose.vps.yml` contem `pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}`
    - `deploy/docker-compose.vps.yml` contem `condition: service_healthy` para postgres no backend
  </acceptance_criteria>
  <verify>
    <automated>rg -n "healthcheck|pg_isready|service_healthy" deploy/docker-compose.vps.yml</automated>
  </verify>
  <done>Compose VPS passa a aguardar prontidao real do Postgres antes de subir backend.</done>
</task>

<task type="auto">
  <name>Task 2: Declarar env vars de readiness no stack env e registrar contexto operacional</name>
  <read_first>deploy/stack.env.example, .planning/STATE.md</read_first>
  <files>deploy/stack.env.example, .planning/STATE.md</files>
  <action>Adicionar em `deploy/stack.env.example` as variaveis `WAIT_FOR_DB_TIMEOUT_MS=120000` e `WAIT_FOR_DB_INTERVAL_MS=2000` na secao backend. Atualizar `.planning/STATE.md` em `Active Context` com nota breve de que a fase 9 planeja readiness gate + migration idempotente no runtime Docker para resolver falha em updates de compose.</action>
  <acceptance_criteria>
    - `deploy/stack.env.example` contem `WAIT_FOR_DB_TIMEOUT_MS=120000`
    - `deploy/stack.env.example` contem `WAIT_FOR_DB_INTERVAL_MS=2000`
    - `.planning/STATE.md` contem texto sobre fase 9 e readiness gate no Docker
  </acceptance_criteria>
  <verify>
    <automated>rg -n "WAIT_FOR_DB_TIMEOUT_MS|WAIT_FOR_DB_INTERVAL_MS|readiness gate|migration idempotente" deploy/stack.env.example .planning/STATE.md</automated>
  </verify>
  <done>Parametros operacionais de readiness ficam explicitamente configuraveis e rastreados no estado do projeto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker Compose -> PostgreSQL container | Status healthy decide quando backend pode iniciar |
| Environment configuration -> backend bootstrap | Timeout/retry controlam comportamento de disponibilidade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Denial | deploy/docker-compose.vps.yml | mitigate | Gate `service_healthy` reduz falha por startup antecipado do backend |
| T-09-05 | Tampering | deploy/stack.env.example | accept | Valores em exemplo sao defaults; segredo segue fora de codigo |
| T-09-06 | Repudiation | logs de update | mitigate | estado de fase e env vars documentados para auditoria de operacao |
</threat_model>

<verification>
- Compose VPS contem healthcheck de Postgres e dependencias condicionais
- Env example contem variaveis de timeout/retry do readiness check
- Estado do projeto registra escopo operacional da fase 9
</verification>

<success_criteria>
- Orquestracao de startup foi endurecida no compose VPS
- Parametros de readiness estao explicitados no ambiente
- Diagnostico de falha de migration fica mais rapido por separacao readiness/migration
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/09-fluxo-de-migration-idempotente/09-02-SUMMARY.md`
</output>
