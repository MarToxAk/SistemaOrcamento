---
phase: 31-hist-rico-nfs-e-consulta-nf-athos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/athos/athos.service.ts
  - apps/backend/src/modules/integrations/athos/athos.controller.ts
  - apps/backend/src/modules/integrations/athos/athos-notas-fiscais.test.ts
autonomous: true
requirements: [NFAT-01, NFAT-02]
must_haves:
  truths:
    - "Backend consulta no Athos as notas fiscais (NF-e nao-servico) de um cliente via query read-only"
    - "A lista retorna no maximo 50 notas, ordenadas por dataemissao desc, apenas notas ativas"
    - "Busca por numero exato filtra a consulta server-side no Athos (nao apenas no frontend)"
    - "Busca sem resultado retorna array vazio (nao erro)"
  artifacts:
    - path: "apps/backend/src/modules/integrations/athos/athos.service.ts"
      provides: "Metodo buscarNotasFiscaisCliente(idcliente, numero?)"
      contains: "buscarNotasFiscaisCliente"
    - path: "apps/backend/src/modules/integrations/athos/athos.controller.ts"
      provides: "Rota GET /athos/clientes/:idcliente/notas-fiscais"
      contains: "clientes/:idcliente/notas-fiscais"
    - path: "apps/backend/src/modules/integrations/athos/athos-notas-fiscais.test.ts"
      provides: "Testes unitarios de buscarNotasFiscaisCliente"
      contains: "buscarNotasFiscaisCliente"
  key_links:
    - from: "athos.controller.ts"
      to: "athos.service.ts"
      via: "this.athosService.buscarNotasFiscaisCliente"
      pattern: "buscarNotasFiscaisCliente"
    - from: "athos.service.ts"
      to: "venda JOIN venda_nota JOIN nota"
      via: "query SQL read-only ao Athos"
      pattern: "venda_nota"
---

<objective>
Adicionar a consulta de notas fiscais nao-servico (NF-e) de um cliente no Athos via query read-only, com listagem de ate 50 registros e filtro opcional por numero exato (server-side). Expoe novo metodo no `AthosService` e nova rota no `AthosController` existente. Atende NFAT-01 (lista) e NFAT-02 (busca por numero) — D-09 a D-16.

Purpose: O operador precisa consultar NF-e de produto registradas no Athos para o cliente, para referencia/conciliacao, e localizar uma nota especifica por numero. Athos e read-only: somente SELECT.
Output: Metodo `buscarNotasFiscaisCliente(idcliente, numero?)`, rota `GET /athos/clientes/:idcliente/notas-fiscais?numero=X` e testes unitarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-hist-rico-nfs-e-consulta-nf-athos/31-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar AthosService.buscarNotasFiscaisCliente(idcliente, numero?)</name>
  <files>apps/backend/src/modules/integrations/athos/athos.service.ts, apps/backend/src/modules/integrations/athos/athos-notas-fiscais.test.ts</files>
  <read_first>
    - apps/backend/src/modules/integrations/athos/athos.service.ts (metodo `verificarNFTitulos()` L1816-1862 — padrao de query `venda_nota JOIN nota` com `COALESCE(n.cancelada, false) = false` e `n.nfechaveacesso IS NOT NULL`; metodo `getPool()` L505; padrao try/finally com `client.release()`; conversao de Date para ISO em `buscarTitulosClienteContasReceber()` L1797-1800)
    - apps/backend/src/modules/integrations/athos/athos.service.test.ts (padrao de mock do modulo `pg`: jest.mock("pg") + mock client.query; setup de env ATHOS_PG_*)
    - .planning/phases/31-hist-rico-nfs-e-consulta-nf-athos/31-CONTEXT.md (Claude's Discretion: estrutura SQL sugerida com `n.valornota` e `'NF-e' as tipo` — verificar campos exatos da instancia Athos)
  </read_first>
  <behavior>
    - Dado idcliente com 3 notas ativas, retorna array com 3 entradas ordenadas por dataemissao desc, cada uma com { numero, dataemissao (ISO YYYY-MM-DD), valor (Number), tipo ("NF-e") }.
    - Dado idcliente com mais de 50 notas, retorna no maximo 50 (LIMIT 50).
    - Dado numero="12345", a query usa filtro WHERE n.numero = $numero (match exato) e retorna apenas notas com aquele numero.
    - Dado numero sem correspondencia, retorna array vazio [].
    - Notas canceladas (COALESCE(n.cancelada, false) = true) sao excluidas.
  </behavior>
  <action>
    Criar metodo publico assincrono `buscarNotasFiscaisCliente(idcliente: number, numero?: string)` no `AthosService`, retornando `Promise<Array<{ numero: string; dataemissao: string | null; valor: number; tipo: string }>>`. Usar `this.getPool()` + `pool.connect()` + try/finally com `client.release()`, EXATAMENTE como `verificarNFTitulos()` (L1822-1861). Query base: SELECT a partir de `venda v JOIN venda_nota vn ON vn.idvenda = v.idvenda JOIN nota n ON n.idnota = vn.idnota WHERE v.idcliente = $1 AND COALESCE(n.cancelada, false) = false AND n.nfechaveacesso IS NOT NULL` (filtro de nota ativa nao-servico identico ao padrao de verificarNFTitulos). Selecionar `n.numero`, `n.dataemissao`, `n.valornota` (campo de valor da nota — verificar nome exato na instancia Athos; o CONTEXT sugere valornota) e a literal `'NF-e' as tipo`. Quando `numero` for fornecido (string nao vazia), adicionar `AND n.numero = $2` ao WHERE e passar `[idcliente, numero]` como params; caso contrario passar apenas `[idcliente]`. Adicionar `ORDER BY n.dataemissao DESC LIMIT 50` (D-11). Mapear cada row: `numero: String(row.numero ?? "").trim()`, `dataemissao` convertido para ISO YYYY-MM-DD seguindo o padrao instanceof Date check de buscarTitulosClienteContasReceber (L1797-1800), `valor: Number(row.valornota ?? 0)`, `tipo: "NF-e"`. Em caso de erro de query, logar com `this.logger.warn` e retornar `[]` (mesmo padrao defensivo de buscarTodasNfesParaTitulos L1899-1901) para nao quebrar a pagina. Criar `athos-notas-fiscais.test.ts` seguindo o mock de `pg` de athos.service.test.ts, cobrindo: (a) lista com 3 notas mapeadas e ordenadas; (b) com numero passa 2 params e filtra; (c) sem resultado retorna []; (d) limite de 50 presente no SQL.
  </action>
  <verify>
    <automated>cd apps/backend && npx jest athos-notas-fiscais --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "buscarNotasFiscaisCliente" apps/backend/src/modules/integrations/athos/athos.service.ts` retorna a definicao do metodo.
    - `grep -n "LIMIT 50" apps/backend/src/modules/integrations/athos/athos.service.ts` confirma o limite no novo metodo.
    - `grep -n "COALESCE(n.cancelada, false) = false" apps/backend/src/modules/integrations/athos/athos.service.ts` confirma filtro de notas ativas reutilizado.
    - `grep -n "venda_nota" apps/backend/src/modules/integrations/athos/athos.service.ts` confirma o JOIN venda_nota no novo metodo.
    - `npx jest athos-notas-fiscais` passa com os 4 cenarios (lista, filtro numero, vazio, limite).
  </acceptance_criteria>
  <done>Metodo buscarNotasFiscaisCliente implementado com query read-only ao Athos, filtro opcional por numero exato, limite 50, notas ativas, com testes passando.</done>
</task>

<task type="auto">
  <name>Task 2: Expor GET /athos/clientes/:idcliente/notas-fiscais no AthosController</name>
  <files>apps/backend/src/modules/integrations/athos/athos.controller.ts</files>
  <read_first>
    - apps/backend/src/modules/integrations/athos/athos.controller.ts (rota `titulosClienteContasReceber` L259-271 — padrao de validacao manual de idcliente: Number() + Number.isFinite check + BadRequestException; uso de `validateAthosToken(authorization, xApiToken)` no inicio; decorators Swagger @ApiOperation/@ApiParam/@ApiQuery/@ApiOkResponse/@ApiUnauthorizedResponse)
    - apps/backend/src/modules/integrations/athos/athos.service.ts (assinatura de buscarNotasFiscaisCliente criada na Task 1)
  </read_first>
  <action>
    Adicionar metodo no `AthosController` com `@Get("clientes/:idcliente/notas-fiscais")` (D-10). Assinatura: `@Param("idcliente") idcliente: string`, `@Query("numero") numero?: string`, `@Headers("authorization") authorization?: string`, `@Headers("x-api-token") xApiToken?: string`. No corpo: chamar `this.validateAthosToken(authorization, xApiToken)` primeiro; converter `const id = Number(idcliente)` e lancar `BadRequestException("idcliente invalido")` se `!Number.isFinite(id) || id <= 0` (padrao identico a `titulosClienteContasReceber` L266-269). Tratar `numero`: passar `numero?.trim() || undefined` para o service (string vazia vira undefined). Retornar `this.athosService.buscarNotasFiscaisCliente(id, numeroTratado)`. Adicionar decorators Swagger: `@ApiOperation({ summary: "Notas fiscais (NF-e) do cliente no Athos" })`, `@ApiParam({ name: "idcliente" })`, `@ApiQuery({ name: "numero", required: false })`, `@ApiOkResponse`, `@ApiUnauthorizedResponse` — seguindo o padrao das rotas vizinhas. Posicionar proximo a `dadosCadastraisClienteContasReceber` (L298) ou ao final da classe.
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc -p tsconfig.build.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "clientes/:idcliente/notas-fiscais" apps/backend/src/modules/integrations/athos/athos.controller.ts` retorna a definicao da rota.
    - `grep -n "buscarNotasFiscaisCliente" apps/backend/src/modules/integrations/athos/athos.controller.ts` confirma a chamada ao service.
    - `grep -n "validateAthosToken" apps/backend/src/modules/integrations/athos/athos.controller.ts` no novo metodo confirma autenticacao (fail-closed igual as outras rotas Athos).
    - `npx tsc -p tsconfig.build.json --noEmit` compila sem erros.
  </acceptance_criteria>
  <done>Rota GET /athos/clientes/:idcliente/notas-fiscais?numero=X exposta com validacao de token, valida idcliente e delega para buscarNotasFiscaisCliente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend Route Handler -> Backend NestJS | Requisicao autenticada via x-api-token (validateAthosToken na rota Athos) |
| Backend -> Banco Athos (Postgres read-only) | SELECT em venda/venda_nota/nota; idcliente e numero vem da requisicao |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering / Injection | parametros idcliente e numero na query SQL | mitigate | Query parametrizada ($1/$2) — nunca interpolar strings; idcliente validado por Number.isFinite antes da query |
| T-31-05 | Information Disclosure | GET /athos/clientes/:idcliente/notas-fiscais | mitigate | validateAthosToken com timingSafeEqual; rota nao e @Public(); fail-closed quando ATHOS_API_TOKEN ausente |
| T-31-06 | Tampering (integridade Athos) | banco Athos | mitigate | Apenas SELECT; nenhum INSERT/UPDATE/DELETE — Athos permanece read-only conforme regra do projeto |
| T-31-07 | Denial of Service | listagem de notas | mitigate | LIMIT 50 hardcoded (D-11) impede retorno de conjunto ilimitado |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependencia nova instalada nesta fase; nada a verificar |
</threat_model>

<verification>
- `npx jest athos-notas-fiscais` passa.
- `npx tsc -p tsconfig.build.json --noEmit` compila o backend sem erros.
- Query usa params posicionais e LIMIT 50; Athos somente leitura.
</verification>

<success_criteria>
1. `buscarNotasFiscaisCliente(idcliente, numero?)` consulta o Athos via SELECT parametrizado, retorna ate 50 notas ativas ordenadas por dataemissao desc.
2. Filtro por numero exato e aplicado server-side (WHERE n.numero = $2) quando numero informado.
3. `GET /athos/clientes/:idcliente/notas-fiscais?numero=X` exposto com autenticacao por token.
4. Busca sem resultado retorna array vazio; erro de query nao quebra o endpoint.
5. Build backend e testes passam.
</success_criteria>

<output>
Create `.planning/phases/31-hist-rico-nfs-e-consulta-nf-athos/31-02-SUMMARY.md` when done
</output>
