---
phase: 31-hist-rico-nfs-e-consulta-nf-athos
plan: 03
type: execute
wave: 2
depends_on: ["31-01", "31-02"]
files_modified:
  - apps/frontend/src/app/api/cobranca/nfse/cliente/[idcliente]/route.ts
  - apps/frontend/src/app/api/athos/clientes/[idcliente]/notas-fiscais/route.ts
  - apps/frontend/src/app/contas-receber/[idcliente]/page.tsx
autonomous: true
requirements: [NFR-05, NFAT-01, NFAT-02]
must_haves:
  truths:
    - "D-01: Secoes empilhadas com <hr> + cabecalho abaixo da tabela de titulos (sem abas)"
    - "D-02: Carregamento lazy via Intersection Observer — cada secao so busca dados ao entrar na viewport"
    - "D-03: Secoes colapsaveis com cabecalho clicavel, fechadas por padrao ao carregar"
    - "D-04: Estado vazio com mensagem cinza centralizada em cada secao"
    - "D-05: Secao NFS-e com colunas completas (Data | Nº NFS-e | Nº RPS | Valor | Titulos vinculados | download condicional quando linkNfse existe)"
    - "D-06: Botao Cancelar por linha reutiliza DELETE /api/cobranca/nfse/[id] (remove do banco local)"
    - "D-07: Dupla camada de aviso de cancelamento — tooltip no botao + aviso no modal de confirmacao"
    - "D-15: Lista de 50 NFAT permanece visivel quando ha resultado de busca (os dois conjuntos coexistem)"
    - "D-16: Resultado da busca NFAT exibido ACIMA da lista de 50, no topo da secao"
    - "Route Handler GET /api/cobranca/nfse/cliente/[idcliente] encaminha para o backend com x-internal-api-key"
    - "Route Handler GET /api/athos/clientes/[idcliente]/notas-fiscais?numero=X encaminha numero para o backend com x-api-token"
    - "Secao NFAT lista ate 50 notas e tem campo de busca por numero com botao Buscar"
  artifacts:
    - path: "apps/frontend/src/app/api/cobranca/nfse/cliente/[idcliente]/route.ts"
      provides: "Proxy GET historico NFS-e do cliente"
      contains: "backendFetch"
    - path: "apps/frontend/src/app/api/athos/clientes/[idcliente]/notas-fiscais/route.ts"
      provides: "Proxy GET notas fiscais Athos do cliente com filtro numero"
      contains: "notas-fiscais"
    - path: "apps/frontend/src/app/contas-receber/[idcliente]/page.tsx"
      provides: "Duas secoes colapsaveis com lazy load"
      contains: "IntersectionObserver"
  key_links:
    - from: "page.tsx (secao NFS-e)"
      to: "/api/cobranca/nfse/cliente/[idcliente]"
      via: "fetch ao entrar na viewport"
      pattern: "/api/cobranca/nfse/cliente"
    - from: "page.tsx (secao NFAT)"
      to: "/api/athos/clientes/[idcliente]/notas-fiscais"
      via: "fetch lista + fetch busca por numero"
      pattern: "/api/athos/clientes/.*notas-fiscais"
    - from: "page.tsx (botao Cancelar NFS-e)"
      to: "/api/cobranca/nfse/[id]"
      via: "DELETE existente da Phase 30"
      pattern: "method: \"DELETE\""
---

<objective>
Adicionar a camada frontend do historico/consulta: dois Route Handlers (proxy) e duas secoes colapsaveis com lazy load via Intersection Observer na pagina `/contas-receber/[idcliente]`. Implementa NFR-05 (secao NFS-e Emitidas, D-01..D-08), NFAT-01 (lista NF Athos, D-09..D-12) e NFAT-02 (busca por numero, D-13..D-16) no frontend.

Purpose: Tornar visivel ao operador, na propria pagina do cliente, o historico de NFS-e emitidas (banco proprio) e as NF-e do Athos, com cancelamento de NFS-e (banco local + tentativa SOAP via DELETE existente) e busca de NF por numero.
Output: 2 Route Handlers novos + page.tsx estendida com 2 secoes empilhadas, colapsaveis, lazy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-hist-rico-nfs-e-consulta-nf-athos/31-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar os dois Route Handlers (proxy NFS-e historico + notas-fiscais Athos)</name>
  <files>apps/frontend/src/app/api/cobranca/nfse/cliente/[idcliente]/route.ts, apps/frontend/src/app/api/athos/clientes/[idcliente]/notas-fiscais/route.ts</files>
  <read_first>
    - apps/frontend/src/app/api/cobranca/boleto/route.ts (padrao backendFetch + Content-Type — o backendFetch injeta x-internal-api-key automaticamente, NAO duplicar header)
    - apps/frontend/src/app/api/athos/contas-receber/cliente/[idcliente]/route.ts (padrao de Route Handler GET dinamico: params Promise + await, validacao Number, injecao de x-api-token via process.env.INTERNAL_API_KEY)
    - apps/frontend/src/app/api/cobranca/nfse/[id]/route.ts (padrao do DELETE existente que ja proxia para /cobranca/nfse/:id — NAO recriar, ja existe)
  </read_first>
  <action>
    Criar `app/api/cobranca/nfse/cliente/[idcliente]/route.ts` exportando `GET(_req, { params }: { params: Promise<{ idcliente: string }> })`. Fazer `const { idcliente } = await params`, validar `Number(idcliente)` (>0, finite) retornando 400 se invalido. Chamar `backendFetch(\`/cobranca/nfse/cliente/${id}\`, { method: "GET" })`. NAO adicionar x-internal-api-key manualmente (backendFetch ja injeta via internalHeaders — vide boleto/route.ts). Retornar `NextResponse.json(data, { status: res.status })` com fallback de erro identico ao padrao. Criar `app/api/athos/clientes/[idcliente]/notas-fiscais/route.ts` exportando `GET(req: NextRequest, { params }: { params: Promise<{ idcliente: string }> })`. Fazer `const { idcliente } = await params`, validar Number. Ler `const numero = req.nextUrl.searchParams.get("numero")`. Montar a URL backend `/athos/clientes/${id}/notas-fiscais` e anexar `?numero=${encodeURIComponent(numero)}` somente quando numero presente e nao vazio. Injetar header `x-api-token` a partir de `process.env.INTERNAL_API_KEY` EXATAMENTE como em contas-receber/cliente/[idcliente]/route.ts (L15-18). Chamar backendFetch com method GET + extraHeaders e retornar JSON com status. NAO criar/alterar o DELETE de nfse/[id] (ja existe da Phase 30).
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `apps/frontend/src/app/api/cobranca/nfse/cliente/[idcliente]/route.ts` existe e exporta `GET`.
    - Arquivo `apps/frontend/src/app/api/athos/clientes/[idcliente]/notas-fiscais/route.ts` existe e exporta `GET`.
    - `grep -n "backendFetch" apps/frontend/src/app/api/cobranca/nfse/cliente/[idcliente]/route.ts` confirma uso do client.
    - `grep -n "numero" apps/frontend/src/app/api/athos/clientes/[idcliente]/notas-fiscais/route.ts` confirma encaminhamento do filtro.
    - `grep -n "x-api-token" apps/frontend/src/app/api/athos/clientes/[idcliente]/notas-fiscais/route.ts` confirma injecao do token (padrao Athos).
    - `npx tsc --noEmit` do frontend passa sem erros.
  </acceptance_criteria>
  <done>Os dois Route Handlers existem, proxiam para os endpoints backend corretos (com numero encaminhado no NFAT) e compilam sem erro.</done>
</task>

<task type="auto">
  <name>Task 2: Secao "NFS-e Emitidas" colapsavel com lazy load, cancelamento e dupla camada de aviso</name>
  <files>apps/frontend/src/app/contas-receber/[idcliente]/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/contas-receber/[idcliente]/page.tsx (estados existentes L53-104, funcoes formatBRL/formatDate/badgeClassName L31-44, bloco de remocao de NFS-e existente L743-766 que ja faz DELETE /api/cobranca/nfse/[id] com confirm — reutilizar a mesma chamada; estrutura de tabela Bootstrap L695; useRef/useEffect existentes)
    - .planning/phases/31-hist-rico-nfs-e-consulta-nf-athos/31-CONTEXT.md (D-01 empilhadas com hr+cabecalho; D-02 lazy Intersection Observer; D-03 colapsavel fechada por padrao; D-04 estado vazio cinza centralizado; D-05 colunas completas; D-06 botao cancelar reutiliza DELETE; D-07 dupla camada de aviso tooltip+modal; specifics com snippets de layout colapsavel, IntersectionObserver e tooltip de cancelamento)
  </read_first>
  <action>
    Definir tipo `NfseEmitidaCliente = { id: number; numeroNfse: string | null; numeroRps: number; valorServico: number; linkNfse: string | null; dataEmissao: string; titulos: number[] }`. Adicionar estados: `nfseAberta` (boolean, default false — D-03 fechada), `nfseCarregada` (boolean default false), `nfseEmitidas` (NfseEmitidaCliente[] default []), `loadingNfse` (boolean), e `nfseRef = useRef<HTMLDivElement>(null)`. Adicionar funcao `carregarNfseEmitidas()` que faz `fetch(\`/api/cobranca/nfse/cliente/${idcliente}\`, { cache: "no-store" })`, seta `nfseEmitidas` e `nfseCarregada=true`. Adicionar `useEffect` que cria um `IntersectionObserver` observando `nfseRef.current`: ao `entry.isIntersecting && !nfseCarregada` chama carregarNfseEmitidas() (seguir snippet do CONTEXT specifics); cleanup com `observer.disconnect()`; dependencia `[nfseAberta, nfseCarregada]`. Renderizar a secao APOS o fechamento da `div.orcamento-section` (depois de L792, antes da barra de acoes sticky) precedida de `<hr/>` + cabecalho de secao (D-01): botao colapsavel (`▼`/`►` + "NFS-e Emitidas", padrao do snippet CONTEXT). Quando `nfseAberta`, renderizar `<div ref={nfseRef}>` com: spinner enquanto loadingNfse; tabela com colunas Data emissao | Nº NFS-e | Nº RPS | Valor | Titulos vinculados | Acoes (D-05). Titulos vinculados: renderizar cada idcontareceber como badge separado (Claude's Discretion — badges separados por virgula). Botao download (`bi-file-earmark-arrow-down`) apenas quando `linkNfse` nao for null (D-05). Botao "Cancelar ⓘ" com atributo `title="Remove do sistema. O cancelamento na prefeitura pode nao ser suportado."` (D-07a tooltip). Ao clicar Cancelar: abrir modal de confirmacao (pode reusar `confirm()` com texto que INCLUI o aviso "O cancelamento na prefeitura pode nao ser suportado" — D-07b) e, se confirmado, `fetch(\`/api/cobranca/nfse/${id}\`, { method: "DELETE" })`; em sucesso, remover do estado nfseEmitidas (filter por id) e tambem disparar `setRefetchKey(k => k+1)` para sincronizar a tabela de titulos. Estado vazio (D-04): quando `nfseCarregada && nfseEmitidas.length === 0`, exibir `<p className="text-muted text-center py-3">Nenhuma NFS-e emitida para este cliente</p>`. Formatar valor com formatBRL e data com formatDate existentes.
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit -p tsconfig.json && npx next lint --file src/app/contas-receber/[idcliente]/page.tsx 2>&1 | grep -v "^$" | (grep -i "error" && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "NFS-e Emitidas" apps/frontend/src/app/contas-receber/[idcliente]/page.tsx` confirma o cabecalho da secao.
    - `grep -n "IntersectionObserver" apps/frontend/src/app/contas-receber/[idcliente]/page.tsx` confirma lazy load.
    - `grep -n "nfseRef" apps/frontend/src/app/contas-receber/[idcliente]/page.tsx` confirma o ref da secao.
    - `grep -n "Nenhuma NFS-e emitida para este cliente" apps/frontend/src/app/contas-receber/[idcliente]/page.tsx` confirma estado vazio (D-04).
    - `grep -n "cancelamento na prefeitura pode" apps/frontend/src/app/contas-receber/[idcliente]/page.tsx` confirma a dupla camada de aviso (tooltip + texto de confirmacao).
    - `grep -n "/api/cobranca/nfse/cliente" apps/frontend/src/app/contas-receber/[idcliente]/page.tsx` confirma a chamada ao Route Handler.
    - `npx tsc --noEmit` do frontend passa.
  </acceptance_criteria>
  <done>Secao NFS-e Emitidas colapsavel, lazy, com colunas completas, download condicional, cancelamento (DELETE) com aviso em tooltip e na confirmacao, e estado vazio — compila sem erro.</done>
</task>

<task type="auto">
  <name>Task 3: Secao "Notas Fiscais Athos" colapsavel com lazy load, lista de 50 e busca por numero</name>
  <files>apps/frontend/src/app/contas-receber/[idcliente]/page.tsx</files>
  <read_first>
    - apps/frontend/src/app/contas-receber/[idcliente]/page.tsx (a secao NFS-e adicionada na Task 2 — replicar o padrao colapsavel + Intersection Observer para a segunda secao; formatBRL/formatDate L31-37)
    - .planning/phases/31-hist-rico-nfs-e-consulta-nf-athos/31-CONTEXT.md (D-09 campos Nº/Data/Valor/Tipo; D-10 endpoint; D-11 limite 50; D-13 acionamento manual com botao Buscar/Enter; D-14 match exato; D-15 lista de 50 permanece visivel com resultado de busca; D-16 resultado da busca acima da lista; specifics com snippet do resultado de busca separado e mensagem de "Nenhuma nota encontrada com este numero")
  </read_first>
  <action>
    Definir tipo `NotaFiscalAthos = { numero: string; dataemissao: string | null; valor: number; tipo: string }`. Adicionar estados: `nfatAberta` (boolean default false), `nfatCarregada` (boolean default false), `notasFiscaisAthos` (NotaFiscalAthos[] default []), `loadingNfat` (boolean), `buscaNumeroNf` (string default ""), `resultadoBuscaNf` (NotaFiscalAthos[] | null default null), `buscandoNf` (boolean), e `nfatRef = useRef<HTMLDivElement>(null)`. Funcao `carregarNotasFiscaisAthos()` que faz `fetch(\`/api/athos/clientes/${idcliente}/notas-fiscais\`, { cache: "no-store" })`, seta `notasFiscaisAthos` + `nfatCarregada=true`. useEffect com IntersectionObserver observando `nfatRef.current` (mesmo padrao da Task 2), dependencia `[nfatAberta, nfatCarregada]`. Funcao `buscarNotaPorNumero()` (D-13 manual): se `buscaNumeroNf.trim()` vazio, nao faz nada; senao `fetch(\`/api/athos/clientes/${idcliente}/notas-fiscais?numero=${encodeURIComponent(buscaNumeroNf.trim())}\`)`, seta `resultadoBuscaNf` (array, possivelmente vazio). Renderizar a secao APOS a secao NFS-e (empilhada, D-01) com `<hr/>` + botao colapsavel "Notas Fiscais Athos". Quando `nfatAberta`, `<div ref={nfatRef}>` com: campo de busca (input text + botao "Buscar"; disparar buscarNotaPorNumero ao clicar e ao pressionar Enter — onKeyDown key==="Enter"). Resultado da busca (D-15/D-16): quando `resultadoBuscaNf !== null`, renderizar ACIMA da lista geral um bloco destacado (`alert alert-info` ou similar) com "Resultado da busca — Nota Nº {buscaNumeroNf}:"; se `resultadoBuscaNf.length === 0` mostrar "Nenhuma nota encontrada com este numero" (D-04/specifics); senao tabela com as notas encontradas. Abaixo, SEMPRE a lista geral de ate 50 (`notasFiscaisAthos`) — permanece visivel mesmo com busca ativa (D-15). Tabela de notas (compartilhar markup): colunas Nº da nota | Data emissao | Valor | Tipo (D-09). Estado vazio da lista geral (D-04): quando `nfatCarregada && notasFiscaisAthos.length === 0`, exibir `<p className="text-muted text-center py-3">Nenhuma nota fiscal encontrada no Athos</p>`. Spinner enquanto loadingNfat/buscandoNf. Formatar valor com formatBRL e data com formatDate.
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit -p tsconfig.json && npx next lint --file src/app/contas-receber/[idcliente]/page.tsx 2>&1 | grep -v "^$" | (grep -i "error" && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Notas Fiscais Athos" apps/frontend/src/app/contas-receber/[idcliente]/page.tsx` confirma o cabecalho da secao.
    - `grep -n "nfatRef" apps/frontend/src/app/contas-receber/[idcliente]/page.tsx` confirma o ref da segunda secao.
    - `grep -n "buscarNotaPorNumero" apps/frontend/src/app/contas-receber/[idcliente]/page.tsx` confirma a funcao de busca manual.
    - `grep -n "Nenhuma nota encontrada com este número\|Nenhuma nota encontrada com este numero" apps/frontend/src/app/contas-receber/[idcliente]/page.tsx` confirma a mensagem de busca sem resultado (D-04).
    - `grep -n "Nenhuma nota fiscal encontrada no Athos" apps/frontend/src/app/contas-receber/[idcliente]/page.tsx` confirma estado vazio da lista geral.
    - `grep -n "notas-fiscais?numero=" apps/frontend/src/app/contas-receber/[idcliente]/page.tsx` confirma a busca server-side com filtro numero (NFAT-02).
    - `npx tsc --noEmit` do frontend passa.
  </acceptance_criteria>
  <done>Secao Notas Fiscais Athos colapsavel, lazy, com lista de ate 50 e busca manual por numero (resultado acima da lista, lista permanece visivel), estados vazios e compila sem erro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Next.js Route Handler | Requisicao do navegador do operador; sem dados sensiveis no body |
| Next.js Route Handler -> Backend | backendFetch injeta x-internal-api-key (cobranca) / x-api-token (athos) server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-08 | Information Disclosure | token de backend | mitigate | Tokens injetados server-side no Route Handler (process.env / internalHeaders); nunca expostos ao browser |
| T-31-09 | Tampering / Injection | buscaNumeroNf enviado na query | mitigate | encodeURIComponent no frontend + query parametrizada no backend ($2); sem interpolacao direta |
| T-31-10 | Tampering | param idcliente no Route Handler | mitigate | Validacao Number.isFinite + retorno 400 antes de proxiar |
| T-31-11 | Repudiation | cancelamento de NFS-e | accept | Cancelamento reutiliza DELETE existente (Phase 30) que ja loga remocao no service; sem novo risco |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependencia nova instalada (Intersection Observer e API nativa do browser) |
</threat_model>

<verification>
- `npx tsc --noEmit` do frontend passa apos as 3 tasks.
- Route Handlers e secoes presentes via grep.
- Lazy load via IntersectionObserver nativo (sem dependencia nova).
</verification>

<success_criteria>
1. Route Handler de historico NFS-e e de notas-fiscais Athos (com filtro numero) existem e proxiam corretamente.
2. Pagina exibe duas secoes empilhadas, colapsaveis (fechadas por padrao), lazy via Intersection Observer.
3. Secao NFS-e: colunas completas, download condicional, cancelamento com dupla camada de aviso, estado vazio.
4. Secao NFAT: lista de ate 50, busca manual por numero (resultado acima da lista, lista geral permanece), estados vazios.
5. Frontend compila sem erros de tipo.
</success_criteria>

<output>
Create `.planning/phases/31-hist-rico-nfs-e-consulta-nf-athos/31-03-SUMMARY.md` when done
</output>
