---
phase: 31-hist-rico-nfs-e-consulta-nf-athos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/cobranca/cobranca.service.ts
  - apps/backend/src/modules/cobranca/cobranca.controller.ts
  - apps/backend/src/modules/cobranca/cobranca.service.cliente.test.ts
autonomous: true
requirements: [NFR-05]
must_haves:
  truths:
    - "Backend retorna a lista de NFS-e emitidas de um cliente lendo do banco proprio (Prisma)"
    - "Cada NFS-e retornada inclui os idcontareceber dos titulos vinculados"
    - "Cliente sem NFS-e retorna array vazio (nao erro)"
  artifacts:
    - path: "apps/backend/src/modules/cobranca/cobranca.service.ts"
      provides: "Metodo buscarNfseEmitidaCliente(idclienteAthos)"
      contains: "buscarNfseEmitidaCliente"
    - path: "apps/backend/src/modules/cobranca/cobranca.controller.ts"
      provides: "Rota GET /cobranca/nfse/cliente/:idclienteAthos"
      contains: "nfse/cliente/:idclienteAthos"
    - path: "apps/backend/src/modules/cobranca/cobranca.service.cliente.test.ts"
      provides: "Testes unitarios de buscarNfseEmitidaCliente"
      contains: "buscarNfseEmitidaCliente"
  key_links:
    - from: "cobranca.controller.ts"
      to: "cobranca.service.ts"
      via: "this.cobrancaService.buscarNfseEmitidaCliente"
      pattern: "buscarNfseEmitidaCliente"
    - from: "cobranca.service.ts"
      to: "prisma.nfseEmitida"
      via: "consulta Prisma por idclienteAthos"
      pattern: "nfseEmitida\\.findMany"
---

<objective>
Adicionar a leitura do historico de NFS-e emitidas de um cliente a partir do banco proprio (Prisma), expondo um novo metodo no `CobrancaService` e uma nova rota no `CobrancaController` existente. Atende a camada backend de NFR-05 (D-05, D-08).

Purpose: O operador precisa ver todas as NFS-e ja emitidas para o cliente na pagina de detalhe. Os dados vivem no banco proprio (Athos e read-only), entao a fonte de verdade e `NfseEmitida` via Prisma.
Output: Metodo `buscarNfseEmitidaCliente(idclienteAthos)`, rota `GET /cobranca/nfse/cliente/:idclienteAthos` e testes unitarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-hist-rico-nfs-e-consulta-nf-athos/31-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar CobrancaService.buscarNfseEmitidaCliente(idclienteAthos)</name>
  <files>apps/backend/src/modules/cobranca/cobranca.service.ts, apps/backend/src/modules/cobranca/cobranca.service.cliente.test.ts</files>
  <read_first>
    - apps/backend/src/modules/cobranca/cobranca.service.ts (metodos `buscarNfseEmitidaParaTitulos()` L573 e `buscarBoletosCliente()` L555 — padrao de consulta Prisma + include de titulos + map para Number; replicar mesma forma)
    - apps/backend/src/modules/cobranca/cobranca.service.unit.test.ts (padrao de teste unitario: `Object.create(CobrancaService.prototype)` + mock manual de `prisma`)
    - apps/backend/prisma/schema.prisma (modelos `NfseEmitida` L256 e `NfseEmitidaTitulo` L271 — campos disponiveis: numeroNfse, numeroRps, valorServico, linkNfse, dataEmissao, idclienteAthos, idvenda; relacao `titulos` com idcontareceber e valor)
  </read_first>
  <behavior>
    - Dado idclienteAthos com 2 NFS-e emitidas, retorna array com 2 entradas ordenadas por dataEmissao desc.
    - Cada entrada inclui: id, numeroNfse, numeroRps, valorServico (Number), linkNfse, dataEmissao, e titulos como array de idcontareceber (number[]).
    - Dado idclienteAthos sem nenhuma NFS-e, retorna array vazio [].
    - valorServico e Decimal no Prisma e deve ser convertido para Number na resposta (igual ao padrao de buscarBoletosCliente que faz Number(b.valor)).
  </behavior>
  <action>
    Criar metodo publico assincrono `buscarNfseEmitidaCliente(idclienteAthos: number)` no `CobrancaService`. Usar `this.prisma.nfseEmitida.findMany` com `where: { idclienteAthos }`, `orderBy: { dataEmissao: "desc" }` e `include: { titulos: { select: { idcontareceber: true } } }`. Mapear cada registro para objeto com: `id`, `numeroNfse` (string | null), `numeroRps` (number), `valorServico: Number(n.valorServico)`, `linkNfse` (string | null), `dataEmissao`, e `titulos: n.titulos.map(t => t.idcontareceber)` (number[]). Seguir EXATAMENTE o padrao de conversao Decimal->Number usado em `buscarBoletosCliente()` (L564-569). NAO adicionar paginacao (D-11 nao se aplica aqui — sao registros do proprio banco, listar todos do cliente). Criar o arquivo de teste `cobranca.service.cliente.test.ts` cobrindo: (a) cliente com 2 NFS-e retorna 2 entradas com titulos mapeados; (b) cliente sem NFS-e retorna []; (c) valorServico convertido para Number. Mockar `this.prisma.nfseEmitida.findMany` seguindo o padrao de `cobranca.service.unit.test.ts` (Object.create + atribuir prisma mock).
  </action>
  <verify>
    <automated>cd apps/backend && npx jest cobranca.service.cliente --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "buscarNfseEmitidaCliente" apps/backend/src/modules/cobranca/cobranca.service.ts` retorna ao menos 1 linha (definicao do metodo).
    - `grep -n "nfseEmitida.findMany" apps/backend/src/modules/cobranca/cobranca.service.ts` confirma uso de Prisma para NfseEmitida.
    - `grep -n "orderBy" apps/backend/src/modules/cobranca/cobranca.service.ts` no novo metodo confirma ordenacao por dataEmissao desc.
    - `npx jest cobranca.service.cliente` passa com os 3 cenarios (2 NFS-e, vazio, conversao Number).
  </acceptance_criteria>
  <done>Metodo buscarNfseEmitidaCliente implementado, retorna NFS-e do cliente com titulos vinculados e valorServico como Number, com testes passando.</done>
</task>

<task type="auto">
  <name>Task 2: Expor GET /cobranca/nfse/cliente/:idclienteAthos no CobrancaController</name>
  <files>apps/backend/src/modules/cobranca/cobranca.controller.ts</files>
  <read_first>
    - apps/backend/src/modules/cobranca/cobranca.controller.ts (rota existente `GET boleto/cliente/:idcliente` L61-64 usando `@Get` + `@Param("idcliente", ParseIntPipe)` — replicar mesma assinatura)
    - apps/backend/src/modules/cobranca/cobranca.service.ts (assinatura de buscarNfseEmitidaCliente criada na Task 1)
  </read_first>
  <action>
    Adicionar metodo no `CobrancaController` com decorator `@Get("nfse/cliente/:idclienteAthos")`. Receber `@Param("idclienteAthos", ParseIntPipe) idclienteAthos: number` e retornar `this.cobrancaService.buscarNfseEmitidaCliente(idclienteAthos)`. Seguir EXATAMENTE o padrao da rota `boletosCliente` (L61-64): mesmo uso de ParseIntPipe, sem validacao manual adicional. Posicionar o metodo proximo ao `cancelarNfse` (DELETE nfse/:id, L55-58) para agrupar rotas de NFS-e. NAO duplicar o `DELETE /cobranca/nfse/:id` — ja existe (L55). A autenticacao e via `InternalAuthGuard` global, nao adicionar guard manual.
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc -p tsconfig.build.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "nfse/cliente/:idclienteAthos" apps/backend/src/modules/cobranca/cobranca.controller.ts` retorna a definicao da rota.
    - `grep -n "buscarNfseEmitidaCliente" apps/backend/src/modules/cobranca/cobranca.controller.ts` confirma a chamada ao service.
    - `npx tsc -p tsconfig.build.json --noEmit` compila sem erros (o ParseIntPipe e o tipo de retorno batem).
    - `grep -c "@Delete(\"nfse/:id\")" apps/backend/src/modules/cobranca/cobranca.controller.ts` retorna 1 (DELETE existente nao foi duplicado nem removido).
  </acceptance_criteria>
  <done>Rota GET /cobranca/nfse/cliente/:idclienteAthos exposta, compila sem erro, e delega para buscarNfseEmitidaCliente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend Route Handler -> Backend NestJS | Requisicao interna autenticada via x-internal-api-key (InternalAuthGuard global) |
| Backend -> Banco proprio (Prisma/Postgres) | Leitura de NfseEmitida; idclienteAthos vem da rota |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | `:idclienteAthos` param | mitigate | ParseIntPipe rejeita valores nao-inteiros antes de chegar ao service |
| T-31-02 | Information Disclosure | GET /cobranca/nfse/cliente | mitigate | InternalAuthGuard global exige x-internal-api-key; endpoint nao e @Public() |
| T-31-03 | Information Disclosure | resposta NfseEmitida | accept | Dados de NFS-e do proprio cliente; sem PII alem de numero/valor; consumidor e o painel interno |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependencia nova instalada nesta fase; nada a verificar |
</threat_model>

<verification>
- `npx jest cobranca.service.cliente` passa.
- `npx tsc -p tsconfig.build.json --noEmit` compila o backend sem erros.
- Metodo de service e rota de controller presentes via grep.
</verification>

<success_criteria>
1. `buscarNfseEmitidaCliente(idclienteAthos)` retorna NFS-e do cliente lidas do banco proprio com titulos vinculados (idcontareceber[]).
2. `GET /cobranca/nfse/cliente/:idclienteAthos` exposto e delega ao service.
3. Cliente sem NFS-e retorna array vazio sem erro.
4. Build backend e testes passam.
</success_criteria>

<output>
Create `.planning/phases/31-hist-rico-nfs-e-consulta-nf-athos/31-01-SUMMARY.md` when done
</output>
