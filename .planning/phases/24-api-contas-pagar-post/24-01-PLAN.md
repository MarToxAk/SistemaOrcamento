---
phase: 24-api-contas-pagar-post
plan: 24-01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/athos/dto/create-conta-pagar.dto.ts
  - apps/backend/src/modules/integrations/athos/athos.service.ts
  - apps/backend/src/modules/integrations/athos/athos.controller.ts
autonomous: true
requirements: [CPAG-01, CPAG-02, CPAG-03, CPAG-04]

must_haves:
  truths:
    - "POST /athos/contas-pagar com body valido insere em conta_pagar e retorna { idcontapagar: number }"
    - "GET /athos/contas-pagar?statusconta=ABE filtra resultados pela coluna statusconta"
    - "Sem ATHOS_API_TOKEN no env, qualquer endpoint retorna 500 InternalServerErrorException"
    - "Token incorreto ou ausente no header retorna 401 UnauthorizedException"
    - "Build do backend conclui sem erros TypeScript"
  artifacts:
    - path: "apps/backend/src/modules/integrations/athos/dto/create-conta-pagar.dto.ts"
      provides: "DTO com campos obrigatorios e opcionais de conta_pagar"
      exports: ["CreateContaPagarDto"]
    - path: "apps/backend/src/modules/integrations/athos/athos.service.ts"
      provides: "Metodo criarContaPagar() e statusconta em listarContasPagar()"
      contains: "criarContaPagar"
    - path: "apps/backend/src/modules/integrations/athos/athos.controller.ts"
      provides: "Metodo privado validateAthosToken(), POST contas-pagar, GET com statusconta"
      contains: "validateAthosToken"
  key_links:
    - from: "athos.controller.ts @Post(contas-pagar)"
      to: "athosService.criarContaPagar(dto)"
      via: "delegacao direta"
      pattern: "criarContaPagar"
    - from: "athos.service.ts criarContaPagar"
      to: "INSERT INTO <table.tableName>"
      via: "pg.Pool client.query com RETURNING idcontapagar (tabela resolvida via findExistingTable)"
      pattern: "RETURNING idcontapagar"
    - from: "athos.controller.ts (todos os endpoints)"
      to: "validateAthosToken()"
      via: "chamada no inicio de cada handler"
      pattern: "this\\.validateAthosToken"
---

<objective>
Implementar o endpoint POST /athos/contas-pagar e corrigir a autenticacao fail-closed em todos os endpoints do modulo Athos.

Purpose: Expor a capacidade de lancar contas a pagar diretamente no banco Athos legado (PostgreSQL), garantindo que nenhum endpoint do controller fique acessivel quando ATHOS_API_TOKEN nao esta configurado.

Output:
- `dto/create-conta-pagar.dto.ts` — DTO com class-validator
- `athos.service.ts` modificado — metodo `criarContaPagar()` + filtro `statusconta` em `listarContasPagar()`
- `athos.controller.ts` modificado — `validateAthosToken()` privado, GET atualizado, POST novo
</objective>

<execution_context>
@D:\Projetos\cloudeproject\SistemaOrcamento\.planning\phases\24-api-contas-pagar-post\24-RESEARCH.md
</execution_context>

<context>
@D:\Projetos\cloudeproject\SistemaOrcamento\.planning\PROJECT.md
@D:\Projetos\cloudeproject\SistemaOrcamento\.planning\ROADMAP.md

<interfaces>
<!-- Contratos extraidos do codigo existente. Executor usa diretamente — sem exploracao de codebase. -->

De apps/backend/src/modules/integrations/athos/athos.service.ts:
```typescript
// Pool singleton — usar getPool() para todos os metodos
private getPool(): Pool  // retorna Pool com max=5, idleTimeoutMillis=30000

// Assinatura ATUAL (a ser modificada para incluir statusconta):
async listarContasPagar(dataInicio?: string, dataFinal?: string): Promise<ContaPagarRow[]>

// Padrao do client — SEMPRE em try/finally com client.release():
const client: PoolClient = await pool.connect();
try { ... } catch { ... } finally { client.release(); }

// findExistingTable — funcao module-level disponivel no athos.service.ts:
async function findExistingTable(client: Pick<Client, "query">, tableCandidates: string[])
// Retorna: { tableName: string, columns: Set<string> } | null

// Error handling pattern:
import { BadRequestException, InternalServerErrorException, Logger, NotFoundException } from "@nestjs/common";
this.logger.log(`[Athos] mensagem`);
this.logger.error(`Erro ao ...: ${error}`);
if (error instanceof InternalServerErrorException) throw error;
throw new InternalServerErrorException("Mensagem usuario");
```

De apps/backend/src/modules/integrations/athos/athos.controller.ts (estado atual — BUG a corrigir):
```typescript
import { Controller, Get, Headers, Query, UnauthorizedException } from "@nestjs/common";

// BUG ATUAL nos dois endpoints:
const requiredToken = process.env.ATHOS_API_TOKEN;
if (requiredToken) {   // ← falha: quando undefined, bloco eh pulado
  ...
}
```

De apps/backend/src/modules/quotes/dto/create-quote.dto.ts (padrao de DTO do projeto):
```typescript
import { IsDateString, IsNumber, IsOptional, IsString, Min } from "class-validator";
// Sem @Type() para campos escalares; @Type(() => X) apenas para objetos aninhados
// Campos obrigatorios: sem @IsOptional(), com ! no nome
// Campos opcionais: @IsOptional() primeiro, depois o decorator de tipo, com ? no nome
```

Schema da tabela conta_pagar (DATABASE_SCHEMA.md linha 1083):
```sql
idcontapagar    serial NOT NULL  -- PK, gerado pelo banco — NAO incluir no INSERT
descricaoconta  descricao        -- nullable, obrigatorio por req
datavencimento  dmdata           -- nullable, obrigatorio por req
valorconta      monetario        -- nullable, obrigatorio por req
dataemissao     dmdata           -- nullable, opcional
observacao      descricao        -- nullable, opcional
idfornecedor    integer          -- nullable, opcional (FK)
numerodocumento descricao        -- nullable, opcional
statusconta     character varying(3)  -- nullable (ABE/PAG/CAN)
ultimaalteracao timestamp DEFAULT now()   -- NAO incluir no INSERT
sincronizado    boolean DEFAULT false     -- NAO incluir no INSERT
recorrenciafornecedor boolean NOT NULL DEFAULT true  -- NAO incluir no INSERT
exibemsgrecorrencia   boolean NOT NULL DEFAULT true  -- NAO incluir no INSERT
agruparconta    boolean DEFAULT false     -- NAO incluir no INSERT
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar DTO CreateContaPagarDto</name>
  <files>apps/backend/src/modules/integrations/athos/dto/create-conta-pagar.dto.ts</files>

  <read_first>
    - apps/backend/src/modules/quotes/dto/create-quote.dto.ts — padrao de class-validator do projeto (ver imports, formato de campos obrigatorios vs opcionais)
    - .planning/DATABASE_SCHEMA.md linhas 1077-1144 — DDL completo da tabela conta_pagar (tipos e nullable de cada coluna)
    - .planning/phases/24-api-contas-pagar-post/24-RESEARCH.md secao "DTO & Validation" — decisoes de design e pitfalls
  </read_first>

  <behavior>
    - descricaoconta string obrigatorio → @IsString() sem @IsOptional() → validacao falha com 400 se ausente
    - datavencimento string obrigatorio → @IsDateString() sem @IsOptional() → rejeita strings nao-ISO
    - valorconta number obrigatorio → @IsNumber() @Min(0.01) sem @IsOptional() → rejeita 0 e negativos
    - dataemissao string opcional → @IsOptional() @IsDateString() → aceita ausencia no body
    - observacao string opcional → @IsOptional() @IsString() → aceita ausencia no body
    - idfornecedor number opcional → @IsOptional() @IsNumber() @IsInt() → aceita ausencia; rejeita strings
    - numerodocumento string opcional → @IsOptional() @IsString() @MaxLength(50) → aceita ausencia; rejeita > 50 chars
    - Campos extras no body → 400 automatico (ValidationPipe global com forbidNonWhitelisted: true)
  </behavior>

  <action>
  Criar o arquivo `apps/backend/src/modules/integrations/athos/dto/create-conta-pagar.dto.ts` com o seguinte conteudo exato:

  ```typescript
  import {
    IsDateString,
    IsInt,
    IsNotEmpty,
    IsNumber,
    IsOptional,
    IsString,
    MaxLength,
    Min,
  } from "class-validator";

  export class CreateContaPagarDto {
    @IsString()
    @IsNotEmpty()
    descricaoconta!: string;

    @IsDateString()
    datavencimento!: string;

    @IsNumber()
    @Min(0.01)
    valorconta!: number;

    @IsOptional()
    @IsDateString()
    dataemissao?: string;

    @IsOptional()
    @IsString()
    observacao?: string;

    @IsOptional()
    @IsInt()
    idfornecedor?: number;

    @IsOptional()
    @IsString()
    @MaxLength(50)
    numerodocumento?: string;
  }
  ```

  Notas de implementacao:
  - Usar @IsNotEmpty() em descricaoconta para rejeitar string vazia "" (alem de ausente)
  - Usar @IsInt() em idfornecedor (nao @IsNumber()) porque FK deve ser inteiro
  - NAO usar @Type() — o DTO eh plano, sem objetos aninhados; class-transformer nao eh necessario
  - NAO usar @IsPositive() em valorconta — usar @Min(0.01) igual ao padrao de CreateQuoteItemDto.valoritem
  - O arquivo fica em subpasta dto/ que pode precisar ser criada
  </action>

  <verify>
    <automated>cd apps/backend && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "create-conta-pagar" | head -5; echo "EXIT:$?"</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo existe em `apps/backend/src/modules/integrations/athos/dto/create-conta-pagar.dto.ts`
    - `grep -c "export class CreateContaPagarDto" apps/backend/src/modules/integrations/athos/dto/create-conta-pagar.dto.ts` retorna 1
    - `grep -c "IsNotEmpty" apps/backend/src/modules/integrations/athos/dto/create-conta-pagar.dto.ts` retorna 1 (em descricaoconta)
    - `grep -c "MaxLength(50)" apps/backend/src/modules/integrations/athos/dto/create-conta-pagar.dto.ts` retorna 1 (em numerodocumento)
    - `grep -c "IsInt" apps/backend/src/modules/integrations/athos/dto/create-conta-pagar.dto.ts` retorna 1 (em idfornecedor)
    - `grep -c "Min(0.01)" apps/backend/src/modules/integrations/athos/dto/create-conta-pagar.dto.ts` retorna 1 (em valorconta)
    - Build TypeScript sem erros relacionados ao arquivo
  </acceptance_criteria>

  <done>Arquivo criado com 7 campos (3 obrigatorios + 4 opcionais), exports corretos, sem erros de compilacao TypeScript.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corrigir auth fail-closed e adicionar POST no AthosController</name>
  <files>apps/backend/src/modules/integrations/athos/athos.controller.ts</files>

  <read_first>
    - apps/backend/src/modules/integrations/athos/athos.controller.ts — LEIA INTEIRO antes de qualquer modificacao; estado atual com bug de auth
    - .planning/phases/24-api-contas-pagar-post/24-RESEARCH.md secao "Auth Fix Pattern" — logica exata do validateAthosToken()
    - .planning/codebase/CONCERNS.md linhas 27-31 — descricao da vulnerabilidade HIGH a ser corrigida
  </read_first>

  <behavior>
    - ATHOS_API_TOKEN ausente no env → InternalServerErrorException("ATHOS_API_TOKEN nao configurado no servidor") — 500
    - Token presente no env, header ausente no request → UnauthorizedException("Token invalido ou ausente") — 401
    - Token presente no env, header errado → UnauthorizedException("Token invalido ou ausente") — 401
    - Token correto → handler executa normalmente
    - GET contas-pagar passa statusconta para o service
    - POST contas-pagar recebe body dto e delega para service.criarContaPagar(dto)
  </behavior>

  <action>
  Reescrever `apps/backend/src/modules/integrations/athos/athos.controller.ts` com as seguintes mudancas:

  1. **Atualizar imports** — adicionar `Body`, `InternalServerErrorException`, `Post` ao import de `@nestjs/common`. Adicionar import do DTO:
     ```typescript
     import { Body, Controller, Get, Headers, InternalServerErrorException, Post, Query, UnauthorizedException } from "@nestjs/common";
     import { AthosService } from "./athos.service";
     import { CreateContaPagarDto } from "./dto/create-conta-pagar.dto";
     ```

  2. **Adicionar metodo privado `validateAthosToken()`** logo apos o constructor:
     ```typescript
     private validateAthosToken(authorization?: string, xApiToken?: string): void {
       const requiredToken = process.env.ATHOS_API_TOKEN;
       if (!requiredToken) {
         throw new InternalServerErrorException("ATHOS_API_TOKEN nao configurado no servidor");
       }
       const provided =
         xApiToken ||
         (authorization?.startsWith("Bearer ") ? authorization.slice(7) : authorization) ||
         undefined;
       if (!provided || provided !== requiredToken) {
         throw new UnauthorizedException("Token invalido ou ausente");
       }
     }
     ```

  3. **Refatorar `listarContasPagar()`** — substituir o bloco `if (requiredToken)` pela chamada `this.validateAthosToken(authorization, xApiToken)`, adicionar `@Query("statusconta") statusconta?: string` nos parametros, e passar `statusconta` ao service:
     ```typescript
     @Get("contas-pagar")
     async listarContasPagar(
       @Query("dataInicio") dataInicio?: string,
       @Query("dataFinal") dataFinal?: string,
       @Query("datainicio") dataInicioLegacy?: string,
       @Query("datafinal") dataFinalLegacy?: string,
       @Query("statusconta") statusconta?: string,
       @Headers("authorization") authorization?: string,
       @Headers("x-api-token") xApiToken?: string,
     ) {
       this.validateAthosToken(authorization, xApiToken);
       return this.athosService.listarContasPagar(dataInicio ?? dataInicioLegacy, dataFinal ?? dataFinalLegacy, statusconta);
     }
     ```

  4. **Refatorar `buscarClientes()`** — substituir o bloco `if (requiredToken)` pela chamada `this.validateAthosToken(authorization, xApiToken)`. O restante do metodo permanece identico.

  5. **Adicionar endpoint `@Post("contas-pagar")`** ao final da classe:
     ```typescript
     @Post("contas-pagar")
     async criarContaPagar(
       @Body() dto: CreateContaPagarDto,
       @Headers("authorization") authorization?: string,
       @Headers("x-api-token") xApiToken?: string,
     ) {
       this.validateAthosToken(authorization, xApiToken);
       return this.athosService.criarContaPagar(dto);
     }
     ```

  IMPORTANTE: NAO usar @UsePipes() no controller — ValidationPipe global em main.ts ja cobre todos os endpoints.
  IMPORTANTE: NAO deixar nenhum bloco `if (requiredToken)` no arquivo — todos devem ser substituidos por `this.validateAthosToken(...)`.
  </action>

  <verify>
    <automated>grep -c "this.validateAthosToken" apps/backend/src/modules/integrations/athos/athos.controller.ts
# esperado: 3 (um para cada endpoint: GET contas-pagar, GET clientes, POST contas-pagar)

grep -c "if (requiredToken)" apps/backend/src/modules/integrations/athos/athos.controller.ts
# esperado: 0 (linha deve ter sido removida)</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "private validateAthosToken" apps/backend/src/modules/integrations/athos/athos.controller.ts` retorna 1
    - `grep -c "InternalServerErrorException" apps/backend/src/modules/integrations/athos/athos.controller.ts` retorna pelo menos 2 (import + uso)
    - `grep -c "if (requiredToken)" apps/backend/src/modules/integrations/athos/athos.controller.ts` retorna 0 (bug removido)
    - `grep -c "@Post" apps/backend/src/modules/integrations/athos/athos.controller.ts` retorna 1
    - `grep -c "CreateContaPagarDto" apps/backend/src/modules/integrations/athos/athos.controller.ts` retorna pelo menos 2 (import + uso)
    - `grep -c "statusconta" apps/backend/src/modules/integrations/athos/athos.controller.ts` retorna pelo menos 2 (Query decorator + passagem ao service)
    - `grep -c "this.validateAthosToken" apps/backend/src/modules/integrations/athos/athos.controller.ts` retorna 3 (um por endpoint: GET contas-pagar, GET clientes, POST contas-pagar)
    - Build TypeScript sem erros no arquivo
  </acceptance_criteria>

  <done>Controller com 3 endpoints todos protegidos por validateAthosToken(); bug fail-open eliminado; POST contas-pagar adicionado; GET passa statusconta ao service.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implementar criarContaPagar() e filtro statusconta no AthosService</name>
  <files>apps/backend/src/modules/integrations/athos/athos.service.ts</files>

  <read_first>
    - apps/backend/src/modules/integrations/athos/athos.service.ts — LEIA INTEIRO antes de qualquer modificacao; focar nos metodos listarContasPagar() (linhas 432-536) e buscarVendaCaixa() (linhas 921-941) como referencia de padrao INSERT/query
    - apps/backend/src/modules/integrations/athos/dto/create-conta-pagar.dto.ts — DTO criado na Task 1 (tipos dos parametros)
    - .planning/phases/24-api-contas-pagar-post/24-RESEARCH.md secoes "INSERT Pattern" e "statusconta Filter"
  </read_first>

  <behavior>
    - criarContaPagar(dto) com campos validos → INSERT executa, retorna { idcontapagar: 42 } (numero real gerado)
    - criarContaPagar(dto) → client.release() chamado sempre no finally (mesmo em erro)
    - criarContaPagar(dto) com erro de conexao PG → lanca InternalServerErrorException
    - listarContasPagar(dataInicio, dataFinal, "ABE") → query contem "statusconta = $N" com valor "ABE"
    - listarContasPagar(dataInicio, dataFinal, undefined) → query NAO contem condicao statusconta (comportamento atual preservado)
    - listarContasPagar(dataInicio, dataFinal, "  pag  ") → normaliza para "PAG" (trim + toUpperCase)
  </behavior>

  <action>
  Modificar `apps/backend/src/modules/integrations/athos/athos.service.ts` com as seguintes mudancas:

  **Mudanca 1: Adicionar import do DTO** no topo do arquivo (apos os imports existentes de @nestjs/common):
  ```typescript
  import { CreateContaPagarDto } from "./dto/create-conta-pagar.dto";
  ```

  **Mudanca 2: Atualizar assinatura de `listarContasPagar()`** — adicionar terceiro parametro `statusconta?: string` e adicionar filtro na array de conditions, APOS os filtros de data existentes e ANTES da montagem da query:

  Assinatura nova:
  ```typescript
  async listarContasPagar(dataInicio?: string, dataFinal?: string, statusconta?: string) {
  ```

  Adicionar APOS o bloco `if (end)` que adiciona filtro de data final (linha ~509 no service atual), e ANTES da linha `const query = \`SELECT * FROM ...`:
  ```typescript
  if (typeof statusconta === "string" && statusconta.trim()) {
    params.push(statusconta.trim().toUpperCase());
    conditions.push(`statusconta = $${params.length}`);
  }
  ```

  **Mudanca 3: Adicionar metodo `criarContaPagar()`** ao final da classe `AthosService`, antes do fechamento `}` da classe:

  ```typescript
  async criarContaPagar(dto: CreateContaPagarDto): Promise<{ idcontapagar: number }> {
    const pool = this.getPool();
    const client: PoolClient = await pool.connect();
    try {
      const candidates = [
        "conta_pagar",
        "contaspagar",
        "contas_pagar",
        "contasapagar",
        "conta_pagar_fornecedor",
      ];
      const table = await findExistingTable(client, candidates);
      if (!table) {
        throw new NotFoundException("Tabela de contas a pagar nao encontrada");
      }
      const result = await client.query<{ idcontapagar: number }>(
        `INSERT INTO ${table.tableName}
           (descricaoconta, datavencimento, valorconta, dataemissao, observacao, idfornecedor, numerodocumento)
         VALUES ($1, CAST($2 AS date), $3, CAST($4 AS date), $5, $6, $7)
         RETURNING idcontapagar`,
        [
          dto.descricaoconta,
          dto.datavencimento,
          dto.valorconta,
          dto.dataemissao ?? null,
          dto.observacao ?? null,
          dto.idfornecedor ?? null,
          dto.numerodocumento ?? null,
        ],
      );
      const idcontapagar = result.rows[0].idcontapagar;
      this.logger.log(`[Athos] conta_pagar criada: idcontapagar=${idcontapagar}`);
      return { idcontapagar };
    } catch (error) {
      this.logger.error(`Erro ao criar conta a pagar no Athos: ${error}`);
      if (error instanceof InternalServerErrorException) throw error;
      if (error instanceof NotFoundException) throw error;
      throw new InternalServerErrorException("Erro ao criar conta a pagar no Athos");
    } finally {
      client.release();
    }
  }
  ```

  IMPORTANTE: Usar `findExistingTable(client, candidates)` para obter o nome real da tabela antes do INSERT — o mesmo padrao de `listarContasPagar()`. Se `!table`, lancar `NotFoundException`.
  IMPORTANTE: Usar `${table.tableName}` no INSERT (nao o literal `'conta_pagar'`).
  IMPORTANTE: Usar `CAST($2 AS date)` e `CAST($4 AS date)` para datavencimento e dataemissao — tipo dmdata nao tem CREATE DOMAIN definido no schema exportado; o cast explicito garante compatibilidade com o driver pg.
  IMPORTANTE: NAO incluir `ultimaalteracao`, `sincronizado`, `recorrenciafornecedor`, `exibemsgrecorrencia`, `agruparconta` — todos tem DEFAULT e o banco preenche automaticamente.
  </action>

  <verify>
    <automated>cd apps/backend && grep -c "criarContaPagar" src/modules/integrations/athos/athos.service.ts && grep -c "RETURNING idcontapagar" src/modules/integrations/athos/athos.service.ts && grep -c "statusconta" src/modules/integrations/athos/athos.service.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "async criarContaPagar" apps/backend/src/modules/integrations/athos/athos.service.ts` retorna 1
    - `grep -c "RETURNING idcontapagar" apps/backend/src/modules/integrations/athos/athos.service.ts` retorna 1
    - `grep -c "findExistingTable" apps/backend/src/modules/integrations/athos/athos.service.ts` retorna pelo menos 2 (em listarContasPagar + em criarContaPagar)
    - `grep -c "NotFoundException" apps/backend/src/modules/integrations/athos/athos.service.ts` retorna pelo menos 2 (uso existente em listarContasPagar + uso novo em criarContaPagar)
    - `grep -c "table.tableName" apps/backend/src/modules/integrations/athos/athos.service.ts` retorna pelo menos 2 (uso existente + uso novo no INSERT)
    - `grep -c "client.release" apps/backend/src/modules/integrations/athos/athos.service.ts` retorna pelo menos 6 (um por metodo existente + o novo)
    - `grep -c "statusconta" apps/backend/src/modules/integrations/athos/athos.service.ts` retorna pelo menos 2 (condicao no conditions[] + o push em params)
    - `grep -c "statusconta.trim().toUpperCase" apps/backend/src/modules/integrations/athos/athos.service.ts` retorna 1
    - `grep -c "CAST($2 AS date)" apps/backend/src/modules/integrations/athos/athos.service.ts` retorna 1 (datavencimento com cast explicito)
    - Build: `npm run build --workspace=apps/backend` conclui sem erros TypeScript
  </acceptance_criteria>

  <done>
    Service com metodo criarContaPagar() que: (1) usa findExistingTable() para resolver o nome real da tabela, (2) lanca NotFoundException se tabela nao encontrada, (3) executa INSERT parametrico com CAST($N AS date) para campos dmdata e RETURNING, (4) loga o idcontapagar gerado, (5) chama client.release() no finally.
    listarContasPagar() aceita terceiro parametro statusconta e adiciona filtro condicional na query.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → AthosController | Qualquer caller externo; credencial via header x-api-token ou Authorization |
| AthosController → AthosService | Chamada interna autenticada; DTO ja validado pelo ValidationPipe global |
| AthosService → Athos PostgreSQL | Banco legado externo; queries sempre com parametros $N |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Elevation of Privilege | AthosController — todos os endpoints | mitigate | validateAthosToken() lanca InternalServerErrorException quando ATHOS_API_TOKEN ausente — fail-closed; lanca UnauthorizedException quando token incorreto — nunca ha modo aberto |
| T-24-02 | Tampering | AthosService.criarContaPagar() — INSERT | mitigate | Query usa parametros posicionais $1..$7 exclusivamente; nenhuma interpolacao de string com dados do usuario; campos extras rejeitados pelo ValidationPipe (forbidNonWhitelisted: true) |
| T-24-03 | Tampering | CreateContaPagarDto.numerodocumento | mitigate | @MaxLength(50) limita o campo; @IsString() garante tipo; nao eh usado como identifier em SQL (vai como $7 na query parametrica) |
| T-24-04 | Tampering | AthosService.listarContasPagar() — statusconta filter | accept | statusconta vai como parametro $N na query (nao em identifier position); nao ha risco de SQL injection; trim()+toUpperCase() normaliza mas nao eh barreira de seguranca |
| T-24-05 | Spoofing | AthosController — header x-api-token | accept | Comparacao de string simples (timing attack possivel em teoria); aceitavel para API interna sem requisito de timing-safe comparison em ASVS L1 para este nivel de exposicao |
</threat_model>

<verification>

```bash
# 1. Build sem erros TypeScript
cd apps/backend && npm run build 2>&1 | tail -5

# 2. Verificar que validateAthosToken() existe e aparece 3 vezes (uma por endpoint)
grep -c "this.validateAthosToken" apps/backend/src/modules/integrations/athos/athos.controller.ts

# 3. Verificar que o bug if(requiredToken) foi eliminado
grep -c "if (requiredToken)" apps/backend/src/modules/integrations/athos/athos.controller.ts

# 4. Verificar INSERT com findExistingTable no service
grep "findExistingTable" apps/backend/src/modules/integrations/athos/athos.service.ts
grep "RETURNING idcontapagar" apps/backend/src/modules/integrations/athos/athos.service.ts

# 5. Smoke test POST (requer servidor rodando com ATHOS_API_TOKEN e banco Athos acessivel)
curl -s -X POST http://localhost:4000/api/athos/contas-pagar \
  -H "x-api-token: $ATHOS_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"descricaoconta":"Teste fase 24","datavencimento":"2026-05-31","valorconta":150.00}' \
  | jq .

# 6. Smoke test GET com filtro statusconta
curl -s "http://localhost:4000/api/athos/contas-pagar?statusconta=ABE" \
  -H "x-api-token: $ATHOS_API_TOKEN" \
  | jq 'length'

# 7. Smoke test auth fail-closed (sem token no env — apenas em ambiente de teste com env limpa)
# Esperado: 500 com mensagem "ATHOS_API_TOKEN nao configurado no servidor"

# 8. Smoke test auth 401 (token errado)
curl -s http://localhost:4000/api/athos/contas-pagar \
  -H "x-api-token: token_errado" \
  | jq .statusCode
# Esperado: 401
```

</verification>

<success_criteria>
1. `POST /athos/contas-pagar` com body `{"descricaoconta":"X","datavencimento":"2026-05-31","valorconta":10.00}` e token correto retorna `{"idcontapagar": <numero>}` (HTTP 201).
2. `GET /athos/contas-pagar?statusconta=ABE` retorna apenas contas com statusconta="ABE".
3. Qualquer endpoint sem ATHOS_API_TOKEN no env retorna HTTP 500.
4. Qualquer endpoint com token incorreto retorna HTTP 401.
5. `npm run build --workspace=apps/backend` conclui sem erros.
6. Nenhum bloco `if (requiredToken)` permanece no controller.
</success_criteria>

<output>
Apos a conclusao, criar `.planning/phases/24-api-contas-pagar-post/24-01-SUMMARY.md` com:
- Arquivos modificados/criados
- Assinatura final de `criarContaPagar()` e `listarContasPagar()` (nova assinatura com statusconta)
- Confirmacao de que o bug CONCERNS.md HIGH foi corrigido
- Resultado do build
</output>
