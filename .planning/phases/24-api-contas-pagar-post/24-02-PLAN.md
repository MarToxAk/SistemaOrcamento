---
phase: 24-api-contas-pagar-post
plan: 24-02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - apps/backend/src/modules/integrations/athos/athos.service.test.ts
  - apps/backend/src/modules/integrations/athos/athos.controller.test.ts
autonomous: true
requirements: [CPAG-01, CPAG-02, CPAG-03, CPAG-04]

must_haves:
  truths:
    - "Suite Jest para AthosService cobre criarContaPagar() com INSERT bem-sucedido, erro de conexao e client.release()"
    - "Suite Jest para AthosService cobre filtro statusconta em listarContasPagar()"
    - "Suite Jest para AthosController cobre ATHOS_API_TOKEN ausente → 500"
    - "Suite Jest para AthosController cobre token incorreto → 401"
    - "Todos os testes passam sem erros: npx jest athos --no-coverage"
  artifacts:
    - path: "apps/backend/src/modules/integrations/athos/athos.service.test.ts"
      provides: "describe block criarContaPagar + describe block listarContasPagar statusconta"
      contains: "criarContaPagar"
    - path: "apps/backend/src/modules/integrations/athos/athos.controller.test.ts"
      provides: "Testes de autenticacao fail-closed para todos os endpoints"
      contains: "validateAthosToken"
  key_links:
    - from: "athos.service.test.ts describe(criarContaPagar)"
      to: "AthosService.criarContaPagar()"
      via: "jest.mock('pg') + mockResolvedValueOnce({ rows: [{ idcontapagar: 42 }] })"
      pattern: "idcontapagar.*42"
    - from: "athos.controller.test.ts"
      to: "AthosController.validateAthosToken()"
      via: "delete process.env.ATHOS_API_TOKEN antes do teste"
      pattern: "InternalServerErrorException"
---

<objective>
Adicionar cobertura de testes unitarios para os novos comportamentos da fase 24: metodo criarContaPagar(), filtro statusconta, e autenticacao fail-closed.

Purpose: Garantir regressoes detectaveis para CPAG-01 (INSERT + RETURNING), CPAG-02 (validacao DTO), CPAG-03 (filtro statusconta), e CPAG-04 (auth fail-closed) sem depender de banco real.

Output:
- `athos.service.test.ts` modificado — 2 novos describe blocks (criarContaPagar e statusconta filter)
- `athos.controller.test.ts` criado — testes de auth fail-closed para os 3 endpoints
</objective>

<execution_context>
@D:\Projetos\cloudeproject\SistemaOrcamento\.planning\phases\24-api-contas-pagar-post\24-RESEARCH.md
</execution_context>

<context>
@D:\Projetos\cloudeproject\SistemaOrcamento\.planning\PROJECT.md

<interfaces>
<!-- Padroes de teste extraidos do arquivo existente athos.service.test.ts -->

Padrao de mock do modulo pg (ja estabelecido no arquivo existente — NAO duplicar o jest.mock):
```typescript
// jest.mock("pg", ...) JA EXISTE no topo do arquivo — nao adicionar de novo
// Acesso ao mock:
const pgMock = require("pg");  // JA EXISTE como variavel no topo

// Para cada it(), criar novo pool+client mock:
const pool = pgMock.Pool.mock.results[0]?.value ?? new (pgMock.Pool)();
const client = { query: jest.fn(), release: jest.fn() };
pool.connect = jest.fn().mockResolvedValue(client);

// Para simular RETURNING idcontapagar:
client.query.mockResolvedValueOnce({ rows: [{ idcontapagar: 42 }] });

// Para simular erro de conexao:
pool.connect = jest.fn().mockRejectedValue(new Error("connection refused"));

// Para simular erro na query:
client.query.mockRejectedValueOnce(new Error("db error"));
```

Padrao de describe/beforeAll/beforeEach (seguir o mesmo do arquivo existente):
```typescript
describe("AthosService - criarContaPagar", () => {
  let service: AthosService;

  beforeAll(() => {
    process.env.ATHOS_PG_HOST = "localhost";
    process.env.ATHOS_PG_DB = "athos";
    process.env.ATHOS_PG_USER = "user";
    process.env.ATHOS_PG_PASS = "pass";
    process.env.ATHOS_PG_PORT = "5432";
  });

  beforeEach(async () => {
    jest.clearAllMocks();
    const module: TestingModule = await Test.createTestingModule({
      providers: [AthosService],
    }).compile();
    service = module.get<AthosService>(AthosService);
  });

  afterEach(() => jest.clearAllMocks());
  // ... its
});
```

Padrao de controller test com NestJS Testing:
```typescript
import { INestApplication, ValidationPipe } from "@nestjs/common";
import { Test, TestingModule } from "@nestjs/testing";
import * as request from "supertest";
import { AthosController } from "./athos.controller";
import { AthosService } from "./athos.service";

describe("AthosController - autenticacao", () => {
  let app: INestApplication;
  const mockAthosService = {
    listarContasPagar: jest.fn().mockResolvedValue([]),
    criarContaPagar: jest.fn().mockResolvedValue({ idcontapagar: 1 }),
    buscarClientes: jest.fn().mockResolvedValue({ total: 0, page: 1, take: 20, items: [] }),
  };

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      controllers: [AthosController],
      providers: [{ provide: AthosService, useValue: mockAthosService }],
    }).compile();
    app = module.createNestApplication();
    app.useGlobalPipes(new ValidationPipe({ whitelist: true, transform: true, forbidNonWhitelisted: true }));
    await app.init();
  });

  afterEach(async () => {
    await app.close();
    jest.clearAllMocks();
  });
  // ... its com supertest
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar testes de criarContaPagar e statusconta ao athos.service.test.ts</name>
  <files>apps/backend/src/modules/integrations/athos/athos.service.test.ts</files>

  <read_first>
    - apps/backend/src/modules/integrations/athos/athos.service.test.ts — LEIA INTEIRO para entender o padrao de mock do pg e a estrutura de describe/beforeAll/beforeEach/afterEach existente
    - apps/backend/src/modules/integrations/athos/athos.service.ts — metodos criarContaPagar() e listarContasPagar() implementados na Wave 1 (assinaturas e comportamento)
    - .planning/phases/24-api-contas-pagar-post/24-RESEARCH.md secao "Padrao do teste Jest para o INSERT"
  </read_first>

  <action>
  ADICIONAR ao final de `apps/backend/src/modules/integrations/athos/athos.service.test.ts` (sem alterar os describe blocks existentes) dois novos describe blocks:

  **Bloco 1 — criarContaPagar (CPAG-01 e CPAG-02 lado service):**

  ```typescript
  describe("AthosService - criarContaPagar", () => {
    let service: AthosService;

    beforeAll(() => {
      process.env.ATHOS_PG_HOST = "localhost";
      process.env.ATHOS_PG_DB = "athos";
      process.env.ATHOS_PG_USER = "user";
      process.env.ATHOS_PG_PASS = "pass";
      process.env.ATHOS_PG_PORT = "5432";
    });

    beforeEach(async () => {
      jest.clearAllMocks();
      const module: TestingModule = await Test.createTestingModule({
        providers: [AthosService],
      }).compile();
      service = module.get<AthosService>(AthosService);
    });

    afterEach(() => jest.clearAllMocks());

    it("deve retornar idcontapagar ao inserir conta_pagar com campos obrigatorios", async () => {
      const pool = pgMock.Pool.mock.results[0]?.value ?? new (pgMock.Pool)();
      const client = { query: jest.fn(), release: jest.fn() };
      pool.connect = jest.fn().mockResolvedValue(client);

      client.query.mockResolvedValueOnce({ rows: [{ idcontapagar: 42 }] });

      const result = await service.criarContaPagar({
        descricaoconta: "Fornecedor X",
        datavencimento: "2026-05-20",
        valorconta: 150.0,
      });

      expect(result.idcontapagar).toBe(42);
      expect(client.query).toHaveBeenCalledWith(
        expect.stringContaining("INSERT INTO conta_pagar"),
        expect.arrayContaining(["Fornecedor X", "2026-05-20", 150.0]),
      );
      expect(client.release).toHaveBeenCalled();
    });

    it("deve passar null para campos opcionais ausentes no INSERT", async () => {
      const pool = pgMock.Pool.mock.results[0]?.value ?? new (pgMock.Pool)();
      const client = { query: jest.fn(), release: jest.fn() };
      pool.connect = jest.fn().mockResolvedValue(client);

      client.query.mockResolvedValueOnce({ rows: [{ idcontapagar: 99 }] });

      await service.criarContaPagar({
        descricaoconta: "Conta sem opcionais",
        datavencimento: "2026-06-01",
        valorconta: 50.0,
      });

      const callArgs = client.query.mock.calls[0][1] as unknown[];
      // posicoes 3..6 (indices 3,4,5,6) sao os campos opcionais — devem ser null
      expect(callArgs[3]).toBeNull(); // dataemissao
      expect(callArgs[4]).toBeNull(); // observacao
      expect(callArgs[5]).toBeNull(); // idfornecedor
      expect(callArgs[6]).toBeNull(); // numerodocumento
    });

    it("deve incluir campos opcionais quando fornecidos", async () => {
      const pool = pgMock.Pool.mock.results[0]?.value ?? new (pgMock.Pool)();
      const client = { query: jest.fn(), release: jest.fn() };
      pool.connect = jest.fn().mockResolvedValue(client);

      client.query.mockResolvedValueOnce({ rows: [{ idcontapagar: 77 }] });

      const result = await service.criarContaPagar({
        descricaoconta: "Fornecedor Y",
        datavencimento: "2026-06-15",
        valorconta: 200.0,
        dataemissao: "2026-06-01",
        observacao: "Nota fiscal 123",
        idfornecedor: 5,
        numerodocumento: "NF-123",
      });

      expect(result.idcontapagar).toBe(77);
      const callArgs = client.query.mock.calls[0][1] as unknown[];
      expect(callArgs[3]).toBe("2026-06-01"); // dataemissao
      expect(callArgs[4]).toBe("Nota fiscal 123"); // observacao
      expect(callArgs[5]).toBe(5); // idfornecedor
      expect(callArgs[6]).toBe("NF-123"); // numerodocumento
    });

    it("deve chamar client.release() mesmo quando a query falha", async () => {
      const pool = pgMock.Pool.mock.results[0]?.value ?? new (pgMock.Pool)();
      const client = { query: jest.fn(), release: jest.fn() };
      pool.connect = jest.fn().mockResolvedValue(client);

      client.query.mockRejectedValueOnce(new Error("db error"));

      await expect(
        service.criarContaPagar({
          descricaoconta: "Teste erro",
          datavencimento: "2026-05-31",
          valorconta: 10.0,
        }),
      ).rejects.toThrow("Erro ao criar conta a pagar no Athos");

      expect(client.release).toHaveBeenCalled();
    });

    it("deve lancar InternalServerErrorException quando pool.connect falha", async () => {
      const pool = pgMock.Pool.mock.results[0]?.value ?? new (pgMock.Pool)();
      pool.connect = jest.fn().mockRejectedValue(new Error("connection refused"));

      await expect(
        service.criarContaPagar({
          descricaoconta: "Teste conexao",
          datavencimento: "2026-05-31",
          valorconta: 10.0,
        }),
      ).rejects.toThrow("Erro ao criar conta a pagar no Athos");
    });

    it("deve incluir RETURNING idcontapagar na query SQL", async () => {
      const pool = pgMock.Pool.mock.results[0]?.value ?? new (pgMock.Pool)();
      const client = { query: jest.fn(), release: jest.fn() };
      pool.connect = jest.fn().mockResolvedValue(client);

      client.query.mockResolvedValueOnce({ rows: [{ idcontapagar: 1 }] });

      await service.criarContaPagar({
        descricaoconta: "X",
        datavencimento: "2026-05-31",
        valorconta: 1.0,
      });

      const sql = client.query.mock.calls[0][0] as string;
      expect(sql).toContain("RETURNING idcontapagar");
    });
  });
  ```

  **Bloco 2 — listarContasPagar com statusconta (CPAG-03):**

  ```typescript
  describe("AthosService - listarContasPagar com statusconta", () => {
    let service: AthosService;

    beforeAll(() => {
      process.env.ATHOS_PG_HOST = "localhost";
      process.env.ATHOS_PG_DB = "athos";
      process.env.ATHOS_PG_USER = "user";
      process.env.ATHOS_PG_PASS = "pass";
      process.env.ATHOS_PG_PORT = "5432";
    });

    beforeEach(async () => {
      jest.clearAllMocks();
      const module: TestingModule = await Test.createTestingModule({
        providers: [AthosService],
      }).compile();
      service = module.get<AthosService>(AthosService);
    });

    afterEach(() => jest.clearAllMocks());

    // Helper: simula information_schema retornando a tabela conta_pagar com colunas necessarias
    function setupContasPagarTable(client: { query: jest.Mock }) {
      // 1. findExistingTable: information_schema retorna colunas da conta_pagar
      client.query
        .mockResolvedValueOnce({
          rows: [
            { column_name: "idcontapagar" },
            { column_name: "datavencimento" },
            { column_name: "descricaoconta" },
            { column_name: "valorconta" },
            { column_name: "statusconta" },
            { column_name: "dataemissao" },
          ],
        })
        // 2. SELECT real com filtros
        .mockResolvedValueOnce({ rows: [] });
    }

    it("deve adicionar condicao statusconta na query quando fornecido", async () => {
      const pool = pgMock.Pool.mock.results[0]?.value ?? new (pgMock.Pool)();
      const client = { query: jest.fn(), release: jest.fn() };
      pool.connect = jest.fn().mockResolvedValue(client);

      setupContasPagarTable(client);

      await service.listarContasPagar(undefined, undefined, "ABE");

      // A segunda chamada ao query eh a SELECT real
      const selectCall = client.query.mock.calls[1];
      const sql = selectCall[0] as string;
      const params = selectCall[1] as string[];

      expect(sql).toContain("statusconta = $");
      expect(params).toContain("ABE");
    });

    it("deve normalizar statusconta para maiusculas (pag → PAG)", async () => {
      const pool = pgMock.Pool.mock.results[0]?.value ?? new (pgMock.Pool)();
      const client = { query: jest.fn(), release: jest.fn() };
      pool.connect = jest.fn().mockResolvedValue(client);

      setupContasPagarTable(client);

      await service.listarContasPagar(undefined, undefined, "  pag  ");

      const selectCall = client.query.mock.calls[1];
      const params = selectCall[1] as string[];
      expect(params).toContain("PAG");
    });

    it("deve NAO adicionar condicao statusconta quando parametro ausente", async () => {
      const pool = pgMock.Pool.mock.results[0]?.value ?? new (pgMock.Pool)();
      const client = { query: jest.fn(), release: jest.fn() };
      pool.connect = jest.fn().mockResolvedValue(client);

      setupContasPagarTable(client);

      await service.listarContasPagar();

      const selectCall = client.query.mock.calls[1];
      const sql = selectCall[0] as string;
      expect(sql).not.toContain("statusconta =");
    });

    it("deve chamar client.release() apos listar com statusconta", async () => {
      const pool = pgMock.Pool.mock.results[0]?.value ?? new (pgMock.Pool)();
      const client = { query: jest.fn(), release: jest.fn() };
      pool.connect = jest.fn().mockResolvedValue(client);

      setupContasPagarTable(client);

      await service.listarContasPagar(undefined, undefined, "CAN");

      expect(client.release).toHaveBeenCalled();
    });
  });
  ```

  IMPORTANTE: Os dois describe blocks devem ser ADICIONADOS AO FINAL do arquivo existente. NAO remover nem alterar os describe blocks existentes (verificarPagamentoPorOrcamento).
  IMPORTANTE: NAO adicionar um segundo `jest.mock("pg", ...)` — o mock ja existe no topo do arquivo.
  IMPORTANTE: NAO adicionar um segundo `const pgMock = require("pg")` — a variavel ja existe.
  IMPORTANTE: Os imports de `Test, TestingModule` ja existem no topo do arquivo — NAO duplicar.
  </action>

  <verify>
    <automated>cd apps/backend && npx jest src/modules/integrations/athos/athos.service.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "describe.*criarContaPagar" apps/backend/src/modules/integrations/athos/athos.service.test.ts` retorna 1
    - `grep -c "describe.*listarContasPagar.*statusconta" apps/backend/src/modules/integrations/athos/athos.service.test.ts` retorna 1
    - `grep -c "RETURNING idcontapagar" apps/backend/src/modules/integrations/athos/athos.service.test.ts` retorna 1
    - `grep -c "client.release.*toHaveBeenCalled" apps/backend/src/modules/integrations/athos/athos.service.test.ts` retorna pelo menos 4 (um por it que verifica release)
    - Suite roda sem erros: `npx jest athos.service.test.ts --no-coverage` termina com "Tests: X passed"
    - Testes existentes (verificarPagamentoPorOrcamento) continuam passando
  </acceptance_criteria>

  <done>
    Arquivo com 2 novos describe blocks adicionados ao final, sem alterar os testes existentes.
    Suite completa passa: todos os its de verificarPagamentoPorOrcamento + criarContaPagar + listarContasPagar statusconta.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar athos.controller.test.ts com testes de autenticacao fail-closed</name>
  <files>apps/backend/src/modules/integrations/athos/athos.controller.test.ts</files>

  <read_first>
    - apps/backend/src/modules/integrations/athos/athos.controller.ts — controller implementado na Wave 1 (assinaturas dos endpoints e logica de validateAthosToken)
    - apps/backend/src/modules/integrations/athos/athos.service.test.ts — padrao de Test.createTestingModule e estrutura de describe/beforeEach/afterEach do projeto
    - apps/backend/jest.config.js — configuracao do jest (para confirmar testRegex e resolver de path)
  </read_first>

  <action>
  CRIAR o arquivo `apps/backend/src/modules/integrations/athos/athos.controller.test.ts` com o seguinte conteudo:

  ```typescript
  import { INestApplication, InternalServerErrorException, UnauthorizedException, ValidationPipe } from "@nestjs/common";
  import { Test, TestingModule } from "@nestjs/testing";
  import * as request from "supertest";
  import { AthosController } from "./athos.controller";
  import { AthosService } from "./athos.service";

  const VALID_TOKEN = "test-token-fase-24";

  const mockAthosService = {
    listarContasPagar: jest.fn().mockResolvedValue([]),
    criarContaPagar: jest.fn().mockResolvedValue({ idcontapagar: 1 }),
    buscarClientes: jest.fn().mockResolvedValue({ total: 0, page: 1, take: 20, items: [] }),
  };

  describe("AthosController - autenticacao fail-closed (CPAG-04)", () => {
    let app: INestApplication;

    beforeEach(async () => {
      jest.clearAllMocks();

      // Estado inicial: token configurado
      process.env.ATHOS_API_TOKEN = VALID_TOKEN;

      const module: TestingModule = await Test.createTestingModule({
        controllers: [AthosController],
        providers: [{ provide: AthosService, useValue: mockAthosService }],
      }).compile();

      app = module.createNestApplication();
      app.useGlobalPipes(
        new ValidationPipe({ whitelist: true, transform: true, forbidNonWhitelisted: true }),
      );
      await app.init();
    });

    afterEach(async () => {
      await app.close();
      jest.clearAllMocks();
      // Restaurar token apos cada teste
      process.env.ATHOS_API_TOKEN = VALID_TOKEN;
    });

    // --- CPAG-04: ATHOS_API_TOKEN ausente → 500 ---

    it("GET /athos/contas-pagar sem ATHOS_API_TOKEN no env deve retornar 500", async () => {
      delete process.env.ATHOS_API_TOKEN;

      const res = await request(app.getHttpServer())
        .get("/athos/contas-pagar")
        .expect(500);

      expect(res.body.message).toMatch(/ATHOS_API_TOKEN nao configurado/i);
      expect(mockAthosService.listarContasPagar).not.toHaveBeenCalled();
    });

    it("POST /athos/contas-pagar sem ATHOS_API_TOKEN no env deve retornar 500", async () => {
      delete process.env.ATHOS_API_TOKEN;

      const res = await request(app.getHttpServer())
        .post("/athos/contas-pagar")
        .send({ descricaoconta: "Teste", datavencimento: "2026-05-31", valorconta: 10.0 })
        .expect(500);

      expect(res.body.message).toMatch(/ATHOS_API_TOKEN nao configurado/i);
      expect(mockAthosService.criarContaPagar).not.toHaveBeenCalled();
    });

    it("GET /athos/clientes sem ATHOS_API_TOKEN no env deve retornar 500", async () => {
      delete process.env.ATHOS_API_TOKEN;

      const res = await request(app.getHttpServer())
        .get("/athos/clientes")
        .query({ nome: "teste" })
        .expect(500);

      expect(res.body.message).toMatch(/ATHOS_API_TOKEN nao configurado/i);
      expect(mockAthosService.buscarClientes).not.toHaveBeenCalled();
    });

    // --- CPAG-04: Token incorreto → 401 ---

    it("GET /athos/contas-pagar com token incorreto deve retornar 401", async () => {
      const res = await request(app.getHttpServer())
        .get("/athos/contas-pagar")
        .set("x-api-token", "token-errado")
        .expect(401);

      expect(res.body.message).toMatch(/token invalido ou ausente/i);
    });

    it("POST /athos/contas-pagar com token incorreto deve retornar 401", async () => {
      const res = await request(app.getHttpServer())
        .post("/athos/contas-pagar")
        .set("x-api-token", "token-errado")
        .send({ descricaoconta: "Teste", datavencimento: "2026-05-31", valorconta: 10.0 })
        .expect(401);

      expect(res.body.message).toMatch(/token invalido ou ausente/i);
    });

    it("GET /athos/clientes com token incorreto deve retornar 401", async () => {
      process.env.ATHOS_API_TOKEN = VALID_TOKEN;
      const res = await request(app.getHttpServer())
        .get("/athos/clientes")
        .query({ nome: 'test' })
        .set('x-api-token', 'token-errado');
      expect(res.status).toBe(401);
    });

    it("GET /athos/contas-pagar sem header de token deve retornar 401", async () => {
      await request(app.getHttpServer())
        .get("/athos/contas-pagar")
        .expect(401);
    });

    // --- Token correto → sucesso ---

    it("GET /athos/contas-pagar com token correto via x-api-token deve passar", async () => {
      await request(app.getHttpServer())
        .get("/athos/contas-pagar")
        .set("x-api-token", VALID_TOKEN)
        .expect(200);

      expect(mockAthosService.listarContasPagar).toHaveBeenCalledTimes(1);
    });

    it("GET /athos/contas-pagar com token correto via Authorization Bearer deve passar", async () => {
      await request(app.getHttpServer())
        .get("/athos/contas-pagar")
        .set("Authorization", `Bearer ${VALID_TOKEN}`)
        .expect(200);

      expect(mockAthosService.listarContasPagar).toHaveBeenCalledTimes(1);
    });

    it("POST /athos/contas-pagar com token correto e body valido deve retornar idcontapagar", async () => {
      const res = await request(app.getHttpServer())
        .post("/athos/contas-pagar")
        .set("x-api-token", VALID_TOKEN)
        .send({ descricaoconta: "Teste", datavencimento: "2026-05-31", valorconta: 150.0 })
        .expect(201);

      expect(res.body.idcontapagar).toBe(1);
      expect(mockAthosService.criarContaPagar).toHaveBeenCalledWith(
        expect.objectContaining({
          descricaoconta: "Teste",
          datavencimento: "2026-05-31",
          valorconta: 150.0,
        }),
      );
    });

    // --- CPAG-02: Validacao de campos obrigatorios via ValidationPipe ---

    it("POST /athos/contas-pagar sem descricaoconta deve retornar 400", async () => {
      await request(app.getHttpServer())
        .post("/athos/contas-pagar")
        .set("x-api-token", VALID_TOKEN)
        .send({ datavencimento: "2026-05-31", valorconta: 10.0 })
        .expect(400);
    });

    it("POST /athos/contas-pagar sem datavencimento deve retornar 400", async () => {
      await request(app.getHttpServer())
        .post("/athos/contas-pagar")
        .set("x-api-token", VALID_TOKEN)
        .send({ descricaoconta: "Teste", valorconta: 10.0 })
        .expect(400);
    });

    it("POST /athos/contas-pagar sem valorconta deve retornar 400", async () => {
      await request(app.getHttpServer())
        .post("/athos/contas-pagar")
        .set("x-api-token", VALID_TOKEN)
        .send({ descricaoconta: "Teste", datavencimento: "2026-05-31" })
        .expect(400);
    });

    it("POST /athos/contas-pagar com valorconta=0 deve retornar 400 (Min 0.01)", async () => {
      await request(app.getHttpServer())
        .post("/athos/contas-pagar")
        .set("x-api-token", VALID_TOKEN)
        .send({ descricaoconta: "Teste", datavencimento: "2026-05-31", valorconta: 0 })
        .expect(400);
    });

    it("POST /athos/contas-pagar com campo extra desconhecido deve retornar 400 (forbidNonWhitelisted)", async () => {
      await request(app.getHttpServer())
        .post("/athos/contas-pagar")
        .set("x-api-token", VALID_TOKEN)
        .send({ descricaoconta: "Teste", datavencimento: "2026-05-31", valorconta: 10.0, campoExtra: "intruso" })
        .expect(400);
    });

    // --- CPAG-03: GET com statusconta ---

    it("GET /athos/contas-pagar?statusconta=ABE deve passar statusconta ao service", async () => {
      await request(app.getHttpServer())
        .get("/athos/contas-pagar")
        .set("x-api-token", VALID_TOKEN)
        .query({ statusconta: "ABE" })
        .expect(200);

      expect(mockAthosService.listarContasPagar).toHaveBeenCalledWith(
        undefined,
        undefined,
        "ABE",
      );
    });
  });
  ```

  IMPORTANTE: `supertest` provavelmente ja esta no package.json de dev dependencies do backend. Se nao estiver, substituir os testes HTTP pelo padrao de teste unitario direto chamando controller.listarContasPagar() com mocks dos params — mas verificar primeiro com `grep "supertest" apps/backend/package.json`.

  Se supertest nao estiver disponivel, usar teste unitario direto:
  ```typescript
  // alternativa sem supertest:
  it("deve lancar InternalServerErrorException quando ATHOS_API_TOKEN ausente", async () => {
    delete process.env.ATHOS_API_TOKEN;
    const controller = new AthosController(mockAthosService as unknown as AthosService);
    await expect(
      controller.listarContasPagar(undefined, undefined, undefined, undefined, undefined, undefined, undefined)
    ).rejects.toThrow(InternalServerErrorException);
  });
  ```
  </action>

  <verify>
    <automated>cd apps/backend && npx jest src/modules/integrations/athos/athos.controller.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo criado em `apps/backend/src/modules/integrations/athos/athos.controller.test.ts`
    - `grep -c "ATHOS_API_TOKEN nao configurado" apps/backend/src/modules/integrations/athos/athos.controller.test.ts` retorna pelo menos 3 (um por endpoint testado para ausencia de token)
    - `grep -c "token invalido ou ausente" apps/backend/src/modules/integrations/athos/athos.controller.test.ts` retorna pelo menos 3 (GET contas-pagar, POST contas-pagar e GET clientes com token errado)
    - `grep -c "expect(400)" apps/backend/src/modules/integrations/athos/athos.controller.test.ts` retorna pelo menos 5 (testes de validacao DTO)
    - Suite roda sem erros: `npx jest athos.controller.test.ts --no-coverage` termina com "Tests: X passed, 0 failed"
  </acceptance_criteria>

  <done>
    Arquivo criado cobrindo CPAG-04 (auth fail-closed) em todos os 3 endpoints + CPAG-02 (ValidationPipe) + CPAG-03 (statusconta passado ao service).
    Suite completa passa sem erros.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → AthosController (via supertest) | Testes HTTP simulam caller externo; validam barreiras de auth e validacao |
| Test → AthosService (via jest.mock) | Service mockado isola testes do controller de dependencias de banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | Elevation of Privilege | Teste de auth — coverage gap | mitigate | it() para cada endpoint (GET contas-pagar, POST contas-pagar, GET clientes) cobre ausencia de token; nao ha endpoint sem teste de auth |
| T-24-07 | Tampering | Testes de DTO — campos nao whitelisted | mitigate | it() para campo extra detecta regressao no forbidNonWhitelisted; it() para valorconta=0 detecta regressao no Min(0.01) |
</threat_model>

<verification>

```bash
# Suite completa do modulo Athos
cd apps/backend && npx jest src/modules/integrations/athos/ --no-coverage 2>&1 | tail -30

# Suite especifica do service (novos describe blocks)
cd apps/backend && npx jest src/modules/integrations/athos/athos.service.test.ts --no-coverage -t "criarContaPagar" 2>&1 | tail -10

# Suite especifica do controller
cd apps/backend && npx jest src/modules/integrations/athos/athos.controller.test.ts --no-coverage 2>&1 | tail -10

# Suite completa do backend (verifica que nao quebrou outros testes)
cd apps/backend && npx jest --no-coverage 2>&1 | tail -10
```

</verification>

<success_criteria>
1. `npx jest src/modules/integrations/athos/ --no-coverage` retorna "Tests: X passed, 0 failed, 0 skipped".
2. Testes existentes (verificarPagamentoPorOrcamento) continuam passando sem alteracao.
3. Pelo menos 6 testes em `criarContaPagar` describe block: INSERT ok, campos opcionais null, campos opcionais presentes, release em erro, connect falha, RETURNING na query.
4. Pelo menos 4 testes em `listarContasPagar statusconta` describe block: statusconta presente, normalizacao PAG, ausente sem condicao, release chamado.
5. Pelo menos 16 testes em `athos.controller.test.ts`: 3 para token ausente (500), 4 para token incorreto (401 — GET contas-pagar, POST contas-pagar, GET clientes, GET contas-pagar sem header), 2 para token correto via header, 5 para validacao DTO, 1 para statusconta passado ao service.
</success_criteria>

<output>
Apos a conclusao, criar `.planning/phases/24-api-contas-pagar-post/24-02-SUMMARY.md` com:
- Arquivos modificados/criados
- Contagem de testes adicionados por arquivo
- Resultado da suite completa (X passed, 0 failed)
- Confirmacao de cobertura de CPAG-01, CPAG-02, CPAG-03, CPAG-04
</output>
