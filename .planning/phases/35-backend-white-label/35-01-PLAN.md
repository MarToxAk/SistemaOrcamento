---
phase: 35-backend-white-label
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/nfse/nfse.service.ts
  - apps/backend/src/modules/integrations/nfse/nfse.service.test.ts
autonomous: true
requirements: [NFSE-01, CFG-04]
must_haves:
  truths:
    - "NFS-e emitida usa o codigo IBGE lido de EMPRESA_MUNICIPIO_IBGE, nao a string fixa 3520400"
    - "Sem EMPRESA_MUNICIPIO_IBGE definida, o getter retorna o fallback 3520400 (deploy Ilhabela continua funcionando)"
    - "A propriedade fixa private readonly CODIGO_MUNICIPIO = \"3520400\" nao existe mais no codigo"
  artifacts:
    - path: "apps/backend/src/modules/integrations/nfse/nfse.service.ts"
      provides: "Getter computado CODIGO_MUNICIPIO que le EMPRESA_MUNICIPIO_IBGE via ConfigService"
      contains: "private get CODIGO_MUNICIPIO"
    - path: "apps/backend/src/modules/integrations/nfse/nfse.service.test.ts"
      provides: "Teste do getter CODIGO_MUNICIPIO (env var presente e fallback)"
      contains: "EMPRESA_MUNICIPIO_IBGE"
  key_links:
    - from: "apps/backend/src/modules/integrations/nfse/nfse.service.ts"
      to: "EMPRESA_MUNICIPIO_IBGE"
      via: "this.config.get<string>"
      pattern: "config\\.get<string>\\(\"EMPRESA_MUNICIPIO_IBGE\"\\)"
---

<objective>
Substituir a constante `private readonly CODIGO_MUNICIPIO = "3520400"` em `NfseService` por um getter computado que le `EMPRESA_MUNICIPIO_IBGE` via `ConfigService`, mantendo `"3520400"` apenas como fallback. Implementa NFSE-01 e a leitura runtime de CFG-04.

Purpose: Permitir que a NFS-e emitida use o codigo IBGE do municipio do deploy atual sem editar codigo-fonte — base do white-label fiscal.
Output: `nfse.service.ts` com getter computado + cobertura de teste no `nfse.service.test.ts` existente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-backend-white-label/35-CONTEXT.md
@.planning/phases/35-backend-white-label/35-RESEARCH.md
@.planning/phases/35-backend-white-label/35-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Converter CODIGO_MUNICIPIO em getter computado lendo EMPRESA_MUNICIPIO_IBGE</name>
  <files>apps/backend/src/modules/integrations/nfse/nfse.service.ts, apps/backend/src/modules/integrations/nfse/nfse.service.test.ts</files>
  <read_first>
    - apps/backend/src/modules/integrations/nfse/nfse.service.ts (linha 60: constante atual; linhas 63-64: DEFAULT_ENDPOINT/DEFAULT_AUX_URL; linhas 66-68: padrao de getter WSDL_URL/ENDPOINT/AUX_URL a replicar; linhas 70-75: construtor com this.config injetado; demais usos de CODIGO_MUNICIPIO nas linhas 226, 970, 1022, 1092)
    - apps/backend/src/modules/integrations/nfse/nfse.service.test.ts (linhas 70-96: padrao de mockConfig com get jest.fn e buildService via TestingModule)
    - .planning/phases/35-backend-white-label/35-RESEARCH.md (secao "D-12 Decision Support" e "Code Examples > Getter computado para NFSE-01")
  </read_first>
  <behavior>
    - Quando EMPRESA_MUNICIPIO_IBGE="3550308" esta definida, o getter CODIGO_MUNICIPIO retorna "3550308"
    - Quando EMPRESA_MUNICIPIO_IBGE esta ausente (config.get retorna undefined), o getter retorna o fallback "3520400"
    - O XML do RPS construido por buildRpsXml usa o valor do getter no elemento de codigo de municipio
  </behavior>
  <action>
    Substituir a linha 60 `private readonly CODIGO_MUNICIPIO = "3520400";` por um getter computado `private get CODIGO_MUNICIPIO()` que retorna `this.config.get<string>("EMPRESA_MUNICIPIO_IBGE") ?? "3520400"`. Seguir exatamente o estilo dos getters existentes WSDL_URL/ENDPOINT/AUX_URL (linhas 66-68) que ja delegam ao ConfigService. Nao alterar a assinatura do construtor — `this.config` (ConfigService) ja esta injetado. Per D-11.

    NAO parametrizar DEFAULT_ENDPOINT nem DEFAULT_AUX_URL (linhas 63-64): per D-12 e analise do RESEARCH.md, esses URLs sao especificos da prefeitura de Ilhabela (dominio ilhabela2.iibr.com.br) e ja sao sobrescritos por NFSE_SOAP_URL/NFSE_AUX_URL. Manter como estao.

    Confirmar que todos os demais consumidores internos de CODIGO_MUNICIPIO (linhas ~226, ~970, ~1022, ~1092) acessam via `this.CODIGO_MUNICIPIO` — um getter mantem a mesma sintaxe de acesso, entao nenhum call-site precisa mudar. Verificar apos a troca que nao restou nenhum acesso direto a uma propriedade de instancia removida.

    No arquivo de teste existente, adicionar um describe/it cobrindo o getter: (a) mockConfig.get retornando "3550308" para EMPRESA_MUNICIPIO_IBGE -> esperar que o getter/comportamento use "3550308"; (b) mockConfig.get retornando undefined -> esperar fallback "3520400". Usar o padrao de mockConfig e buildService ja presente nas linhas 70-96 do arquivo de teste. Acessar o getter via cast a unknown/any se for membro privado, OU verificar atraves do XML gerado por um metodo publico que consuma CODIGO_MUNICIPIO, conforme for mais direto dado o codigo existente lido no read_first.
  </action>
  <verify>
    <automated>npm --workspace @bomcusto/backend test -- --testPathPattern=nfse.service</automated>
  </verify>
  <acceptance_criteria>
    - `nfse.service.ts` contem `private get CODIGO_MUNICIPIO`
    - `nfse.service.ts` contem `config.get<string>("EMPRESA_MUNICIPIO_IBGE")`
    - `nfse.service.ts` NAO contem mais `private readonly CODIGO_MUNICIPIO`
    - `nfse.service.ts` ainda contem `DEFAULT_ENDPOINT` e `DEFAULT_AUX_URL` inalterados (D-12)
    - `nfse.service.test.ts` contem o token `EMPRESA_MUNICIPIO_IBGE`
    - `npm --workspace @bomcusto/backend test -- --testPathPattern=nfse.service` sai com codigo 0
  </acceptance_criteria>
  <done>O getter computado le EMPRESA_MUNICIPIO_IBGE com fallback 3520400, a constante fixa foi removida, DEFAULT_ENDPOINT/DEFAULT_AUX_URL permanecem intactos, e o teste do getter passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador do servidor -> env var | EMPRESA_MUNICIPIO_IBGE e definida no .env do deploy pelo operador, nao por input de usuario final |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | EMPRESA_MUNICIPIO_IBGE -> XML do RPS NFS-e | accept | Valor lido de env var controlada pelo operador do servidor (nao input de usuario). Codigo IBGE incorreto seria erro de configuracao de deploy, nao vetor de ataque. Fallback "3520400" garante deploy Ilhabela funcional. |
</threat_model>

<verification>
- `npm --workspace @bomcusto/backend test -- --testPathPattern=nfse.service` verde
- Busca por `private readonly CODIGO_MUNICIPIO` no codigo retorna zero ocorrencias
- Busca por `config.get<string>("EMPRESA_MUNICIPIO_IBGE")` retorna ao menos uma ocorrencia em nfse.service.ts
</verification>

<success_criteria>
- NFS-e usa codigo IBGE de EMPRESA_MUNICIPIO_IBGE quando definida (CFG-04, NFSE-01)
- A string "3520400" como propriedade fixa nao existe mais (sobrevive apenas como fallback no getter, per success criteria 3 do ROADMAP)
- Teste automatizado cobre presenca e ausencia da env var
</success_criteria>

<output>
Create `.planning/phases/35-backend-white-label/35-01-SUMMARY.md` when done
</output>
