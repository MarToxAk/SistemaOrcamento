---
phase: 35-backend-white-label
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/templates/quote-default.hbs
  - apps/backend/Dockerfile
autonomous: true
requirements: [PDF-02, PDF-03, PDF-05, CFG-02, CFG-03, CFG-05]
must_haves:
  truths:
    - "Existe um arquivo de template externo apps/backend/templates/quote-default.hbs com o conteudo do template PDF"
    - "O template usa variaveis Handlebars de empresa (empresaNome, empresaCnpj, empresaEndereco, empresaLogoUrl, empresaCor) no lugar das strings BomCusto hardcoded"
    - "A imagem Docker copia o diretorio templates/ para a imagem runtime"
    - "O logo so renderiza uma tag <img> quando empresaLogoUrl esta presente"
    - "A cor primaria e injetada como CSS custom property --primary no :root do template"
  artifacts:
    - path: "apps/backend/templates/quote-default.hbs"
      provides: "Template PDF externo com variaveis de empresa e bloco de documentacao Handlebars"
      contains: "{{empresaNome}}"
      min_lines: 100
    - path: "apps/backend/Dockerfile"
      provides: "COPY do diretorio templates para a imagem runtime"
      contains: "templates ./apps/backend/templates"
  key_links:
    - from: "apps/backend/templates/quote-default.hbs"
      to: "empresaLogoUrl"
      via: "bloco condicional Handlebars"
      pattern: "\\{\\{#if empresaLogoUrl\\}\\}"
    - from: "apps/backend/templates/quote-default.hbs"
      to: "empresaCor"
      via: "CSS custom property"
      pattern: "--primary: \\{\\{empresaCor\\}\\}"
---

<objective>
Extrair o template Handlebars da string TypeScript inline `QUOTES_PDF_HTML_TEMPLATE` para um arquivo externo `apps/backend/templates/quote-default.hbs`, substituindo as strings BomCusto hardcoded por variaveis Handlebars de empresa, documentando as variaveis disponiveis via comentarios `{{! ... }}`, aplicando a cor primaria como CSS custom property, e adicionando a copia do diretorio `templates/` ao Dockerfile.

Purpose: Tornar o template PDF customizavel por empresa (via variaveis) e por volume Docker (arquivo externo) sem rebuild da imagem nem edicao de TypeScript. Implementa PDF-02, PDF-03, PDF-05 e a parte de template de CFG-02/CFG-03/CFG-05.
Output: Novo `quote-default.hbs` + linha COPY no Dockerfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-backend-white-label/35-CONTEXT.md
@.planning/phases/35-backend-white-label/35-RESEARCH.md
@.planning/phases/35-backend-white-label/35-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extrair template para quote-default.hbs com variaveis de empresa e documentacao</name>
  <files>apps/backend/templates/quote-default.hbs</files>
  <read_first>
    - apps/backend/src/modules/quotes/quotes-pdf.template.ts (string QUOTES_PDF_HTML_TEMPLATE completa; hardcodes BomCusto nas linhas ~148 logo, ~151 nome, ~152 CNPJ, ~155 endereco, ~276 assinatura; bloco <style> com :root e cores de branding; linhas 156-161 telefone/email/Instagram)
    - .planning/phases/35-backend-white-label/35-PATTERNS.md (secao do quote-default.hbs: substituicoes linha-a-linha exatas, bloco {{! ... }} de documentacao, CSS custom property --primary)
    - .planning/phases/35-backend-white-label/35-RESEARCH.md (Pattern 4: Variaveis Handlebars de empresa; Open Question 1 sobre contato BomCusto)
  </read_first>
  <action>
    Criar o arquivo `apps/backend/templates/quote-default.hbs` com o conteudo INTEGRAL da string `QUOTES_PDF_HTML_TEMPLATE` de `quotes-pdf.template.ts` (extracao direta — copiar todo o HTML/CSS atual), aplicando exatamente estas substituicoes:

    1. Logo (linha ~148 do template atual): substituir o `<img src="{{#if logoUrl}}...{{else}}https://autopyweb.com.br/logo_new.svg{{/if}}" ... alt="Bom Custo">` por bloco condicional `{{#if empresaLogoUrl}}<img src="{{empresaLogoUrl}}" class="doc-header__logo" alt="{{empresaNome}}">{{/if}}`. Per D-08: sem fallback de URL externa; sem empresaLogoUrl, nenhuma tag img e renderizada (evita imagem quebrada).
    2. Nome (linha ~151): `<h1>Bom Custo Papelaria &amp; Grafica Rapida LTDA</h1>` -> `<h1>{{empresaNome}}</h1>`.
    3. CNPJ (linha ~152): `<div class="cnpj"><b>CNPJ</b> 62.391.927/0001-57</div>` -> `<div class="cnpj"><b>CNPJ</b> {{empresaCnpj}}</div>`.
    4. Endereco (linha ~155): substituir o texto fixo "Rua Olimpio Leite da Silva, 39 ..." por `{{empresaEndereco}}`.
    5. Assinatura (linha ~276): `<b>Atenciosamente, equipe Bom Custo.</b>` -> `<b>Atenciosamente, equipe {{empresaNome}}.</b>`.

    Per D-09 / CFG-05: no inicio do bloco `:root` dentro do `<style>`, adicionar a linha `--primary: {{empresaCor}};`. Em seguida, identificar as cores de branding hardcoded no template atual (cor do cabecalho, bordas de tabela, area de totais — avaliar o CSS lido no read_first) e substitui-las por `var(--primary)` de forma consistente. empresaCor sempre chega preenchido (fallback no servico), entao o template sempre recebe um valor concreto.

    Per D-10 / PDF-05: adicionar no TOPO do arquivo um bloco de documentacao Handlebars `{{! ... }}` listando todas as variaveis disponiveis com exemplos BomCusto como referencia — variaveis de empresa (empresaNome, empresaCnpj, empresaEndereco, empresaLogoUrl, empresaCor) e variaveis de orcamento (idorcamento, dataorcamento, cliente.nome/telefone/email, vendedorNome, validade, prazoEntrega, condicaoPagamento, observacoes, itens[] com sequenciaitem/produto/quantidadeitem/valoritem/orcamentovalorfinalitem, carimbos.itens[] com numero/carimbo/dimensoes/descricao, totais.valor/desconto). Usar a lista exata do 35-PATTERNS.md.

    Per Open Question 1 do RESEARCH (escopo minimo): manter telefone, e-mail e Instagram (linhas 156-161) como texto estatico no template padrao — NAO parametrizar nesta fase. O mecanismo de customizacao avancada e o template externo via EMPRESA_PDF_TEMPLATE_PATH (Plano 03).

    Manter TODAS as demais variaveis de orcamento Handlebars existentes intactas — esta tarefa so troca os hardcodes de empresa e branding, sem mexer na logica de itens/carimbos/totais.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('apps/backend/templates/quote-default.hbs','utf8');const must=['{{empresaNome}}','{{empresaCnpj}}','{{empresaEndereco}}','{{#if empresaLogoUrl}}','--primary: {{empresaCor}}'];const missing=must.filter(t=>!s.includes(t));if(missing.length){console.error('FALTANDO:',missing);process.exit(1)}if(/Bom Custo Papelaria/.test(s)){console.error('Nome hardcoded ainda presente');process.exit(1)}if(/62\.391\.927/.test(s)){console.error('CNPJ hardcoded ainda presente');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/templates/quote-default.hbs` existe e tem mais de 100 linhas
    - contem `{{empresaNome}}`, `{{empresaCnpj}}`, `{{empresaEndereco}}`
    - contem o bloco condicional `{{#if empresaLogoUrl}}`
    - contem `--primary: {{empresaCor}}` no bloco :root
    - contem `var(--primary)` em ao menos um elemento de branding
    - NAO contem mais a string `Bom Custo Papelaria` como nome de empresa hardcoded
    - NAO contem mais o CNPJ literal `62.391.927/0001-57` na area de cabecalho da empresa
    - contem um bloco `{{!` de documentacao no topo listando as variaveis de empresa e orcamento
    - contem `Atenciosamente, equipe {{empresaNome}}.`
  </acceptance_criteria>
  <done>Arquivo .hbs externo criado com variaveis de empresa substituindo hardcodes, CSS custom property --primary aplicada, logo condicional, documentacao no topo, e contato BomCusto mantido como texto estatico.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar COPY de templates/ ao stage runtime do Dockerfile</name>
  <files>apps/backend/Dockerfile</files>
  <read_first>
    - apps/backend/Dockerfile (stage runtime, linhas ~31-36: sequencia de COPY --from=build para dist/prisma/scripts; identificar a linha que copia scripts)
    - .planning/phases/35-backend-white-label/35-PATTERNS.md (secao Dockerfile: padrao de COPY existente e linha exata a inserir)
  </read_first>
  <action>
    No stage `runtime` do Dockerfile, inserir imediatamente apos a linha `COPY --from=build /app/apps/backend/scripts ./apps/backend/scripts` uma nova linha `COPY --from=build /app/apps/backend/templates ./apps/backend/templates`. Per D-04: mesmo padrao de prisma/ e scripts/. Isso garante que `quote-default.hbs` exista em `/app/apps/backend/templates/quote-default.hbs` na imagem — o mesmo path que `path.resolve(process.cwd(), "apps/backend/templates/quote-default.hbs")` resolve no container com WORKDIR /app (Pitfall 2 do RESEARCH). Nao alterar nenhuma outra linha do Dockerfile.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('apps/backend/Dockerfile','utf8');if(!/COPY --from=build \/app\/apps\/backend\/templates \.\/apps\/backend\/templates/.test(s)){console.error('linha COPY templates ausente');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/Dockerfile` contem `COPY --from=build /app/apps/backend/templates ./apps/backend/templates`
    - a nova linha aparece no stage runtime, apos a linha de COPY de scripts
  </acceptance_criteria>
  <done>O Dockerfile copia o diretorio templates/ para a imagem runtime, garantindo que o template padrao .hbs esteja disponivel em producao no path esperado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador -> arquivo .hbs no build | O template e um artefato estatico copiado no build da imagem; conteudo controlado pelo repositorio |
| empresaLogoUrl -> tag <img> renderizada por Puppeteer | URL vem de env var; Puppeteer resolve a URL localmente no container ao renderizar o HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02 | Information Disclosure | EMPRESA_LOGO_URL usada em `<img src="">` no template | accept | URL vinda de env var controlada pelo operador, nao de input de usuario. Puppeteer resolve a URL no contexto local do container ao renderizar; nao ha fetch server-side iniciado pelo backend. Risco controlado para este cenario (ASVS L1). |
| T-35-03 | Tampering | conteudo do template .hbs na imagem | mitigate | Template padrao copiado no build a partir do repositorio versionado; nenhuma escrita em runtime. Template customizado externo (volume) tratado no Plano 03 com checagem de existencia explicita. |
</threat_model>

<verification>
- `apps/backend/templates/quote-default.hbs` existe com variaveis de empresa e sem nome/CNPJ BomCusto hardcoded
- Dockerfile copia templates/ para runtime
- Busca por `{{#if empresaLogoUrl}}` e `--primary: {{empresaCor}}` no .hbs retorna match
</verification>

<success_criteria>
- Template PDF extraido para arquivo externo .hbs (PDF-03)
- Template usa variaveis de empresa em vez de texto hardcoded (PDF-02, CFG-02/03/05 no nivel de template)
- Template documenta todas as variaveis disponiveis via comentarios Handlebars (PDF-05)
- Imagem Docker inclui o diretorio templates/ (suporte a PDF-03 em producao)
</success_criteria>

<output>
Create `.planning/phases/35-backend-white-label/35-02-SUMMARY.md` when done
</output>
