---
phase: 35-backend-white-label
plan: 03
type: execute
wave: 2
depends_on: ["35-02"]
files_modified:
  - apps/backend/src/modules/quotes/quotes-pdf-storage.service.ts
  - apps/backend/src/modules/quotes/quotes-pdf-storage.service.test.ts
  - apps/backend/src/modules/app.module.ts
  - .env.example
autonomous: true
requirements: [PDF-01, PDF-04, CFG-01, CFG-02, CFG-03, CFG-05]
must_haves:
  truths:
    - "renderHtml resolve o template na ordem: EMPRESA_PDF_TEMPLATE_PATH -> quote-default.hbs externo -> string TS embutida"
    - "EMPRESA_PDF_TEMPLATE_PATH definida com arquivo ausente lanca erro explicito (nao falha silenciosa)"
    - "renderHtml passa empresaNome, empresaCnpj, empresaEndereco, empresaLogoUrl, empresaCor ao contexto Handlebars"
    - "empresaLogoUrl permanece undefined quando ausente (nao string vazia)"
    - "empresaCor usa fallback #0d6efd quando EMPRESA_COR_PRIMARIA ausente"
    - "Backend nao sobe sem EMPRESA_NOME, EMPRESA_CNPJ, EMPRESA_ENDERECO, EMPRESA_MUNICIPIO_IBGE"
    - ".env.example documenta todas as vars EMPRESA_* com defaults BomCusto e comentarios"
  artifacts:
    - path: "apps/backend/src/modules/quotes/quotes-pdf-storage.service.ts"
      provides: "Cadeia de resolucao de template + injecao de dados de empresa no contexto Handlebars"
      contains: "EMPRESA_PDF_TEMPLATE_PATH"
    - path: "apps/backend/src/modules/quotes/quotes-pdf-storage.service.test.ts"
      provides: "Testes da cadeia de fallback, erro de template ausente e contexto de empresa"
      contains: "EMPRESA_PDF_TEMPLATE_PATH"
    - path: "apps/backend/src/modules/app.module.ts"
      provides: "4 vars EMPRESA_* obrigatorias + hint .env.example no erro"
      contains: "EMPRESA_NOME"
    - path: ".env.example"
      provides: "Secao Empresa (White-Label) com todas as vars documentadas"
      contains: "EMPRESA_MUNICIPIO_IBGE"
  key_links:
    - from: "apps/backend/src/modules/quotes/quotes-pdf-storage.service.ts"
      to: "apps/backend/templates/quote-default.hbs"
      via: "fs.readFileSync na cadeia de fallback"
      pattern: "quote-default\\.hbs"
    - from: "apps/backend/src/modules/app.module.ts"
      to: ".env.example"
      via: "mensagem de erro de startup com hint"
      pattern: "See \\.env\\.example for EMPRESA_\\* setup instructions"
---

<objective>
Atualizar `QuotesPdfStorageService.renderHtml()` para resolver o template via cadeia de fallback (EMPRESA_PDF_TEMPLATE_PATH -> arquivo externo quote-default.hbs -> string TS embutida) e injetar os dados de empresa lidos via ConfigService no contexto Handlebars; adicionar as 4 vars EMPRESA_* obrigatorias ao `REQUIRED_ENV_VARS` de `app.module.ts` com hint no erro; e documentar todas as vars EMPRESA_* no `.env.example`.

Purpose: Conectar o template externo (Plano 02) ao renderer, habilitar customizacao por volume Docker com falha explicita, e garantir fail-fast + documentacao das novas vars. Implementa PDF-01, PDF-04, CFG-01 e a leitura runtime de CFG-02/03/05.
Output: `quotes-pdf-storage.service.ts` + testes, `app.module.ts`, `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-backend-white-label/35-CONTEXT.md
@.planning/phases/35-backend-white-label/35-RESEARCH.md
@.planning/phases/35-backend-white-label/35-PATTERNS.md
@apps/backend/templates/quote-default.hbs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cadeia de resolucao de template + dados de empresa em renderHtml()</name>
  <files>apps/backend/src/modules/quotes/quotes-pdf-storage.service.ts, apps/backend/src/modules/quotes/quotes-pdf-storage.service.test.ts</files>
  <read_first>
    - apps/backend/src/modules/quotes/quotes-pdf-storage.service.ts (linhas 1-6 imports; linha 39 const HTML_TEMPLATE; linha 45 construtor com configService; linhas 99-142 renderHtml atual; linhas 189-202 padrao de fallback ?? em buildMinioClient; linhas 230-238 padrao requireEnv com InternalServerErrorException)
    - apps/backend/templates/quote-default.hbs (template externo criado no Plano 02 — confirmar nomes das variaveis de empresa esperadas)
    - apps/backend/src/modules/integrations/nfse/nfse.service.test.ts (linhas 70-96: padrao mockConfig + buildService via TestingModule — replicar para o novo arquivo de teste)
    - .planning/phases/35-backend-white-label/35-RESEARCH.md (Pattern 3: cadeia de fallback; Code Examples > bloco de dados de empresa; Pitfall 1 process.cwd; Pitfall 3 empresaLogoUrl undefined; Validation Architecture > Wave 0 Gaps)
    - .planning/phases/35-backend-white-label/35-PATTERNS.md (secao quotes-pdf-storage.service.ts: renderHtml atualizado, imports node:fs/node:path, Test Pattern com mock de ConfigService)
  </read_first>
  <behavior>
    - Com EMPRESA_PDF_TEMPLATE_PATH definida e arquivo existente: renderHtml le esse arquivo (custom)
    - Com EMPRESA_PDF_TEMPLATE_PATH definida e arquivo ausente: renderHtml lanca InternalServerErrorException citando o path (D-06)
    - Sem EMPRESA_PDF_TEMPLATE_PATH e com quote-default.hbs presente: renderHtml le o template padrao externo
    - Sem EMPRESA_PDF_TEMPLATE_PATH e sem quote-default.hbs: renderHtml usa a string TS QUOTES_PDF_HTML_TEMPLATE (fallback)
    - O contexto passado ao template contem empresaNome, empresaCnpj, empresaEndereco, empresaLogoUrl, empresaCor
    - Quando EMPRESA_LOGO_URL ausente, empresaLogoUrl no contexto e undefined (nao "")
    - Quando EMPRESA_COR_PRIMARIA ausente, empresaCor no contexto e "#0d6efd"
  </behavior>
  <action>
    Adicionar imports `import { existsSync, readFileSync } from "node:fs";` e `import path from "node:path";` ao topo de quotes-pdf-storage.service.ts (nenhum pacote novo — built-ins do Node).

    Reescrever o inicio de renderHtml() (linha ~100) para resolver `templateSource` por cadeia de fallback per D-05/D-06: (1) ler `customPath = this.configService.get<string>("EMPRESA_PDF_TEMPLATE_PATH")`; se definida, verificar com `existsSync(customPath)` — se ausente, lancar `InternalServerErrorException` com mensagem citando o path (seguir o padrao de InternalServerErrorException ja usado em requireEnv, linhas 230-238); se presente, `readFileSync(customPath, "utf-8")`. (2) Se customPath nao definida: `defaultPath = path.resolve(process.cwd(), "apps/backend/templates/quote-default.hbs")`; se `existsSync(defaultPath)` -> `readFileSync(defaultPath, "utf-8")`. (3) caso contrario -> `QUOTES_PDF_HTML_TEMPLATE` (string TS, zero risco de regressao). Usar `templateSource` em `Handlebars.compile(templateSource)` no lugar da constante HTML_TEMPLATE.

    Ler os dados de empresa via ConfigService per D-07: `empresaNome`, `empresaCnpj`, `empresaEndereco` com `?? ""` (defensivo — ja validadas como obrigatorias); `empresaLogoUrl = this.configService.get<string>("EMPRESA_LOGO_URL")` SEM `?? ""` (deve permanecer undefined quando ausente — Pitfall 3 / D-08); `empresaCor = this.configService.get<string>("EMPRESA_COR_PRIMARIA") ?? "#0d6efd"` (D-02/CFG-05). Adicionar essas 5 chaves ao objeto retornado por `template({...})` (linha ~120), preservando TODOS os campos existentes (idorcamento, dataorcamento, cliente, itens, carimbos, totais, etc.) sem alteracao.

    Criar o arquivo de teste `apps/backend/src/modules/quotes/quotes-pdf-storage.service.test.ts` (Wave 0 gap do RESEARCH). Usar `jest.mock("node:fs")` para mockar existsSync/readFileSync e o padrao de mockConfig + TestingModule do nfse.service.test.ts. Cobrir: (a) custom path definido + existsSync true -> usa o conteudo do custom; (b) custom path definido + existsSync false -> espera throw de InternalServerErrorException; (c) sem custom path + default existe -> usa quote-default.hbs; (d) sem custom path + default ausente -> usa QUOTES_PDF_HTML_TEMPLATE; (e) EMPRESA_LOGO_URL ausente -> contexto recebe empresaLogoUrl undefined; (f) EMPRESA_COR_PRIMARIA ausente -> contexto recebe "#0d6efd". Testar renderHtml diretamente (cast a any para acessar metodo privado) ou via um spy em Handlebars.compile capturando o contexto, conforme mais direto dado o codigo lido. NAO invocar Puppeteer no teste (mockar/evitar renderPdfBuffer).
  </action>
  <verify>
    <automated>npm --workspace @bomcusto/backend test -- --testPathPattern=quotes-pdf</automated>
  </verify>
  <acceptance_criteria>
    - `quotes-pdf-storage.service.ts` contem `from "node:fs"` e `from "node:path"`
    - contem `EMPRESA_PDF_TEMPLATE_PATH`
    - contem `path.resolve(process.cwd(), "apps/backend/templates/quote-default.hbs")`
    - contem `this.configService.get<string>("EMPRESA_LOGO_URL")` SEM operador `?? ""` na mesma expressao
    - contem `"#0d6efd"` como fallback de EMPRESA_COR_PRIMARIA
    - o objeto passado a `template({...})` inclui as chaves empresaNome, empresaCnpj, empresaEndereco, empresaLogoUrl, empresaCor
    - lanca InternalServerErrorException quando EMPRESA_PDF_TEMPLATE_PATH definida e arquivo ausente
    - `quotes-pdf-storage.service.test.ts` existe e contem o token `EMPRESA_PDF_TEMPLATE_PATH`
    - `npm --workspace @bomcusto/backend test -- --testPathPattern=quotes-pdf` sai com codigo 0
  </acceptance_criteria>
  <done>renderHtml resolve o template via cadeia de 3 niveis com erro explicito para custom ausente, injeta os 5 campos de empresa no contexto (logo undefined quando ausente, cor com fallback), e os testes cobrem todos os ramos da cadeia.</done>
</task>

<task type="auto">
  <name>Task 2: Vars EMPRESA_* obrigatorias no app.module.ts + secao no .env.example</name>
  <files>apps/backend/src/modules/app.module.ts, .env.example</files>
  <read_first>
    - apps/backend/src/modules/app.module.ts (linhas 22-30: REQUIRED_ENV_VARS atual; linhas 32-43: validateEnv com o throw a atualizar)
    - .env.example (secao NFS-e ~linhas 68-74 e secao Seguranca ~linha 76 — ponto de insercao entre elas; estilo de seccoes comentadas)
    - .planning/phases/35-backend-white-label/35-PATTERNS.md (secao app.module.ts: array atualizado e novo throw; secao .env.example: nova secao completa e posicao de insercao)
    - .planning/phases/35-backend-white-label/35-RESEARCH.md (Code Examples > REQUIRED_ENV_VARS atualizado com hint; Code Examples > Seção do .env.example)
  </read_first>
  <action>
    Em app.module.ts, adicionar ao array `REQUIRED_ENV_VARS` (apos `"ATHOS_SISTEMA_USUARIO_ID"`) as 4 entradas obrigatorias per D-01: `"EMPRESA_NOME"`, `"EMPRESA_CNPJ"`, `"EMPRESA_ENDERECO"`, `"EMPRESA_MUNICIPIO_IBGE"`. Atualizar a mensagem do `throw new Error(...)` em validateEnv (linha ~39) per D-03 para terminar com ` — See .env.example for EMPRESA_* setup instructions`, ficando: `Missing required environment variables: ${missing.join(", ")} — See .env.example for EMPRESA_* setup instructions`. Nao alterar a logica de filtragem de missing.

    No `.env.example`, inserir uma nova secao `# Empresa (White-Label)` entre a secao NFS-e e a secao Seguranca, seguindo o estilo de secoes comentadas existente. Conteudo per CFG-01..05 e o exemplo do 35-PATTERNS.md: vars obrigatorias com defaults BomCusto reais — `EMPRESA_NOME=Bom Custo Papelaria & Grafica Rapida LTDA`, `EMPRESA_CNPJ=62.391.927/0001-57`, `EMPRESA_ENDERECO=Rua Olimpio Leite da Silva, 39 - Loja 07, Perequê · Ilhabela / SP · CEP 11633-078`, `EMPRESA_MUNICIPIO_IBGE=3520400`; e vars opcionais comentadas com explicacao — `# EMPRESA_LOGO_URL=` (opcional, sem definicao o logo e omitido do PDF), `# EMPRESA_COR_PRIMARIA=#0d6efd` (opcional, fallback azul Bootstrap), `# EMPRESA_PDF_TEMPLATE_PATH=/app/apps/backend/templates/custom.hbs` (opcional, montavel via volume Docker, com exemplo de mount `-v`). Incluir o comentario de atencao per D-12: para deploy em outro municipio, definir tambem NFSE_SOAP_URL e NFSE_AUX_URL (os defaults apontam para Ilhabela/iiBrasil). Cada var deve ter comentario explicativo.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('apps/backend/src/modules/app.module.ts','utf8');const e=fs.readFileSync('.env.example','utf8');const reqM=['\"EMPRESA_NOME\"','\"EMPRESA_CNPJ\"','\"EMPRESA_ENDERECO\"','\"EMPRESA_MUNICIPIO_IBGE\"','See .env.example for EMPRESA_* setup instructions'];const reqE=['EMPRESA_NOME=','EMPRESA_CNPJ=','EMPRESA_ENDERECO=','EMPRESA_MUNICIPIO_IBGE=','EMPRESA_LOGO_URL','EMPRESA_COR_PRIMARIA','EMPRESA_PDF_TEMPLATE_PATH'];const mm=reqM.filter(t=>!m.includes(t));const me=reqE.filter(t=>!e.includes(t));if(mm.length){console.error('app.module faltando:',mm);process.exit(1)}if(me.length){console.error('.env.example faltando:',me);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app.module.ts REQUIRED_ENV_VARS contem `"EMPRESA_NOME"`, `"EMPRESA_CNPJ"`, `"EMPRESA_ENDERECO"`, `"EMPRESA_MUNICIPIO_IBGE"`
    - app.module.ts contem a string `See .env.example for EMPRESA_* setup instructions`
    - .env.example contem `EMPRESA_NOME=`, `EMPRESA_CNPJ=`, `EMPRESA_ENDERECO=`, `EMPRESA_MUNICIPIO_IBGE=` com valores BomCusto
    - .env.example menciona `EMPRESA_LOGO_URL`, `EMPRESA_COR_PRIMARIA`, `EMPRESA_PDF_TEMPLATE_PATH` (comentadas como opcionais)
    - .env.example inclui o aviso sobre NFSE_SOAP_URL/NFSE_AUX_URL para outro municipio
  </acceptance_criteria>
  <done>As 4 vars obrigatorias estao no REQUIRED_ENV_VARS com fail-fast e hint .env.example; o .env.example documenta todas as 7 vars EMPRESA_* com defaults BomCusto e comentarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador -> EMPRESA_PDF_TEMPLATE_PATH | path de template lido de env var controlada pelo operador do servidor, nao input de usuario final |
| operador -> .env de producao | vars obrigatorias EMPRESA_* devem existir no .env antes do deploy (fail-fast por design) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Tampering | EMPRESA_PDF_TEMPLATE_PATH -> fs.readFileSync | accept | Path lido de env var controlada pelo operador (nao input de usuario). Path traversal nao aplicavel: operador define o proprio path. existsSync + erro explicito evitam leitura silenciosa de arquivo errado. ASVS L1 — risco controlado. |
| T-35-05 | Denial of Service | startup fail-fast por var ausente | accept | Comportamento intencional (D-01): backend recusa subir sem as 4 vars obrigatorias. Mensagem de erro aponta para .env.example. Mitiga deploy com configuracao fiscal incorreta. |
</threat_model>

<verification>
- `npm --workspace @bomcusto/backend test -- --testPathPattern=quotes-pdf` verde
- `npm --workspace @bomcusto/backend test` (suite completa) verde no merge da wave
- app.module.ts e .env.example contem todas as vars EMPRESA_*
- Cadeia de fallback de template testada nos 3 niveis + erro de path ausente
</verification>

<success_criteria>
- Backend passa dados de empresa ao renderizador via env vars (PDF-01)
- EMPRESA_PDF_TEMPLATE_PATH aponta para template customizado com fallback e erro explicito (PDF-04)
- .env.example documenta todas as vars EMPRESA_* com defaults BomCusto e comentarios (CFG-01)
- Vars de empresa lidas via ConfigService chegam ao contexto Handlebars (CFG-02/03/05)
- Fail-fast garante que novo deploy exige preenchimento das vars obrigatorias
</success_criteria>

<output>
Create `.planning/phases/35-backend-white-label/35-03-SUMMARY.md` when done
</output>
