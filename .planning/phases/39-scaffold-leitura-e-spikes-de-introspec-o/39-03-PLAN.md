---
phase: 39-scaffold-leitura-e-spikes-de-introspec-o
plan: 03
type: execute
wave: 2
depends_on: ["39-02"]
files_modified:
  - apps/backend/src/modules/integrations/athos/athos-produto-composto.service.ts
  - apps/backend/src/modules/integrations/athos/athos-produto-composto.service.test.ts
  - apps/backend/src/modules/integrations/athos/athos-produto-composto.controller.ts
  - apps/backend/src/modules/integrations/athos/dto/create-produto-composto.dto.ts
  - apps/backend/src/modules/integrations/athos/dto/update-produto-composto.dto.ts
  - apps/backend/src/modules/integrations/athos/athos.module.ts
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "GET /athos/produtos/:idprodutomaster/composicao retorna lista plana enriquecida (descricaoproduto + statusproduto do produto detail via JOIN único, sem N+1)"
    - "GET retorna 404 quando idprodutomaster não existe no catálogo produto"
    - "GET retorna [] (HTTP 200) quando o master existe mas não tem componentes em produto_composto"
    - "GET lista TODOS os componentes incluindo os de detail inativo (statusproduto=false) — sem filtro (D-04)"
    - "LEFT JOIN expõe linhas órfãs (idprodutodetail sem produto) com descricaoproduto/statusproduto null em vez de escondê-las"
  artifacts:
    - "apps/backend/src/modules/integrations/athos/athos-produto-composto.service.ts"
    - "apps/backend/src/modules/integrations/athos/athos-produto-composto.controller.ts"
    - "apps/backend/src/modules/integrations/athos/athos-produto-composto.service.test.ts"
    - "apps/backend/src/modules/integrations/athos/dto/create-produto-composto.dto.ts"
    - "apps/backend/src/modules/integrations/athos/dto/update-produto-composto.dto.ts"
  key_links:
    - "ProdutoCompostoController @Get(':idprodutomaster/composicao') → AthosProdutoCompostoService.listarPorMaster"
    - "AthosModule.providers += AthosProdutoCompostoService; AthosModule.controllers += ProdutoCompostoController"
    - "@ApiSecurity('InternalApiKey') no controller → guard x-internal-api-key cobre a rota"
---

<objective>
Entregar o endpoint de leitura `GET /athos/produtos/:idprodutomaster/composicao` (COMP-01): lista plana de componentes de um kit, enriquecida com `descricaoproduto` e `statusproduto` do produto detail via JOIN único (sem N+1). Inclui o scaffold do `AthosProdutoCompostoService` (apenas `listarPorMaster`), o controller, os DTOs base (scaffold — decorators finais de `quantidade` ficam para a Fase 40, pendentes do spike (a)), e o registro de ambos no `AthosModule`. Mirror exato do padrão de read-API do Athos (v2.2/v2.4): Pool lazy singleton com env `ATHOS_PG_*`, query parametrizada, guard `x-internal-api-key`, ordenação de rotas estática-antes-de-paramétrica.

Purpose: Provar o caminho de leitura do módulo `produto_composto` usando apenas o SELECT GRANT já existente — sem depender do write GRANT (que só bloqueia a Fase 40). Resolve os critérios #1, #2, #3 do ROADMAP.

Output: serviço + controller + teste Jest + DTOs scaffold + módulo wireado.

## Artifacts this phase produces (Plan 03)
- `athos-produto-composto.service.ts` (novo) — `AthosProdutoCompostoService.listarPorMaster(idprodutomaster)` com Pool lazy singleton
- `athos-produto-composto.controller.ts` (novo) — `ProdutoCompostoController` com `GET :idprodutomaster/composicao`
- `athos-produto-composto.service.test.ts` (novo) — testes Jest (pg mock) cobrindo 404, [], lista enriquecida, detail inativo, LEFT JOIN, erro de conexão
- `dto/create-produto-composto.dto.ts` + `dto/update-produto-composto.dto.ts` (novos — scaffold; decorators de quantidade pendentes do spike (a) / Fase 40)
- `athos.module.ts` (modificado) — providers += service; controllers += controller
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-CONTEXT.md
@.planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-RESEARCH.md
@apps/backend/src/modules/integrations/athos/athos-produto.service.ts
@apps/backend/src/modules/integrations/athos/athos-produto.controller.ts
@apps/backend/src/modules/integrations/athos/athos.module.ts
@apps/backend/src/modules/integrations/athos/athos-produto.service.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AthosProdutoCompostoService.listarPorMaster + teste Jest (pg mock)</name>
  <files>apps/backend/src/modules/integrations/athos/athos-produto-composto.service.ts, apps/backend/src/modules/integrations/athos/athos-produto-composto.service.test.ts</files>
  <read_first>
    - .planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-RESEARCH.md §"COMP-01 — GET Endpoint Implementation" (service completo proposto + Test Skeleton em §"Validation Architecture") e §"LEFT JOIN rationale"
    - apps/backend/src/modules/integrations/athos/athos-produto.service.ts linhas 22-45 (getPool/getDbConfig a espelhar) e o padrão de catch try/finally com client.release()
    - apps/backend/src/modules/integrations/athos/athos-produto.service.test.ts linhas 6-49 (jest.mock("pg") + mClient/mPool + beforeAll com env ATHOS_PG_*)
  </read_first>
  <behavior>
    - 404: quando o SELECT de existência do master retorna `{ rows: [] }`, lança `NotFoundException` (`Produto <id> nao encontrado no Athos`) e chama `client.release()`.
    - []: quando o master existe mas a query de componentes retorna `{ rows: [] }`, devolve `[]` (HTTP 200).
    - Lista enriquecida: devolve as linhas tal qual o pg retorna (idprodutocomposto, idprodutodetail, descricaoproduto, statusproduto, quantidade), ordenadas por idprodutocomposto.
    - Detail inativo (D-04): uma linha com `statusproduto=false` é incluída no resultado, não filtrada.
    - LEFT JOIN: a SQL de componentes usa `LEFT JOIN produto` (não INNER) — linha órfã (detail sem produto) aparece com descricaoproduto/statusproduto null.
    - Erro de conexão: se `pool.connect` rejeita, a Promise rejeita (erro propagado).
  </behavior>
  <action>
    Criar `athos-produto-composto.service.ts` com `@Injectable() AthosProdutoCompostoService` espelhando o padrão de `AthosProdutoService`: `Logger` próprio, `_pool: Pool | null`, `getPool()` (lazy singleton, `max:5, idleTimeoutMillis:30000, connectionTimeoutMillis:5000`, handler `pool.on("error")`) e `getDbConfig()` lendo `ATHOS_PG_HOST/DB/USER/PASS/PORT` com falha-fechado (`InternalServerErrorException`) quando faltar config — copiar do RESEARCH §"COMP-01". Implementar `async listarPorMaster(idprodutomaster: number): Promise<ComposicaoItem[]>`: (1) `connect()`; (2) checagem de existência do master `SELECT 1 FROM produto WHERE idproduto = $1 LIMIT 1` → `NotFoundException` se vazio; (3) query de componentes — SELECT de `pc.idprodutocomposto, pc.idprodutodetail, p.descricaoproduto, p.statusproduto, pc.quantidade FROM produto_composto pc LEFT JOIN produto p ON p.idproduto = pc.idprodutodetail WHERE pc.idprodutomaster = $1 ORDER BY pc.idprodutocomposto`; (4) `logger.log` com idprodutomaster + count; (5) `return result.rows`; (6) catch: re-throw de `NotFoundException`, senão log + `InternalServerErrorException`; (7) `finally { client.release() }`. Definir a interface `ComposicaoItem` (idprodutocomposto:number, idprodutodetail:number, descricaoproduto:string|null, statusproduto:boolean|null, quantidade:string) inline no service. Usar SEMPRE parâmetro `$1` — idprodutomaster nunca interpolado na string SQL. Este é um endpoint SOMENTE de leitura: nenhuma operação de escrita nesta fase.

    Criar `athos-produto-composto.service.test.ts` espelhando o Test Skeleton do RESEARCH §"Validation Architecture": `jest.mock("pg")` com mClient/mPool, `beforeAll` setando env `ATHOS_PG_*`, `beforeEach` com `Test.createTestingModule({ providers: [AthosProdutoCompostoService] })`. Cobrir os 6 behaviors: (a) COMP-01-404 → master vazio lança NotFoundException + release chamado; (b) COMP-01-empty → master existe, componentes vazios → `[]`; (c) COMP-01-list → linha enriquecida retornada e ordenada; (d) COMP-01-inactive → linha com statusproduto=false incluída; (e) COMP-01-leftjoin → inspeciona as SQLs emitidas e confirma que a query de componentes contém `LEFT JOIN` (assertiva positiva por substring); (f) propaga erro quando `pool.connect` rejeita. Mockar `client.query` em sequência (`mockResolvedValueOnce` para o master-check, depois para os componentes).
  </action>
  <verify>
    <automated>cd apps/backend && npx jest athos-produto-composto --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - behavior: listarPorMaster lança 404 sem master, devolve [] sem componentes, devolve lista enriquecida ordenada, inclui detail inativo, usa LEFT JOIN, e propaga erro de conexão.
    - test-command: `cd apps/backend && npx jest athos-produto-composto --no-coverage` passa com os 6 casos verdes.
    - source-assertion: A query de componentes usa `LEFT JOIN produto` e parâmetro `$1`; o serviço não emite nenhuma operação de escrita; getPool/getDbConfig espelham AthosProdutoService.
  </acceptance_criteria>
  <done>athos-produto-composto.service.ts implementa listarPorMaster (404 / [] / lista enriquecida / LEFT JOIN / D-04); teste Jest verde com 6 casos.</done>
</task>

<task type="auto">
  <name>Task 2: ProdutoCompostoController (GET) + registro no AthosModule</name>
  <files>apps/backend/src/modules/integrations/athos/athos-produto-composto.controller.ts, apps/backend/src/modules/integrations/athos/athos.module.ts</files>
  <read_first>
    - .planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-RESEARCH.md §"COMP-01 — athos-produto-composto.controller.ts" e §"AthosModule updated registration"
    - apps/backend/src/modules/integrations/athos/athos-produto.controller.ts linhas 28-37 (decorators de classe @ApiTags/@ApiSecurity/@Controller, injeção de service) e 103-116 (padrão @Get com ParseIntPipe)
    - apps/backend/src/modules/integrations/athos/athos.module.ts (arrays providers/controllers/exports a estender)
  </read_first>
  <action>
    Criar `athos-produto-composto.controller.ts` com `ProdutoCompostoController` decorado `@ApiTags("Athos")`, `@ApiSecurity("InternalApiKey")`, `@Controller("athos/produtos")` (mesmo prefixo do ProdutoController; D-03). Injetar `AthosProdutoCompostoService` no construtor + `Logger` próprio. Único método `@Get(":idprodutomaster/composicao") async listarComposicao(@Param("idprodutomaster", ParseIntPipe) idprodutomaster: number)`: faz `logger.log` e retorna `this.athosProdutoCompostoService.listarPorMaster(idprodutomaster)`. Anotar com `@ApiOperation`/`@ApiParam`/`@ApiOkResponse` descrevendo: lista plana enriquecida, inclui detail inativo, 404 quando master ausente, [] quando sem componentes (D-02/D-03/D-04). A rota `:idprodutomaster/composicao` tem dois segmentos e NÃO colide com a rota de um segmento `:idproduto` do ProdutoController — sem preocupação de ordenação para o único GET desta fase.

    Modificar `athos.module.ts`: importar `AthosProdutoCompostoService` e `ProdutoCompostoController`; adicionar `AthosProdutoCompostoService` ao array `providers` e `ProdutoCompostoController` ao array `controllers`. NÃO adicionar o service ao `exports` (nenhum outro módulo precisa dele no v2.5). Não alterar imports/exports existentes além dessas duas adições.
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc --noEmit -p tsconfig.json && npx jest athos-produto-composto --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - behavior: A rota `GET /athos/produtos/:idprodutomaster/composicao` existe, é protegida pelo guard InternalApiKey (decorator de classe) e delega para listarPorMaster com idprodutomaster validado por ParseIntPipe.
    - test-command: `cd apps/backend && npx tsc --noEmit` compila sem erro (controller + módulo wireados) e a suíte composto continua verde.
    - source-assertion: AthosModule.providers inclui AthosProdutoCompostoService e AthosModule.controllers inclui ProdutoCompostoController; controller usa @ApiSecurity("InternalApiKey") e ParseIntPipe.
  </acceptance_criteria>
  <done>Controller criado com a rota GET; AthosModule registra service + controller; compila e testes verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold dos DTOs create/update de produto_composto (decorators de quantidade pendentes do spike a / Fase 40)</name>
  <files>apps/backend/src/modules/integrations/athos/dto/create-produto-composto.dto.ts, apps/backend/src/modules/integrations/athos/dto/update-produto-composto.dto.ts</files>
  <read_first>
    - .planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-RESEARCH.md §"DTO Scaffolds (Phase 40 gates — decorators TBD pending spike (a))"
    - apps/backend/src/modules/integrations/athos/dto/create-produto.dto.ts (padrão de decorators class-validator + @ApiProperty + @Type)
  </read_first>
  <action>
    Criar `dto/create-produto-composto.dto.ts` exportando `CreateProdutoCompostoDto` com dois campos: `idprodutodetail` (`@ApiProperty`, `@IsInt`, `@Type(() => Number)`) e `quantidade` (`@ApiProperty`, `@IsNumber`, `@Min(0.001)`, `@Type(() => Number)`). Criar `dto/update-produto-composto.dto.ts` exportando `UpdateProdutoCompostoDto` com apenas `quantidade` (mesmos decorators). Estes DTOs são SCAFFOLD para a Fase 40 (nenhum endpoint de escrita os consome nesta fase). O default seguro de `quantidade` é `@IsNumber() + @Min(0.001)` conforme D-01/RESEARCH §"DTO Scaffolds" — os decorators FINAIS (@IsInt vs @IsNumber, valor exato de @Min) só serão fixados na Fase 40 após o resultado do spike (a) sobre o domínio `quantidade`. Documentar essa pendência com um comentário curto no campo quantidade referenciando o spike (a) — descrever a pendência em prosa conceitual, sem reproduzir literais de gate negativos. NÃO importar nem registrar estes DTOs em controller/service nesta fase (são apenas scaffold).
  </action>
  <verify>
    <automated>cd apps/backend && npx tsc --noEmit -p tsconfig.json && test -f src/modules/integrations/athos/dto/create-produto-composto.dto.ts && test -f src/modules/integrations/athos/dto/update-produto-composto.dto.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - behavior: Os dois DTOs existem, compilam, e usam o default seguro de quantidade (@IsNumber + @Min(0.001)) com pendência documentada para o spike (a).
    - test-command: `cd apps/backend && npx tsc --noEmit` compila sem erro com os dois DTOs presentes.
    - source-assertion: CreateProdutoCompostoDto tem idprodutodetail (@IsInt) + quantidade (@IsNumber/@Min(0.001)); UpdateProdutoCompostoDto tem apenas quantidade; nenhum dos dois é consumido por controller/service nesta fase.
  </acceptance_criteria>
  <done>Dois DTOs scaffold criados, compilando, com a pendência do spike (a) documentada; não wireados a nenhum endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → API | Param de rota `idprodutomaster` cruza aqui (input não confiável) |
| API → DB Athos | SELECT parametrizado read-only contra produto + produto_composto (SELECT GRANT já existente; sem escrita nesta fase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-03-01 | Tampering | SQL injection via param de rota idprodutomaster | low | mitigate | `ParseIntPipe` rejeita não-inteiro; SQL usa `$1` parametrizado em ambas as queries — idprodutomaster nunca interpolado na string |
| T-39-03-02 | Spoofing | acesso não autenticado ao GET | low | mitigate | `@ApiSecurity("InternalApiKey")` no controller aplica o guard `x-internal-api-key` a todas as rotas da classe (mesmo padrão do ProdutoController) |
| T-39-03-03 | Information Disclosure | exposição de linhas órfãs/detail inativo no payload | low | accept | Intencional por D-04 — LEFT JOIN expõe null para órfãs e statusproduto=false para inativos; ferramenta interna; o caller decide |
| T-39-03-04 | Denial of Service | over-fetch de kit muito grande (GET sem LIMIT) | low | accept | Escala interna atual aceitável; nota registrada para reavaliar LIMIT/paginação na Fase 40 se kits crescerem |
</threat_model>

<verification>
- `npx jest athos-produto-composto --no-coverage` verde (6 casos: 404, [], lista, inativo, LEFT JOIN, erro de conexão).
- `npx tsc --noEmit` compila com controller + módulo + DTOs.
- AthosModule registra AthosProdutoCompostoService (providers) e ProdutoCompostoController (controllers).
- Nenhuma operação de escrita no service (endpoint somente de leitura).
</verification>

<success_criteria>
- COMP-01 satisfeito: GET retorna lista enriquecida via JOIN único (#1), [] quando master sem componentes (#2), 404 quando master ausente (#3).
- ROADMAP critérios #1, #2, #3 atendidos; #5 preservado (suíte athos completa verde).
</success_criteria>

<output>
Create `.planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-03-SUMMARY.md` when done. Registrar na SUMMARY: a rota final exposta, a decisão LEFT JOIN, e a pendência de decorators de quantidade (scaffold) para a Fase 40 consumir junto com o resultado do spike (a).
</output>
