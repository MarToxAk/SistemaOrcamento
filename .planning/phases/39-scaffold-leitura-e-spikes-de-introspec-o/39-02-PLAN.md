---
phase: 39-scaffold-leitura-e-spikes-de-introspec-o
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/integrations/athos/athos-fk.util.ts
  - apps/backend/src/modules/integrations/athos/athos-fk.util.test.ts
  - apps/backend/src/modules/integrations/athos/athos-produto.service.ts
autonomous: true
requirements: [COMP-08]

must_haves:
  truths:
    - "validarFkExiste existe como função module-level exportada em athos-fk.util.ts (não mais método privado)"
    - "AthosProdutoService importa validarFkExiste do util e os 3 call-sites chamam a função module-level (sem this.)"
    - "Os testes existentes de athos-produto.service continuam verdes sem nenhuma alteração no arquivo de teste — zero mudança de comportamento"
    - "validarFkExiste lança UnprocessableEntityException (422) quando a FK não existe e resolve silenciosamente quando existe"
  artifacts:
    - "apps/backend/src/modules/integrations/athos/athos-fk.util.ts"
    - "apps/backend/src/modules/integrations/athos/athos-fk.util.test.ts"
  key_links:
    - "AthosProdutoService → import { validarFkExiste } from './athos-fk.util' (3 call-sites em criarProduto/editarProduto)"
    - "athos-fk.util.ts → reutilizável pela Fase 40 (AthosProdutoCompostoService valida master + detail)"
---

<objective>
Extrair `validarFkExiste` — hoje um método privado de `AthosProdutoService` (linhas 58-74) sem nenhuma referência a `this` — para uma função module-level exportada em `athos-fk.util.ts`, sem qualquer mudança de comportamento (COMP-08). Atualizar os 3 call-sites em `AthosProdutoService` para importar e chamar a função module-level. Os testes existentes de `athos-produto.service.test.ts` são o guarda de regressão e devem continuar verdes sem alteração.

Purpose: Centralizar a validação dual de FK num util testável em isolamento, base reutilizável pela Fase 40 (que valida tanto master quanto detail). A extração é pré-condição para o write CRUD da próxima fase sem duplicar código de validação.

Output: `athos-fk.util.ts` (função extraída) + `athos-fk.util.test.ts` (teste unitário do util em isolamento) + `athos-produto.service.ts` re-wireado.

## Artifacts this phase produces (Plan 02)
- `apps/backend/src/modules/integrations/athos/athos-fk.util.ts` (novo) — `validarFkExiste(client, tabela, coluna, id, nomeEntidade)` module-level
- `apps/backend/src/modules/integrations/athos/athos-fk.util.test.ts` (novo) — teste unitário do util (PoolClient mockado)
- `apps/backend/src/modules/integrations/athos/athos-produto.service.ts` (modificado, mínimo) — remove método privado, adiciona import, remove prefixo `this.` nos 3 call-sites
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-CONTEXT.md
@.planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-RESEARCH.md
@apps/backend/src/modules/integrations/athos/athos-produto.service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar athos-fk.util.ts (validarFkExiste module-level) + teste unitário do util</name>
  <files>apps/backend/src/modules/integrations/athos/athos-fk.util.ts, apps/backend/src/modules/integrations/athos/athos-fk.util.test.ts</files>
  <read_first>
    - .planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-RESEARCH.md §"COMP-08 — validarFkExiste Extraction" (corpo exato da função proposta + import a usar)
    - apps/backend/src/modules/integrations/athos/athos-produto.service.ts linhas 58-74 (corpo atual do método privado — copiar VERBATIM)
    - apps/backend/src/modules/integrations/athos/athos-produto.service.test.ts (padrão jest: describe/it, sem necessidade de jest.mock("pg") para o util pois recebe um PoolClient mockado direto)
  </read_first>
  <behavior>
    - Resolve sem lançar quando `client.query` retorna pelo menos uma linha (FK existe).
    - Lança `UnprocessableEntityException` com a mensagem exata `${nomeEntidade} com id ${id} nao encontrado no Athos` quando `client.query` retorna `{ rows: [] }`.
    - Chama `client.query` com a query parametrizada (id passado como `$1` no array de params, nunca interpolado) — o id aparece em `params[0]`, não no texto da SQL.
  </behavior>
  <action>
    Criar `athos-fk.util.ts` exportando `async function validarFkExiste(client: PoolClient, tabela: string, coluna: string, id: number, nomeEntidade: string): Promise<void>` com o corpo COPIADO VERBATIM do método privado atual (athos-produto.service.ts linhas 58-74) — mesma query `SELECT 1 FROM "<tabela>" WHERE "<coluna>" = $1 LIMIT 1`, mesmo array de params `[id]`, mesma checagem `rows.length === 0`, mesma `UnprocessableEntityException` com a mesma string de mensagem. Importar `UnprocessableEntityException` de `@nestjs/common` e `PoolClient` de `pg`. A ÚNICA diferença em relação ao método de classe é remover o modificador `private` e torná-lo export module-level — o corpo tem zero referência a `this`, então a equivalência é total (ver Assumption A1 do RESEARCH). Adicionar um docblock curto descrevendo o propósito (valida existência de FK; lança 422 se ausente; extraído de AthosProdutoService para reuso na Fase 40).

    Criar `athos-fk.util.test.ts` cobrindo os 3 behaviors acima: (1) resolve quando o mock de `client.query` devolve `{ rows: [{ "?column?": 1 }] }`; (2) rejeita com `UnprocessableEntityException` e mensagem contendo `nomeEntidade` + `id` + `nao encontrado no Athos` quando o mock devolve `{ rows: [] }`; (3) o id é passado como segundo argumento de `client.query` (array `[id]`), confirmando parametrização. O mock do client é um objeto simples `{ query: jest.fn() }` — NÃO precisa de `jest.mock("pg")` porque o util recebe o client por parâmetro. Mirror do estilo describe/it de athos-produto.service.test.ts.
  </action>
  <verify>
    <automated>cd apps/backend && npx jest athos-fk.util --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - behavior: O util resolve quando a FK existe e lança 422 com a mensagem `<nomeEntidade> com id <id> nao encontrado no Athos` quando ausente; id parametrizado via `$1`.
    - test-command: `cd apps/backend && npx jest athos-fk.util --no-coverage` passa com os 3 casos verdes.
    - source-assertion: O corpo de validarFkExiste em athos-fk.util.ts é idêntico ao das linhas 58-74 de athos-produto.service.ts, exceto pela troca de `private` método→export module-level.
  </acceptance_criteria>
  <done>athos-fk.util.ts exporta validarFkExiste module-level com corpo verbatim; athos-fk.util.test.ts verde com os 3 casos.</done>
</task>

<task type="auto">
  <name>Task 2: Re-wirear AthosProdutoService para importar do util (guarda de regressão)</name>
  <files>apps/backend/src/modules/integrations/athos/athos-produto.service.ts</files>
  <read_first>
    - apps/backend/src/modules/integrations/athos/athos-produto.service.ts (linhas 58-74 método a remover; linhas 83, 87, 89 call-sites em criarProduto; linhas 278, 282, 284 call-sites em editarProduto)
    - .planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-RESEARCH.md §"COMP-08 — AthosProdutoService updated import" e Pitfall 4 (atualizar TODOS os call-sites)
  </read_first>
  <action>
    Remover o método privado `validarFkExiste` (linhas 58-74) de `AthosProdutoService`. Adicionar `import { validarFkExiste } from "./athos-fk.util";` no topo do arquivo. Atualizar TODOS os call-sites `this.validarFkExiste(client, ...)` para `validarFkExiste(client, ...)` (remover o prefixo `this.`) — confirmar a contagem com grep antes e depois. Os call-sites estão em `criarProduto` (3 chamadas: departamento, grupo, marca) e em `editarProduto` (3 chamadas: departamento, grupo, marca) — total 6 chamadas. Se `UnprocessableEntityException` deixar de ser referenciado diretamente no service após a remoção do método, manter o import apenas se ainda usado no catch (ele é usado no `instanceof` dos catch blocks — manter). NÃO alterar nenhuma outra lógica: defaults, INSERT dinâmico, mapeamento de erro 23503, allowlists de UPDATE permanecem intactos. Esta é uma extração com ZERO mudança de comportamento (ROADMAP critério #5). NÃO editar athos-produto.service.test.ts — ele é o guarda de regressão e deve passar sem alteração.
  </action>
  <verify>
    <automated>cd apps/backend && grep -rn "this.validarFkExiste" src/modules/integrations/athos/athos-produto.service.ts | grep -v '^#'; npx jest athos-produto.service athos-fk.util --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - behavior: AthosProdutoService não declara mais o método privado validarFkExiste; importa a função module-level e a chama nos 6 call-sites sem `this.`.
    - test-command: `cd apps/backend && npx jest athos-produto.service athos-fk.util --no-coverage` passa — suíte existente de produto 100% verde sem edição do arquivo de teste; o grep de `this.validarFkExiste` retorna zero linhas.
    - source-assertion: Nenhuma mudança em defaults/INSERT/UPDATE/catch além da remoção do método e da troca dos call-sites; athos-produto.service.test.ts inalterado (git diff vazio para esse arquivo).
  </acceptance_criteria>
  <done>athos-produto.service.ts importa validarFkExiste do util; 6 call-sites atualizados; suíte existente verde sem editar o teste; behavior preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → API (inalterado) | Refactor interno puro; nenhuma superfície HTTP nova; o caminho de validação de FK existente é preservado byte-a-byte |
| service → DB Athos | `validarFkExiste` emite `SELECT 1 FROM "<tabela>" WHERE "<coluna>" = $1` — id parametrizado; tabela/coluna são identificadores hardcoded pelos call-sites (nunca input do operador) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-02-01 | Tampering | extração de validarFkExiste (regressão silenciosa) | low | mitigate | Corpo copiado verbatim; suíte existente athos-produto.service.test.ts é guarda de regressão e deve passar sem edição; teste unitário dedicado do util confirma 422-on-absent |
| T-39-02-02 | Tampering | identificadores `tabela`/`coluna` interpolados na SQL (`"${tabela}"`) | low | accept | Os argumentos `tabela`/`coluna` são literais hardcoded pelos call-sites (produto_departamento/grupo/marca em Fase 39; produto/produto_composto em Fase 40), nunca derivados de input do operador; o `id` — único valor de origem externa — é sempre parametrizado via `$1`. Comportamento idêntico ao pré-existente; risco inalterado pela extração |
</threat_model>

<verification>
- `npx jest athos-fk.util --no-coverage` verde (3 casos do util).
- `npx jest athos-produto.service --no-coverage` verde SEM edição do arquivo de teste (guarda de regressão = zero mudança de comportamento).
- `grep "this.validarFkExiste" athos-produto.service.ts` retorna zero linhas.
- `git diff` em athos-produto.service.test.ts é vazio.
</verification>

<success_criteria>
- COMP-08 satisfeito: validarFkExiste extraído para athos-fk.util.ts; AthosProdutoService importa do util; testes existentes do serviço de produto continuam passando sem alteração.
- ROADMAP critério #5 atendido.
</success_criteria>

<output>
Create `.planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-02-SUMMARY.md` when done. Registrar na SUMMARY: contagem de call-sites atualizados, confirmação de que athos-produto.service.test.ts não foi editado, e a assinatura final exportada por athos-fk.util.ts (para consumo da Fase 40).
</output>
