---
phase: 39-scaffold-leitura-e-spikes-de-introspec-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-SPIKES.md
autonomous: false
requirements: [COMP-07]
user_setup:
  - service: "Banco de referência Athos (192.168.3.198)"
    why: "Os 3 spikes de introspecção exigem acesso live ao DB de referência read-only; o executor em CI/cloud NÃO alcança a rede 192.168.3.198 (D-01 modo b)"
    dashboard_config:
      - task: "Rodar as 3 queries de introspecção em psql conectado ao 192.168.3.198 e colar os resultados em 39-SPIKES.md"
        location: "psql / cliente SQL conectado a 192.168.3.198 (DB de referência read-only)"

must_haves:
  truths:
    - "O domínio quantidade (tipo-base + cláusula CHECK) está documentado a partir de resultado real do DB de referência"
    - "A presença/ausência de constraint UNIQUE em (idprodutomaster, idprodutodetail) está documentada"
    - "O inventário de triggers e rules de produto_composto está documentado (vazio é resultado válido)"
  artifacts:
    - ".planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-SPIKES.md"
  key_links:
    - "39-SPIKES.md → Phase 40 DTO decorators (quantidade) e lista de colunas do INSERT dependem destes resultados"
---

<objective>
Entregar os 3 spikes de introspecção (COMP-07) no DB de referência read-only 192.168.3.198 no modo (b) da decisão D-01: o executor escreve um arquivo deliverable com as 3 queries prontas (copiadas de 39-RESEARCH.md), e o USUÁRIO roda cada query no 192.168.3.198 e cola os resultados de volta. O executor em CI/cloud NÃO alcança essa rede — esta é uma verificação manual por natureza.

Purpose: Resolver as 3 incógnitas do banco (tipo-base + CHECK do domínio `quantidade`; presença de UNIQUE em `(idprodutomaster, idprodutodetail)`; triggers/rules em `produto_composto`) ANTES de finalizar qualquer DTO/INSERT. Estes resultados NÃO bloqueiam o endpoint GET da Fase 39 (reads não usam `quantidade` em filtros), mas gatilham os decorators do DTO e a lista de colunas do INSERT da Fase 40.

Output: `39-SPIKES.md` com as 3 queries e os resultados reais colados pelo usuário.

## Artifacts this phase produces (Plan 01)
- `39-SPIKES.md` (novo) — 3 queries de introspecção + blocos de resultado preenchidos pelo usuário
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-CONTEXT.md
@.planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever 39-SPIKES.md com as 3 queries de introspecção</name>
  <files>.planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-SPIKES.md</files>
  <read_first>
    - .planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-RESEARCH.md §"COMP-07 — Introspection Spike Queries" (linhas 116-227 — contém as 3 queries SQL prontas e os blocos de placeholder de resultado)
  </read_first>
  <action>
    Criar o arquivo deliverable `39-SPIKES.md` no diretório da fase, copiando VERBATIM os três blocos de query SQL de 39-RESEARCH.md §"COMP-07 — Introspection Spike Queries": Spike (a) domínio `quantidade` (tipo-base + CHECK), Spike (b) constraints UNIQUE/PRIMARY KEY em `produto_composto`, e Spike (c) triggers (c-1) e rules (c-2). Para cada spike, incluir: (1) um cabeçalho que identifique o spike (rótulos "SPIKE (a)", "SPIKE (b)", "SPIKE (c-1)", "SPIKE (c-2)"); (2) o bloco SQL exato dentro de fence ```sql; (3) a nota "What the result decides" resumida do RESEARCH; (4) um bloco de placeholder de resultado claramente marcado para o usuário colar a saída (ex.: bloco vazio rotulado "RESULTADO — colar saída do 192.168.3.198 aqui"). Abrir o arquivo com uma nota de instrução: o executor NÃO alcança 192.168.3.198; o usuário roda as queries em psql conectado ao DB de referência e cola os resultados; estes gatilham os decorators de `quantidade` e a lista de colunas do INSERT da Fase 40 (não o GET da Fase 39). NÃO inventar nem preencher resultados — os blocos de resultado ficam vazios até o usuário colar (Task 2). Implementa D-01.
  </action>
  <verify>
    <automated>test -f .planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-SPIKES.md && [ "$(grep -c 'SPIKE (' .planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-SPIKES.md)" -ge 4 ] && grep -q "192.168.3.198" .planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-SPIKES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - behavior: O arquivo 39-SPIKES.md existe e contém os 4 rótulos de spike (a, b, c-1, c-2), cada um com seu bloco SQL e um placeholder de resultado vazio.
    - test-command: `grep -c 'SPIKE (' 39-SPIKES.md` retorna >= 4 e o arquivo menciona 192.168.3.198.
    - source-assertion: As queries SQL são idênticas às de 39-RESEARCH.md §"COMP-07"; nenhum resultado foi inventado.
  </acceptance_criteria>
  <done>39-SPIKES.md criado com as 3 queries (4 blocos) e placeholders de resultado vazios; instruções de execução manual presentes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>O arquivo 39-SPIKES.md com as 3 queries de introspecção prontas para copiar e colar (Spike a/b/c).</what-built>
  <how-to-verify>
    1. Abrir `.planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-SPIKES.md`.
    2. Conectar via psql (ou cliente SQL) ao banco de referência read-only em 192.168.3.198 (NÃO o Athos de produção do `ATHOS_PG_*`).
    3. Rodar a query do Spike (a) — tipo-base + CHECK do domínio `quantidade`. Colar a saída completa no bloco de resultado do Spike (a).
    4. Rodar a query do Spike (b) — constraints UNIQUE/PRIMARY KEY em `produto_composto`. Colar a saída no bloco do Spike (b). Se nenhuma linha UNIQUE retornar, registrar explicitamente "sem UNIQUE em (idprodutomaster, idprodutodetail)".
    5. Rodar as queries do Spike (c-1) triggers e (c-2) rules. Colar ambas as saídas. Zero linhas é resultado válido — registrar "nenhum trigger/rule".
    6. Salvar 39-SPIKES.md com os 3 resultados colados.
  </how-to-verify>
  <resume-signal>Digite "resultados colados" após colar as 3 saídas em 39-SPIKES.md, ou descreva qualquer erro de conexão/permissão encontrado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor/CI → rede 192.168.3.198 | O executor NÃO cruza esta fronteira (rede isolada); o humano executa as queries manualmente |
| operador → DB de referência | SELECT read-only contra catálogo do Postgres (pg_type, pg_constraint, information_schema, pg_rules) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Information Disclosure | queries de introspecção no DB de referência | low | accept | Queries são SELECT-only contra catálogo de schema; nenhum dado de negócio é exposto; DB de referência é read-only |
| T-39-02 | Tampering | escrita acidental no DB de referência | low | mitigate | Spikes usam apenas SELECT; nenhuma DDL/DML; instrução explícita de que 192.168.3.198 é read-only e distinto do Athos de produção |
</threat_model>

<verification>
- 39-SPIKES.md existe com 4 blocos de spike e placeholders.
- Após o checkpoint humano: os 3 resultados estão colados (não mais "[AGUARDANDO RESULTADO DO USUÁRIO]").
</verification>

<success_criteria>
- COMP-07 satisfeito: tipo-base + CHECK do domínio `quantidade`, presença/ausência de UNIQUE, e inventário de triggers/rules de `produto_composto` documentados em 39-SPIKES.md a partir de execução real no 192.168.3.198.
- Critério ROADMAP #4 atendido.
</success_criteria>

<output>
Create `.planning/phases/39-scaffold-leitura-e-spikes-de-introspec-o/39-01-SUMMARY.md` when done. Inclua na SUMMARY os 3 resultados de spike colados (ou link para 39-SPIKES.md) para consumo da Fase 40.
</output>
