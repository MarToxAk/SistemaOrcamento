# Requirements Archive: v1.5 — Correcao NFS-e

**Milestone:** v1.5
**Archived:** 2026-05-04
**Status:** All requirements delivered ✅

---

## v1.5 Requirements — Final Status

### NFSFIX-01 — Corrigir encoding das descrições de serviço NFS-e
**Priority:** High | **Scope:** Backend
**Status:** ✅ Delivered — Phase 15

**Outcome:** Constante `SERVICOS` em `nfse.service.ts` corrigida. Strings como `"ConfecÃ§Ã£o..."` restauradas para `"Confecção de carimbos, banners, placas e sinalização"`. Todos os logs, comentários e mensagens de erro do arquivo também corrigidos. Arquivo salvo como UTF-8.

**Acceptance verified:**
- [x] `"Confecção de carimbos, banners, placas e sinalização"` ✓
- [x] `"Gravação de objetos e joias"` ✓
- [x] `"Composição gráfica e confecção de matrizes"` ✓
- [x] `"Encadernação e acabamento"` ✓
- [x] Arquivo salvo como UTF-8 sem BOM ✓

---

### NFSFIX-02 — Corrigir proxy API NFS-e para repassar body do POST
**Priority:** High | **Scope:** Frontend (route.ts)
**Status:** ✅ Delivered — Phase 15

**Outcome:** Rota `/api/quotes/[id]/nfse/route.ts` corrigida com `_req.text()` + repasse ao backend via `headers: { "Content-Type": "application/json" }`.

**Acceptance verified:**
- [x] POST para `/api/quotes/[id]/nfse` lê o body da request ✓
- [x] Body é repassado integralmente ao backend via `backendFetch` ✓
- [x] Campos como `descontoAtivo`, `descontoPorcentagem`, `descontoValor` chegam ao backend ✓

---

### NFSFIX-03 — UI de desconto no modal de emissão NFS-e
**Priority:** High | **Scope:** Frontend (page.tsx)
**Status:** ✅ Delivered — Phase 16

**Outcome:** Modal de emissão expandido com switch "Aplicar desconto" e painel bidirecional. Ao ativar o switch o Valor total é pré-preenchido com o valor base do orçamento. Campo Valor total não aceita valor acima do base (bloqueio duplo: atributo `max` + validação em `syncDesconto`).

**Acceptance verified:**
- [x] Switch `descontoAtivo` habilita/desabilita a seção de desconto ✓
- [x] Campo "% desconto" recalcula R$ e Total automaticamente ✓
- [x] Campo "R$ desconto" recalcula % e Total automaticamente ✓
- [x] Campo "Valor total" recalcula R$ e % automaticamente ✓
- [x] Valor total base vem de `quote.totais.valor` ✓
- [x] `handleEmitirNfse()` inclui `descontoAtivo`, `descontoPorcentagem`, `descontoValor` no body ✓
- [x] Campos só são enviados se `descontoAtivo` for true ✓

---

## Traceability Table

| Req ID    | Description                              | Phase | Status    |
|-----------|------------------------------------------|-------|-----------|
| NFSFIX-01 | Encoding mojibake nfse.service.ts        | 15    | ✅ shipped |
| NFSFIX-02 | Proxy POST repassa body ao backend       | 15    | ✅ shipped |
| NFSFIX-03 | UI desconto bidirecional no modal NFS-e  | 16    | ✅ shipped |

**Coverage:** 3/3 requirements (100%)
