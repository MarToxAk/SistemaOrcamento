# Requirements Archive — v1.6: Correcao NFS-e: Calculo de Desconto e Valor Final

**Milestone:** v1.6
**Shipped:** 2026-05-04
**Status:** All requirements satisfied

---

## v1.6 Requirements

### Correcao do Modal NFS-e

- [x] **NFSC-01**: Usuario ve o campo "valor total" pre-preenchido com o total real do orcamento ao abrir a secao de desconto no modal NFS-e
  - **Outcome:** Validated — `setNfseValorTotal((quote?.body?.totais?.valor ?? 0).toFixed(2))` na linha 833
- [x] **NFSC-02**: Usuario digita um percentual de desconto e os campos R$ desconto e valor total atualizam automaticamente com base no total correto do orcamento
  - **Outcome:** Validated — `const base = Number(quote?.body?.totais?.valor ?? 0)` na linha 304; `syncDesconto("percent")` calcula corretamente
- [x] **NFSC-03**: Usuario digita um valor de desconto em R$ e os campos % e valor total atualizam automaticamente com base no total correto do orcamento
  - **Outcome:** Validated — `syncDesconto("valor")` usa o mesmo `base` correto (L304); logica em L318-327
- [x] **NFSC-04**: Usuario digita um valor total e os campos % e R$ desconto atualizam automaticamente; sistema impede valor total maior que o total do orcamento
  - **Outcome:** Validated — `max={(quote?.body?.totais?.valor ?? 0).toFixed(2)}` em L885 + logica de clamp em L330-331
- [x] **NFSC-05**: Ao emitir a NFS-e com desconto ativo, o valor pos-desconto e enviado corretamente ao backend e ao SOAP da NFS-e
  - **Outcome:** Validated (code + checkpoint humano) — `Record<string, string | number | boolean>` em L349; `descontoAtivo = true` em L363; `Number()` em L364-365; backend usa `=== true` (nfse.service.ts:472); checkpoint aprovado pelo usuario

---

## Traceability

| REQ-ID  | Phase | Status    | Delivered in   |
|---------|-------|-----------|----------------|
| NFSC-01 | 17    | complete  | 9bc9f6a        |
| NFSC-02 | 17    | complete  | 9bc9f6a        |
| NFSC-03 | 17    | complete  | 9bc9f6a        |
| NFSC-04 | 17    | complete  | 9bc9f6a        |
| NFSC-05 | 17    | complete  | b4138e1        |

---

## Dropped / Out of Scope

- Desconto por item individual na NFS-e — fora do escopo v1.6 (backlog)
- Historico de descontos aplicados por NFS-e — fora do escopo v1.6 (backlog)
- Recalculo retroativo de NFS-e ja emitida — fora do escopo (schema nao modificado)
