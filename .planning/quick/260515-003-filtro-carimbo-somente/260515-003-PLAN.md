---
status: in-progress
quick_id: 260515-003
date: 2026-05-15
slug: filtro-carimbo-somente
---

# Plan - 260515-003: Filtro "Somente Carimbos" na Página /status

## Objetivo
Adicionar um filtro ON/OFF que mostra apenas orçamentos que têm carimbo (pagamento confirmado), excluindo os em "Aguardando pagamento".

## Requisitos
- Checkbox/toggle na barra de cabeçalho ao lado dos outros filtros
- Label: "Somente carimbos" ou "Com carimbo"
- Quando ativo: mostra APENAS orçamentos com `paidInCashier=true` OU `paymentConfirmedAt!=null`
- Quando inativo (padrão): mostra todos (comportamento atual)
- Combinável com os filtros de Status e Tipo de Carimbo existentes

## Implementação

### Arquivo: `apps/frontend/src/app/status/page.tsx`

1. **Adicionar estado:**
   ```typescript
   const [onlyWithBadge, setOnlyWithBadge] = useState<boolean>(false);
   ```

2. **Criar função helper:**
   ```typescript
   function hasBadge(quote: QuoteRow): boolean {
     const orderNumber = quote.orderNumber ?? (quote.saleExternalId != null ? String(quote.saleExternalId) : null);
     const paidInCashier = quote.paidInCashier ?? Boolean(orderNumber);
     return paidInCashier || Boolean(quote.paymentConfirmedAt);
   }
   ```

3. **Atualizar lógica de `visibleQuotes`:**
   ```typescript
   const visibleQuotes = quotes.filter((quote) => {
     const statusMatch = selectedStatusFilter === "TODOS" || quote.statusKey === selectedStatusFilter;
     const badgeMatch = selectedBadgeFilter === "TODOS" || getBadgeType(quote) === selectedBadgeFilter;
     const badgeExistsMatch = !onlyWithBadge || hasBadge(quote);
     return statusMatch && badgeMatch && badgeExistsMatch;
   });
   ```

4. **Adicionar UI na barra de cabeçalho:**
   - Checkbox com label "Somente carimbos"
   - Posicionar depois do filtro de "Carimbos"
   - Usar `form-check form-check-inline` do Bootstrap
   - `id="only-badge-checkbox"` para acessibilidade

## Resultado
Usuário consegue ativar o filtro para ver apenas orçamentos com confirmação de pagamento, facilitando acompanhamento dos que já têm carimbo.

## Build Validation
- `npm --workspace @bomcusto/frontend run build` — sem erros
