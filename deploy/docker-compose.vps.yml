services:
  ts-webserver2:
    image: tailscale/tailscale:latest
    container_name: bomcusto-ts-webserver2
    hostname: bomcusto-webserver
    restart: always
    environment:
      TS_AUTHKEY: ${TS_AUTHKEY}
      TS_EXTRA_ARGS: --accept-routes --advertise-tags=tag:container
      TS_STATE_DIR: /var/lib/tailscale
      TS_USERSPACE: false
    volumes:
      - tailscale_data:/var/lib/tailscale
    devices:
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    networks:
      - app_net # Adicionado à rede para que outros o vejam

  postgres:
    image: postgres:16-alpine
    container_name: bomcusto-postgres
    restart: always
    networks:
      - app_net
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    image: ${BACKEND_IMAGE:-ghcr.io/martoxak/bomcusto-backend:latest}
    container_name: bomcusto-backend
    restart: always
    depends_on:
      - postgres
      - ts-webserver2

    networks:
      - app_net # VOLTOU para networks normal
    ports:
      - "4001:4000" # AGORA FUNCIONA SEM ERRO
    # REMOVIDO: network_mode: service:ts-webserver2

  frontend:
    image: ${FRONTEND_IMAGE:-ghcr.io/martoxak/bomcusto-frontend:latest}
    container_name: bomcusto-frontend
    restart: always
    depends_on:
      - backend
    networks:
      - app_net
    ports:
      - "3001:3000"

volumes:
  postgres_data:
  tailscale_data:

networks:
  app_net:
